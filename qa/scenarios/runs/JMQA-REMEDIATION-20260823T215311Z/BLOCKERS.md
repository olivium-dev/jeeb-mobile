# Current blockers

This register is the current superseding view as of 2026-08-24 15:23 UTC. The
append-only [test log](TEST-LOG.md) and [structured data](DATA.json) retain the
earlier observations; resolved historical blockers are not silently erased.

## Open release blockers

| ID | Severity | Current blocker | Required closure and proof |
|---|---|---|---|
| REM-BLK-101 | P0 | Approved staging changes are implemented in isolated work but not deployed. Live behavior remains old. | Land and deploy through the approved immutable workflow; retain candidate/running digest, readiness, failure-action, rollback, and post-rollback verification. No direct host hotpatch or manual service replacement. |
| REM-BLK-102 | P0 | Normal phone OTP is disabled and returns 503. | Deploy the restricted `JEEB_*` Twilio configuration; prove send, receiver receipt, verify, wrong/expired code, resend, lockout, and rate limiting on reserved synthetic numbers. |
| REM-BLK-103 | P0 | Super Login Plus and demo-user surfaces remain open on live staging. | Deploy closed defaults and prove every public bypass/demo route rejects use before any device login. |
| REM-BLK-104 | P0 | Live staging voice uses fake transcription. | Deploy the real server-side OpenAI path and prove a non-empty result through the public gateway with the committed synthetic Arabic fixture; retain no key or transcript. |
| REM-BLK-105 | P0 | No public authenticated WSS 101 path exists for chat/tracking. | Deploy the edge and scoped gateway-issued membership descriptor; prove invalid/wildcard credentials fail and two physical personas send, receive, order, read, reconnect, and deduplicate. |
| REM-BLK-106 | P0 | AASA and `assetlinks.json` are not live at `app.jeeb.fds-1.com`; Cloudflare target-account access is unresolved. | Serve both well-known JSON files without redirect and with correct content type; include Apple app ID `K5RDQ8J7AN.com.olivium.jeeb`, Android package `com.olivium.jeeb`, and Play app-signing SHA-256; then execute JMS-LINK-001. |
| REM-BLK-107 | P0 | Neither signed candidate is store-delivered. Apple validation is not TestFlight delivery. | After live preflight is green, upload the exact hashes, wait for processing, install from Play Internal Testing/TestFlight, and record store provenance. Do not substitute a sideload. |
| REM-BLK-108 | P0 | Samsung S24 is ADB-unauthorized and the physical iPhone is offline. | Accept the S24 USB prompt and bring the reserved iPhone online before the paired/device matrix opens. |
| REM-BLK-109 | P0 | Normal-OTP customer/Jeeber personas, authoritative KYC variants, and cleanup graph are not frozen. | Reserve two adult synthetic personas and resettable request/chat/delivery/KYC fixtures; record owners, before-state, nonce, and cleanup read-back. |
| REM-BLK-110 | P0 | Receiver-side Firebase push is not proven with the canonical registrations and store builds. | On store-installed builds, prove installations/token account boundaries plus foreground/background/terminated receiver delivery without exposing device tokens. |
| REM-BLK-111 | P1 | Play app-signing certificate is known, but Play acceptance of the local AAB upload certificate has not run. | Upload through the internal track after staging closure, record Play acceptance, then inspect delivered split signing against the Play app-signing certificate. |
| REM-BLK-115 | P0 | `jeeb-infrastructure/staging` has no least-privilege Cloudflare Worker API token; the current authenticated identity is denied target-account Worker/token authority. | A target-account admin must grant token authority or create a custom token with only Account → Workers Scripts → Edit for account `58198…`, then install it as `CLOUDFLARE_API_TOKEN` in infrastructure/staging. Add no Zone, DNS, KV, R2, route, or broader permission. |
| REM-BLK-116 | P0 | OTP's mandatory real-SMS rollback-window canary has no confirmed owner-controlled recipient. Placeholder/test-persona numbers are not acceptable. | Confirm one physical-device E.164 recipient distinct from the sender and install it only as `one-time-password/staging` environment secret `JEEB_STAGING_SMS_CANARY_TO`; retain receipt/verify evidence without recording the number. |
| REM-BLK-117 | P1 | Voice PR #27 exact head `e5b3142409f9936aa4aba749a9c2775a2423bf04` is remote-green and closes all five prior findings with 173/173 tests, but independent executable review proved its rollback is still image-transactional: a same-digest secret/env change followed by Arabic-gate failure leaves the candidate Spec active. | Snapshot/fingerprint the full incumbent Spec and version, roll back every changed Spec regardless of digest, verify exact restored Spec plus image/task/readiness, add same-digest and drifted-restoration negative tests, obtain exact-head approval, then deploy and prove a real uncached Arabic transcription. |
| REM-BLK-118 | P0 | The source-current AAB/IPA came from an incoherent dirty mobile overlay at `a8810345`: 60 staged and 92 unstaged tracked paths with 19 collisions (133 unique changed tracked paths), plus 245 untracked paths. Staged tests depend on unstaged implementations; raw `.codex-proof` evidence and signer material are also untracked. | Preserve the dirty tree, keep proof/signing material out of Git, reconstruct coherent scoped changes in clean worktrees, obtain independent mobile release review, then rebuild or cryptographically bind the exact approved commit to the uploaded artifacts. Do not commit the current index. |
| REM-BLK-119 | P0 | Gateway PR #523 exact head `63b19dba2b1b2aa94c107d719173a2ebfc4bde33` is remote-CI green and independently approved, but required QA, Product Owner, Staff Tech Lead, CODEOWNER, cross-repo readiness, merge, and live deployment gates remain open. | Complete the remaining protocol approvals, merge only with compatible realtime/OTP/voice/edge revisions, deploy the immutable image, and retain digest, readiness, public-provider, and verified rollback evidence. |
| REM-BLK-120 | P0 | Realtime PR #14 exact head `ac05b6481a4d8ba3c0024ca7d36bc3c23301ef65` is local/remote green but exact-head review found three P1 and one P2: same-digest Spec changes can skip rollback and falsely report restoration; the exact runtime verifier runs after rollback disarm; authenticated exact-topic positive plus cross-topic/forged-ticket WSS probes are absent from the armed transaction; and alternate checkout `with.ref` can bypass exact-SHA policy. | Make recovery full-Spec transactional, move exact runtime verification and all three authenticated WSS probes inside the armed rollback window, reject alternate checkout authority, rerun full/auth/rollout mutation suites, obtain exact-head approval, then retain staged live WSS rotation evidence. |
| REM-BLK-121 | P1 | The public edge returns HTTP 200 without redirect and accepts TLS 1.1. | Deploy and verify the HTTPS redirect, and have the target-zone owner enforce minimum TLS 1.2 or newer; retain negative TLS 1.0/1.1 and positive TLS 1.2+ probes. Do not broaden the Worker deployment token with unrelated zone authority. |
| REM-BLK-122 | P0 | OTP PR #27 exact head `29ff7af77e22d48f1ba63ff03df988c2b5e8b104` is remote-green and independently approved with 51 unit, 13 integration, and 32 rollout/policy tests, including full-Spec same-digest recovery. The protected physical SMS recipient, merge, deployment, and real canary remain open. | Confirm and install the owner-controlled staging recipient, merge only with compatible approved revisions, then use the protected rollback-window SMS canary before any legacy credential revocation or live acceptance. |

## Closed or superseded blockers

| Prior area | Current closure |
|---|---|
| Native/store identity mismatch | Closed: Android and iOS source, Firebase, Apple App ID, App Store draft, and Play draft use `com.olivium.jeeb`. |
| Missing canonical Firebase registrations | Closed: `jeeb-5a293` now contains protected Android and iOS registrations for `com.olivium.jeeb`. No new Firebase project was created. |
| Android post-refresh artifact | Closed at the artifact layer: forced-clean source-current AAB SHA-256 `4586571b76cd6f952bfa037d04f712076bbfe693ae4c2be70f47c534c48bf62b` passes signing, Bundletool, manifest, Firebase/Maps, staging, and forbidden-surface inspection. Source-review lineage remains REM-BLK-118. |
| Current iOS artifact / REM-BLK-114 | Closed at the artifact layer: clean source-current IPA SHA-256 `eeebd0e2fa25aca08a78b308546660f2305ec2156c6f589ed75c08c8e6e7ef94` passes strict signing, protected configuration, entitlements, forbidden-surface inspection, branded launch-screen inspection, and Apple validation with warning 90068 only. Source-review lineage remains REM-BLK-118. |
| iOS Maps key | Closed at artifact layer: a dedicated bundle/API-restricted Maps iOS key is injected through a protected path and inspection passes. |
| Associated-domain source decision | Closed in source/artifact: both platforms claim `app.jeeb.fds-1.com`; live association files and OS proof remain REM-BLK-106. |
| Broad mobile realtime token mint | Closed in mobile source/artifact inspection; end-to-end live enforcement remains REM-BLK-105. |
| Play signer in Firebase | Closed at configuration layer: Play SHA-1 `2E:CF:AF:7F:13:AB:9E:B5:34:E4:04:AD:3B:A9:F6:B2:A1:EA:77:12` and SHA-256 are registered on the existing app; read-back shows Play plus upload pairs. Store behavior remains REM-BLK-107/110/111. |
| Firebase Android client key / REM-BLK-123 | Closed at provider/security-disposition layer: official Firebase guidance classifies Firebase-only client keys/config as public identifiers, not authorization secrets. The existing Firebase-only API allowlist is unchanged and four exact Android package/signer restrictions now read back. Rotation/history rewrite is not required for this identifier; the two real config files must still stay out of the reconstructed head under REM-BLK-118. |

## Non-acceptable closures

The following do not close any blocker: Super Login Plus, demo users, mocked
OTP/push/chat, fake transcription, crafted role/KYC state, direct service or
LAN access, cleartext transport, manual host hotpatching, sender-only logs,
custom-scheme/direct-intent substitutes, sideloaded store candidates, or screen
reach without authoritative read-back. Jeeb remains cash-on-delivery only.
