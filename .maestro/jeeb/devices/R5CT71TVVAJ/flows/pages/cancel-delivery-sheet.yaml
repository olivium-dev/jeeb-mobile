# PAGE OBJECT: cancel-delivery-sheet — "Cancel Delivery" bottom sheet.
# Reached by: waiting-no-coverage -> Cancel request. Coordinate taps (no semantics).
# Params: none. Primary action: Cancel (confirm) -> tears down the broadcast.
# Other CTA: Keep delivery 50%,94.6% (dismiss the sheet, stay on waiting).
# Selectors: CancelConfirm 50%,88.1% / KeepDelivery 50%,94.6%.
---
- takeScreenshot: r5_cancel_delivery_sheet
- tapOn:
    point: "50%,88.1%"        # Cancel (confirm)
- waitForAnimationToEnd:
    timeout: 4000
