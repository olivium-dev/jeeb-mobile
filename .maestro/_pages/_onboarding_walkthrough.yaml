# Expected app Semantics identifiers from checkpoint 008:
#   onboarding_skip_button
#   onboarding_next_button
#   onboarding_get_started_button
#
# Current blocker:
#   onboarding_screen.dart currently exposes Flutter Keys for the controls but
#   not Semantics(identifier: ...). This subflow is intentionally id-only.
appId: ${APP_ID}
---
- extendedWaitUntil:
    visible:
      id: "onboarding_next_button"
    timeout: 20000
- assertVisible:
    id: "onboarding_skip_button"
- tapOn:
    id: "onboarding_next_button"
- waitForAnimationToEnd:
    timeout: 5000
- tapOn:
    id: "onboarding_next_button"
- waitForAnimationToEnd:
    timeout: 5000
- extendedWaitUntil:
    visible:
      id: "onboarding_get_started_button"
    timeout: 10000
- tapOn:
    id: "onboarding_get_started_button"
- waitForAnimationToEnd:
    timeout: 5000
