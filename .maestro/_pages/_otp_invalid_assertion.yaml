# App Semantics identifier:
#   registration_otp_error
appId: ${APP_ID}
---
- extendedWaitUntil:
    visible:
      id: "registration_otp_error"
    timeout: 15000
- assertVisible:
    id: "registration_verify_button"
