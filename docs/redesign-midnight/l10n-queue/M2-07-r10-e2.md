# l10n queue — M2-07 · R10 Offers + E2 waiting-for-offers

Lane: `lib/features/client_offers/**`. Every key below is a **tile literal** the
screen cannot render yet; each call site ships the nearest existing key with a
`TODO(midnight): l10n-queued` comment. Nothing in `lib/l10n/*.arb` was edited.

## New keys

| key | EN | AR | placeholders | call site (nearest key in use today) |
|---|---|---|---|---|
| `offersTitle` | `Offers` | `العروض` | — | `client_offers_screen.dart` top bar (using `offersScreenTitle` = "Choose a Jeeber") |
| `offersCardEtaIn` | `in {minutes} mins` | `خلال {minutes} دقيقة` | `minutes` (int) | `offer_card.dart` ETA (using `offersCardEtaMinutes` = "{minutes} min ETA") |
| `offersCardEtaInHours` | `in {hours} hr` | `خلال {hours} ساعة` | `hours` (int) | same slot — the tile rolls over at 60 min ("in 1 hr") |
| `offersWaitingTitle` | `Broadcasting to nearby Jeebers…` | `يجري البث إلى الجيبرز القريبين…` | — | `offers_waiting_state.dart` headline (using `offersEmptyTitle` = "Waiting for offers") |
| `offersWaitingTitleCount` | `Broadcasting to {count} Jeebers…` | `يجري البث إلى {count} جيبر…` | `count` (int) | same slot — **only wire-able once the gateway sends broadcast reach** (doc-13 Pattern A) |
| `offersWaitingBody` | `First offers usually land within 4 minutes. We'll ping you the second one does.` | `عادةً تصل أول العروض خلال ٤ دقائق. سننبّهك فور وصول أول عرض.` | — | `offers_waiting_state.dart` body (using `offersEmptyBody`) |
| `offersWaitingWindowChip` | `Window closes in {time}` | `تُغلق المهلة خلال {time}` | `time` (String) | `offers_waiting_state.dart` chip (using `offersWindowStripZero` = "No offers yet · window closes in {time}") |
| `offersWaitingAddDetailsCta` | `Add details to attract offers` | `أضف تفاصيل لجذب العروض` | — | **not mounted** — see open question Q3; no destination exists |
| `offerReviewCancelCtaFree` | `Cancel request — free before you accept` | `إلغاء الطلب — مجاناً قبل القبول` | — | `client_offers_screen.dart` footer (using `offerReviewCancelCta` = "Cancel request") |
| `offersLoadErrorTitle` | `Couldn't load offers` | `تعذّر تحميل العروض` | — | `client_offers_screen.dart` error headline (currently the failure sentence itself is the headline; this key splits title from body) |

## Existing keys the board CONFIRMS — do not touch

- `offersWindowStrip*` plural family already renders the tile literal exactly:
  `3 offers in · window closes in 04:12`.
- `chatOfferAcceptOnlyOne` — tile draws `Accept only one offer.` with a full stop;
  the existing string has none. Trivial; flagged, not queued.
