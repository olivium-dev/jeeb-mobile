# M2-11 · R15 Mutual rating — l10n queue

Owner: the l10n lane (`lib/l10n/*.arb` is never edited by a screen lane).

## Already landed — no action

Every literal the R15 tile draws already resolves through `AppLocalizations`:
`mutualRatingHeadlineNamed` ("How was Karim?") · `mutualRatingTagsLabel` ("What stood out?") ·
`mutualRatingTagPunctuality|Courtesy|Communication|PackageCondition|Navigation`
(Punctual · Friendly · Communication · Careful · Navigation) · `mutualRatingStarLabel1..5`
("Great" at 4) · `ratingCommentHint` ("Add a note (optional)…") · `mutualRatingSubmit`
("Submit rating") · `ratingLoadRetry` ("Try again", now the error-state CTA).

## NEW — please add

The board draws no in-flight frame; the loading headline is honest-state copy. The screen
renders `mutualRatingSubmit` as the interim headline and carries `TODO(midnight): l10n-queued`
at `mutual_rating_screen.dart` `_SubmittingView`.

| key | EN | AR | where |
|---|---|---|---|
| `mutualRatingSubmittingHeadline` | `Sending your rating` | `جارٍ إرسال تقييمك` | `JeebEmptyState(status: loading)` headline while `POST /v1/ratings/jeeb/submit` is in flight (`mutual_rating_loading`) |

```json
"mutualRatingSubmittingHeadline": "Sending your rating",
"@mutualRatingSubmittingHeadline": {"description": "R15 loading-state headline (mutual_rating_loading) while the mutual rating is being submitted."}
```

## REVISE — two existing values drift from the tile

Both are value-only edits (no new getters, no test churn: nothing asserts either string).
`mutual_rating_screen.dart` `_BlindRevealNote` carries the `TODO(midnight): l10n-queued` marker
and renders today's values until the lane lands these.

| key | today | tile literal / proposed | AR |
|---|---|---|---|
| `mutualRatingBlindNoteNamed` | `{name} rates you too. Both ratings reveal together — neither of you sees the other's first.` | `{name} rates you too. Both ratings reveal together.` | `{name} يقيّمك أيضاً. يظهر التقييمان معاً.` |
| `mutualRatingSubtitle` | `Your rating is confidential until both sides submit.` | `They rate you too. Both ratings reveal together.` | `هم يقيّمونك أيضاً. يظهر التقييمان معاً.` |

Rationale: the tile draws the short two-sentence promise, and the trailing clause wraps the
strip to two lines at 440dp (the board's is one line). The nameless fallback is the doc-13 P1
interim fix — it must state SIMULTANEOUS REVEAL, not confidentiality, because the counterpart
name is still not plumbed on the ACTIVE-delivery leg (see the lane report's open questions).
`app_en.arb:4951`'s unused spec string can be retired with this edit.
