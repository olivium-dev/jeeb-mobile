# l10n queue — M2-04-r9 (R9 · Request type + tier catalog)

Owner of `lib/l10n/*.arb` this wave: the l10n lane. These keys are needed by
`lib/features/request_type/`; every call site currently references the nearest
existing key behind a `TODO(midnight): l10n-queued` comment.

## 1. Tier row summaries (5) — the R9 tile literals

R9 draws one ellipsized line per tier, sentence case, middot-separated (`·`,
U+00B7 — **not** the bullet `•` the legacy `tier*Value` keys use). Interim call
site: `tierCatalogSummary()` in
`lib/features/request_type/presentation/widgets/tier_catalog_lexicon.dart`,
which returns `tier<X>Value` today.

| key | EN (tile literal) | AR |
|---|---|---|
| `requestTypeTierSummaryFlash` | `Under 1 hour · Highest price · Priority pickup` | `أقل من ساعة · أعلى سعر · استلام بأولوية` |
| `requestTypeTierSummaryExpress` | `1–2 hours · Higher price · Fast pickup` | `1–2 ساعة · سعر أعلى · استلام سريع` |
| `requestTypeTierSummaryStandard` | `2–4 hours · Balanced price` | `2–4 ساعات · سعر متوازن` |
| `requestTypeTierSummaryOnTheWay` | `Someone already heading there · Lower price` | `شخص متوجه إلى هناك · سعر أقل` |
| `requestTypeTierSummaryEco` | `Today, no rush · Lowest price · Greenest` | `اليوم، دون استعجال · أقل سعر · الأكثر مراعاة للبيئة` |

`@` description for all five: "One-line tier summary on the R9 compact radio
row. Runs are separated by ` · ` (U+00B7). Ellipsized to one line — keep short."

## 2. Deliver-to card, second line

R9 draws `Home — Achrafieh, Beirut` over `Current location` (sentence case). The
address itself is **not wired** (the destination is picked on the next screen —
`TODO(midnight): omitted, not faked` at
`request_type_screen.dart` `_LocationSection`). The existing
`requestTypeCurrentLocation` is title case (`Current Location`) and is currently
doing double duty as the primary line.

| key | EN | AR |
|---|---|---|
| `requestTypeCurrentLocationQualifier` | `Current location` | `الموقع الحالي` |

`@`: "Secondary line under the resolved destination on the R9 Deliver-to card.
Sentence case, unlike the title-cased `requestTypeCurrentLocation`."

## 3. Empty tier catalog

`GET /v1/tiers` → `200 OK` with an empty list (no tier configured for the
caller's city). Today the screen falls back to the network-error copy
(`requestSummaryErrorNetwork` + `requestSummaryRetry`) inside `JeebEmptyState`,
so empty and error render identically.

| key | EN | AR |
|---|---|---|
| `requestTypeTiersEmptyHeadline` | `No delivery speeds here yet` | `لا توجد سرعات توصيل هنا بعد` |
| `requestTypeTiersEmptyBody` | `We don't have delivery options for this area right now. Try again in a moment.` | `لا تتوفر خيارات توصيل في هذه المنطقة حالياً. حاول مرة أخرى بعد قليل.` |

## Retired by this item (values kept in the ARB, call sites gone)

- `tierCatalogSubtitle` — R9 draws no subtitle band above the rows.
- `tierCatalogPricingNote` — R9 draws no info note below them.

Both stay valid strings; only `TierCatalogSection` stopped rendering them
(doc-13 P0-3). Delete them from the ARB only if no other lane claims them.
