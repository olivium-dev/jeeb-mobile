# M2-13 · R17 Offer composer — l10n queue

Owner: the l10n lane (`lib/l10n/*.arb` is never edited by a screen lane).

## Context — this screen has TWO l10n layers

1. `lib/l10n/*.arb` via `AppLocalizations` — the real store.
2. `lib/features/offers/presentation/offer_composer_l10n.dart` — a **feature-local EN/AR map**
   sanctioned by the JM-045 lane (`50_ROUTE_REQUESTS.md`, wiring request WR-5) as the interim
   until the integrator lands the `offerComposer*` / `insufficientBalance*` keys. Its class doc
   says "delete this file once the integrator adds the keys". That backlog is unchanged by this
   wave and is NOT re-queued here — only the strings this lane newly moved out of Dart code are.

## NEW — please add

The carry-in defect: `offer_submission_cubit.dart:127-132` emitted **hardcoded English**
validation copy (`'Price must be greater than 0'`, `'ETA must be greater than 0'`) straight into
`OfferFormState.priceError` / `.etaError`, and the screen rendered it verbatim — so an Arabic
build showed English under the price field. Fixed this wave: the cubit now emits **stable reason
codes** (`OfferFormState.priceErrorRequired` / `etaErrorRequired`) and the screen resolves the
copy. The resolver getters live in `offer_composer_l10n.dart` (`priceRequiredError`,
`etaRequiredError`) and carry `TODO(midnight): l10n-queued` until these keys exist.

| key | EN | AR | where |
|---|---|---|---|
| `offerComposerPriceRequired` | `Enter a price above 0` | `أدخل سعراً أكبر من ٠` | error line under `offer_composer_price_field` |
| `offerComposerEtaRequired` | `Pick a pickup ETA` | `اختر وقت الاستلام` | error line under `offer_composer_eta_dropdown` |

```json
"offerComposerPriceRequired": "Enter a price above 0",
"@offerComposerPriceRequired": {"description": "R17 offer composer — validation under the price field when no positive price is entered (was hardcoded English in OfferFormCubit)."},
"offerComposerEtaRequired": "Pick a pickup ETA",
"@offerComposerEtaRequired": {"description": "R17 offer composer — validation under the ETA pill row when no pickup ETA is selected (was hardcoded English in OfferFormCubit)."}
```

## RETIRED by this lane — no key needed

The full-band ETA picker sheet is deleted (the board draws exactly three pills, no `Other`
affordance). With it go the only two consumers of `OfferComposerL10n.etaOther` ("Other" /
"أخرى") and `.etaSheetTitle` ("Pickup ETA" / "وقت الاستلام"). Both getters were removed; neither
was ever an ARB key, so there is nothing to retire in the store. Do **not** add them.

## Tile-literal deltas deliberately NOT adopted — decisions needed, not keys

Recorded here so the l10n lane does not "fix" them from the tile:

| tile literal | shipped | why |
|---|---|---|
| `Jeeb fee (10%)` | `Platform fee (10%)` | D37/D44 fixes the framing as "Platform fee"; `JeebMoneyBreakdown` asserts on the framing and its own doc quotes 17's copy as "Platform fee". |
| `You keep (cash)` | `You keep` | The cash handed over at the door IS the full offer — the fee comes out of the wallet reserve. `(cash)` beside `price − fee` states a falsehood; the reserve footnote carries the mechanics (redesign C2, `offer_composer_l10n.dart:150-157`). |
| `PICKUP ETA · Flash allows ≤ 60 min` | `PICKUP ETA · ≤ 120 min` | The route carries only `:id` — no tier on the wire (wiring request WR-6), so the tier name is omitted, never guessed. The ceiling is the real band's. |
