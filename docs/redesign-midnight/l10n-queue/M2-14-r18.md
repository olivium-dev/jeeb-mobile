# l10n queue — M2-14 · R18 Active delivery (Jeeber)

Owner: the l10n lane (`lib/l10n/*.arb`). This lane did not edit the ARBs.
Screen files carry `TODO(midnight): l10n-queued` at every site that reads a
stand-in key.

## A. RE-VALUE existing keys (tile is sentence case; the ARBs are Title Case)

doc-13 line 328 already flagged this as the "18 Title Case trio". The MIDNIGHT
tile confirms it.

| key | current EN | tile EN | AR |
|---|---|---|---|
| `activeDeliveryTitle` | `Active Delivery` | `Active delivery` | `توصيلة نشطة` (unchanged) |
| `activeDeliveryStatusInTransit` | `In Transit` | `In transit` | `في الطريق` (unchanged) |
| `activeDeliveryStatusAtDoor` | `At Door` | `At door` | `عند الباب` (unchanged) |

`activeDeliveryStatusOrdered` (`Ordered`) and `activeDeliveryStatusPicked`
(`Picked`) already match the tile.

**Breaks on landing (the l10n lane must update these in the same commit):**
`test/features/active_delivery_jeeber/delivery_status_stepper_test.dart` pins
the five labels in its `_labels` map and pins the two advance-CTA strings
(`Mark as In Transit`). No Maestro flow asserts on visible copy here — only on
`Semantics(identifier:)` — so nothing outside the repo moves.

## B. NEW keys

The tile draws no loading/error surface, so these are the honest headlines for
the two states that currently reuse the screen title.

| key | EN | AR |
|---|---|---|
| `activeDeliveryLoadingHeadline` | `Loading your delivery` | `جارٍ تحميل التوصيلة` |
| `activeDeliveryErrorHeadline` | `We couldn't load this delivery` | `تعذّر تحميل هذه التوصيلة` |

Consumed at `active_delivery_jeeber_screen.dart` `_buildBody` (both branches);
today both render `l10n.activeDeliveryTitle`.

## C. Still served from the feature-local resolver (pass-1 carry, unchanged)

`active_delivery_jeeber_l10n.dart` continues to serve the eight board strings
the pass-1 lane queued and the integrator has not landed:
`activeDeliveryHandoffTitle` · `activeDeliveryProofPhotoTile` ·
`activeDeliveryDoorCodePrompt` · `activeDeliveryCollectCash` ·
`activeDeliveryCollectCashNoAmount` · `activeDeliveryQuickActionMaps` ·
`activeDeliveryQuickActionChat` · `activeDeliveryQuickActionCosts`, plus the
re-value of `activeDeliveryOtpSubmit` to `Verify code & complete`.

`activeDeliveryDirectionsCta` is **withdrawn** — MIDNIGHT draws no trailing
directions circle on the drop-off card, so the getter was deleted this wave.

## D. Not queued (deliberate)

The tile's collect line reads `Ring twice · collect $8 + $6.50 goods` — a fee /
goods-cost split. No gateway field exposes the goods cost (`JeeberDelivery`
carries `amountText` only), so the string stays as shipped and the existing
`TODO(redesign-24)` at `_AddressCard` stands: omitted, not faked.
