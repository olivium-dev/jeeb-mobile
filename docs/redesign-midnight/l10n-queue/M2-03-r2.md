# l10n queue — M2-03 R2 Voice recording

Owner of `lib/l10n/*.arb` this wave applies these. Screen file:
`lib/features/voice_request/presentation/voice_recording_screen.dart`
(+ `presentation/widgets/live_transcript_band.dart`, `recording_readout.dart`).

## NEW keys

| key | EN | AR | used by |
|---|---|---|---|
| `voiceRecordingLiveTranscriptLabel` | `Live transcript` | `النص المباشر` | `live_transcript_band.dart` — the band's `JeebSectionLabel` (tile literal, drawn uppercase by the kit) |
| `voiceRecordingTranscriptSemantic` | `Live transcript — waiting for words` | `النص المباشر — بانتظار الكلام` | `live_transcript_band.dart` — spoken label for the band; the visual slot is deliberately wordless until a partial-transcript source exists |

Placeholder in code today: `l10n.transcriptionFieldLabel` ("Transcription") for
both, marked `TODO(midnight): l10n-queued`.

## VALUE CHANGE

| key | current EN | tile EN | tile AR |
|---|---|---|---|
| `voiceRecordingStatusRecording` | `Recording — release to stop` | `Recording — release to send` | `جارٍ التسجيل — أفلت للإرسال` |

Measured on `02-r2-voice-recording.png` (the caption under the `00:07 / 1:00`
timer). The em dash is the board's; keep it. `voiceRecordingReleaseToStop`
("Release to stop") is a different string (the waveform's spoken label) and is
NOT affected.

## Not queued (already exact tile literals)

`voiceRecordingNewRequestTitle` = "New request" · `voiceRecordingHoldToRecord` =
"Hold to record" · `voiceRecordingSlideToCancel` = "Slide" ·
`voiceRecordingTypeInstead` = "Type".
