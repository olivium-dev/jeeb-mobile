# l10n queue — M2-12 (R16 Jeeber home + E3 empty)

Lane owner does NOT edit `lib/l10n/*.arb`. Every key below is used in code today
via the nearest existing key with a `TODO(midnight): l10n-queued` marker; the
l10n lane lands the real key and the follow-up is a one-line swap.

Tiles: `16-r16-jeeber-home.png`, `29-e3-empty-no-requests-nearby.png`.

## 1. Header greeting

| key | EN | AR |
|---|---|---|
| `jeeberGreetingAhlan` | `Ahlan, {name}` | `أهلاً، {name}` |

- Placeholder: `name` (String, example `Omar`).
- Tile literal: "Ahlan, Omar". Used today: `homeGreetingNamed` ("Hello, {name}").
- Call site: `jeeber_home_greeting.dart` `_resolveGreeting`.
- NOTE: the client side (R1) keeps "Hello, {name}"; only the jeeber dashboard
  greets in the transliterated Arabic. Do not repoint `homeGreetingNamed`.

## 2. Availability strip

| key | EN | AR |
|---|---|---|
| `availabilityStatusOnlineShort` | `Online` | `متصل` |
| `availabilityStatusOnlineUntil` | `Online · goes offline in {remaining}` | `متصل · ستُنهى الجلسة خلال {remaining}` |
| `availabilityZoneLine` | `{zone} zone` | `منطقة {zone}` |

- Placeholders: `remaining` (String, pre-formatted, example `1h 40m`), `zone`
  (String, example `Achrafieh`).
- Tile literals: "Online · goes offline in 1h 40m" / "Achrafieh zone · Extend".
- Used today: `availabilityStatusOnline` ("You're online — receiving requests").
- **BLOCKED, queued for when the wire lands** (see §open in the lane report):
  the gateway parses no zone back (`_onlinePayload` sends `'zone': 'default'`)
  and deliberately nulls `lastActivityAt`, so the countdown has no anchor.
  `availabilityStatusOnlineShort` is landable immediately; the other two are
  not until the gateway supplies the fields.
- `availabilityExtendAction` ("Extend") already exists and is unchanged.

## 3. E3 — empty, no requests nearby

| key | EN | AR |
|---|---|---|
| `jeeberFeedQuietStreetTitle` | `Quiet street right now` | `الشارع هادئ الآن` |
| `jeeberFeedQuietStreetBody` | `No requests nearby — you're online, and we'll ping you the second one lands.` | `لا توجد طلبات قريبة — أنت متصل، وسننبّهك فور وصول أول طلب.` |
| `jeeberFeedQuietStreetBodyZoned` | `No requests nearby — you're online, and we'll ping you the second one lands in {zone}.` | `لا توجد طلبات قريبة — أنت متصل، وسننبّهك فور وصول أول طلب في {zone}.` |
| `jeeberFeedPullToRefreshAction` | `Pull to refresh` | `اسحب للتحديث` |
| `jeeberFeedWidenZoneAction` | `Widen my zone` | `وسّع نطاق منطقتي` |
| `jeeberFeedBusiestHours` | `Busiest hours in your zone: {range}` | `أكثر الساعات ازدحامًا في منطقتك: {range}` |

- Placeholders: `zone` (String), `range` (String, pre-formatted, e.g. `6–9 pm`).
- Used today: `requestFeedEmptyTitle` / `requestFeedEmptySubtitle`, and
  `availabilityLoadRetry` ("Retry") on the refresh pill.
- `...BodyZoned` is the tile's exact sentence; ship the unzoned form until the
  gateway returns a zone. `jeeberFeedWidenZoneAction` and
  `jeeberFeedBusiestHours` are queued but NOT wired — no service-zone surface
  and no busiest-hours feed exist (both omitted in code with a
  `TODO(midnight): omitted`).

## 4. Feed card age line

The tile prints the age WITHOUT the "ago" tail: `2 min`, not `2 min ago`. The
existing families (`jeeberFeedMinutesAgo*` / `HoursAgo*` / `DaysAgo*`, six CLDR
forms each) keep the tail. Requested: a parallel short family, same plural
shape, same placeholders.

| key | EN | AR |
|---|---|---|
| `jeeberFeedMinutesShortZero` | `Just now` | `الآن` |
| `jeeberFeedMinutesShortOne` | `1 min` | `دقيقة` |
| `jeeberFeedMinutesShortTwo` | `2 min` | `دقيقتان` |
| `jeeberFeedMinutesShortFew` | `{count} min` | `{count} دقائق` |
| `jeeberFeedMinutesShortMany` | `{count} min` | `{count} دقيقة` |
| `jeeberFeedMinutesShortOther` | `{count} min` | `{count} دقيقة` |
| `jeeberFeedHoursShort*` | `{count} h` (same six forms) | `{count} ساعة` etc. |
| `jeeberFeedDaysShort*` | `{count} d` (same six forms) | `{count} يوم` etc. |

- Placeholder: `count` (int).
- Call site: `jeeber_feed_card.dart` `_Timestamp`.
- LOW priority: the "ago" tail is a readability gain over the tile, so this is a
  fidelity call the owner may decline. Flagged, not assumed.
