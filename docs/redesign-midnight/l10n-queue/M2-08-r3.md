# M2-08 R3 Live tracking — l10n queue

**NO new MIDNIGHT copy.** Every literal R3 draws already resolves through an existing ARB getter
or through the feature-local `LiveTrackingL10n` stopgap map. Nothing was invented this wave, so
nothing new needs translating.

Tile literals → what ships:

| Tile literal | Resolver | ARB status |
|---|---|---|
| `Arriving in 20 min` | `LiveTrackingL10n.arrivingIn(int)` | **stopgap** — queued below |
| `Ordered` / `Picked` / `In transit` / `Delivered` | `trackingStepOrdered` / `trackingStepPicked` / `trackingStepInTransit` / `trackingStepCompleted` | shipped |
| `At Door` (P6/A5 relabel) | `activeDeliveryStatusAtDoor` | shipped |
| `Karim is on the way` | `LiveTrackingL10n.courierOnTheWay(name)` | **stopgap** |
| `$8 cash on delivery` | `LiveTrackingL10n.cashOnDelivery(amount)` | **stopgap** |
| `Door code — share only at handoff` | `LiveTrackingL10n.doorCodeNote` | **stopgap** |
| `2 1 4 4` | `JeebCodeCells.strip` (wire value) | n/a |
| `★ 4.9` | **not drawn** — blind-reveal privacy contract, `TODO(midnight): omitted, not faked` | n/a |

## Carry-forward: the `LiveTrackingL10n` stopgaps still awaiting real ARB keys

These were REQUESTED in the redesign-2026-08 batch and have not landed. They are unchanged by this
wave — listed so the l10n lane can close `live_tracking_l10n.dart` out in one pass. Every consumer
reads them through the resolver, so landing the keys is a one-file swap.

| Proposed key | EN | AR |
|---|---|---|
| `trackingArrivingIn` | `Arriving in {minutes} min` | `الوصول خلال {minutes} دقيقة` |
| `trackingCourierOnTheWay` | `{name} is on the way` | `{name} في الطريق` |
| `trackingCashOnDelivery` | `{amount} cash on delivery` | `{amount} نقداً عند التسليم` |
| `trackingDoorCodeNote` | `Door code — share only at handoff` | `رمز الباب — شاركه عند التسليم فقط` |
| `trackingCashShort` | `cash` | `نقداً` |
| `trackingDisputeCta` | `Open dispute` | `فتح نزاع` |
| `trackingNoShowCta` | `Report no-show` | `الإبلاغ عن عدم الحضور` |
| `trackingNoShowTitle` | `Jeeber didn’t show up?` | `لم يصل الجيبر؟` |
| `trackingNoShowBody` | `You can pick another offer for this request, or send it out again to nearby Jeebers.` | `يمكنك اختيار عرض آخر لهذا الطلب، أو إعادة إرساله إلى الجيبرز القريبين.` |
| `trackingNoShowReassignCta` | `Choose another offer` | `اختيار عرض آخر` |
| `trackingNoShowRebroadcastCta` | `Send request again` | `إعادة إرسال الطلب` |
| `trackingNoShowKeepCta` | `Keep waiting` | `متابعة الانتظار` |
| `orderSummaryPriceLabel` | `Price` | `السعر` |
| `orderSummaryCashLabel` | `Pay cash on delivery` | `ادفع نقداً عند التسليم` |
| `trackingEtaPending` | `ETA pending` | `الوقت المقدّر قيد التحديد` |
| `trackingPositionStale` | `Jeeber's location is {minutes} min old` | `موقع الجيبر قديم منذ {minutes} دقيقة` |
| `trackingPositionLost` | `No signal from the Jeeber — last seen {minutes} min ago` | `لا إشارة من الجيبر — آخر ظهور قبل {minutes} دقيقة` |
| `trackingPositionLostNoAge` | `No signal from the Jeeber` | `لا إشارة من الجيبر` |

`trackingCashShort` is now **unused on this screen** (the meta line it fed is gone); it stays queued
only because `LiveTrackingL10n` still exposes it.
