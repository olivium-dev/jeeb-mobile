# l10n queue — M2-02 R1 Client home + E1 empty

Owner of `lib/l10n/*.arb` this wave applies these. Screen files:
`lib/features/home_client/presentation/client_home_screen.dart`
(+ `widgets/client_home_request_hero.dart`, `widgets/client_home_empty_view.dart`,
`tabs/pending_requests_tab.dart`, `widgets/replies_card.dart`).

Every row below is rendered TODAY through the "placeholder in code" key, marked
`TODO(midnight): l10n-queued` at the call site.

## NEW keys

| key | EN | AR | used by / placeholder today |
|---|---|---|---|
| `homeHeroPromptMorning` | `What do you need today?` | `ماذا تحتاج اليوم؟` | `client_home_request_hero.dart` R1 headline · placeholder `homeEmptyTitle` |
| `homeHeroPromptAfternoon` | `What do you need this afternoon?` | `ماذا تحتاج بعد الظهر؟` | same |
| `homeHeroPromptEvening` | `What do you need tonight?` | `ماذا تحتاج الليلة؟` | same — **this is the literal the R1 tile draws**; the trio mirrors the existing `homeGreetingEyebrow*` time-of-day split |
| `homeCapsuleHoldToTalk` | `Hold to talk` | `اضغط مطوّلاً للتحدث` | voice-capsule title · placeholder `homeMicLabel` ("Hold to speak") |
| `homeCapsuleOrTapToType` | `or tap to type` | `أو اضغط للكتابة` | voice-capsule subtitle · placeholder `homeHeroSubtitle` |
| `homeCapsuleFirstRequestTitle` | `Hold to talk — Jeeb it` | `اضغط مطوّلاً للتحدث — جيبها` | E1's capsule title · placeholder `homeMicLabel` |
| `homeCapsuleFirstRequestSubtitle` | `or tap to type your first request` | `أو اضغط لكتابة طلبك الأول` | E1's capsule subtitle · placeholder `homeHeroSubtitle` |
| `homePendingEmptyMidnight` | `No pending requests — say it, and offers from nearby Jeebers arrive in minutes.` | `لا توجد طلبات معلّقة — قلها، وستصلك عروض من جيبرز قريبين خلال دقائق.` | `client_home_empty_view.dart` E1 body · placeholder `homePendingEmpty` |
| `pendingTabBroadcastingLabel` | `Broadcasting` | `جارٍ البث` | `pending_requests_tab.dart` live status · placeholder `pendingTabSearchingLabel` ("Searching for Jeebers…") — this is doc-13 P1 "04 pending copy" |
| `homeTabPending` | `Pending` | `معلّقة` | `client_home_screen.dart` segmented toggle · placeholder `homeTabPendingRequests` ("Pending Requests"); the E1 tile draws the short form, and the long one is what forces the toggle wider than the board's |
| `homeRepliesViewOffersCta` | `View offers` | `عرض العروض` | `replies_card.dart` single pill · placeholder `homeRepliesCheckOffersCta` ("Check Offers"); **the ar value is already exactly right**, only EN drifts |

## Blocked on the gateway (Pattern A — do NOT add yet)

`pendingCardReachCount` — the tile's `12 Jeebers reached`. There is no broadcast
reach field on `GET /v1/requests` (doc-13 Pattern A row 2), so the slot renders
nothing and carries a `TODO(midnight)` instead of a faked number. Add the key
with the field, not before.

## Not queued (already exact tile literals)

`homeGreetingEyebrowEvening` = "Good evening" · `homeGreetingNamed` =
"Hello, {name}" · `homeEmptyTitle` = "What do you need?" (E1's own headline) ·
`onboardingTagline` = "جيب لي أي شي" (reused verbatim as R1's orange tagline —
one brand line, one key).
