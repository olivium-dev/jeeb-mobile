# l10n queue — M2-16 · R20 order chat

Keys the R20 tile draws that no ARB entry covers. **Not edited into `lib/l10n/*.arb`**
(integrator-owned); every call site uses the nearest existing key with a
`TODO(midnight): l10n-queued` marker.

| key | EN | AR | call site / nearest key used today |
|---|---|---|---|
| `chatComposerHintMessage` | `Message…` | `رسالة…` | `chat_screen.dart` `ChatComposer(hintText:)` — currently falls through to `chatComposerHint` ("Type a message"). The board's composer placeholder is the single word + ellipsis. |

## Already queued elsewhere, still unresolved (restated, not re-queued)

* `chatCounterpartReplyLatency` — the tile's header subtitle reads
  `★ 4.9 · usually replies in 1 min`. No reply-latency field exists on any wire model, so
  the run is **omitted, not faked** (`chat_app_bar.dart` already carries the
  `TODO(redesign-24)` marker).
* The seven `ChatRedesignL10n` strings (`messageReadLabel`, the three quick replies,
  `quickReplyRowA11y`, `systemChipWithTime`, `counterpartRatingA11y`) are the pass-1 queue
  recorded in `docs/redesign-2026-08/wiring/21-order-chat.md`. Unchanged by this row.
