# l10n queue — M2-17 (R21 order history + E4 no-orders empty)

Owner lane: l10n. Do NOT edit `lib/l10n/*.arb` from this row.
Every site below currently renders the **nearest existing key** behind a
`TODO(midnight): l10n-queued` comment, so nothing is blank or faked.

## NEW keys

| key | EN | AR | site |
|---|---|---|---|
| `orderHistoryEmptyBody` | `Your first errand is one voice note away — medicine, groceries, that thing you forgot.` | `أول طلب لك على بُعد رسالة صوتية — دواء، خضار، أو الشي يلي نسيتو.` | `order_history_screen.dart` → `_OrdersEmptyView.body` (currently `orderHistoryEmptyActive`) |
| `orderHistoryEmptyCta` | `Jeeb it — your first request` | `جيبها — طلبك الأول` | `order_history_screen.dart` → `_OrdersEmptyView.action` (currently `homeEmptyCta`) |
| `orderHistoryRebroadcastCta` | `Re-broadcast` | `أعد البث` | `order_history_card.dart` → `_MetaRow._rebroadcast` (currently `orderHistoryReorderCta`) |
| `orderHistoryLoadingHeadline` | `Loading your orders` | `عم نجيب طلباتك` | `order_history_screen.dart` → `_StateBlock` loading (currently `deliveryStatusLoading`) |

`@` descriptions to attach:

- `orderHistoryEmptyBody` — E4 body copy under "No orders yet"; Active bucket only.
- `orderHistoryEmptyCta` — E4 orange CTA; enters the create flow (`request-type`).
- `orderHistoryRebroadcastCta` — the expired row's inline orange text action;
  re-broadcasts a request that drew no offers. Carries the FROZEN identifier
  `order_history_reorder_cta_<id>`, so it is a *label* change only.
- `orderHistoryLoadingHeadline` — R21 loading-state headline; must not repeat
  the header band's own "Delivery" title.

## VALUE changes to existing keys (tile literals, pinned tests attached)

| key | today | tile | blocked by |
|---|---|---|---|
| `orderHistoryStatusEnRoute` | `En route` | `In transit` | `test/features/order_history/orders_stale_status_chip_test.dart` pins `find.text('En route')`; doc-13 P2 already lists this (`app_en.arb:2906`). Change the key AND the pinned assertion in the same commit. |

## NOT queued (deliberate)

- The tile's expired row reads `expired`, not `Cancelled`. The wire folds
  `expired` into `cancelled` (`OrderRequestStatus.parse`), so the app cannot
  tell the two apart — the existing `orderHistoryStatusCancelled` stays.
- The tile's `no offers` in the amount slot needs an offer count that
  `GET /v1/requests` does not send. The em-dash + `orderHistoryAmountUnavailable`
  a11y label stays (Pattern A: omitted, not faked).
