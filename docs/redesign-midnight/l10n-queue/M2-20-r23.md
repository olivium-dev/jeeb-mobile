# l10n queue — M2-20-r23 (R23 Become a Jeeber)

Lane did NOT edit `lib/l10n/*.arb`. Items below are for the l10n lane.

## 1. Re-value `kycIdCaptureCta` — board literal is "Capture", app ships "Take photo"

The tile's live capture pill reads **`Capture`**; `app_en.arb:1507` currently holds
`"kycIdCaptureCta": "Take photo"`. Same slot, same role — a re-value, not a new key.
Shipped as-is with `TODO(midnight): l10n-queued` at the call site
(`kyc_identity_step.dart`, both gov-ID rows).

| key | EN (proposed) | AR (proposed) |
|---|---|---|
| `kycIdCaptureCta` | `Capture` | `التقاط` |

Current AR for reference: check `app_ar.arb` — the existing string translates "Take photo".

## 2. `kycScrollForSelfieHint` is now UNUSED — retire it

`_ScrollForSelfieHint` (`kyc_scroll_hint`) was deleted this lane: doc-13 P1 moved the
contract ID band below the selfie, so the fold the cue advertised no longer exists and the
pill re-split the three-row checklist the P1 fix healed. No Maestro flow referenced it.
The key has no remaining reader.

- `kycScrollForSelfieHint` → **retire** (EN + AR), or park it if the owner reverses the
  deletion (see the lane report's open questions).

## 3. LEGAL HOLD — `kycReviewPrivacyNote` deliberately NOT changed

The tile draws:

> Review usually takes **under 24 hours**. Your documents are encrypted and never shown to
> customers.

The app ships the ratified held pair and this lane left both strings byte-identical:

- `kycReviewTimeTitle` = `Review usually takes under 24 hours`
- `kycReviewPrivacyNote` = `Your documents are never shown to customers.`

The `encrypted and` clause stays out pending owner/legal sign-off (C4, recorded in
`app_en.arb:5067`'s own `@` description). **Do not add it here without that sign-off.**
Two consequences reported to the owner, not resolved by this lane:

1. the board renders the note as ONE flowing `bodySmall` paragraph with a bold white
   `under 24 hours` span; we keep the stacked title/sub form because merging the two strings
   would *move* consent copy that is under hold;
2. the board's ink is `inkSoft` (`#B9C0F0`) for the paragraph. We keep the checkbox/ToS ink
   at the brighter shipped value — under a hold, consent copy may be more prominent than the
   board, never less.

## 4. No new keys requested

Every other literal on the tile already has a key: `kycWizardTitle`, `kycWizardSubmit`,
`kycWizardProgressStepLabel`, `kycWizardNextStepHint`, `kycIdFrontLabel`, `kycIdBackLabel`,
`kycIdRetake`, `kycIdCaptureHint`, `kycCaptureCaptured`, `kycSelfieStepTitle`,
`kycSelfieLockedHint`, `kycTosAgreeLine`.

`kycCaptureCaptured` stays `Captured` (not the tile's `Captured · looks sharp ✓`): the
"looks sharp" verdict needs a capture-quality signal `PhotoAttachment` does not carry, and
the omission is already documented in the ARB description and a `TODO(midnight)`.
