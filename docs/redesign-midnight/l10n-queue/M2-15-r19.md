# M2-15 · R19 Earnings — l10n queue

Owner: the l10n lane (`lib/l10n/*.arb` is never edited by a screen lane).

Board source: `Jeeb Rich UI.dc.html` `tpl 1136–1194`.

## Already landed — no action

`earningsTitle` ("Earnings") · `earningsPeriodToday` ("Today") · `earningsPeriodWeek`
("This week") · `earningsLoadFailed` · `earningsLoadRetry` · `earningsExportButton`
("Export PDF") · `earningsEmpty`.

Feature-local strings (`earnings_dashboard_l10n.dart`, still awaiting the dedicated
`earnings*` getters requested in `50_ROUTE_REQUESTS.md` "JM-052") already match the tile
verbatim: `Paid to you directly — never through Jeeb.` · `10% per won offer, from your wallet`
(rate interpolated from `kJeebCommissionPercent`) · `Deliveries` · `Avg kept / offer`.

## REVISE — one landed key drifts from the tile

| key | today | tile literal (`tpl 1144`) | AR |
|---|---|---|---|
| `earningsPeriodMonth` | `This month` | `Month` | `شهر` |

`_PeriodPill._label` carries `TODO(midnight): l10n-queued` and renders today's value until
the lane lands this. Nothing asserts the string (`earnings_period_month` is the frozen id).

## REVISED IN PLACE — feature-local, no ARB action

`totalCashLabel` `Total cash earned` → **`Cash collected`** / `النقد المُحصَّل` (`tpl 1147`,
doc-13 Pattern F). The string lives in `earnings_dashboard_l10n.dart`, not the ARB, so it
shipped with this lane. Two assertions in `earnings_dashboard_data_truth_test.dart` followed
it (`TOTAL CASH EARNED` → `CASH COLLECTED`).

**Not revised — recorded refusal, do not relitigate:** `feesPaidLabel` stays
`Platform fees paid`; the board's `Jeeb fees paid` is the D41/D44 copy-policy ruling
(`per-screen-revised/19-earnings.md` §2, 02-STUDY-NOTES §5).

## NEW — please add

The board draws neither a loading frame nor a "no rows in this funded period" frame; both are
honest-state copy the MIDNIGHT states sweep needs. Both call sites carry
`TODO(midnight): l10n-queued` and render the nearest landed key meanwhile.

| key | EN | AR | where |
|---|---|---|---|
| `earningsLoadingHeadline` | `Loading your earnings` | `جارٍ تحميل أرباحك` | `JeebEmptyState(status: loading)` headline (`earnings_loading`); interim = `earningsTitle` |
| `earningsBreakdownEmptyTitle` | `No deliveries this period` | `لا توصيلات في هذه الفترة` | `earnings_breakdown_empty` — funded totals with an empty `deliveries` list; interim = `earningsEmpty` |

```json
"earningsLoadingHeadline": "Loading your earnings",
"@earningsLoadingHeadline": {"description": "R19 loading-state headline (earnings_loading) while GET /v1/jeeb/earnings is in flight."},
"earningsBreakdownEmptyTitle": "No deliveries this period",
"@earningsBreakdownEmptyTitle": {"description": "R19 breakdown-band empty headline (earnings_breakdown_empty): the period has totals but the wire returned no per-delivery rows."}
```

## BLOCKED on the wire — no key to add yet

The tile's third hero stat is `★ 4.8 / This week` and each row reads `⚡ Pharmacy run · Fri`.
There is no period-scoped rating, tier or item name on `GET /v1/jeeb/earnings` (doc-13
Pattern A), so no label is queued: the slots carry `TODO(midnight): omitted, not faked`
rather than a string with nothing to say.
