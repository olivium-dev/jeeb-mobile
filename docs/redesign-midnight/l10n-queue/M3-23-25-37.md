# l10n queue — M3-23 / 25 / 37 · settings subtree

Two keys are **required**: both rows gained a cold-read frame they never drew,
and `JeebEmptyState` requires a headline. Nothing else in this lane needed new
copy — the error frame reuses the exact strings the screen already shipped.

Verified against `lib/l10n/app_en.arb` / `app_ar.arb` on 2026-08-04.

| Key | EN | AR | Status |
|---|---|---|---|
| `liveSettingsLoadingHeadline` | `Loading your settings…` | `جارٍ تحميل إعداداتك…` | REQUIRED |
| `profileEditLoadingHeadline` | `Loading your profile…` | `جارٍ تحميل ملفك الشخصي…` | REQUIRED |

## 1. `liveSettingsLoadingHeadline` (REQUIRED)

Call site: `_LiveSettingsLoading` in `live_settings_screen.dart`, identifier
`live_settings_loading`. Before MIDNIGHT this frame was an `OMDSAppBar` over a
bare `CircularProgressIndicator` — no copy at all, so nothing is being replaced.

**Nearest existing key in use meanwhile:** `settingsOpenSubtitle` — *"Profile,
notifications, addresses, and more"*. It names exactly the payload of the
`GET /v1/users/me` read that is in flight, so it cannot say anything false on
this frame, and it is the only settings-domain string that describes the
surface's contents rather than an action.

Rejected: `settingsTitle` ("Settings") — the in-body [JeebTopBar] on the same
frame already renders it at 20/w700, so using it again as the centred `h1`
headline draws the same word twice on one screen. `deliveryStatusLoading`
("Loading delivery…") names another surface's noun.

## 2. `profileEditLoadingHeadline` (REQUIRED)

Call site: `_ProfileEditLoading` in `profile_edit_screen.dart`, identifier
`profile_edit_loading`. Also a frame that did not exist before this row — the
screen used to build the form against a not-yet-loaded profile, which is the
defect the row fixed.

**Nearest existing key in use meanwhile:** `profileEditSubtitle` — *"Update your
name and avatar"*. Same argument as §1: it is this screen's own subtitle one
level up (it is what R22's identity card renders under the name), it names the
two fields being fetched, and it does not repeat the top bar's "Edit profile".

## Deliberately NOT queued

- **No error copy for either row.** `live_settings` keeps `settingsNetworkError`
  ("Something went wrong. Please try again.") as its headline and `kycRetry`
  ("Retry") on the pill — both already shipped on this screen, both noun-free,
  so the restyle changed the frame and not a word of the copy.
- **Nothing for M3-25.** `notification_preferences_screen.dart` renders no
  strings of its own; every string on that route belongs to M3-24.
- **No save-failure copy for M3-23.** `SettingsCubit.saveProfile` swallows a
  remote failure and banners success regardless, so there is no error state to
  name yet. Queueing copy for it would imply a state the product cannot reach —
  see the row report's open question instead.
