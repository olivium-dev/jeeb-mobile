# M4-35 — order-history pagination footer

The next-page wait (`order_history_loading_more`) now draws the kit's `parcel`
skeleton with a headline. There is no string for "the NEXT page is loading", so
the call site borrows the COLD-load line and carries a
`TODO(midnight): l10n-queued` marker.

Call site: `lib/features/order_history/presentation/order_history_screen.dart`
— `_NextPageWait.build`, currently `l10n.orderHistoryLoadingHeadline`.

## Key to add

| key | EN | AR |
|---|---|---|
| `orderHistoryLoadingMore` | `Loading more orders` | `عم نجيب طلبات أكتر` |

`@orderHistoryLoadingMore` description: R21 pagination-footer headline under the
parcel skeleton. Distinct from `orderHistoryLoadingHeadline`, which is the COLD
first-page load — rows are already on screen here.

## Swap

Replace `l10n.orderHistoryLoadingHeadline` with `l10n.orderHistoryLoadingMore`
in `_NextPageWait.build` and delete the two-line TODO above it. The assertion in
`test/features/order_history/order_history_next_page_wait_test.dart`
("the wait announces itself instead of spinning silently") reads the key by
name and must be repointed in the same edit.
