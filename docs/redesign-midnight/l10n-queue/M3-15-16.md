# l10n queue — M3-15/16 · Settlement pair (DELETION)

> **STATUS 2026-08-04 (l10n lane, M3 Tier 4).** Re-verified: all 11 keys still
> have **zero** call sites outside `lib/l10n/` (the cubit/repo/domain under
> `lib/features/settlement/` survive; only the three presentation files are gone).
> **Nothing deleted** — this file's own §Recommended disposition says HOLD until
> owner Q9 confirms, and these keys are the cheapest part to restore. This file
> is deliberately kept so the pending sweep is not lost; delete it, and the 11
> keys + 13 getters, once Q9 is confirmed.

**This row adds ZERO new keys.** M3-15/16 is the ratified orphan *deletion*
(02-STUDY-NOTES §ORPHAN, owner confirm = §8 Q9), so the only l10n item is the
reverse: 11 keys are now **orphaned** in both ARBs and 13 getters are orphaned
in `lib/l10n/app_localizations.dart`.

Reported, **not removed** — `lib/l10n` is the l10n lane's; this row did not
touch it. Verified against `lib/l10n/app_en.arb` / `app_ar.arb` on 2026-08-04.

## Orphaned by this deletion

All 11 keys existed **only** to serve `settlement_screen.dart` /
`settlement_detail_screen.dart` / `settlement_status_pill.dart`, all three now
deleted. Zero remaining call sites in `lib/`.

| Key | EN | Sole (now-deleted) call site |
|---|---|---|
| `settlementTitle` | `Settlement Statements` | `_SettlementScaffold` top bar |
| `settlementEmptyMessage` | `No settlement statements yet.` | list empty state |
| `settlementLoadError` | `Unable to load statements.` | list error state |
| `settlementUnavailable` | `Statements unavailable.` | `_Unavailable` (no-repo) |
| `settlementStatusPaid` | `Paid` | `SettlementStatusPill.labelFor` |
| `settlementStatusPending` | `Pending` | `SettlementStatusPill.labelFor` |
| `settlementDownloadTooltip` | `Download PDF` | row download `IconButton` |
| `settlementRowSemantics` | `{amount} — {status}` | row + payout-hero semantics |
| `settlementBreakdownTitle` | `Delivery Breakdown` | detail section label |
| `settlementTotalPayout` | `Total cash kept` | detail payout hero |
| `settlementCommissionLabel` | `Platform fee: {amount}` | detail per-delivery line |

Matching getters in `app_localizations.dart` (13 — `settlementRowSemantics` and
`settlementCommissionLabel` are methods): lines ~2166–2179.

## Recommended disposition

**Hold, do not delete yet.** Owner confirm on the deletion is still open
(§8 Q9), and these keys are the cheapest part to restore if product wires the
screens. Sweep them only once Q9 is confirmed; the screens themselves are
restorable from git either way.

## One key is load-bearing beyond settlement — check before sweeping

`settlementCommissionLabel` (`"Platform fee: {amount}"`) carried the **D41/D44
fee-only framing**. That lock has been re-homed by this row onto
`OfferComposerL10n.feeRowLabel` / `.feeLinePending`
(`test/decision_violations_test.dart`), which are feature-local strings, **not**
ARB keys. So sweeping `settlementCommissionLabel` does not weaken D41/D44 — but
do not treat its removal as licence to relax the framing anywhere else.
