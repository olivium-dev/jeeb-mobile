# l10n queue — M3-32 `dispute_status`

> **STATUS 2026-08-04 (l10n lane, M3 Tier 5).** Both keys are **LANDED** —
> `disputeStatusLoading` and `disputeStatusEvidenceEmpty` are in `app_en.arb` +
> `app_ar.arb` with `@` descriptions, and both getters are in
> `app_localizations.dart` (§`MIDNIGHT M3-32`). Do NOT re-apply them.
> **The two getter bodies were deliberately left alone**: `lib/features/dispute_status/`
> was held by another lane this wave. One follow-up remains, owned by whoever
> holds `dispute_status` next — in `presentation/dispute_status_l10n.dart` swap
> `loadingHeadline` → `_l10n.disputeStatusLoading` and `evidenceEmptyHeadline` →
> `_l10n.disputeStatusEvidenceEmpty`, dropping both `TODO(midnight): l10n-queued`
> tags. The resolver literals are byte-identical to the ARB values, so the swap
> is a no-op on screen and on every `find.text`. Delete this file once it lands.

Two keys. Requested for `lib/l10n/app_en.arb` + `app_ar.arb`; **not** edited by this
lane (the l10n lane owns `lib/l10n`).

| Key | EN | AR | Where |
|---|---|---|---|
| `disputeStatusLoading` | `Checking this dispute` | `جارٍ التحقق من هذا النزاع` | headline of the cold-read `JeebEmptyState` (`dispute_status_loading`) |
| `disputeStatusEvidenceEmpty` | `No evidence attached` | `لا توجد أدلة مرفقة` | headline of the inline E4 block (`dispute_status_evidence_empty`) |

`@disputeStatusLoading`: *"JM-065 dispute-status D30 cold-read headline — shown on
the `JeebEmptyState` radar skeleton while `GET /v1/disputes/:id` is in flight."*

`@disputeStatusEvidenceEmpty`: *"JM-065 dispute-status D53 empty evidence summary —
headline of the compact parcel `JeebEmptyState` drawn when the wire carries no
reason, comment, photo, voice, chat snapshot or timeline."*

## Interim shipping state

Both strings ship **now** from the feature-local resolver
`lib/features/dispute_status/presentation/dispute_status_l10n.dart`
(`DisputeStatusL10n.loadingHeadline` / `.evidenceEmptyHeadline`, EN + AR), tagged
`TODO(midnight): l10n-queued`. That resolver's own docstring is the sanctioned
mechanism for this feature: ~20 of its strings are already feature-local pending
integrator ARB keys (the JM-057/JM-058 precedent). Nothing was fabricated in
`lib/l10n/*.arb`.

When the keys land, replace the getter bodies with `_l10n.disputeStatusLoading`
and `_l10n.disputeStatusEvidenceEmpty` — no call-site change.

## Not queued (deliberate)

- No error-state key is queued: the three D30 messages (`loadError`,
  `notFoundError`, `networkError`) already exist in the resolver and are reused
  verbatim as the error `JeebEmptyState` headline.
- No body/sub-line is queued for either pre-load frame. A second line would be
  invented copy, not carried copy.
