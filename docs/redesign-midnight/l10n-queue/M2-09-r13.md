# M2-09 · R13 OTP handover — l10n queue

Owner: the l10n lane (`lib/l10n/*.arb` is never edited by a screen lane).

## Already landed — no action

The nine screen-13 keys the pass-1 lane queued through `docs/redesign-2026-08/wiring/13-otp-handover.md`
are now in `app_en.arb` / `app_ar.arb` with generated getters, so this lane **deleted the
feature-local stopgap** `lib/features/otp_handover/presentation/otp_handover_l10n.dart` and
re-pointed its two consumers at `AppLocalizations`:

`otpClientShareSubtitle` · `otpClientShareSubtitleNamed` · `otpArrivalAtDoor` ·
`otpArrivalOnTheWay` · `otpArrivalSubtitle` · `otpClientResendSmsPrompt` ·
`otpClientResendSmsAction` · `otpDisputeCta` · `otpResendFailed`

Values verified identical to the stopgap's EN/AR, so every literal assertion in
`otp_handover_screen_test.dart` still holds.

## NEW — please add

The board draws neither state, so both are honest-state copy rather than tile literals. The
screen renders `otpHandoverClientTitle` / `otpHandoverJeeberTitle` as an interim headline and
carries `TODO(midnight): l10n-queued` at `otp_handover_screen.dart` `_stateHeadline`.

| key | EN | AR | where |
|---|---|---|---|
| `otpHandoverLoadingHeadline` | `Getting your code` | `جارٍ إحضار رمزك` | `JeebEmptyState(status: loading)` headline while `GET /otp` / the code store is in flight |
| `otpHandoverErrorHeadline` | `Code unavailable` | `الرمز غير متاح` | `JeebEmptyState(status: error)` headline above the mapped `otpError*` body + `otpRetry` |

ARB metadata:

```json
"otpHandoverLoadingHeadline": "Getting your code",
"@otpHandoverLoadingHeadline": {"description": "R13 loading-state headline (otp_handover_loading) while the handover code is being read from the store or the gateway."},
"otpHandoverErrorHeadline": "Code unavailable",
"@otpHandoverErrorHeadline": {"description": "R13 error-state headline (otp_handover_error); the mapped otpErrorNetwork/otpErrorServer/otpErrorGeneric string is the body."}
```
