# 07 · Request type

screen-label: `07 Request type`

## Designer note (verbatim)

Enhanced: the 5-tier spectrum reads as one scannable list — selected tier snaps to solid navy with an orange check, "Most picked" nudge on Standard, speed + price value on every row, and the delivery location is confirmed on the same step with a one-tap Change.
