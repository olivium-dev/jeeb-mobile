# 21 · Order chat

screen-label: `21 Order chat`

## Designer note (verbatim)

Enhanced: the order rides pinned above the thread (status + cash + Track) so chat never loses context; voice notes and photos are first-class WhatsApp-style bubbles; system events (accepted, ETA) render as quiet timeline chips; one-tap quick replies incl. Arabic; mic kept primary in the composer.
