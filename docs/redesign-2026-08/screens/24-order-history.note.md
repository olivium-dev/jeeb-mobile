# 24 · Order history

screen-label: `24 Order history`

## Designer note (verbatim)

Enhanced: the active order stands out with an orange frame + live ETA + Track; completed rows earn a one-tap "Jeeb it again" re-order (the killer retention action the shipped list lacks); expired requests offer Re-broadcast instead of a dead end; tab counts and a readable date-range chip replace the bare filter icon.
