export const meta = {
  name: 'jeeb-w0-seamfix',
  description: 'Close W0: implement the dev-seam session-seeding test harness (unblocks 9 flows), fix 2 flow bugs, rebuild + re-run all 11 W0 flows, update sign-offs',
  phases: [
    { title: 'SeamHarness', detail: 'Opus: implement jeeb.seam.session + per-flow seeds end-to-end (MainActivity + DevSeamConfig + bootstrap), debug-only, extensible for role/kyc' },
    { title: 'FlowFix', detail: 'Sonnet: align 11 flows to the implemented seam contract; fix jm-006 AC1 id + bump cold-start timeouts to 30s' },
    { title: 'Rerun', detail: 'Sonnet: rebuild APK, reinstall, re-run all 11 W0 flows, report pass/fail' },
    { title: 'Signoff', detail: 'Opus PO: update signoffs/JM-###.md from the re-run' },
  ],
}

const APP = '/Users/oudaykhaled/Desktop/olivium/jeeb/jeeb-mobile'
const OUT = APP + '/docs/build-out'
const BASE = 'Jeeb mobile build-out, WAVE 0 closeout. Context: ' + OUT + '/00_CTO_BRIEF.md, ' + OUT + '/61_W0_QA_RESULTS.md (the on-device run: 0/11 pass, root causes RC-1 seam-not-wired for 9 flows + RC-2 two flow bugs - READ IT), ' + OUT + '/60_W0_TEST_PLAN.md (identifier contract), ' + OUT + '/41_GUARDRAILS_TESTING.md (run recipe section 3). App at ' + APP + '. Mock running on :4010 (fixed dev codes per 42 W-1 FLOOR CLOSED: OTP=123456, RECOVERY=654321). Android emulator jeeb_test, host alias 10.0.2.2:4010, run with --device emulator-5554.'

// ---------- Phase 1: Session seam harness (Opus) ----------
phase('SeamHarness')
const seam = await agent(BASE + '\n\nROLE: Senior Principal Flutter Engineer (Opus) - DEV-SEAM SESSION HARNESS. The W0 Maestro flows seed session/journey state via jeeb.seam.* intent-extras that are NOT wired (RC-1 in 61_W0_QA_RESULTS.md). Implement this DEBUG-ONLY test harness end-to-end so flows can deterministically start mid-journey. This is foundational test infra reused by ALL future waves - design it to extend cleanly to customer/jeeber role + kycStatus states.\n\nImplement:\n1. android/app/src/main/kotlin/app/jeeb/mobile/MainActivity.kt - add ALL jeeb.seam.* keys to the seamKeys whitelist so the intent-extras pass through the channel: jeeb.seam.session, jeeb.seam.otp_code, jeeb.seam.otp_countdown_expired, jeeb.seam.signup_collision, jeeb.seam.social_login, jeeb.seam.recovery_code, jeeb.seam.recovery_countdown_expired, jeeb.seam.set_password_mode.\n2. lib/core/dev_seam/dev_seam_config.dart - add typed fields (sessionSeed, otpCode, otpCountdownExpired, signupCollision, socialLogin, recoveryCode, recoveryCountdownExpired, setPasswordMode) and map them in fromMap(). Keep everything gated by kDebugMode (release builds ignore all of it).\n3. A SessionSeamBootstrap (new file lib/core/dev_seam/session_seam_bootstrap.dart, or extend the existing app bootstrap) that runs BEFORE the GoRouter first-run redirect fires and seeds app state from jeeb.seam.session: find the real storage the OnboardingCubit (onboarding-complete flag), the SessionGate/SessionCubit (token store - see lib/core/session/ and the auth token store), the RoleCubit (customer/jeeber), and the BiometricLockCubit read, and seed them. Required session values: customer_logged_in (onboarding done + valid token + role customer -> lands on Requests shell), jeeber_logged_in (-> DELIVERY tab), logged_out_returning (onboarding done + NO token -> /login), biometric_enrolled (onboarding done + token + biometric LOCKED -> /lock), biometric_enrolled_logged_out, suspended (seed so getMe/status resolves suspended -> /account-status). Use the real userId persisted from auth so JM-006 reads GET /users/:id (not /users/me, per 42 note).\n4. Per-flow seeds: otp_code / recovery_code - the mock already accepts fixed dev codes 123456 / 654321, so prefer NOT needing app seeding for those (the flows can just type the fixed code); only implement what genuinely needs app/mock seeding. signup_collision / social_login(collision_409) - decide the simplest deterministic path (the mock 409s on a duplicate email; document how a flow triggers it). countdown_expired - if the resend countdown is app-driven, add a debug override to zero it.\n5. Run flutter analyze (clean of new errors). Do NOT run the emulator/maestro (later phases do).\n6. Write ' + OUT + '/62_SEAM_HARNESS.md: the FINAL seam contract - every jeeb.seam.* key, its accepted values, and EXACTLY what app/mock state each seeds and the resulting start destination. This is the contract the flow-fix QA codes to and future waves reuse.\nReturn: files changed, the seam contract summary (keys -> behavior -> start destination), the storage keys you seeded, and confirmation analyze is clean.', { label: 'w0:seam-harness', phase: 'SeamHarness' })

// ---------- Phase 2: Flow fixes (Sonnet) ----------
phase('FlowFix')
const flowfix = await agent(BASE + '\n\nROLE: Senior Principal QA Engineer (Sonnet) - W0 FLOW FIXES. The session seam is now implemented; its FINAL contract is in ' + OUT + '/62_SEAM_HARNESS.md (and summarized below). Align all 11 W0 flows (' + APP + '/.maestro/flows/jm-0*.yaml) to the implemented seam, and fix the 2 RC-2 flow bugs:\n1. Update each flow that uses jeeb.seam.* keys so the key names/values EXACTLY match what the harness implemented (per 62_SEAM_HARNESS.md). Where the harness says to use a fixed mock dev code (OTP 123456 / RECOVERY 654321) instead of a seam key, update the flow to type the fixed code.\n2. jm-006-splash-routing AC1: do NOT assert walkthrough_get_started_cta on first launch (it is only on slide 3). Assert walkthrough_slide_1 instead (first launch IS the walkthrough). For the get-started path, navigate slide_1 -> next -> next to reach slide 3 then assert walkthrough_get_started_cta.\n3. Raise EVERY cold-start extendedWaitUntil timeout (the first assertion after a launchApp/clearState) to 30000 ms across all 11 W0 flows (jm-010 AC2 specifically failed on a 15000 ms timeout).\n4. Keep flows asserting by Semantics id only (never visible text). Validate each YAML parses.\nReturn: the per-flow edits you made and confirmation all 11 parse.\n\n=== SEAM CONTRACT (from SeamHarness) ===\n' + seam, { label: 'w0:flow-fix', phase: 'FlowFix', model: 'sonnet' })

// ---------- Phase 3: Rebuild + re-run (Sonnet) ----------
phase('Rerun')
const rerun = await agent(BASE + '\n\nROLE: Senior Principal QA Engineer (Sonnet) - W0 RE-RUN. The seam harness (native + dart) and the flows changed, so a rebuild is required. Steps:\n1. Confirm mock up on :4010 (curl; start it from ' + APP + '/../jeeb-mock-backend with npm run dev if down). Ensure emulator jeeb_test is booted (boot it: ~/Library/Android/sdk/emulator/emulator -avd jeeb_test -no-snapshot-save -no-audio -no-boot-anim in background; adb wait-for-device; poll sys.boot_completed=1).\n2. Rebuild + install ONCE: cd ' + APP + ' ; /Users/oudaykhaled/flutter/bin/flutter build apk --debug --flavor dev --dart-define=JEEB_MOCK_BASE_URL=http://10.0.2.2:4010 ; ~/Library/Android/sdk/platform-tools/adb install -r build/app/outputs/flutter-apk/app-dev-debug.apk\n3. export JAVA_HOME via /usr/libexec/java_home. Re-run ALL 11 W0 flows (jm-005..jm-022) with ~/.maestro/bin/maestro --device emulator-5554 test -e APP_ID=app.jeeb.mobile.dev --format JUNIT <flow>. Capture per-flow PASS/FAIL + failing step + category (APP_DEFECT/PRECONDITION/MOCK_GAP/FLOW_BUG) for any remaining fails.\n4. Overwrite ' + OUT + '/61_W0_QA_RESULTS.md with the new run (keep the same structure; add a Run 2 header with date). \nReturn: the new per-flow results (PASS/FAIL counts), and for any remaining FAIL the category + precise owner action so the CTO can re-engage the right owner.', { label: 'w0:rerun', phase: 'Rerun', model: 'sonnet' })

// ---------- Phase 4: Sign-off (Opus) ----------
phase('Signoff')
const signoff = await agent(BASE + '\n\nROLE: Product Owner (Opus) - W0 SIGN-OFF (update). Using the re-run results below + the W0 ACs in 30_BACKLOG.md, write/update one sign-off per W0 item at ' + OUT + '/signoffs/JM-###.md: SIGNED for items whose flow now PASSES (AC-to-evidence table citing the green flow + DoD checklist), PARTIAL/BLOCKED for any still failing (exact failing AC + category + precise owner action). Explicitly state the status of the demo-critical four (JM-007 login, JM-009 OTP, JM-006 splash, JM-005 lock). Return: counts SIGNED vs PARTIAL/BLOCKED, demo-critical status, and the prioritized remaining-fix list (empty if W0 is fully closed).\n\n=== RE-RUN RESULTS ===\n' + rerun, { label: 'w0:signoff', phase: 'Signoff' })

return { seam_done: !!seam, flows_fixed: !!flowfix, rerun_summary: rerun, signoff_summary: signoff }
