export const meta = {
  name: 'jeeb-w0-code',
  description: 'Wave 0 code build: integrator wires auth routes+gates+DI+l10n+stubs, then parallel engineers implement the auth screens to contract, then static-green + reviewers',
  phases: [
    { title: 'Integrator', detail: 'W0-INT: routes, _firstRunRedirect, account-status gate, DI, l10n, stub screens, signoffs dir' },
    { title: 'Engineers', detail: 'parallel engineers implement each W0 auth screen body to the identifier + auth contract' },
    { title: 'StaticGreen', detail: 'flutter analyze + flutter test green (bounded fix loop)' },
    { title: 'Review', detail: 'per-item reviewers check diffs vs ACs and guardrails' },
  ],
}

const APP = '/Users/oudaykhaled/Desktop/olivium/jeeb/jeeb-mobile'
const OUT = APP + '/docs/build-out'
const COMMON = 'Jeeb mobile build-out, WAVE 0 (auth funnel + session/status gates). READ FIRST: ' + OUT + '/00_CTO_BRIEF.md (S6 contracts), ' + OUT + '/01_CTO_DECISIONS.md (CTO-D1 email-first sign-up to phone-OTP; email/password login; biometric for returning users), ' + OUT + '/40_GUARDRAILS_ARCH.md (how to add a screen/feature here), ' + OUT + '/41_GUARDRAILS_TESTING.md (Semantics identifier rule + run recipe), ' + OUT + '/42_GUARDRAILS_MOCK.md (esp. the section W-1 FLOOR CLOSED with the verified auth endpoint contract), ' + OUT + '/30_BACKLOG.md (Wave 0 items + Given/When/Then ACs), ' + OUT + '/50_EXECUTION_PLAN.md (WAVE 0 section + exact route table), ' + OUT + '/60_W0_TEST_PLAN.md (the EXACT Semantics identifiers every screen must expose). The red Maestro flows already exist at ' + APP + '/.maestro/flows/jm-0*.yaml - your code must make them satisfiable. App at ' + APP + '. Use OMDS components (omds_library). Clean architecture (data/domain/presentation), flutter_bloc, GoRouter, GetIt, Dio. Every interactive/asserted widget MUST carry Semantics(identifier: ...) using the EXACT id from 60_W0_TEST_PLAN.md. Assert nothing here; just build to contract. Do NOT invent product decisions (CTO-D covers the open ones; otherwise apply the least-surprising blueprint-consistent option and record it inline).'

// ---------- Phase 1: Integrator (serial, FIRST) ----------
phase('Integrator')
const integrator = await agent(COMMON + '\n\nROLE: W0 INTEGRATOR (Opus). You alone edit the shared files this wave; engineers will only touch their own feature files AFTER you finish, so you must leave the app COMPILING. Do exactly the W0 integrator edits from 50_EXECUTION_PLAN.md (the Exact W0 integrator route additions table) and 21_NAV_PLAN.md:\n1. lib/core/router/app_router.dart - ADD GoRoutes: /login (name login -> LoginScreen), /sign-up (name sign-up -> SignUpScreen), /recover (name recover-password -> RecoverPasswordScreen), nested /recover/verify (name recover-verify -> VerifyRecoveryCodeScreen), /set-password (name set-password -> SetPasswordScreen, reads mode query recovery|in-app-social), /account-status (name account-status -> AccountStatusScreen stub). REPLACE the /lock builder to use the real BiometricLockScreen. EDIT _firstRunRedirect for session-aware branches (JM-006): first-run -> /onboarding; logged-in customer -> / (last tab, D75); jeeber -> DELIVERY tab; biometric enabled -> /lock; logged-out returning -> /login; getMe status==suspended|locked -> /account-status (D5, JM-066 gate predicate). Keep the existing dev-seam behavior intact. IMPORTANT (from 42 W-1 FLOOR CLOSED): the mock authStub resolves ANY bearer to user-client-001 on GET /users/me, so identity-based splash routing (JM-006) must GET /users/:id by the persisted userId (from the auth response user.userId), NOT rely on /users/me. New auth datasources must POST the /v1/auth/... paths (the contract carries them to :4010).\n2. Create STUB screen classes so routes compile, at canonical paths: lib/features/auth/presentation/login_screen.dart (LoginScreen), sign_up_screen.dart (SignUpScreen), recover_password_screen.dart (RecoverPasswordScreen), verify_recovery_code_screen.dart (VerifyRecoveryCodeScreen), set_password_screen.dart (SetPasswordScreen); lib/features/account_status/presentation/account_status_screen.dart (AccountStatusScreen). Each stub = a minimal Scaffold whose root carries Semantics(identifier of the screen, e.g. login screen root has a child with id login_email_field placeholder) - keep it compiling but clearly TODO; engineers will replace the body.\n3. lib/core/di/injection_container.dart - register the real BiometricLockCubit and the auth login/recover/set-password repositories+datasources wired to the verified mock auth contract (42 W-1 FLOOR CLOSED). Use stub markers // INTEGRATOR-STUB(JM-###) only if a backend route is genuinely absent.\n4. lib/l10n/app_en.arb + app_ar.arb - add every i18n key the W0 screens need (login*, signup*, recover*, verifyCode*, setpw*, accountStatus*). Run flutter gen-l10n if the project uses it.\n5. Create the directory ' + OUT + '/signoffs/ (add a .gitkeep).\n6. Do NOT implement screen bodies (engineers do that) beyond compiling stubs.\nGATE: run `cd ' + APP + ' && /Users/oudaykhaled/flutter/bin/flutter analyze` until CLEAN on the files you touched and `flutter test test/` does not regress (note pre-existing failures). Confirm every new route resolves to its stub. Return: the exact routes/names added, the stub file paths created, DI registrations, l10n keys added, and analyze result.', { label: 'w0:integrator', phase: 'Integrator' })

// ---------- Phase 2: Engineers (parallel, disjoint files) ----------
phase('Engineers')
const ITEMS = [
  { jm: 'JM-007', label: 'login', file: 'lib/features/auth/presentation/login_screen.dart', note: 'Email+password login. Wire to the verified mock login endpoint; on success route to Requests tab (assert via shell_tab_requests). Password visibility toggle, forgot-password link -> /recover, signup link -> /sign-up, social CTAs (google/facebook/apple), biometric affordance for enrolled returning users (D23).' },
  { jm: 'JM-009', label: 'phone-otp', file: 'existing OTP verification screen (locate: grep otp_verification / phone-otp in lib/features; reuse it)', note: 'Re-parent the phone-OTP screen behind sign-up/social as the verify step. 6-digit input (phone_otp_input), verify CTA, resend with countdown. On verify success route to Requests tab. Add D23 returning-user bypass note. Do NOT create a new route (stays inside /register per plan).' },
  { jm: 'JM-005', label: 'biometric-unlock', file: 'lib/features/biometric_auth/presentation/biometric_lock_screen.dart (+ make BiometricLockCubit real, lift from lib/features/biometric_login/)', note: 'Real biometric unlock: prompt, authenticate CTA -> shell last tab, use-password link -> /login. D23 (no OTP on biometric unlock).' },
  { jm: 'JM-008', label: 'sign-up', file: 'lib/features/auth/presentation/sign_up_screen.dart', note: 'CTO-D1 email-first: name+email+password fields, password strength hint + visibility toggle, submit -> phone-OTP (JM-009). login link -> /login, social row, 409 email collision -> social-collision sheet (JM-019). Email NOT verified here (D21).' },
  { jm: 'JM-018', label: 'social-login', file: 'lib/features/auth/social/ (extend; native sheet/dialog, no new route)', note: 'FB+Google+Apple. Social success with no phone on file -> phone-OTP (G8). 409 -> social-collision sheet. Provide identifiers login_social_* / signup_social_*.' },
  { jm: 'JM-019', label: 'collision-sheet', file: 'lib/features/auth/presentation/social_collision_sheet.dart (sheet/dialog, no route)', note: 'social_collision_sheet: continue CTA -> /login, other-email CTA -> /sign-up (D22).' },
  { jm: 'JM-020', label: 'recover-password', file: 'lib/features/auth/presentation/recover_password_screen.dart', note: 'recover_email_field + submit -> /recover/verify (code requested via mock). signup + back-to-signin links.' },
  { jm: 'JM-021', label: 'verify-code', file: 'lib/features/auth/presentation/verify_recovery_code_screen.dart', note: 'verify_code_input (reuse OmdsOtpInput) + submit -> /set-password?mode=recovery. wrong/expired shows verify_code_error; resend stays on screen. NOT phone-anchored.' },
  { jm: 'JM-022', label: 'set-password', file: 'lib/features/auth/presentation/set_password_screen.dart', note: 'setpw_new_field+setpw_confirm_field (both eye toggles), submit: mode=recovery -> /login; mode=in-app-social -> customer-profile (D90). Mismatch/strength -> setpw_validation_error.' },
  { jm: 'JM-010', label: 'walkthrough', file: 'lib/features/onboarding/onboarding_screen.dart (+ presentation/onboarding_screen.dart)', note: 'Expose walkthrough_slide_1/2/3, walkthrough_next_cta, walkthrough_get_started_cta, walkthrough_skip_cta. Get Started / Skip -> /sign-up (CTO-D1). Keep the onboarding_next_button id the foundation added.' },
  { jm: 'JM-006', label: 'splash', file: 'lib/features/* branded splash widget (routing is in the integrator; you only ensure the splash widget exposes no blocking dwell, D79/D85)', note: 'Splash routing logic already lives in _firstRunRedirect (integrator). Ensure the splash/branded widget renders without an artificial dwell and does not fight the redirect.' },
]
const engResults = await parallel(ITEMS.map((it) => () =>
  agent(COMMON + '\n\nROLE: Senior Principal Flutter Engineer (Opus). Implement ' + it.jm + ' (' + it.label + '). Target file(s): ' + it.file + '. ' + it.note + '\n\nRead the ' + it.jm + ' ACs in 30_BACKLOG.md and the exact identifiers for this screen in 60_W0_TEST_PLAN.md - expose EVERY one via Semantics(identifier:). Wire data via the verified mock auth contract in 42 (W-1 FLOOR CLOSED) using the existing Dio/datasource patterns (40_GUARDRAILS_ARCH.md). Touch ONLY your own feature file(s); do NOT edit app_router.dart, injection_container.dart, the .arb files, or shell_screen.dart (the integrator already wired routes/DI/l10n - reference the keys/routes it created, see integrator output below). If you need a key/route that is missing, append a line to ' + OUT + '/50_ROUTE_REQUESTS.md and code against the intended name. Do NOT run ANY flutter command (no analyze, no test, no build, no emulator) - a dedicated serial StaticGreen phase compiles everything and a later QA pass does device verification; concurrent flutter runs in one tree contend. Just write correct, compiling code to contract. Return: files changed, the exact Semantics identifiers you exposed (must match 60_W0_TEST_PLAN), endpoints wired, and any contract gaps you hit.\n\n=== INTEGRATOR OUTPUT (routes/DI/l10n/stubs already in place) ===\n' + integrator,
    { label: 'eng:' + it.label, phase: 'Engineers' })
)).then(r => r)

// ---------- Phase 3: Static green (serial) ----------
phase('StaticGreen')
const staticGreen = await agent(COMMON + '\n\nROLE: Build Integration Engineer (Opus). All W0 engineers have implemented their screens in their own feature files. Make the project STATICALLY GREEN. Run `cd ' + APP + ' && /Users/oudaykhaled/flutter/bin/flutter analyze`. Fix any compile errors / new analyze issues introduced by the W0 work (import errors, missing l10n keys, signature mismatches, unused imports). If a fix requires a real engineering decision in a screen you do not own, make the minimal correct fix and note it. Then run `flutter test test/` and report pass/fail (pre-existing failures: the inter_font_weight test and any mock_gateway tests already updated by Foundation are not your concern unless your wave touched them). Do a bounded effort (do not rewrite features). Return: final `flutter analyze` result (must be CLEAN of errors), `flutter test` summary, and a list of any files you had to fix and why. If something cannot be made green without a screen rewrite, name the JM item + file + exact error so the next iteration can re-engage that engineer.\n\n=== ENGINEER OUTPUTS ===\n' + JSON.stringify(engResults.map((r, i) => ({ jm: ITEMS[i].jm, summary: typeof r === 'string' ? r.slice(0, 1500) : r }))), { label: 'w0:static-green', phase: 'StaticGreen' })

// ---------- Phase 4: Reviewers (parallel) ----------
phase('Review')
const reviews = await parallel(ITEMS.map((it, i) => () =>
  agent(COMMON + '\n\nROLE: Senior Principal Flutter Engineer - CODE REVIEW (Opus) for ' + it.jm + ' (' + it.label + '). Review the implementation in ' + it.file + ' against: (1) the ' + it.jm + ' ACs in 30_BACKLOG.md; (2) the identifier contract in 60_W0_TEST_PLAN.md (every required Semantics id present + exact spelling, no leading underscores); (3) 40_GUARDRAILS_ARCH.md (clean-arch layering, bloc usage, no shared-file edits, OMDS components, no hardcoded strings - uses l10n); (4) navigation honesty (targets are real routes). Read the actual file(s). Return a verdict object: {jm, approved: true|false, must_fix: [..], nits: [..], identifiers_ok: true|false}. Be strict but only block on real AC/guardrail violations.\n\n=== ENGINEER SUMMARY ===\n' + (typeof engResults[i] === 'string' ? engResults[i].slice(0, 1500) : JSON.stringify(engResults[i])),
    { label: 'review:' + it.label, phase: 'Review' })
)).then(r => r)

return {
  integrator_done: !!integrator,
  engineers: engResults.length,
  static_green: staticGreen,
  reviews_count: reviews.filter(Boolean).length,
}
