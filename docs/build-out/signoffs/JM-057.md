# JM-057 — Notifications List (inbox + deep-link dispatch)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W4 FINAL sign-off). **Wave:** W4.
> **Flow:** `.maestro/flows/jm-057-notifications-list.yaml`.
> **Authoritative run (`68_W34_QA_RESULTS.md` Batch A #4):** **FAIL** — `notif_row_notif-001` not visible.
> **Demo-critical:** P0.

## STATUS: BLOCKED — PRECONDITION (missing seam/journey seed + stable notif ids). The notifications inbox cannot render typed rows because the `has_notifications` journey seed is not implemented and the stable notif ids are not seeded. No app defect proven.

The notifications-list screen depends on seeded notification rows. The flow launches with journey
`has_notifications` and asserts `notif_row_notif-001`, but the journey seed is not wired in the seam
bootstrap and the stable ids (`notif-offers-001`, `notif-wallet-001`, `notif-status-001`) are not
seeded, so the inbox shows empty and the row assertion times out. This is a seam + mock-seed item,
not an app defect — the screen shell + bell entry are built (W3/W4 integrator).

### AC → Evidence

| AC | Requirement | Evidence (Batch A #4) | Result |
|----|-------------|-----------------------|--------|
| AC | typed `notif_row_<id>` (offer/accepted/status/low-balance/fee/refund-penalty/topup/KYC) with timestamp; tap dispatches per D84; empty state; mark-read | `notif_row_notif-001` not visible — no rows seeded | **FAIL — PRECONDITION (missing seed)** |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: notif_row_notif-001 is visible`.
- **Category:** **PRECONDITION** — missing seed.

### Missing seed (name exactly — do NOT fix in QA)

- **Seam/journey seed:** `jeeb.seam.journey=has_notifications` (`POST /__mock/seed/journey {"journey":"has_notifications"}` not implemented).
- **Stable ids:** `notif-offers-001`, `notif-wallet-001`, `notif-status-001` must be seeded by the journey fixture so the deep-link dispatch ACs are assertable.

### Owner action to close

1. **backender (mock), P0:** add the `has_notifications` fixture to `jeeb-mock-backend/src/fixtures/journey-seed.ts` so `GET /notification-service/v1/notifications?userId=` returns the typed rows with the stable ids above.
2. **seam (app), P0:** add `has_notifications` to the `JourneySeed` landing contract (`lib/core/dev_seam/`) so the seed POST fires and the route pins to `/notifications`.
3. **QA (JM-057), P1:** after the seed lands, re-run; verify typed rows + per-D84 deep-link dispatch + mark-read.

### Definition of Done checklist (CTO brief §10)

- [x] notifications-list route + screen shell + header bell entry built (W3/W4 integrator).
- [ ] `has_notifications` journey seed + stable notif ids — **BLOCKED (backender + seam)**.
- [ ] typed rows + deep-link dispatch + mark-read (AC) — not reachable until seed lands.
- [ ] **Maestro flow passes** — FAIL at `notif_row_notif-001` (PRECONDITION).
- [ ] **PO signed off** — **BLOCKED** on the `has_notifications` seed + stable notif ids.
