# JM-055 — Wallet Activity List (typed ledger)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W3 FINAL sign-off). **Wave:** W3.
> **Flow:** `.maestro/flows/jm-055-wallet-activity.yaml`.
> **Authoritative run (`68_W34_QA_RESULTS.md` Batch A #2):** **FAIL** — `wallet_activity_row_ledger-row-reserve-001` not visible.
> **Demo-critical:** P1.

## STATUS: BLOCKED — PRECONDITION (missing seam/journey seed + mock endpoint). The typed-ledger rows cannot render because the ledger journey seed and the W2m ledger endpoint are not implemented. No app defect proven.

The wallet-activity-list screen depends entirely on seeded ledger data. The flow launches with
journey `jeeber_wallet_ledger` and asserts a typed row id (`wallet_activity_row_ledger-row-reserve-001`),
but the journey seed is not wired in the seam bootstrap and the backing ledger endpoint (W2m) does
not exist, so no rows are returned and the assertion times out. This is the blocking question #2
(wallet data contract W2m) in `30_BACKLOG.md §Blocking product questions` — a backender + seam item,
not an app defect.

### AC → Evidence

| AC | Requirement | Evidence (Batch A #2) | Result |
|----|-------------|-----------------------|--------|
| AC | `wallet_activity_row_<id>` typed rows (Reserve/Fee-won/Released/Refund/Penalty/Top up/Gift) amount+sign+icon+ref; infinite scroll + skeletons [D73]; tap → `transaction-detail` [JM-056] | `wallet_activity_row_ledger-row-reserve-001` not visible — no ledger rows seeded | **FAIL — PRECONDITION (missing seed)** |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: wallet_activity_row_ledger-row-reserve-001 is visible`.
- **Category:** **PRECONDITION** — missing seed.

### Missing seed (name exactly — do NOT fix in QA)

- **Seam/journey seed:** `jeeb.seam.journey=jeeber_wallet_ledger` (seam bootstrap must accept and route-pin it; `POST /__mock/seed/journey {"journey":"jeeber_wallet_ledger"}` not implemented).
- **Mock endpoint (W2m):** `GET /wallet-service/v1/jeeb/wallet/ledger` returning typed rows with stable ids (`ledger-row-reserve-001`, etc.).

### Owner action to close

1. **backender (mock), P0 — W2m:** define the wallet ledger endpoint `GET /wallet-service/v1/jeeb/wallet/ledger` with typed rows + stable ids, and add the `jeeber_wallet_ledger` journey fixture to `jeeb-mock-backend/src/fixtures/journey-seed.ts`.
2. **seam (app), P0:** add `jeeber_wallet_ledger` to the `JourneySeed` enum + landing contract (`lib/core/dev_seam/`) so the seam route-pins the wallet-activity screen and the mock-seed POST fires.
3. **QA (JM-055), P1:** after the seed + endpoint land, re-run; verify typed rows render and tap → `transaction-detail` (JM-056).

### Definition of Done checklist (CTO brief §10)

- [x] wallet-activity-list route + screen shell built (W3/W4 integrator).
- [ ] `jeeber_wallet_ledger` journey seed + W2m endpoint — **BLOCKED (backender + seam)**.
- [ ] typed rows + infinite scroll + tap-to-detail (AC) — not reachable until seed lands.
- [ ] **Maestro flow passes** — FAIL at first row assertion (PRECONDITION).
- [ ] **PO signed off** — **BLOCKED** on the `jeeber_wallet_ledger` seed + W2m endpoint.
