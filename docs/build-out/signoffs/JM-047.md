# JM-047 — Jeeber Pending Offers (submitted-offers list + withdraw)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W3/W4 + W2-residual FINAL sign-off). **Wave:** W2.
> **Flow:** `.maestro/flows/jm-047-jeeber-pending-offers.yaml`.
> **Authoritative run (`68_W34_QA_RESULTS.md` Batch B #17):** **PASS** — all 4 ACs green. RD-2 (`pending_offers_back`) is now resolved.
> **Demo-critical:** P1.

## STATUS: SIGNED — all ACs green. RD-2 fixed: `pending_offers_back` Semantics id is now present; the pending tab is fully reachable and the withdraw + back edges both assert.

The prior W2 residual-verify FAIL (RD-2: missing `pending_offers_back` id) is **closed**. The
clean-install W3/W4 run confirms the full flow: the pending tab is reachable
(`jeeber_feed_root` + `jeeber_feed_pending_tab`), `pending_offer_0` shows price/ETA/awaiting-label
plus the withdraw CTA (AC1), the withdraw CTA is present (AC2), `pending_offer_0_withdraw_cta`
removes the offer (AC3, `notVisible` after withdraw passed), and the back control now carries the
`pending_offers_back` Semantics id and returns to `shell_tab_dashboard` (AC4).

### AC → Evidence

| AC | Requirement | Evidence (W3/W4 run, Batch B #17) | Result |
|----|-------------|-----------------------------------|--------|
| AC1 | pending tab reachable + `pending_offer_0` shows price + ETA + "Awaiting customer decision" [D15] | pending tab + row content visible | **PASS** |
| AC2 | `pending_offer_0_withdraw_cta` present | withdraw CTA visible | **PASS** |
| AC3 | withdraw removes the offer | `notVisible` after withdraw passed | **PASS** |
| AC4 | back → `delivery-requests` (`pending_offers_back`) | back returns to `shell_tab_dashboard`; `pending_offers_back` now present | **PASS** |

### Result + category

- **Result:** PASS (no failing step). RD-2 carry-over from the W2 residual verify is resolved.
- **Category:** — (green).

### Definition of Done checklist (CTO brief §10)

- [x] pending tab + `pending_offer_0` rows + withdraw CTA built; AC1-3 (rows + withdraw remove) — verified green.
- [x] AC4 `pending_offers_back` Semantics id placed on the back control — **PASS** (RD-2 fixed).
- [x] **Maestro flow passes** — all 4 ACs green.
- [x] **PO signed off** — **SIGNED**.
