# JM-026 — Waiting / No-Coverage state

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (FINAL, W1-QA reliable re-run). **Wave:** W1.
> **Flow:** `.maestro/flows/jm-026-waiting-no-coverage.yaml`.
> **Run result:** **FAIL — APP_DEFECT** (`64_W1_QA_RESULTS.md` row 9).
> **Demo-critical:** breadth (the waiting state is the holding screen after broadcast; the demo
> happy path jumps from broadcast to offers, but this screen renders the broadcast→offers transition).

## STATUS: BLOCKED — APP_DEFECT (`waiting_review_offers_cta` absent after `offers_received` seed)

`64_W1_QA_RESULTS.md` (authoritative, complete — row 9) records jm-026 **FAIL** at
`Assertion is false: id: waiting_review_offers_cta is visible` — the waiting screen is reached but
the review-offers CTA is not present when offers should exist after the `offers_received` journey
seed. The broadcast→offers transition (AC2) is the failing leg: the waiting screen does not surface
`waiting_review_offers_cta`, so the offers-arrived state is not reflected in the UI and the route to
`offer-review-list` cannot be exercised.

### AC → Evidence

| AC | Requirement | Evidence (`64_W1_QA_RESULTS.md` row 9) | Result |
|----|-------------|----------------------------------------|--------|
| AC1 | `waiting_notified_count` + `waiting_countdown` render; no-coverage variant when 0 notified | waiting screen reached (not the failing step) | NOT-CONFIRMED |
| AC2 | offers arrive → `waiting_review_offers_cta` → `offer-review-list` | `waiting_review_offers_cta` NOT visible after `offers_received` seed | **FAIL — APP_DEFECT** |
| AC3 | `waiting_retarget_cta` → request-type [D48]; `waiting_cancel_cta` → cancel sheet [D69] | `waiting_cancel_cta` is itself absent (see JM-030 row 13, FAIL) | NOT-REACHED |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: waiting_review_offers_cta is visible`.
- **Category:** **APP_DEFECT** — the waiting screen does not surface the review-offers CTA when the
  `offers_received` seed indicates offers are present. (Sibling defect: `waiting_cancel_cta` is also
  absent on this screen — see JM-030 row 13 — so the waiting screen's CTA set is incomplete overall.)

### Owner action to close

1. **app-eng (JM-026 AC2), P0:** in the rewritten `no_offer_timeout_screen.dart` → waiting screen
   (`/requests/:id/waiting`), render `waiting_review_offers_cta` with
   `Semantics(identifier: 'waiting_review_offers_cta', container: true)` and show it when the
   request has offers (state derived from `GET /offer-service/v1/offers?requestId=` /
   `POST /matching/v1/matching/broadcast`); tap → `offer-review-list` (`offer_review_list_root`,
   JM-028). Also restore `waiting_cancel_cta` (absent — see JM-030 row 13) on the same screen.
   Confirm both ids via `maestro hierarchy` under the `offers_received` seed. File:
   `lib/features/.../no_offer_timeout_screen.dart`.
2. **backender (mock), P0:** confirm `jeeb.seam.journey=offers_received` seeds
   `GET /offer-service/v1/offers?requestId=req-client-001-offers` with ≥2 offers (per
   `63_W1_TEST_PLAN §4.1/§4.3`) so the waiting screen can compute the offers-arrived state and reveal
   `waiting_review_offers_cta`. File: `jeeb-mock-backend/src/fixtures/` + the journey-seed branch.
3. **QA (JM-026), P1:** once the CTA renders, exercise AC2 (review → `offer_review_list_root`) and
   AC1 (`waiting_notified_count` + `waiting_countdown`) and AC3 (retarget/cancel) edges.
   `.maestro/flows/jm-026-waiting-no-coverage.yaml`.

### Definition of Done checklist (CTO brief §10)

- [ ] `waiting_review_offers_cta` present + routes to offer list when offers exist (AC2) — MISSING.
- [ ] `waiting_notified_count` + `waiting_countdown` (AC1) — unconfirmed.
- [ ] `waiting_cancel_cta` (AC3, shared with JM-030) — also absent (row 13).
- [ ] **Maestro flow passes** — jm-026 **FAIL** at AC2 `waiting_review_offers_cta` (row 9).
- [ ] Reviewer approved / **PO signed off** — BLOCKED pending the review-offers CTA + offers-received seed.
