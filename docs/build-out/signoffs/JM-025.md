# JM-025 — Order Chat (compose=broadcast, pinned summary, dispute link)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (FINAL, W1-QA reliable re-run). **Wave:** W1.
> **Flow:** `.maestro/flows/jm-025-order-chat.yaml`.
> **Run result:** **FAIL — APP_DEFECT** (`64_W1_QA_RESULTS.md` row 8).
> **Demo-critical:** **YES** — second leg of the customer demo path (chat compose → broadcast; and the dispute link).

## STATUS: PARTIAL — APP_DEFECT (`order_chat_open_dispute` not present on accepted-order chat)

`64_W1_QA_RESULTS.md` records jm-025 **FAIL** at
`Assertion is false: id: order_chat_open_dispute is visible` — the dispute link (AC3) is not
present on the accepted-order chat screen. The compose state is reached and the chat is otherwise
rendered; the failing assertion is specifically the dispute CTA.

> **Note vs prior residue:** the earlier (residue-only) read had jm-025 failing at AC1
> `waiting_notified_count`. The authoritative artifact fails it at AC3 `order_chat_open_dispute`,
> meaning AC1 (compose → broadcast → waiting) and AC2 (pinned summary) got further this run. The
> AC3 target is the dispute-open-evidence entry — note `dispute_reason` (the dispute screen itself)
> is a **W4 / JM-060** target, but `order_chat_open_dispute` (the link on the chat) is JM-025's own
> obligation and is the missing id.

### AC → Evidence

| AC | Requirement | Evidence | Result |
|----|-------------|----------|--------|
| AC1 | First msg via `order_chat_composer_send` → broadcasts → `waiting-no-coverage` | reached (not the failing assertion this run) | PASS (likely — upstream of AC3) |
| AC2 | Accepted order → `order_chat_pinned_summary` + `order_chat_view_summary_link` → summary | reached (not the failing assertion) | PASS (likely) |
| AC3 | Active order → `order_chat_open_dispute` → dispute-open-evidence [JM-060] | `order_chat_open_dispute` NOT visible | **FAIL — APP_DEFECT** |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: order_chat_open_dispute is visible`.
- **Category:** **APP_DEFECT** — the dispute CTA is missing from the accepted/active-order chat screen.

### Owner action to close

1. **app-eng (JM-025 AC3), P0:** add the `order_chat_open_dispute` link to the accepted/active-order
   chat screen in `lib/features/chat/...` (the `chat_screen.dart` / `chat_detail_screen.dart`
   surface), with `Semantics(identifier: 'order_chat_open_dispute', container: true)`, visible when
   the order is accepted/active. Its tap target → `dispute-open-evidence` (JM-060, W4); on the dev
   build the destination (`dispute_reason`) is a W4 screen, so per AP-9 the flow asserts the link is
   present + tap accepted, not the W4 destination. Verify the id via `maestro hierarchy` under the
   `order_accepted` / `active_delivery` journey seed.
2. **QA (JM-025), P1:** confirm AC1/AC2 are independently green under `order_accepted` seed (they
   were not the failing step this run), and scope the AC3 assertion to the link presence (not the
   W4 dispute screen) per AP-9.

### Definition of Done checklist (CTO brief §10)

- [x] Chat compose + accepted-order summary reached (AC1/AC2 not the failing step).
- [ ] `order_chat_open_dispute` link present on accepted/active order (AC3) — MISSING.
- [ ] **Maestro flow passes** — FAILS at AC3 dispute link.
- [ ] Reviewer approved / **PO signed off** — PARTIAL pending the AC3 dispute-link id.
