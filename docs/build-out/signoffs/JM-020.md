# JM-020 — Recover Password (request code)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18. **Wave:** W0.
> **Flow:** `.maestro/flows/jm-020-recover-password.yaml`. **Run result:** **PASS** (`61_W0_QA_RESULTS.md` Run 2 + Run 3 — held green).
> **Demo-critical:** No (breadth — recovery flow).

## STATUS: SIGNED

All three ACs pass on-device (Run 2, held green in Run 3). The Run 1 PRECONDITION blocker (RC-1 —
`jeeb.seam.session=logged_out_returning` dropped, so the login entry to `/recover` was
never reachable) is resolved: the session seam is wired, login is reachable via the
`logged_out_returning` seed, and the `/recover` screen exercises cleanly through all three
edges.

### AC → Evidence

| AC | Requirement (from `30_BACKLOG.md`) | Flow evidence | Result |
|----|-----------------------------------|---------------|--------|
| AC1 | `recover_email_field` filled + `recover_submit_cta` → `/recover/verify` (`verify_code_input`) [JM-021] | submit → `verify_code_input` | **PASS** |
| AC2 | `recover_signup_link` → sign-up (`signup_name_field`) | sign-up link → `signup_name_field` | **PASS** |
| AC3 | `recover_back_to_signin_link` → `/login` (`login_email_field`) | back-to-signin → `login_email_field` | **PASS** |

### Exact failing AC + QA category

- None. All ACs PASS (`61_W0_QA_RESULTS.md` Run 2 row jm-020).

### Definition of Done checklist (CTO brief §10)

- [x] Screen matches blueprint + decisions (`recover-password`; D85; non-enumerating per W-1 FLOOR).
- [x] OMDS components used.
- [x] Nav wired both directions (→ `/recover/verify`; → sign-up; → `/login`) — all three proven on-device.
- [x] Wired to mock (request-recovery-code route, B3).
- [x] `Semantics(identifier:)` present (`recover_email_field`/`_submit_cta`/`_signup_link`/`_back_to_signin_link`).
- [x] **Maestro flow passes on emulator** — PASS (Run 2, all 3 ACs).
- [x] `flutter analyze` clean (gate held through Run 2 rebuild).
- [x] Reviewer approved (shared session seam landed).
- [x] **PO signed off.**
