# JM-033 — Confirm Receipt (Customer)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (FINAL, W1-QA reliable re-run). **Wave:** W1.
> **Flow:** `.maestro/flows/jm-033-confirm-receipt.yaml`.
> **Run result:** **FAIL — APP_DEFECT** at `receipt_prompt` (`64_W1_QA_RESULTS.md` Batch B row 16).
> **Demo-critical:** **YES** — the RECEIPT step of the customer demo path (confirm receipt → rate).

## STATUS: BLOCKED — APP_DEFECT (`receipt_prompt` not rendering under `delivery_marked_done` seed)

`64_W1_QA_RESULTS.md` (authoritative, complete — row 16) records jm-033 **FAIL** at
`Assertion is false: id: receipt_prompt is visible` — the receipt-confirm screen is **not reached**:
the `/orders/:id/receipt` route does not render, or `receipt_prompt` is absent, under the
`delivery_marked_done` seed. This is the **W1-owned core** of jm-033 (AC1's signature id) and it
fails at the first W1 assertion — a genuine **APP_DEFECT**, not the W4-dispute over-assert an earlier
read attributed to it. The orphaned `delivery_receipt_screen.dart` rewrite (NEW `/orders/:id/receipt`)
is not rendering its contract id under the seed.

### AC → Evidence

| AC | Requirement | Evidence (`64_W1_QA_RESULTS.md` row 16) | Result |
|----|-------------|------------------------------------------|--------|
| AC1 | `receipt_prompt` "Did you receive your order?" + "Pay $N cash to <Jeeber>" + proof photo [D3] | `receipt_prompt` NOT visible (route not rendering under `delivery_marked_done`) | **FAIL — APP_DEFECT** |
| AC2 | `receipt_confirm_cta` → `rate-jeeber` (`rating_submit_cta`) | not reached | NOT-REACHED |
| AC3 | `receipt_not_yet_cta` → `dispute-open-evidence` (`dispute_reason`) [JM-060, **W4**] | not reached; NOTE W4 cross-wave id — assert AP-9-honestly per `63_W1_TEST_PLAN §2.19` | NOT-REACHED |
| AC4 | `receipt_no_commission_line` NOT visible (customer-facing) [D11] | not reached | NOT-VERIFIED |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: receipt_prompt is visible`.
- **Category:** **APP_DEFECT** — the receipt-confirm route/screen is not reached / does not render
  `receipt_prompt` under the `delivery_marked_done` seed. This also blocks jm-034 (rating), which
  reaches rating via this receipt screen (jm-034 reds at `receipt_confirm_cta` — row 17).

### Owner action to close

1. **app-eng (JM-033 AC1), P0 — unblocks jm-034 too:** in the NEW `/orders/:id/receipt` (rewriting
   `delivery_receipt_screen.dart`), ensure the route is the landed screen under
   `jeeb.seam.journey=delivery_marked_done` and renders `receipt_prompt` with
   `Semantics(identifier: 'receipt_prompt', container: true)` + the cash-to-jeeber copy + proof
   photo (D3) + `receipt_confirm_cta` (→ `rate-jeeber`) + `receipt_not_yet_cta`, and ensure NO
   `receipt_no_commission_line` (AC4, customer-facing per D11). Confirm via `maestro hierarchy`.
2. **backender (mock + D1m), P0:** confirm `jeeb.seam.journey=delivery_marked_done` seeds the
   delivered-state delivery so `/orders/:id/receipt` is the resolved route; and the proof-photo sink
   (D1m) lands for the AC1 photo. File: `jeeb-mock-backend/src/fixtures/` + the seed branch.
3. **QA (JM-033), P1 — after AC1 renders:** verify AC2 confirm → `rating_submit_cta`, AC4 negative
   (`receipt_no_commission_line` absent), and make AC3 AP-9-honest (`receipt_not_yet_cta` tap-accepted
   + `receipt_prompt` survives; `dispute_reason` is W4/JM-060, defer to that flow).
   `.maestro/flows/jm-033-confirm-receipt.yaml`.

### Definition of Done checklist (CTO brief §10)

- [ ] `receipt_prompt` + cash copy + proof photo rendered under `delivery_marked_done` (AC1) — FAILS.
- [ ] `receipt_confirm_cta` → rate (AC2); AC4 `receipt_no_commission_line` absent — not reached.
- [ ] AC3 dispute edge AP-9-honest (not the W4 screen) — to author once AC1 renders.
- [ ] **Maestro flow passes** — jm-033 **FAIL** at `receipt_prompt` (row 16).
- [ ] Reviewer approved / **PO signed off** — BLOCKED pending the receipt route render + `delivery_marked_done` seed + D1m sink.
