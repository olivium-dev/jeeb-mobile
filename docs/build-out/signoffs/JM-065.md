# JM-065 — Dispute Status

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W4 FINAL sign-off). **Wave:** W4.
> **Flow:** `.maestro/flows/jm-065-dispute-status.yaml`.
> **Authoritative run (`68_W34_QA_RESULTS.md` Batch B #12):** **FAIL** — `dispute_status_state` not visible.
> **Demo-critical:** P1.

## STATUS: BLOCKED — PRECONDITION (missing seam/journey seed + dispute mock data). The dispute-status screen needs a seeded open dispute; the `has_open_dispute` journey seed and the dispute-by-id mock endpoint are not implemented. No app defect proven.

The dispute-status screen renders a specific seeded dispute (`dispute-client-001`). The flow launches
with journey `has_open_dispute` + route pin `/disputes/dispute-client-001` and asserts
`dispute_status_state`, but the journey seed is not wired and the dispute-by-id endpoint does not
return seeded data, so the screen has nothing to resolve and the first assertion times out. Seam +
backender item, not an app defect.

### AC → Evidence

| AC | Requirement | Evidence (Batch B #12) | Result |
|----|-------------|------------------------|--------|
| AC1 | `dispute_status_state` (Open/Resolved) + outcome note (refund/penalty, D2) + evidence summary [D53] | `dispute_status_state` not visible — no seeded dispute | **FAIL — PRECONDITION (missing seed)** |
| AC2 | `dispute_status_support` → `support-ticket`; `dispute_status_back` → `order-chat` | not reached | not reached |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: dispute_status_state is visible` (launch with `journey=has_open_dispute` + `/disputes/dispute-client-001`).
- **Category:** **PRECONDITION** — missing seed.

### Missing seed (name exactly — do NOT fix in QA)

- **Seam/journey seed:** `jeeb.seam.journey=has_open_dispute` (`POST /__mock/seed/journey {"journey":"has_open_dispute"}` not implemented).
- **Mock endpoint:** `GET /compliment-service/v1/disputes/dispute-client-001` (and `GET /compliment-service/v1/disputes?status=&userId=`) returning seeded dispute data.

### Owner action to close

1. **backender (mock), P0:** add the `has_open_dispute` fixture to `jeeb-mock-backend/src/fixtures/journey-seed.ts` and wire `GET /compliment-service/v1/disputes/:disputeId` to return it.
2. **seam (app), P0:** add `has_open_dispute` to the `JourneySeed` landing contract (`lib/core/dev_seam/`) so the seed POST fires and the route pins `/disputes/:id`.
3. **QA (JM-065), P1:** after the seed lands, re-run; verify `dispute_status_state` + AC2 support/back links.

### Definition of Done checklist (CTO brief §10)

- [x] dispute-status route + screen shell built (W3/W4 integrator).
- [ ] `has_open_dispute` journey seed + dispute mock data — **BLOCKED (backender + seam)**.
- [ ] state + outcome + links (AC) — not reachable until seed lands.
- [ ] **Maestro flow passes** — FAIL at `dispute_status_state` (PRECONDITION).
- [ ] **PO signed off** — **BLOCKED** on the `has_open_dispute` seed + dispute mock data.
