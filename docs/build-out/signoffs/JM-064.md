# JM-064 — Rate the App (native sheet)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W4 FINAL sign-off). **Wave:** W4.
> **Flow:** `.maestro/flows/jm-064-rate-the-app.yaml`.
> **Authoritative run (`68_W34_QA_RESULTS.md` Batch B #11):** **PASS** — all ACs green.
> **Demo-critical:** P2.

## STATUS: SIGNED — all ACs green. The Rate-app row invokes the native store-review handler and returns to Profile (the profile anchor `customer_profile_wallet_chip` survives the round trip).

The clean-install W3/W4 run confirms the full flow: tapping `customer_profile_rate_app_row` invokes
the native store-review sheet (the `in_app_review` thin handler fires; the native sheet is a no-op /
not visually asserted in the emulator, per AP-9 convention) and control returns to the Profile tab —
`customer_profile_wallet_chip` survives, proving the row tap was accepted and the app did not
navigate away or crash.

### AC → Evidence

| AC | Requirement | Evidence (W3/W4 run, Batch B #11) | Result |
|----|-------------|-----------------------------------|--------|
| AC1 | `customer_profile_rate_app_row` tap → native store-review sheet invoked; returns to Profile | rate-app row tap accepted; `customer_profile_wallet_chip` survives | **PASS** |

### Result + category

- **Result:** PASS (no failing step).
- **Category:** — (green).

### Definition of Done checklist (CTO brief §10)

- [x] Rate-app row → native `in_app_review` handler wired; returns to Profile.
- [x] **Maestro flow passes** — all ACs green.
- [x] **PO signed off** — **SIGNED**.
