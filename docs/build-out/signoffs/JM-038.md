# JM-038 — Service-Area home-base pin (remove distance slider, D51) + chain to KYC

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W2 FINAL sign-off). **Wave:** W2.
> **Flow:** `.maestro/flows/jm-038-service-area-homebase-pin.yaml`.
> **Authoritative run (`66_W2_QA_RESULTS.md` §POST-FIX RE-RUN):** **GREEN** — full flow (33 steps), photo-pick + home-base pin sequence.
> **Demo-critical:** YES — jeeber onboarding leg.

## STATUS: SIGNED ✅

The POST-FIX RE-RUN lands JM-038 GREEN end-to-end (33 steps). The upstream C1 wizard-nav block
(JM-039) is resolved, so the service-area step is reached: the distance slider is removed (D51,
`dm_onboarding_distance_slider` assertNotVisible), the home-base `service_area_map_pin` is required
and set via the pin sequence, and `dm_onboarding_continue` chains to `kyc_wizard_root` (not the Fake
gateway).

### AC → Evidence

| AC | Requirement | Evidence (POST-FIX RE-RUN) | Result |
|----|-------------|----------------------------|--------|
| AC1 | NO `dm_onboarding_distance_slider` [D51]; `service_area_map_pin` required; `dm_onboarding_continue` → `kyc_wizard_root` | slider absent + pin set + continue → KYC | **PASS** |

### Exact result + category

- **Result:** GREEN (full 33-step flow). Distance slider removed (D51); home-base pin required; chains to KYC.
- **Category:** — (no defect).

### Owner action to close

None — SIGNED.

### Definition of Done checklist (CTO brief §10)

- [x] `dm_onboarding_service_area_root` placed + reached via the advancing wizard — verified green.
- [x] distance slider removed (D51) + home-base pin required + continue → `kyc_wizard_root` — verified green.
- [x] **Maestro flow passes** — GREEN (POST-FIX RE-RUN).
- [x] **PO signed off** — **SIGNED**.
