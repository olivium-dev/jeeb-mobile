# JM-034 — Rating (mutual; remove skip; wire submit)

> **✅ FINAL VERIFY 2026-06-19 — SIGNED (GREEN).** Fresh APK, maestro EXIT 0. All ACs pass incl.
> R-2 (`rating_submit_cta`→`orders_home_new_order_fab`; jeeber→`shell_tab_dashboard`). Submit nav
> was already wired; the flow now selects a star (mandatory-rating gate) before submitting. Stale
> STATUS header below superseded. See `69_REGRESSION.md` §"Final Verify".

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (FINAL, W1-QA reliable re-run). **Wave:** W1.
> **Flow:** `.maestro/flows/jm-034-rating.yaml`.
> **Run result:** **FAIL — APP_DEFECT** (`64_W1_QA_RESULTS.md` Batch B row 17).
> **Demo-critical:** **YES** — the terminal RATE step of the customer demo path (rate → orders-home).

## STATUS: BLOCKED — APP_DEFECT (rating unreachable — blocked upstream by the jm-033 receipt screen)

`64_W1_QA_RESULTS.md` (authoritative, complete — row 17) records jm-034 **FAIL** at
`Assertion is false: id: receipt_confirm_cta is visible` — the flow reaches the rating screen **via
the receipt screen** (seed `delivery_marked_done` → receipt → `receipt_confirm_cta` → rate), and the
receipt screen is blocked: `receipt_confirm_cta` is not visible because `receipt_prompt` never renders
(the JM-033 defect, row 16). The rating screen (`rating_root` / `rating_submit_cta`) is therefore
**never reached**, and none of jm-034's own ACs are exercised. The failing step is the **JM-033
prerequisite**, not jm-034's post-rating nav.

### AC → Evidence

| AC | Requirement | Evidence (`64_W1_QA_RESULTS.md` row 17) | Result |
|----|-------------|------------------------------------------|--------|
| AC1 | NO `rating_skip_cta` on the mandatory path; back suppressed [D56] | rating screen never reached (blocked at receipt) | NOT-REACHED |
| AC2 | Customer `rating_submit_cta` → `customer-orders-home` (`orders_home_new_order_fab`) | not reached (receipt screen blocked) | NOT-REACHED |
| AC3 | Jeeber submit (`?mode=jeeber`) → DELIVERY/Dashboard tab (`shell_tab_dashboard`) | not reached | NOT-REACHED |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: receipt_confirm_cta is visible` (the JM-033 receipt
  screen's confirm CTA — the entry to rating).
- **Category:** **APP_DEFECT** — cascades from JM-033 (`receipt_prompt` not rendering under
  `delivery_marked_done`, row 16). The rating screen is unreachable until JM-033 renders.

### Owner action to close

1. **app-eng, P0 — FIRST close JM-033:** render `receipt_prompt` + `receipt_confirm_cta` under
   `delivery_marked_done` (see JM-033 owner action). jm-034's flow cannot reach the rating screen
   until then.
2. **app-eng (JM-034 AC2/AC1), P0 — then:** in `rating_screen.dart` / `mutual_rating_screen.dart`,
   ensure the mandatory rating has NO `rating_skip_cta` and back is suppressed (AC1, D56); customer
   `rating_submit_cta` posts `POST /score-taking-service/v1/ratings/jeeb/submit` then routes to
   `customer-orders-home` so `orders_home_new_order_fab` is visible (AC2). Reconcile `/feedback` vs
   `/mutual-rate` (mutual is compliant). Verify the rating screen is reached from the JM-033 receipt
   confirm.
3. **QA (JM-034), P1 — once receipt → rating lands:** confirm AC1 no-skip negative, AC2 submit →
   orders-home, AC3 jeeber-mode terminal (`jeeber_rating_pending` seed → `shell_tab_dashboard`).
   `.maestro/flows/jm-034-rating.yaml`.

### Definition of Done checklist (CTO brief §10)

- [ ] Rating screen reached via JM-033 receipt confirm — FAILS (`receipt_confirm_cta` not visible).
- [ ] AC1 no-skip negative; AC2 submit → orders-home; AC3 jeeber terminal — not reached.
- [ ] **Maestro flow passes** — jm-034 **FAIL** at `receipt_confirm_cta` (row 17).
- [ ] Reviewer approved / **PO signed off** — BLOCKED on JM-033 first, then the rating submit/no-skip proof.
