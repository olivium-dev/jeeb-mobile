# JM-041 — Onboarding Funding (starter credit explainer)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W2 FINAL sign-off). **Wave:** W2.
> **Flow:** `.maestro/flows/jm-041-onboarding-funding.yaml`.
> **Authoritative run (`66_W2_QA_RESULTS.md` §POST-FIX RE-RUN):** **GREEN** — AC1-3 incl. `funding_continue_cta` → `kyc_status_root`.
> **Demo-critical:** P0 — KYC→funding→status chain.

## STATUS: SIGNED ✅

The POST-FIX RE-RUN lands JM-041 GREEN. The funding screen renders the starter-credit explainer
(`funding_explainer`: fixed non-refundable credit + reserve-10%-per-offer), `funding_topup_cta` →
`wallet-charge-info`, and `funding_continue_cta` → `kyc-pending-status` (`kyc_status_root`). The
earlier `kyc_status_root` non-landing was the KYC status fetch 404ing — fixed by the `/v1/kyc` →
`/user-management/v1/kyc` gateway rewrite (the C2 cause), shared with JM-042.

### AC → Evidence

| AC | Requirement | Evidence (POST-FIX RE-RUN) | Result |
|----|-------------|----------------------------|--------|
| AC1a | `funding_explainer` (starter credit + reserve-10%) | rendered | **PASS** |
| AC1b | `funding_topup_cta` → `wallet-charge-info` | wired + reachable | **PASS** |
| AC1c | `funding_continue_cta` → `kyc-pending-status` (`kyc_status_root`) | continue → `kyc_status_root` lands | **PASS** |

### Exact result + category

- **Result:** GREEN (AC1-3). The continue → kyc-status landing was fixed by the `/v1/kyc` rewrite (C2).
- **Category:** — (no defect).

### Owner action to close

None — SIGNED.

### Definition of Done checklist (CTO brief §10)

- [x] `funding_explainer` + `funding_topup_cta` + `funding_continue_cta` built; continue → kyc-status — verified.
- [x] `kyc_status_root` lands after continue (C2 `/v1/kyc` rewrite) — verified green.
- [x] **Maestro flow passes** — GREEN (POST-FIX RE-RUN).
- [x] **PO signed off** — **SIGNED**.
