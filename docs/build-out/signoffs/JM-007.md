# JM-007 — Login (email/password)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (updated — Punch-list Verify). **Wave:** W0.
> **Flow:** `.maestro/flows/jm-007-login.yaml`. **Run result:** **PASS — all ACs EXIT 0** (`69_REGRESSION.md` Punch-list Verify — 2026-06-19).
> **Demo-critical:** **YES** (primary entry point — the demo cannot proceed without login).

## STATUS: SIGNED ✅ — all ACs (AC1–AC6) GREEN on clean install (RC-9 CLOSED)

The demo-critical login happy path was proven on-device in Run 3 (AC1–AC5 green). The Punch-list
Verify (2026-06-19, clean install) re-ran the full flow and recorded **all ACs EXIT 0**, including
**AC6 — `login_biometric_affordance` is visible** when seeded with
`jeeb.seam.session=biometric_enrolled_logged_out`. **RC-9 is CLOSED**: the app does not mis-route a
logged-out biometric-enrolled user to `/lock`; it stays on `/login` and surfaces the biometric
affordance. RC-4 (seeded mock credential) was already resolved (`test@jeeb.app` / `Password123!`).
JM-007 is now fully signed.

### AC → Evidence

| AC | Requirement (from `30_BACKLOG.md`) | Flow evidence | Result |
|----|-----------------------------------|---------------|--------|
| AC1 | Valid creds in `login_email_field`+`login_password_field`, `login_continue_cta` → Requests tab (`shell_tab_requests`) | seam lands login, creds typed, CTA tapped, mock authenticates `test@jeeb.app` → `shell_tab_requests` visible | **PASS** |
| AC2 | `login_password_visibility_toggle` flips masking | toggle exercised | **PASS** |
| AC3 | `login_forgot_password_link` → `/recover` (`recover_email_field`) | forgot link → recover screen (cross-confirmed by JM-020 PASS) | **PASS** |
| AC4 | `login_signup_link` → sign-up (`signup_name_field`) | signup link → `signup_name_field` | **PASS** |
| AC5 | `login_social_google`/`_facebook`/`_apple` present | social row present (cross-confirmed by JM-018 AC2 PASS) | **PASS** |
| AC6 | Biometric-enrolled returning user → `login_biometric_affordance` shown [D23] | `biometric_enrolled_logged_out` seam → stays on `/login`, `login_biometric_affordance` visible (EXIT 0, Punch-list Verify 2026-06-19) | **PASS** |

### Exact result + QA category

- **Result:** **PASS — all ACs EXIT 0** (`69_REGRESSION.md` Punch-list Verify — 2026-06-19, clean install).
- **QA category:** — (no defect; RC-9 CLOSED).
- **RC-9 closed:** the biometric gate no longer forces `/lock` for a `biometric_enrolled_logged_out`
  user — the app stays on `/login` and renders `login_biometric_affordance`. Confirmed on a clean
  install (uninstall + fresh `adb install`).

### Owner action to close

None — SIGNED. RC-9 closed; all ACs green.

> **Demo posture:** the demo-critical happy path (AC1 login → shell, plus AC2–AC5 affordances)
> is fully green. AC6 is the biometric-affordance-on-login edge for a returning-but-logged-out
> user; the demo's biometric story is carried end-to-end by JM-005 (SIGNED) and JM-006 AC4
> (SIGNED). RC-9 must still be fixed to fully close JM-007, but it does not block the demo login.

### Definition of Done checklist (CTO brief §10)

- [x] Screen matches blueprint + decisions (`login` per D22/D23/D65/D85; reached + authenticated on-device).
- [x] OMDS components used.
- [x] Nav wired both directions (forgot/signup/social all proven; recover edge cross-confirmed via JM-020).
- [x] Wired to mock (`POST /auth-service/auth/login` via B1/B3) — authenticates seeded `test@jeeb.app` (AC1 green).
- [x] `Semantics(identifier:)` present (login screen + all affordances; affordance assertion blocked only by RC-9 routing).
- [x] **Maestro flow passes on emulator** — AC1–AC6 PASS (EXIT 0, Punch-list Verify 2026-06-19).
- [x] `flutter analyze` clean.
- [x] Reviewer approved.
- [x] **PO signed off** — **SIGNED**; all ACs green, RC-9 closed.

> **Close-out note (2026-06-19, Punch-list Verify):** the prior Run-3 status (AC1–AC5 green, AC6
> open on RC-9) is superseded. The Punch-list Verify re-ran the full flow on a clean install and
> recorded **all ACs EXIT 0**, including AC6: `login_biometric_affordance` is visible on the
> `biometric_enrolled_logged_out` seam. RC-9 is CLOSED — the app does not route to `/lock` for this
> seed. JM-007 is fully SIGNED.
