# JM-010 — Walkthrough destination + naming

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18. **Wave:** W0.
> **Flow:** `.maestro/flows/jm-010-walkthrough.yaml`. **Run result:** **PASS** (`61_W0_QA_RESULTS.md` Run 2 + Run 3 — held green).
> **Demo-critical:** **YES** (first impression / onboarding).

## STATUS: SIGNED

All three ACs pass on-device (Run 2, held green in Run 3). The Run 1 FLOW_BUG (RC-2 — the AC2 Skip scenario's
second cold-start `extendedWaitUntil` was too tight at 15 000 ms) is resolved: QA raised
the W0 cold-start timeouts to 30 000 ms. The app behavior was correct in Run 1; only the
test timeout needed the one-line bump. Walkthrough renders all three slides, advances via
`walkthrough_next_cta`, and both Get-Started and Skip route to email-first sign-up per
JM-001.

### AC → Evidence

| AC | Requirement (from `30_BACKLOG.md`) | Flow evidence | Result |
|----|-----------------------------------|---------------|--------|
| AC1 | Last slide, `walkthrough_get_started_cta` tapped → sign-up (`signup_name_field`) [per JM-001 email-first] | slides 1→2→3 advance via `walkthrough_next_cta`; Get Started → `signup_name_field` | **PASS** |
| AC2 | Skip (`walkthrough_skip_cta`) from any slide → sign-up (`signup_name_field`) | second cold-start lands `walkthrough_slide_1`; Skip → `signup_name_field` | **PASS** (timeout raised to 30 000 ms) |
| AC3 | Walkthrough renders `walkthrough_slide_1`/`_2`/`_3`, each visible as `walkthrough_next_cta` is tapped | all three slide ids confirmed visible during navigation | **PASS** |

### Exact failing AC + QA category

- None. All 3 ACs PASS (`61_W0_QA_RESULTS.md` Run 2 row jm-010).

### Definition of Done checklist (CTO brief §10)

- [x] Screen matches blueprint + decisions (`walkthrough`; D79/G14; email-first destination per JM-001).
- [x] OMDS components used.
- [x] Nav wired (Get Started + Skip → sign-up; both confirmed on-device).
- [x] Wired to mock — N/A (no network call).
- [x] `Semantics(identifier:)` present (all slides + next/skip/get-started CTAs confirmed).
- [x] **Maestro flow passes on emulator** — PASS (Run 2, all 3 ACs).
- [x] `flutter analyze` clean (gate held through Run 2 rebuild).
- [x] Reviewer approved.
- [x] **PO signed off.**
