# JM-058 — Notification Preferences (categories + repo path)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W4 FINAL sign-off). **Wave:** W4.
> **Flow:** `.maestro/flows/jm-058-notification-prefs.yaml`.
> **Authoritative run (`68_W34_QA_RESULTS.md` Batch A #5):** **FAIL** — `notif_prefs_transactional_lock_icon` not visible.
> **Demo-critical:** P1.

## STATUS: PARTIAL — APP_DEFECT. `notif_prefs_root` is reached but the `notif_prefs_transactional_lock_icon` Semantics id is not placed on the locked transactional toggle indicator.

The notification-prefs route lands (`notif_prefs_root` reached). The failure is narrow: the locked
transactional category indicator (which must be visibly locked per D64) does not carry the
`notif_prefs_transactional_lock_icon` Semantics id, so the AC asserting the lock affordance times out.

### AC → Evidence

| AC | Requirement | Evidence (Batch A #5) | Result |
|----|-------------|-----------------------|--------|
| AC1 | categories = offers/order-status/wallet/marketing with transactional locked [D64]; `notif_prefs_transactional_lock_icon` | `notif_prefs_root` reached; lock-icon id not placed | **FAIL — APP_DEFECT (id not placed)** |
| AC2 | `notif_prefs_<category>_toggle` debounced PATCH; repo path `/notification-service/v1/notifications/preferences` | not reached | not reached |
| AC3 | `notif_prefs_back` → `customer-profile` | not reached | not reached |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: notif_prefs_transactional_lock_icon is visible`.
- **Category:** **APP_DEFECT** — missing Semantics id on the locked transactional toggle indicator.

### Owner action to close

1. **app-eng (JM-058), P0 — the single fix:** in `notification_prefs_screen.dart`, place
   `Semantics(identifier: 'notif_prefs_transactional_lock_icon')` on the lock indicator of the locked
   transactional category row [D64]. Confirm via `maestro hierarchy`.
2. **QA (JM-058), P1:** re-run; verify AC2 toggle PATCH + AC3 `notif_prefs_back` → `customer-profile`.

### Definition of Done checklist (CTO brief §10)

- [x] notification-prefs route + `notif_prefs_root` built.
- [ ] `notif_prefs_transactional_lock_icon` Semantics id placed — **FAILS** (id missing; one-line fix).
- [ ] AC2 toggle PATCH + AC3 back-nav verified — not reached.
- [ ] **Maestro flow passes** — FAIL at AC1.
- [ ] **PO signed off** — **PARTIAL** pending the lock-icon id placement (app-eng, P0).
