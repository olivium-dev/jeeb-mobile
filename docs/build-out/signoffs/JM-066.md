# JM-066 — Account Status (suspended/locked) + router gate

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W4 FINAL sign-off). **Wave:** W4.
> **Flow:** `.maestro/flows/jm-066-account-status.yaml`.
> **Authoritative run (`68_W34_QA_RESULTS.md` Batch B #13):** **FAIL** at AC2 — `support_root` not visible after `account_status_support_cta` tap. AC1 PASS.
> **Demo-critical:** P1 (P0-shaped — session-integrity gate).

## STATUS: PARTIAL — APP_DEFECT (AC2 only). AC1 PASS: the account-status gate renders, tabs are blocked, and the CTAs are present. AC2 fails because `account_status_support_cta` does not land on `support_root` — same C5 cluster as the JM-063 support-ticket stub.

The router status-gate works: with the suspended/locked status the app forces `/account-status`,
`account_status_root` is visible, tab access is blocked, and both CTAs (`account_status_support_cta`,
`account_status_signout_cta`) render (AC1 PASS). The failure is AC2: tapping
`account_status_support_cta` does not land on a screen carrying `support_root` — the same root cause
as JM-063 (the W4 support-ticket stub does not expose its `support_root` reliably from this entry).

### AC → Evidence

| AC | Requirement | Evidence (Batch B #13) | Result |
|----|-------------|------------------------|--------|
| AC1 | `getMe.status ∈ {suspended,locked}` forces `/account-status` + blocks all tabs [D5]; CTAs present | `account_status_root` visible; tabs blocked; CTAs present | **PASS** |
| AC2 | `account_status_support_cta` → `support-ticket` (`support_root`) | tap does not land on `support_root` | **FAIL — APP_DEFECT (support entry)** |
| AC3 | `account_status_signout_cta` → `logout-delete-account` | not reached | not reached |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: support_root is visible` (after `account_status_support_cta` tap).
- **Category:** **APP_DEFECT** — `account_status_support_cta` → `support_root` does not land; shares the JM-063 support-stub gap.

> Note: AC1 passing confirms the `account_status` seam (suspended/locked status) IS satisfied here — this is NOT a missing-seed PRECONDITION. The defect is the support-navigation edge.

### Owner action to close

1. **app-eng (JM-066 + JM-063), P0 — the shared fix:** ensure the support-ticket screen exposes
   `Semantics(identifier: 'support_root')` on its root and that `account_status_support_cta` navigates
   to it (`goNamed('support-ticket')` / `/support`). Fixing the JM-063 support stub unblocks both
   JM-066 AC2 and JM-063. Confirm via `maestro hierarchy`.
2. **QA (JM-066), P1:** re-run; verify AC2 support landing + AC3 signout → `logout-delete-account`.

### Definition of Done checklist (CTO brief §10)

- [x] account-status router gate + `account_status_root` + tab-block + CTAs (AC1) — verified green.
- [ ] `account_status_support_cta` → `support_root` (AC2) — **FAILS** (shared support-stub gap).
- [ ] AC3 signout → `logout-delete-account` — not reached.
- [ ] **Maestro flow passes** — FAIL at AC2.
- [ ] **PO signed off** — **PARTIAL** pending the shared support-`support_root` fix (app-eng, P0).
