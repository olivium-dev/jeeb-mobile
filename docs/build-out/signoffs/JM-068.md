# JM-068 — All Reviews list

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W4 FINAL sign-off). **Wave:** W4.
> **Flow:** `.maestro/flows/jm-068-reviews-list.yaml`.
> **Authoritative run (`68_W34_QA_RESULTS.md` Batch B #15):** **FAIL** — `reviews_root` not visible.
> **Demo-critical:** P1.

## STATUS: BLOCKED — PRECONDITION (missing seam/journey seed + R1m reviews endpoint + stable review ids). The reviews-list screen needs seeded reviews; the `jeeber_has_reviews` journey seed and the R1m reviews source are not implemented. No app defect proven.

The reviews-list screen renders seeded reviews for a jeeber. The flow launches with journey
`jeeber_has_reviews` + route pin `/profile/delivery-man/user-jeeber-002/reviews` and asserts
`reviews_root`, but the journey seed is not wired and the R1m reviews endpoint does not return seeded
rows, so the screen has nothing to resolve and the first assertion times out. This is the blocking
backender item R1m + a seam seed — not an app defect.

### AC → Evidence

| AC | Requirement | Evidence (Batch B #15) | Result |
|----|-------------|------------------------|--------|
| AC | infinite scroll + skeletons [D73]; reviewer first name [D58]; cold-start hide<5 + New badge [D59]; `review_<id>_report_cta` [D27]; `reviews_back` → `jeeber-profile-reviews` | `reviews_root` not visible — no seeded reviews | **FAIL — PRECONDITION (missing seed)** |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: reviews_root is visible` (launch with `journey=jeeber_has_reviews` + `/profile/delivery-man/user-jeeber-002/reviews`).
- **Category:** **PRECONDITION** — missing seed.

### Missing seed (name exactly — do NOT fix in QA)

- **Seam/journey seed:** `jeeb.seam.journey=jeeber_has_reviews` (`POST /__mock/seed/journey {"journey":"jeeber_has_reviews"}` not implemented).
- **Mock endpoint (R1m):** `GET /score-taking-service/v1/ratings/jeeb/jeeber/:jeeberId/reviews` returning seeded reviews.
- **Stable ids:** `review-001`, `review-002`, `review-003`.

### Owner action to close

1. **backender (mock), P0 — R1m:** define `GET /score-taking-service/v1/ratings/jeeb/jeeber/:jeeberId/reviews` with seeded reviews + stable ids, and add the `jeeber_has_reviews` fixture to `jeeb-mock-backend/src/fixtures/journey-seed.ts`.
2. **seam (app), P0:** add `jeeber_has_reviews` to the `JourneySeed` landing contract (`lib/core/dev_seam/`) so the seed POST fires and the route pins the reviews list.
3. **QA (JM-068), P1:** after the seed + R1m land, re-run; verify `reviews_root`, reviewer first-name, cold-start hide<5, report CTA, and `reviews_back`.

> Coupling: JM-067 (jeeber-profile-reviews) gates the in-app entry to this screen (`profile_view_all_reviews`); JM-068 can also be reached directly via the seeded route pin once `jeeber_has_reviews` + R1m exist.

### Definition of Done checklist (CTO brief §10)

- [x] reviews-list route + screen shell built (W3/W4 integrator).
- [ ] `jeeber_has_reviews` journey seed + R1m endpoint + stable review ids — **BLOCKED (backender + seam)**.
- [ ] list + first-name + cold-start + report CTA + back (AC) — not reachable until seed lands.
- [ ] **Maestro flow passes** — FAIL at `reviews_root` (PRECONDITION).
- [ ] **PO signed off** — **BLOCKED** on the `jeeber_has_reviews` seed + R1m endpoint + stable review ids.
