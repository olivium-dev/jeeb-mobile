# JM-061 — Password & Security

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W4 FINAL sign-off). **Wave:** W4.
> **Flow:** `.maestro/flows/jm-061-password-security.yaml`.
> **Authoritative run (`68_W34_QA_RESULTS.md` Batch A #8):** **FAIL** — `customer_profile_wallet_chip` not visible after `password_back` (back-nav does not return to customer profile).
> **Demo-critical:** P1.

## STATUS: PARTIAL — APP_DEFECT. The password-security screen is reachable but the `password_back` control does not navigate back to `customer-profile` (the `customer_profile_wallet_chip` anchor is not restored). Same C2 cluster as JM-059.

The password-security route lands. The failure is the back edge: tapping `password_back` does not
land on the customer-profile screen — `customer_profile_wallet_chip` is not visible afterward. Same
root cause class as JM-059 (settings-screen back navigation does not resolve to the profile route).

### AC → Evidence

| AC | Requirement | Evidence (Batch A #8) | Result |
|----|-------------|-----------------------|--------|
| AC1 | current/new/confirm fields + validation; social-only `password_set_entry` → `auth-set-password?mode=in-app-social` [D90] | screen reachable | PARTIAL (not the failing step) |
| AC2 | `password_back` → `customer-profile` (`customer_profile_wallet_chip` restored) | back does not land on profile | **FAIL — APP_DEFECT (back-nav target)** |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: customer_profile_wallet_chip is visible` (after `password_back`).
- **Category:** **APP_DEFECT** — back navigation from password-security does not return to `customer-profile`.

### Owner action to close

1. **app-eng (JM-061), P0 — the single fix (shared pattern with JM-059):** in
   `password_security_screen.dart`, wire `password_back` to return to `customer-profile` so
   `customer_profile_wallet_chip` is restored. Confirm via `maestro hierarchy`.
2. **QA (JM-061), P1:** re-run; verify the fields/validation (AC1) and the back edge (AC2).
   Note: `password_set_entry` (social-only branch) also depends on the `account_type=social_only`
   seam — if exercised, it needs that seed.

### Definition of Done checklist (CTO brief §10)

- [x] password-security route registered + screen reachable (W3/W4 integrator).
- [ ] `password_back` → `customer-profile` (restores `customer_profile_wallet_chip`) — **FAILS** (back-nav mis-wired).
- [ ] **Maestro flow passes** — FAIL at the back edge.
- [ ] **PO signed off** — **PARTIAL** pending the `password_back` target fix (app-eng, P0).
