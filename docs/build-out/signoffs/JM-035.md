# JM-035 — Customer Profile Tab

> **✅ FINAL VERIFY 2026-06-19 — SIGNED (GREEN).** Fresh APK, maestro EXIT 0. All ACs pass incl.
> R-3 (`customer_profile_register_delivery_row`→`delivery_register_prompt`). Earlier red was a
> wall-clock timeout only. Stale STATUS header below superseded. See `69_REGRESSION.md` §"Final Verify".

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (FINAL, W1-QA reliable re-run). **Wave:** W1.
> **Flow:** `.maestro/flows/jm-035-customer-profile.yaml`.
> **Run result:** **FAIL — APP_DEFECT** at `shell_tab_requests` (`64_W1_QA_RESULTS.md` Batch B row 18).
> **Demo-critical:** P0 item but not on the create→rate happy path (it is the Profile tab body).

## STATUS: BLOCKED — APP_DEFECT (`shell_tab_requests` absent after `customer_logged_in` seed — shell-landing blocker)

`64_W1_QA_RESULTS.md` (authoritative, complete — row 18) records jm-035 **FAIL** at
`Assertion is false: id: shell_tab_requests is visible` — the app launched (with the
`customer_logged_in` seed) but the shell bottom-nav tab id `shell_tab_requests` is not visible: the
shell navigation tab semantics ids are absent or the shell is not rendering after login. The profile
tab body (which the flow reaches by tapping `shell_tab_profile`) is therefore **never reached** —
this is a **shell-landing APP_DEFECT** at the very first precondition, NOT the `dm_onboarding_continue`
W2 over-assert an earlier read attributed to it. **This same `shell_tab_requests` blocker cascades to
jm-049 (row 19) and jm-050 (row 20)** — both red on the same id — making the shell-tab-after-login
defect the highest-leverage single fix in W1 batch B.

### AC → Evidence

| AC | Requirement | Evidence (`64_W1_QA_RESULTS.md` row 18) | Result |
|----|-------------|------------------------------------------|--------|
| AC1 | Real profile (avatar, name, per-role rating, `customer_profile_wallet_chip`, `customer_profile_bell`) renders (not the QA dev surface) | shell never landed; profile tab not reached | NOT-REACHED |
| AC2 | Rows navigate: register-delivery→W2/JM-039 (`dm_onboarding_continue`), password/notifications/language/contact/logout→W4, addresses→JM-049, wallet/bell→guarded | not reached; NOTE register-delivery + W4 rows are cross-wave ids — assert AP-9-honestly per `63_W1_TEST_PLAN §2.19` | NOT-REACHED |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: shell_tab_requests is visible`.
- **Category:** **APP_DEFECT** — the shell bottom-nav does not render its tab ids
  (`shell_tab_requests` / `shell_tab_profile`) after the `customer_logged_in` seed lands. Shared root
  with jm-049 + jm-050 (rows 19/20). (Re-categorize PRECONDITION only if triage proves the
  `customer_logged_in` mock fixture wedges the shell load — but the symptom is the tab ids absent,
  which points app-side.)

### Owner action to close

1. **app-eng (shell tab ids after login), P0 — HIGHEST LEVERAGE; unblocks jm-035 + jm-049 + jm-050:**
   in `lib/.../shell_screen.dart`, ensure the bottom-nav renders `shell_tab_requests` /
   `shell_tab_profile` / `shell_tab_dashboard` with `Semantics(identifier: 'shell_tab_<id>',
   container: true)` whenever the `customer_logged_in` session lands the shell — render the tab
   scaffold first and load each tab's content async (do not suppress the tab bar during load).
   Confirm via `maestro hierarchy` under `jeeb.seam.session=customer_logged_in`.
2. **app-eng (JM-035 AC1), P0 — once the shell lands:** confirm the Profile tab body is the real
   `CustomerProfileScreen` (not the `profile_tab.dart` dev surface) rendering avatar/name/per-role
   rating/`customer_profile_wallet_chip`/`customer_profile_bell`.
3. **QA (jm-035 AC2), P1 — once the profile body renders:** make every cross-wave row destination
   AP-9-honest (tap-accepted + profile root survives) — `dm_onboarding_continue` (W2/JM-039),
   password/notifications/language/contact/logout (W4) per `63_W1_TEST_PLAN §2.19`; only assert
   W1-owned destinations hard (addresses → `saved_address_add_cta`, itself gated on JM-049).
   `.maestro/flows/jm-035-customer-profile.yaml`.

### Definition of Done checklist (CTO brief §10)

- [ ] Shell renders `shell_tab_requests` / `shell_tab_profile` after `customer_logged_in` — FAILS (tab ids absent).
- [ ] Real profile body renders (AC1) — not reached.
- [ ] Cross-wave row assertions AP-9-honest (not W2/W4 screens) — to author once the profile body renders.
- [ ] **Maestro flow passes** — jm-035 **FAIL** at `shell_tab_requests` (row 18).
- [ ] Reviewer approved / **PO signed off** — BLOCKED pending the shell-tab-after-login fix (+ profile body) + QA cross-wave asserts.
