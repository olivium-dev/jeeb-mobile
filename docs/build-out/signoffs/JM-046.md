# JM-046 — Insufficient Balance to Offer (sheet)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W2 FINAL sign-off — residual verify). **Wave:** W2.
> **Flow:** `.maestro/flows/jm-046-insufficient-balance-sheet.yaml`.
> **Authoritative run (`66_W2_QA_RESULTS.md` §W2 RESIDUAL VERIFY, RD-4):** **FAIL** — `insufficient_balance_sheet` precondition never satisfied; mock returns 200 not 402, send navigates to feed.
> **Demo-critical:** P1.

## STATUS: BLOCKED — backend MOCK_GAP O1+W1m (the 402 insufficient path is not implemented). No app defect; the flow cannot reach any AC until the mock returns 402. No seam bypass exists.

The clean-install residual verify resolves the earlier first-step shell precondition (the boot-hold
is fixed — the composer is now reachable and the send fires). The **entire flow** is now gated by a
single backend gap, shared root with JM-045 AC5 (RD-4): `POST /offer-service/v1/offers` returns
**200** (not 402) even with the seeded insufficient jeeber wallet, so the send succeeds, the app
navigates to `jeeber_feed_root`, and `insufficient_balance_sheet` **never appears**. The flow fails
at its precondition before any AC assertion.

**PO code verification:** the insufficient-balance sheet + both CTAs are app-side ready — the app
routes `offer-insufficient-balance` when the offer POST returns 402, with `insufficient_topup_cta` →
`wallet-charge-info` and `insufficient_keep_editing_cta` → composer (draft preserved). The block is
strictly the mock: `GET /wallet-service/v1/jeeb/wallet` resolves to `user-client-001`
(`affordabilityState:"enough"`, balance 40) and ignores `POST /__mock/seed/wallet {insufficient,
userId:"user-jeeber-002"}`, and the offer-service has no 402 path. There is **no seam bypass** — the
sheet is raised only by a real 402.

### AC → Evidence

| AC | Requirement | Evidence (Residual Verify) | Result |
|----|-------------|----------------------------|--------|
| AC | send-insufficient → `insufficient_balance_sheet` (needed-vs-available); `insufficient_topup_cta` → `wallet-charge-info`; draft preserved + auto-sent after store charge; `insufficient_keep_editing_cta` → composer | mock returns 200, app navigates to feed — sheet never raised; precondition fails | **FAIL — MOCK_GAP (O1+W1m)** |

### Exact failing step + category

- **Failing step:** `insufficient_balance_sheet` precondition never satisfied — `POST /offer-service/v1/offers` returns 200, send navigates to `jeeber_feed_root`. Flow fails before any AC assertion.
- **Category:** **MOCK_GAP (O1+W1m)** — offer-service 402 path + jeeber wallet seed honoring not implemented. NOT an app defect; the sheet + CTAs are app-side ready.

### Owner action to close

1. **backender (mock), P0 — O1 + W1m (the single blocker, shared with JM-045):**
   - **O1:** `POST /offer-service/v1/offers` must return **402 {needed, available}** when the seeded
     jeeber wallet is insufficient.
   - **W1m:** `GET /wallet-service/v1/jeeb/wallet` must resolve to the seeded `user-jeeber-002` balance
     (honor `POST /__mock/seed/wallet {state:"insufficient", userId:"user-jeeber-002"}`), not fall
     back to `user-client-001`/`enough`/40.
   Files: `jeeb-mock-backend` offer-service + wallet-service.
2. **QA (JM-046), P1:** after O1/W1m, re-run — the app already routes the sheet on a 402; verify the
   sheet content (needed-vs-available), `insufficient_topup_cta` → `wallet-charge-info`, draft
   preservation, and `insufficient_keep_editing_cta` → composer.

### Definition of Done checklist (CTO brief §10)

- [x] insufficient-balance sheet + both CTAs app-side ready (routes on 402) — confirmed in source.
- [ ] mock returns 402 for the insufficient jeeber seed (O1+W1m) — **BLOCKED (backend, no seam bypass)**.
- [ ] sheet content + CTAs + draft preservation (AC) — not reachable until the 402 path lands.
- [ ] **Maestro flow passes** — FAIL at precondition (RD-4).
- [ ] **PO signed off** — **BLOCKED** on backend O1+W1m.
