# JM-001 — Auth funnel architecture decision spike (AUTH-OD-1)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18. **Wave:** W0.
> **Type:** decision/spike — **not a screen**, therefore **no Maestro flow** of its own.
> **Source AC:** `30_BACKLOG.md` JM-001. **Evidence basis:** `61_W0_QA_RESULTS.md` (downstream flows).

## STATUS: SIGNED (decision recorded; downstream targets deterministic)

JM-001 is the only W0 item with no on-device test gate — its DoD is "the ruling is
recorded and JM-007/008/010 unblock with a deterministic target" (`30_BACKLOG.md`
JM-001 AC). That condition is met: the ruling is **email-first** (blueprint funnel),
and every downstream flow encodes it deterministically.

### AC → Evidence

| AC | Requirement | Evidence | Met |
|----|-------------|----------|-----|
| AC1 | Given the ruling (email-first vs phone-first), When recorded, Then JM-007/008/010 unblock with a deterministic target | The email-first ruling is encoded verbatim in the W0 flows authored against it: `jm-008-signup.yaml` header ("Sign Up (email-first, per CTO-D1)") + AC1 asserting `signup_name_field` → `signup_email_field` → `signup_password_field` → `signup_submit_cta` → `phone_otp_input`; `jm-010-walkthrough.yaml` AC1/AC2 routing Get-Started **and** Skip → `signup_name_field` (sign-up), not a phone screen; `jm-007-login.yaml` `login_signup_link` → `signup_name_field`. All three flows share one funnel shape: walkthrough → email-first sign-up → phone-OTP. | ✅ |

> **Note:** the ruling being correct and recorded is independent of the seam-wiring
> blocker (RC-1) that reds the downstream flows. The downstream flows are red because of
> dev-seam plumbing, **not** because the funnel direction is ambiguous — every flow
> targets a single deterministic destination chain. JM-001's deliverable (the decision)
> is therefore complete.

### Definition of Done checklist (CTO brief §10)

- [x] Decision recorded (email-first; `/sign-up` route shape; phone-OTP re-parented behind sign-up).
- [x] Downstream items (JM-007/008/010) have a deterministic target — confirmed by the flow contracts.
- [n/a] Screen matches blueprint — not a screen.
- [n/a] OMDS components — not a screen.
- [n/a] Maestro flow passes — decision spike has no flow.
- [n/a] Wired to mock — not a screen.

### Owner action

None. This item is closed.
