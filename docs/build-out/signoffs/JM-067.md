# JM-067 — Jeeber Profile Reviews (remove Helpful/Reply; wire View-all)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W4 FINAL sign-off). **Wave:** W4.
> **Flow:** `.maestro/flows/jm-067-jeeber-profile-reviews.yaml`.
> **Authoritative run (`68_W34_QA_RESULTS.md` Batch B #14):** **FAIL** — `delivery_man_profile_screen_root` not visible via bare route pin `/profile/delivery-man`.
> **Demo-critical:** P1.

## STATUS: PARTIAL — APP_DEFECT. The bare route pin `/profile/delivery-man` (no session seam, no `jeeberId`) does not land on `delivery_man_profile_screen_root` — either the screen root id is not placed or the route requires a `jeeberId` extra/path param the bare pin does not supply.

The DeliveryManProfileScreen exists at `/profile/delivery-man`. The flow pins
`jeeb.route=/profile/delivery-man` with no session seam and no `jeeberId`, and asserts
`delivery_man_profile_screen_root`. The screen does not appear: the router likely guards/redirects the
bare route (to login/splash) or the route needs a `jeeberId` path param / `extra`, and/or the
`delivery_man_profile_screen_root` Semantics id is not placed on the screen root.

### AC → Evidence

| AC | Requirement | Evidence (Batch B #14) | Result |
|----|-------------|------------------------|--------|
| AC1 | profile renders (NO Helpful/Reply [D57]); `delivery_man_profile_screen_root` reachable | `delivery_man_profile_screen_root` not visible via bare route pin | **FAIL — APP_DEFECT (route needs id / root id absent)** |
| AC2 | `profile_view_all_reviews` → `reviews-list` [JM-068]; cold-start hide<5 [D59] + first-name [D58]; `profile_close` → `offer-review-list` | not reached | not reached |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: delivery_man_profile_screen_root is visible` (after route pin `/profile/delivery-man`).
- **Category:** **APP_DEFECT** — the profile screen is not reachable via the bare route pin (route requires a `jeeberId`/session extra, or `delivery_man_profile_screen_root` is not placed on the root).

> Note: this is categorized APP_DEFECT (route/id reachability), NOT a missing-seed PRECONDITION — the flow exercises a route pin, not a journey seed. If the product intent is that the profile MUST be entered with a `jeeberId` (e.g. from `offer_card_<id>_name`), then the QA flow's bare route pin is wrong and this becomes a flow/spec reconciliation; the engineer should confirm the canonical entry contract.

### Owner action to close

1. **app-eng (JM-067), P0 — the fix:** either (a) place `Semantics(identifier: 'delivery_man_profile_screen_root')` on the screen root AND make the bare `/profile/delivery-man` route resolve to a default/self jeeber so the route pin lands; OR (b) confirm the canonical entry requires a `jeeberId` (path param or `extra`) and update the flow + signoff to pin `/profile/delivery-man/:jeeberId`. Confirm via `maestro hierarchy`.
2. **QA (JM-067), P1:** once the screen lands, verify AC2 view-all-reviews → `reviews-list` and `profile_close`.

### Definition of Done checklist (CTO brief §10)

- [x] DeliveryManProfileScreen + `/profile/delivery-man` route exist.
- [ ] `delivery_man_profile_screen_root` reachable (id placed + route resolvable from the pin) — **FAILS**.
- [ ] AC2 view-all + close edges — not reached.
- [ ] **Maestro flow passes** — FAIL at the root assertion.
- [ ] **PO signed off** — **PARTIAL** pending the route/id reachability fix (app-eng, P0).
