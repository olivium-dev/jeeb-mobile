# JM-032 — Order Tracking (4-step stepper + no-show sheet + auto-advance)

> **✅ FINAL VERIFY 2026-06-19 — SIGNED (GREEN).** Fresh APK, maestro EXIT 0. All ACs pass incl.
> R-1 (`tracking_dispute_cta`→`dispute_root`) and AC4 no-show (reassign→offer-review, re-broadcast→
> `waiting_notified_count`). Fix: flow `retry`/`waitForAnimationToEnd` for the no-show sheet +
> mock `/v1/requests/:id` delivery-id→broadcasting-request resolution. The STATUS line below is the
> stale original-wave header (superseded). See `69_REGRESSION.md` §"Final Verify".

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (FINAL, W1-QA reliable re-run). **Wave:** W1.
> **Flow:** `.maestro/flows/jm-032-order-tracking.yaml`.
> **Run result:** **FAIL — APP_DEFECT** at `tracking_stepper` (`64_W1_QA_RESULTS.md` Batch B row 15).
> **Demo-critical:** **YES** — the TRACK step of the customer demo path; auto-advances to receipt.

## STATUS: BLOCKED — APP_DEFECT (`tracking_stepper` not reached under `active_delivery` seed)

`64_W1_QA_RESULTS.md` (authoritative, complete — row 15) records jm-032 **FAIL** at
`Assertion is false: id: tracking_stepper is visible` — the order-tracking screen is **not reached**
under the `active_delivery` seed, or the `tracking_stepper` id is absent. This is the **W1-owned
core** of jm-032 (the 4-step stepper is the primary visual per AC1) and it fails at the very first
W1 assertion — so this is a genuine **APP_DEFECT**, not the W4-dispute over-assert that an earlier
read attributed to it. The order-tracking route/screen is not wired/rendering its signature id under
the seed.

### AC → Evidence

| AC | Requirement | Evidence (`64_W1_QA_RESULTS.md` row 15) | Result |
|----|-------------|------------------------------------------|--------|
| AC1 | `tracking_stepper` (Ordered→Picked→In Transit→Delivered) + `order_summary_pinned` header | `tracking_stepper` NOT visible (screen not reached under `active_delivery`) | **FAIL — APP_DEFECT** |
| AC2 | Jeeber marks delivered → auto-advance to `delivered-receipt-confirm` (`receipt_prompt`) | not reached | NOT-REACHED |
| AC3 | `tracking_dispute_cta` → `dispute-open-evidence` (`dispute_reason`) [JM-060, **W4**] | not reached; NOTE this is a W4 cross-wave id (assert AP-9-honestly per `63_W1_TEST_PLAN §2.19`) | NOT-REACHED |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: tracking_stepper is visible`.
- **Category:** **APP_DEFECT** — the order-tracking screen (`live_tracking_screen.dart`) is not
  reached / does not render `tracking_stepper` under the `active_delivery` journey seed. Also blocked
  by JM-031 (`order_summary_pinned` header, independently red) which AC1 requires.

### Owner action to close

1. **app-eng (JM-032 AC1), P0:** in `lib/features/.../live_tracking_screen.dart`, ensure the tracking
   route is reached under `jeeb.seam.journey=active_delivery` and renders `tracking_stepper` with
   `Semantics(identifier: 'tracking_stepper', container: true)` (4 steps: Ordered→Picked→In Transit→
   Delivered, D70) plus the `order_summary_pinned` header (JM-031). Confirm via `maestro hierarchy`
   under the seed. Bind to `GET /delivery-service/v1/delivery/:deliveryId`.
2. **backender (mock), P0:** confirm `jeeb.seam.journey=active_delivery` seeds the active delivery +
   route fixtures (`GET /delivery-service/v1/delivery/:deliveryId`,
   `GET /geolocation-service/v1/jeeb/geo/route/:deliveryId`) so the tracking screen has data to
   render and is the landed route. File: `jeeb-mock-backend/src/fixtures/` + the seed branch.
3. **QA (JM-032), P1 — after AC1 renders:** verify AC2 auto-advance to `receipt_prompt` under
   `delivery_marked_done`; and make the AC3 dispute edge AP-9-honest (tap `tracking_dispute_cta` →
   re-assert tracking root; `dispute_reason` is W4/JM-060, defer to that flow per `63_W1_TEST_PLAN
   §2.19`). `.maestro/flows/jm-032-order-tracking.yaml`.

### Definition of Done checklist (CTO brief §10)

- [ ] `tracking_stepper` 4-step rendered under `active_delivery` (AC1) — FAILS (screen not reached).
- [ ] `order_summary_pinned` header (AC1) — gated on JM-031 (also red).
- [ ] AC2 auto-advance to receipt; AC3 dispute edge AP-9-honest — not reached.
- [ ] **Maestro flow passes** — jm-032 **FAIL** at `tracking_stepper` (row 15).
- [ ] Reviewer approved / **PO signed off** — BLOCKED pending the tracking-screen render + `active_delivery` seed (+ JM-031 header).
