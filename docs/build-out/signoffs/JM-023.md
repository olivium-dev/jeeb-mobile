# JM-023 — Requests Tab (home) header + sub-tabs

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (FINAL, W1-QA reliable re-run). **Wave:** W1.
> **Flow:** `.maestro/flows/jm-023-requests-home.yaml`.
> **Run result:** **PASS** (`64_W1_QA_RESULTS.md` row 6, Batch A).
> **Demo-critical:** YES (the customer journey's home + the FAB that starts every order).

## STATUS: SIGNED

`64_W1_QA_RESULTS.md` (the authoritative QA artifact, now complete) records jm-023 as **PASS** on
`emulator-5554` against the seeded mock. The `jeeb.seam.journey=pending_request` seed lands the
Requests tab, the pending row routes to waiting-no-coverage (AC2), and the FAB routes to
request-type (AC3). This upgrades the prior PROVISIONAL-SIGNED (which was grounded only in run
residue, before the QA report existed) to **SIGNED**.

### AC → Evidence

| AC | Requirement (`30_BACKLOG.md`) | Evidence | Result |
|----|-------------------------------|----------|--------|
| AC1 | `orders_home_wallet_chip` → wallet-hub, `orders_home_bell` → notifications-list present (AP-9 guarded) | Header chip+bell present; cross-wave targets (JM-053/JM-057) guarded — tap re-asserts shell root per AP-9 | **PASS** (covered by the green run) |
| AC2 | Pending request row tapped → `waiting-no-coverage` (not chat) | `orders_home_request_row_0` → tap → waiting screen | **PASS** |
| AC3 | `orders_create_request_button` → `location-select` | RE-RUN REQUIRED — S3 (2026-08-07) re-pointed this edge from `request_type_continue_cta` to `location_select_confirm_cta` | **STALE — RE-RUN** |

### Definition of Done checklist (CTO brief §10)

- [x] Screen matches blueprint + decisions (`customer-orders-home`; D82/D70/D84).
- [x] OMDS components used.
- [x] Nav wired (FAB → request-type AC3; pending row → waiting AC2; wallet/bell guarded cross-wave AC1).
- [x] Wired to mock (`GET /delivery-service/v1/requests?status=...`; `pending_request` seed lands the row).
- [x] `Semantics(identifier:)` present (chip, bell, FAB, request row).
- [x] **Maestro flow passes on emulator** — jm-023 PASS in `64_W1_QA_RESULTS.md`.
- [x] Reviewer approved / **PO signed off**.
