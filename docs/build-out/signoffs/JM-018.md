# JM-018 — Social Login (FB + post-auth routing)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (updated Run 3). **Wave:** W0.
> **Flow:** `.maestro/flows/jm-018-social-login.yaml`. **Run result:** **PASS** (`61_W0_QA_RESULTS.md` Run 3).
> **Demo-critical:** No (breadth — social login path).

## STATUS: SIGNED

All three ACs pass on-device in Run 3. RC-6 — the Run 2 AC1 failure where the flow asserted
`phone_otp_input` immediately after social login but `/register` renders the phone-entry hero
(`_register_hero`) first — is **resolved** via the QA flow fix: jm-018 AC1 now drives the
phone-entry step (asserts `_register_hero`, taps "Phone number", types `0501234567`, taps
"Send code") before asserting `phone_otp_input`. The app architecture was correct all along
(social cubit intercepts the Facebook tap, emits `requiresPhoneVerification=true`, fires
`goNamed('register')`); the flow now matches the real phone-entry → OTP screen sequence.

### AC → Evidence

| AC | Requirement (from `30_BACKLOG.md`) | Flow evidence | Result |
|----|-----------------------------------|---------------|--------|
| AC1 | Social success with no phone → `phone-otp-verification` (`phone_otp_input`), NOT home [G8] | seam intercepts → cubit emits `requiresPhoneVerification=true` → `goNamed('register')` → `_register_hero` → phone entered + "Send code" → `phone_otp_input` visible | **PASS** |
| AC2 | `social_login_facebook_cta` present alongside Google/Apple | social row present on login | **PASS** |
| AC3 | 409 collision → `social-collision-prompt` (`social_collision_sheet`) [D22, JM-019] | collision branch raises `social_collision_sheet`; exit behavior cross-confirmed by JM-019 (PASS) | **PASS** |

### Exact failing AC + QA category

- None. All 3 ACs PASS (`61_W0_QA_RESULTS.md` Run 3 row jm-018).
- **RC-6 resolution:** flow-only fix (no app change) — jm-018 AC1 drives the phone-entry step
  before asserting `phone_otp_input`, matching the actual screen architecture (phone-entry →
  OTP). The social interception, cubit, and `goNamed('register')` routing were already correct.

### Definition of Done checklist (CTO brief §10)

- [x] Screen matches blueprint + decisions (`social-login`; D8/D22/G8; FB CTA present — AC2 confirms).
- [x] OMDS components used.
- [x] Nav wired (post-auth `goNamed('register')` → phone-entry → OTP — full chain proven by AC1).
- [x] Wired to mock / seam — social seam resolver emits `requiresPhoneVerification=true` correctly.
- [x] `Semantics(identifier:)` present (`login_social_facebook`, `_register_hero`, `phone_otp_input` — AC1/AC2 confirm).
- [x] **Maestro flow passes on emulator** — PASS (Run 3, all 3 ACs).
- [x] `flutter analyze` clean (gate held through Run 3; flow-only fix, no app change).
- [x] Reviewer approved (jm-018 AC1 phone-entry steps landed).
- [x] **PO signed off.**
