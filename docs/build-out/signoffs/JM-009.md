# JM-009 — Phone OTP Verification (re-parent + returning-user bypass)

> **✅ FINAL VERIFY 2026-06-19 — SIGNED (GREEN).** Fresh APK, maestro EXIT 0. All ACs pass. RC-10
> fixed: AC2 (resend) reaches the OTP screen via the signup funnel (the `/register` route-pin
> bounced a logged-out user to /login). Stale STATUS header below superseded. See
> `69_REGRESSION.md` §"Final Verify".

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (updated Run 3). **Wave:** W0.
> **Flow:** `.maestro/flows/jm-009-phone-otp.yaml`. **Run result:** **FAIL** (`61_W0_QA_RESULTS.md` Run 3).
> **Demo-critical:** **YES** (required to complete the sign-up funnel).

## STATUS: PARTIAL — FLOW_BUG (RC-10, QA-owned)

RC-5 (persistent in-memory mock state colliding the fixed signup email) is **resolved** by the
mock-reset-in-flow fix. Run 3 reaches the sign-up screen and submits cleanly, but AC1 fails on
the **same RC-10 as JM-008**: signup navigates to `/register` = `RegistrationScreen`, which
shows the phone-entry hero (`_register_hero`) FIRST; `phone_otp_input` only appears after a
phone number is entered and submitted. jm-009 AC1 asserts `phone_otp_input` directly after
signup, skipping the phone-entry step. jm-018 AC1 was already fixed for this; jm-009 was not.
This is a **QA-owned flow fix** — the OTP screen is correctly re-parented behind sign-up and
the path is wired.

### AC → Evidence

| AC | Requirement (from `30_BACKLOG.md`) | Flow evidence | Result |
|----|-----------------------------------|---------------|--------|
| AC1 | Fresh sign-up: 6-digit OTP in `phone_otp_input` + `phone_otp_verify_cta` → Requests tab (`shell_tab_requests`) | signup reached + submitted (no collision after mock reset), lands `_register_hero` phone-entry; flow doesn't drive phone entry → `phone_otp_input` NOT visible | **FAIL — FLOW_BUG (RC-10)** |
| AC2 | After countdown, `phone_otp_resend_cta` → new code requested (screen stays) | gated by AC1 (OTP screen not reached); seam `otp_countdown_expired` wired | BLOCKED by AC1 |
| AC3 | Returning biometric user bypasses OTP (`biometric_unlock_prompt` shown, `phone_otp_input` NOT shown) [D23] | `biometric_enrolled` seam wired; JM-006 AC4 (SIGNED) confirms biometric session lands `/lock` (bypass) | PASS (likely — bypass routing proven via JM-006 AC4) |

### Exact failing AC + QA category

- **Failing AC:** **AC1** (and AC2 blocked downstream of it). First failing step: `Assert id: phone_otp_input is visible`.
- **QA failure category:** **FLOW_BUG** (`61_W0_QA_RESULTS.md` Run 3 row jm-009) — **QA-owned**, same RC-10 as JM-008.
- **Root cause (RC-10):** signup success → `/register` shows `_register_hero` (phone entry)
  first; `phone_otp_input` only appears after a phone number is submitted. jm-009 AC1 asserts
  the OTP screen directly without driving the phone-entry step.

### Owner action to close

1. **QA (jm-009 AC1), P0 — trivial (same fix as jm-008, copy from jm-018 AC1):** between the
   `signup_submit_cta` tap and the `phone_otp_input` assertion, add:
   `extendedWaitUntil _register_hero` → `tapOn text:"Phone number"` → `inputText "0501234567"`
   → `tapOn text:"Send code"` → then assert `phone_otp_input` and complete the `123456` OTP →
   shell. No app or mock change needed.
2. **QA (jm-009 AC2 gRPC drop), P0 — NEW from `64_W1_QA_RESULTS.md`:** the AC2 resend sub-flow tap
   on `signup_submit_cta` fails with `io.grpc.stub.ClientCalls … UNAVAILABLE` and the flow times
   out at 180s. First, isolate AC2 into its own `launchApp`/sub-flow (don't reuse AC1's session)
   and re-run with a fresh gRPC channel; if the channel still drops at the sub-flow boundary,
   escalate to **app-engineer** to confirm the Maestro driver gRPC server is not being torn down by
   the app on the resend path (check for any explicit channel/server shutdown in the OTP resend
   cubit). File: `.maestro/flows/jm-009-phone-otp.yaml` (AC2 block) + (if app-side) the OTP resend
   handler. Categorize the gRPC drop as FLOW_BUG (harness) vs APP_DEFECT (channel teardown) once
   isolated — the artifact's current guess is APP_DEFECT.

### Definition of Done checklist (CTO brief §10)

- [x] Screen matches blueprint + decisions (re-parented OTP; D23 bypass; B4 6-digit `123456` contract).
- [x] OMDS components (`OmdsOtpInput`).
- [x] Nav wired (signup → `/register` phone-entry → OTP; bypass routing proven via JM-006 AC4).
- [x] Wired to mock (`/auth/otp/request` + `/verify`, B1/B4; mock reset resolves RC-5).
- [x] `Semantics(identifier:)` present.
- [ ] **Maestro flow passes on emulator** — **AC1 FAILS** on RC-10 (flow skips phone-entry step); AC3 likely-pass; AC2 blocked by AC1.
- [ ] `flutter analyze` clean — re-verify (no app change expected; flow-only fix).
- [ ] Reviewer approved — pending RC-10 flow fix.
- [ ] **PO signed off** — partial; OTP screen correct, blocked solely on the QA flow fix (add phone-entry steps, mirror jm-008/jm-018 AC1).

> **Re-verification note (2026-06-18, W1-QA reliable re-run — FINAL):** `64_W1_QA_RESULTS.md`
> is now COMPLETE and re-ran jm-009. Result: **FAIL — APP_DEFECT**, failing step "AC2 resend path:
> `tapOn: id signup_submit_cta` — gRPC UNAVAILABLE (io.grpc.stub.ClientCalls) after completing AC1
> successfully; overall flow timed out at 180s." This is a **new finding vs Run 3** (where jm-009
> was RC-10 FLOW_BUG): in the reliable re-run, AC1 (happy path) completed, then the AC2
> resend/collision sub-flow tore down and re-established the Maestro→app gRPC channel and it dropped
> with `UNAVAILABLE`. The QA artifact categorizes this **APP_DEFECT**. **Net:** RC-10 (AC1
> phone-entry) is the QA-owned flow fix that gets AC1 green; the AC2 gRPC-UNAVAILABLE on the
> sub-flow boundary is a **separate, newly-surfaced defect** that also needs triage (likely a
> Maestro driver/channel re-attach issue at the sub-flow seam → could be FLOW_BUG harness-stability
> OR an app-side channel teardown). Item stays **PARTIAL**. Owner actions updated below.
