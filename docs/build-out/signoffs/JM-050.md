# JM-050 — Address Detail Form

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (FINAL, W1-QA reliable re-run). **Wave:** W1.
> **Flow:** `.maestro/flows/jm-050-address-detail-form.yaml`.
> **Run result:** **FAIL — APP_DEFECT** (`64_W1_QA_RESULTS.md` Batch B row 20).
> **Demo-critical:** No (P2). Not on the customer demo path.

## STATUS: BLOCKED — APP_DEFECT (cascades from the shell + JM-049 chain; address form never reached)

`64_W1_QA_RESULTS.md` (authoritative, complete — row 20) records jm-050 **FAIL** at
`Assertion is false: id: saved_address_add_cta is visible` — and the artifact's own note states the
cause precisely: *"saved-addresses screen not reached (shell + profile chain broken)."* The flow
reaches the address form by tapping `saved_address_add_cta` on the saved-addresses screen, which it
reaches via the shell Profile tab. Two upstream defects break that chain: (a) the
`shell_tab_requests`/`shell_tab_profile` shell-landing defect (jm-035 row 18, jm-049 row 19) and
(b) the saved-addresses CTA render-state (jm-024 AC2 row 7, JM-049). The address-form's own ACs are
**unverifiable** until both are fixed.

**PO grounding (source-checked):** the address-detail route IS registered —
`app_router.dart` l.678-684 (`name: 'address-detail'` → `AddressDetailFormScreen` at
`/settings/addresses/edit`). The form screen is a stub with `address_detail_form_root` +
`address_form_save_cta` signature ids (per the router comment l.673-676), pending the JM-050
engineer filling in the full field set. So JM-050 is blocked on JM-049 first, then needs its own
form-completion verification.

### AC → Evidence

| AC | Requirement | Evidence | Result |
|----|-------------|----------|--------|
| AC1 | Form has map pin/preview + label + Building + Floor/apt + Delivery notes + COD Phone | form never reached (blocked at saved-addresses CTA) | NOT-REACHED |
| AC2 | `address_form_save_cta` → `saved-addresses` | not reached | NOT-REACHED |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: saved_address_add_cta is visible` (the JM-049 CTA the
  flow taps to open the form).
- **Category:** **APP_DEFECT** — cascades from JM-049 (saved-addresses screen render-state defect).

### Owner action to close

1. **app-eng, P0 — FIRST fix the shell + JM-049 chain (cluster):** (a) render
   `shell_tab_requests`/`shell_tab_profile` after `customer_logged_in` (`shell_screen.dart` —
   shared with jm-035/jm-049), then (b) render `saved_address_add_cta` in all states on
   `saved_locations_screen.dart` (shared with jm-024 AC2/jm-049). + backender: seed the
   `has_saved_addresses` saved-locations fixture. JM-050's flow cannot start until both land.
2. **app-eng (JM-050), P2 — then:** fill `lib/features/location/presentation/screens/address_detail_form_screen.dart`
   with the full field set (map pin/preview, label, building, floor/apt, delivery notes, COD phone)
   each carrying its `address_form_*` id, and wire `address_form_save_cta` → `settings-addresses`.
3. **QA (JM-050), P2:** re-run once the shell + JM-049 are green and the form fields land.

### Definition of Done checklist (CTO brief §10)

- [x] Route registered (`address-detail` at `/settings/addresses/edit`).
- [ ] Form fields implemented (stub today — JM-050 engineer fills them).
- [ ] **Maestro flow passes** — FAILS at the JM-049 prerequisite CTA; form not reached.
- [ ] Reviewer approved / **PO signed off** — BLOCKED on JM-049 first, then JM-050 form completion.
