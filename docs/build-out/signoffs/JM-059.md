# JM-059 — Language Settings (register + wire)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W4 FINAL sign-off). **Wave:** W4.
> **Flow:** `.maestro/flows/jm-059-language-settings.yaml`.
> **Authoritative run (`68_W34_QA_RESULTS.md` Batch A #6):** **FAIL** — `customer_profile_wallet_chip` not visible after `language_back` (back-nav does not return to customer profile).
> **Demo-critical:** P1.

## STATUS: PARTIAL — APP_DEFECT. The language screen is reachable but the `language_back` control does not navigate back to `customer-profile` (the `customer_profile_wallet_chip` anchor is not restored).

The language-settings route lands and the RTL-flip option is reachable. The failure is in the back
edge: tapping `language_back` does not land on the customer-profile screen — the
`customer_profile_wallet_chip` widget (the profile-screen anchor) is not visible afterward, so the
return-to-profile AC times out. The back CTA likely uses `context.pop()` to an unexpected stack
position or `context.go` to a non-profile route.

### AC → Evidence

| AC | Requirement | Evidence (Batch A #6) | Result |
|----|-------------|-----------------------|--------|
| AC1 | `language_arabic_option` → instant RTL flip | language screen reachable | PARTIAL (not the failing step) |
| AC2 | `language_back` → `customer-profile` (`customer_profile_wallet_chip` restored) | back does not land on profile | **FAIL — APP_DEFECT (back-nav target)** |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: customer_profile_wallet_chip is visible` (after `language_back`).
- **Category:** **APP_DEFECT** — back navigation from language-settings does not return to `customer-profile`.

### Owner action to close

1. **app-eng (JM-059), P0 — the single fix:** in `language_settings_screen.dart`, wire `language_back`
   to return to `customer-profile` (`goNamed('customer-profile')` or a `pop()` that resolves to the
   profile route) so `customer_profile_wallet_chip` is restored. Confirm via `maestro hierarchy`.
2. **QA (JM-059), P1:** re-run; verify the RTL flip (AC1) and the back edge (AC2).

### Definition of Done checklist (CTO brief §10)

- [x] language-settings route registered + screen reachable (W3/W4 integrator).
- [ ] `language_back` → `customer-profile` (restores `customer_profile_wallet_chip`) — **FAILS** (back-nav mis-wired).
- [ ] **Maestro flow passes** — FAIL at the back edge.
- [ ] **PO signed off** — **PARTIAL** pending the `language_back` target fix (app-eng, P0).
