# JM-037 — Remove Vehicle field from onboarding personal-details (D20)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W2 FINAL sign-off). **Wave:** W2.
> **Flow:** `.maestro/flows/jm-037-remove-vehicle-field.yaml`.
> **Authoritative run (`66_W2_QA_RESULTS.md` §POST-FIX RE-RUN):** **GREEN** — full AC1-3.
> **Demo-critical:** YES — prerequisite for the whole onboarding wizard (JM-038/039/040).

## STATUS: SIGNED ✅

The POST-FIX RE-RUN lands JM-037 GREEN (full AC1-3). The earlier C1 block (photo-step Continue not
advancing to the address step) is resolved by the JM-039 photo-step nav fix; the flow fix added the
media-sheet photo pick the photo step requires before Continue. With the wizard advancing, the
personal-details/address step is reached and the vehicle-field removal (D20) is verified:
`dm_onboarding_address_vehicle_number_field` is `assertNotVisible`, and State/Country/Street/Address
remain.

### AC → Evidence

| AC | Requirement | Evidence (POST-FIX RE-RUN) | Result |
|----|-------------|----------------------------|--------|
| AC1 | NO `dm_onboarding_address_vehicle_number_field` [D20] | assertNotVisible passed | **PASS** |
| AC2 | State/Country/Street/Address remain; `dm_onboarding_continue` → service-area | fields present + continue chains | **PASS** |
| AC3 | wizard advances through the personal-details step | reached on the real screen | **PASS** |

### Exact result + category

- **Result:** GREEN (full AC1-3). Vehicle field removed across widget/`setVehicleNumber`/state/DTO (D20).
- **Category:** — (no defect).

### Owner action to close

None — SIGNED.

### Definition of Done checklist (CTO brief §10)

- [x] `dm_onboarding_address_root` placed + wizard advances to it — verified green.
- [x] AC1 vehicle field removed (assertNotVisible) — verified green.
- [x] AC2 State/Country/Street/Address remain + continue → service-area — verified green.
- [x] **Maestro flow passes** — GREEN (POST-FIX RE-RUN).
- [x] **PO signed off** — **SIGNED**.
