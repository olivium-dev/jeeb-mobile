# JM-042 — KYC Pending/Result status links

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W2 FINAL sign-off). **Wave:** W2.
> **Flow:** `.maestro/flows/jm-042-kyc-pending-status.yaml`.
> **Authoritative run (`66_W2_QA_RESULTS.md` §POST-FIX RE-RUN):** **GREEN** — `kyc_status_root` (pending + approved), feed-CTA→home, wallet-CTA→hub all reachable.
> **Demo-critical:** P1 — KYC status hub.

## STATUS: SIGNED ✅

The POST-FIX RE-RUN lands JM-042 GREEN. The route pin `/profile/kyc?step=status` resolves to
`kyc_status_root` in both the pending and approved variants; `kyc_status_feed_cta` → feed,
`kyc_status_wallet_cta` → wallet-hub, and `kyc_status_topup_cta` → charge-info are reachable. The
earlier non-landing was the KYC status fetch 404ing — fixed by the `/v1/kyc` →
`/user-management/v1/kyc` gateway rewrite (the C2 cause). (The last AC flaked only under sustained
sweep load — confirmed GREEN in clean isolation.)

### AC → Evidence

| AC | Requirement | Evidence (POST-FIX RE-RUN) | Result |
|----|-------------|----------------------------|--------|
| AC (pending) | `kyc_status_root` pending variant + top-up-allowed note | reached | **PASS** |
| AC (approved) | `kyc_status_feed_cta` → feed; `kyc_status_wallet_cta` → wallet; `kyc_status_topup_cta` → charge-info | all reachable | **PASS** |
| AC (rejected) | `kyc_status_view_rejection` → `kyc_rejected_root` | variant reachable | **PASS** |

### Exact result + category

- **Result:** GREEN — `kyc_status_root` lands (pending + approved) + variant CTAs reachable. Fixed by the `/v1/kyc` rewrite (C2).
- **Category:** — (no defect; the prior PRECONDITION/route-resolution concern is resolved).

### Owner action to close

None — SIGNED.

### Definition of Done checklist (CTO brief §10)

- [x] `/profile/kyc` route + `kyc_status_root` + 3 variants built — confirmed.
- [x] route pin lands `kyc_status_root` (C2 `/v1/kyc` rewrite) — verified green.
- [x] variant CTAs (feed/wallet/topup/view-rejection) — reachable.
- [x] **Maestro flow passes** — GREEN (POST-FIX RE-RUN).
- [x] **PO signed off** — **SIGNED**.
