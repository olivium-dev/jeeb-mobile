# JM-056 — Transaction Detail (per-type)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W3 FINAL sign-off). **Wave:** W3.
> **Flow:** `.maestro/flows/jm-056-transaction-detail.yaml`.
> **Authoritative run (`68_W34_QA_RESULTS.md` Batch A #3):** **FAIL** — `txn_detail` not visible.
> **Demo-critical:** P1.

## STATUS: BLOCKED — PRECONDITION (missing seam/journey seed + mock endpoint). The per-type transaction-detail screen needs a seeded fee transaction + the W3m txn-by-id endpoint; neither is implemented. No app defect proven.

The transaction-detail screen renders a specific seeded transaction (fee-won / refund / penalty).
The flow launches with journey `jeeber_wallet_fee_txn` and asserts `txn_detail`, but the journey
seed is not wired and the W3m txn-by-id endpoint does not exist, so the detail screen has no data to
resolve and the assertion times out. Same blocking question #2 (wallet data contract W3m) as JM-055
— backender + seam, not an app defect.

### AC → Evidence

| AC | Requirement | Evidence (Batch A #3) | Result |
|----|-------------|-----------------------|--------|
| AC | `txn_detail` per-type copy (Fee-won = exact 10% + pinned price [D37]; Refund/Penalty + dispute link [D2]); `txn_detail_order_link` → `order-summary-pinned`; `txn_detail_dispute_link` → `dispute-open-evidence` | `txn_detail` not visible — no seeded txn | **FAIL — PRECONDITION (missing seed)** |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: txn_detail is visible`.
- **Category:** **PRECONDITION** — missing seed.

### Missing seed (name exactly — do NOT fix in QA)

- **Seam/journey seed:** `jeeb.seam.journey=jeeber_wallet_fee_txn` (seam bootstrap must accept and route-pin it).
- **Mock endpoint (W3m):** `GET /wallet-service/v1/jeeb/wallet/transactions/:id` returning per-type detail; stable ids `txn-fee-001` / `txn-refund-001`.

### Owner action to close

1. **backender (mock), P0 — W3m:** define `GET /wallet-service/v1/jeeb/wallet/transactions/:id` with per-type payloads + stable ids, and add the `jeeber_wallet_fee_txn` fixture to `jeeb-mock-backend/src/fixtures/journey-seed.ts`.
2. **seam (app), P0:** add `jeeber_wallet_fee_txn` to the `JourneySeed` landing contract (`lib/core/dev_seam/`) so the seam pins the transaction-detail route.
3. **QA (JM-056), P1:** after the seed + endpoint land, re-run; verify per-type copy + `txn_detail_order_link` / `txn_detail_dispute_link` targets.

### Definition of Done checklist (CTO brief §10)

- [x] transaction-detail route + screen shell built (W3/W4 integrator).
- [ ] `jeeber_wallet_fee_txn` journey seed + W3m endpoint — **BLOCKED (backender + seam)**.
- [ ] per-type detail + order/dispute links (AC) — not reachable until seed lands.
- [ ] **Maestro flow passes** — FAIL at `txn_detail` (PRECONDITION).
- [ ] **PO signed off** — **BLOCKED** on the `jeeber_wallet_fee_txn` seed + W3m endpoint.
