# JM-031 — Order Summary + Pinned Price (header widget)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (FINAL, W1-QA reliable re-run). **Wave:** W1.
> **Flow:** `.maestro/flows/jm-031-order-summary-pinned.yaml`.
> **Run result:** **FAIL — APP_DEFECT** (`64_W1_QA_RESULTS.md` Batch B row 14).
> **Demo-critical:** **YES** — the pinned summary is the bridge between chat/track on the demo path
> (jm-031 summary → track) and is asserted in BOTH chat + tracking (AC4).

## STATUS: PARTIAL — APP_DEFECT (`order_summary_pinned` not rendered)

`64_W1_QA_RESULTS.md` records jm-031 **FAIL** at
`Assertion is false: id: order_summary_pinned is visible` — the order-summary pinned widget does
not render its signature id after the journey seed (`order_accepted` / `active_delivery`). The
widget is either not injected into the chat/tracking host or the seeded accepted-order data does
not populate it.

### AC → Evidence

| AC | Requirement | Evidence | Result |
|----|-------------|----------|--------|
| AC1 | `order_summary_pinned` shows accepted price + jeeber name/rating + ETA + tier + "Pay cash on delivery" | `order_summary_pinned` NOT visible | **FAIL — APP_DEFECT** |
| AC2 | `order_summary_open_chat` → `order-chat` | not reached | NOT-REACHED |
| AC3 | `order_summary_track` → `order-tracking` | not reached | NOT-REACHED |
| AC4 | `order_summary_pinned` visible in BOTH chat + tracking | not reached | NOT-REACHED |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: order_summary_pinned is visible`.
- **Category:** **APP_DEFECT** — the pinned-summary widget id is not rendered on the accepted-order
  chat/tracking surface.

### Owner action to close

1. **app-eng (JM-031), P0:** inject the pinned-summary header widget into both the order-chat and
   order-tracking hosts (per the AC: "injected into chat + tracking"), with
   `Semantics(identifier: 'order_summary_pinned', container: true)` plus the field ids
   (`order_summary_price`/`_jeeber_name`/`_eta`/`_tier`/`_cash_label`/`_open_chat`/`_track`). Bind to
   `GET /delivery-service/v1/delivery/:deliveryId` + `GET /delivery-service/v1/requests/:requestId`.
   Confirm the id appears under the `order_accepted` seed via `maestro hierarchy`.
2. **backender (mock), P0:** confirm `jeeb.seam.journey=order_accepted` seeds the accepted request +
   winning offer + conversation + delivery with populated price/jeeber/ETA/tier (per
   `63_W1_TEST_PLAN §4.1`), so the widget has data to render.
3. **QA (JM-031), P1:** after AC1 renders, assert AC4 in both chat and tracking contexts (W1 exit
   checklist item).

### Definition of Done checklist (CTO brief §10)

- [ ] `order_summary_pinned` widget rendered (AC1) — MISSING.
- [ ] Visible in both chat + tracking (AC4) — not reached.
- [ ] **Maestro flow passes** — FAILS at AC1.
- [ ] Reviewer approved / **PO signed off** — PARTIAL pending the widget injection + seed data.
