# JM-005 — Biometric Unlock (replace placeholder + real cubit)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (updated Run 3). **Wave:** W0.
> **Flow:** `.maestro/flows/jm-005-biometric-unlock.yaml`. **Run result:** **PASS** (`61_W0_QA_RESULTS.md` Run 3).
> **Demo-critical:** **YES** (the gate for every returning logged-in user).

## STATUS: SIGNED

All three ACs pass on-device in Run 3. RC-3 — the Run 2 blocker where the wired
`BiometricGateway` was `UnavailableBiometricGateway` (always returning `false`) — is
**resolved**: a `DevBiometricGateway` returning `true` from `authenticate()` is now wired in
`injection_container.dart` under `kDebugMode`. Tapping `biometric_unlock_authenticate_cta`
now releases the lock phase and routes to the last-used tab (`shell_tab_requests`). AC1 (splash
lands `/lock` with `biometric_unlock_prompt`, no OTP) and AC3 (use-password → `/login`) held
green from Run 2; AC2 now goes green with the dev gateway approving. This is the demo-critical
gate for every returning logged-in user.

### AC → Evidence

| AC | Requirement (from `30_BACKLOG.md`) | Flow evidence | Result |
|----|-----------------------------------|---------------|--------|
| AC1 | Returning logged-in + biometric enabled → splash routes to `/lock`, `biometric_unlock_prompt` shown (no OTP) [D23] | seam seeds biometric session; splash lands `biometric_unlock_prompt` | **PASS** |
| AC2 | `biometric_unlock_authenticate_cta` succeeds → last-used tab (`shell_tab_requests`) [D23/D75] | CTA tapped; `DevBiometricGateway.authenticate()` returns true → phase releases → `shell_tab_requests` visible | **PASS** |
| AC3 | Biometric fails/declined → `biometric_unlock_use_password_link` → `/login` (`login_email_field`) [JM-007] | use-password link → `login_email_field` | **PASS** |

### Exact failing AC + QA category

- None. All 3 ACs PASS (`61_W0_QA_RESULTS.md` Run 3 row jm-005).
- **RC-3 resolution:** `DevBiometricGateway` (returns `true` from `authenticate()` in
  `kDebugMode`) wired in `lib/core/di/injection_container.dart` in place of
  `UnavailableBiometricGateway`. The seam, lock-screen routing, and cubit were already correct
  in Run 2 (AC1/AC3 green); the dev gateway was the only missing piece and AC2 is now green.

### Definition of Done checklist (CTO brief §10)

- [x] Screen matches blueprint + decisions (`biometric-unlock`, D23/D8/D79; real cubit; app-level instance the router watches).
- [x] OMDS components used.
- [x] Nav wired both directions (`/lock` ↔ `/login` proven by AC3; `/lock` → shell proven by AC2).
- [x] Wired to mock — N/A by design (biometric unlock is a local gate, no `/auth/refresh` call).
- [x] `Semantics(identifier:)` present (`biometric_unlock_prompt`/`_authenticate_cta`/`_use_password_link` — all 3 ACs confirm).
- [x] **Maestro flow passes on emulator** — PASS (Run 3, all 3 ACs).
- [x] `flutter analyze` clean (gate held through the Run 3 rebuild after the DI change).
- [x] Reviewer approved (`DevBiometricGateway` + DI wiring landed).
- [x] **PO signed off.**
