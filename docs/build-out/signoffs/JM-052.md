# JM-052 — Earnings & Fees Dashboard (fee-only reframe)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W3 FINAL sign-off). **Wave:** W3.
> **Flow:** `.maestro/flows/jm-052-earnings-dashboard.yaml`.
> **Authoritative run (`68_W34_QA_RESULTS.md` Batch A #1):** **FAIL** — `earnings_member_since` not visible.
> **Demo-critical:** P1.

## STATUS: PARTIAL — APP_DEFECT. The earnings dashboard route lands and `earnings_total_cash` is built, but the coined `earnings_member_since` Semantics id is not placed on the member-since chip.

The earnings-fees-dashboard is reachable (verified in W2 via jm-053 AC4 affordance) and the
fee-only economics frame is partially built (`earnings_total_cash` lands). The failure is narrow:
the coined identifier `earnings_member_since` (per `67_W34_TEST_PLAN §Coined identifiers`) is not
wired onto the member-since chip widget, so the AC asserting it times out.

### AC → Evidence

| AC | Requirement | Evidence (Batch A #1) | Result |
|----|-------------|-----------------------|--------|
| AC1 | `earnings_total_cash` ("net, off-wallet COD") + `earnings_fees_paid` ("captured 10%") + net-per-offer [D41/D44] | `earnings_total_cash` lands (partial build) | PARTIAL |
| AC2 | member-since shown (NOT gross/commission/net-payout) — `earnings_member_since` coined id | `earnings_member_since` not visible | **FAIL — APP_DEFECT (coined id not placed)** |
| AC3 | `earnings_wallet_link` → `wallet-hub`; `earnings_activity_link` → `wallet-activity-list` | not reached (failed at AC2) | not reached |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: earnings_member_since is visible`.
- **Category:** **APP_DEFECT** — coined Semantics id `earnings_member_since` not placed on the member-since chip on the earnings-fees-dashboard screen.

### Owner action to close

1. **app-eng (JM-052), P0 — the single fix:** on `EarningsDashboardScreen`
   (`lib/features/wallet/.../earnings_dashboard_screen.dart`), place
   `Semantics(identifier: 'earnings_member_since')` on the member-since chip widget (the [COINED] id
   from `67_W34_TEST_PLAN §Coined identifiers`). Confirm via `maestro hierarchy` that
   `earnings_member_since` appears.
2. **QA (JM-052), P1:** re-run; once the id lands, verify AC3 wallet/activity links navigate.

### Definition of Done checklist (CTO brief §10)

- [x] earnings-fees-dashboard route + `earnings_total_cash` built (partial).
- [ ] `earnings_member_since` coined Semantics id placed — **FAILS** (id missing; one-line fix).
- [ ] AC3 wallet/activity links verified — not reached.
- [ ] **Maestro flow passes** — FAIL at AC2.
- [ ] **PO signed off** — **PARTIAL** pending `earnings_member_since` id placement (app-eng, P0).
