# JM-036 — DELIVERY Tab KYC gate (register-prompt vs feed)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W2 FINAL sign-off). **Wave:** W2.
> **Flow:** `.maestro/flows/jm-036-delivery-tab-kyc-gate.yaml`.
> **Authoritative run (`69_REGRESSION.md` Punch-list Verify — 2026-06-19):** **GREEN** — all ACs EXIT 0; AC4 bell now reaches the real `notifications_root` (F-1 closed — AP-9 placeholder retired).
> **Demo-critical:** YES — the jeeber entry gate.

## STATUS: SIGNED ✅ — all ACs GREEN (AC4 un-AP-9'd; F-1 CLOSED)

The POST-FIX RE-RUN (boot-hold fix: seam mock-POSTs decoupled + Firebase init `.timeout(5s)`) landed
AC1–AC3 GREEN. The Punch-list Verify (2026-06-19, clean install) re-ran the full flow with AC4's
assertion now pointed at the real `notifications_root` (JM-057 shipped) and recorded **all ACs
EXIT 0**. Both gate variants render correctly: not-approved → `delivery_register_prompt`,
KYC-approved → `jeeber_feed_root` (gate keyed off real `SeamJeeberKycStatusGate`); the
`delivery_tab_wallet_chip` → `wallet-hub` edge works; and `delivery_tab_bell` → `notifications_root`
now lands the real target. **F-1 is CLOSED** — the stale AP-9 placeholder (`jeeber_feed_root`)
assertion has been retired in favour of `notifications_root`; the AC4 PASS confirms both the app and
the assertion are correct.

### AC → Evidence

| AC | Requirement | Evidence (Punch-list Verify 2026-06-19) | Result |
|----|-------------|------------------------------------------|--------|
| AC1 | not-approved → `delivery_register_prompt`; `delivery_register_now_cta` → onboarding | register-prompt reached | **PASS** |
| AC2 | KYC-approved → `jeeber_feed_root` | approved-feed reached | **PASS** |
| AC3 | `delivery_tab_wallet_chip` → `wallet-hub` | wallet-chip → hub | **PASS** |
| AC4 | `delivery_tab_bell` → `notifications_root` (JM-057 shipped) | bell tap reaches real `notifications_root` (EXIT 0) | **PASS** |

### Exact result + category

- **Result:** GREEN — all ACs verified; AC4's AP-9 placeholder retired, asserting real `notifications_root`.
- **Category:** — (no defect; F-1 flow-bug closed — assertion swapped to the shipped JM-057 target).

### Owner action to close

None — SIGNED. F-1 closed: AC4 now asserts and reaches `notifications_root`.

### Definition of Done checklist (CTO brief §10)

- [x] register-prompt vs feed gate built + both variants reached (AC1/AC2) — verified green.
- [x] `delivery_tab_wallet_chip` → wallet-hub (AC3) — verified green.
- [x] `delivery_tab_bell` → `notifications_root` (AC4, real target) — verified green; F-1 closed.
- [x] **Maestro flow passes** — all ACs EXIT 0 (Punch-list Verify 2026-06-19).
- [x] **PO signed off** — **SIGNED**.
