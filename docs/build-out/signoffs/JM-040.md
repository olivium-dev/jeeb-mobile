# JM-040 — KYC Identity (remove Vehicle step, D20) + chain to funding

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W2 FINAL sign-off). **Wave:** W2.
> **Flow:** `.maestro/flows/jm-040-kyc-identity.yaml`.
> **Authoritative run (`66_W2_QA_RESULTS.md` §POST-FIX RE-RUN):** **GREEN** — full wizard (36 steps): photo→address→service-area(home-base pin)→KYC submit→funding.
> **Demo-critical:** YES — KYC is the gate to offering.

## STATUS: SIGNED ✅

The POST-FIX RE-RUN lands JM-040 GREEN through the full 36-step wizard. The upstream C1 nav block
(JM-039) is resolved and the wizard advances photo → address → service-area (home-base pin) → KYC →
funding. The KYC wizard has NO vehicle step (D20 — `kyc_vehicle_step`/`VehicleType` removed), the
gov-ID front/back + selfie steps are present, and `kyc_submit_cta` chains to the funding explainer
(`funding_explainer`) — fixed by the `/v1/kyc` gateway rewrite (the K1/C2 cause). Photo-pick +
home-base pin sequence were added by the flow fix.

### AC → Evidence

| AC | Requirement | Evidence (POST-FIX RE-RUN) | Result |
|----|-------------|----------------------------|--------|
| AC1 | NO `kyc_vehicle_step` [D20]; gov-ID front/back + selfie; `kyc_submit_cta` → `funding_explainer` | no vehicle step; id/selfie present; submit → funding | **PASS** |

### Exact result + category

- **Result:** GREEN (full 36-step wizard, submit → funding). Vehicle step removed (D20); KYC gateway path reconciled (the `/v1/kyc` rewrite resolved the K1/C2 cause).
- **Category:** — (no defect).

### Owner action to close

None — SIGNED.

### Definition of Done checklist (CTO brief §10)

- [x] `/profile/kyc` route + `funding_explainer` target — confirmed + reached.
- [x] wizard reaches KYC step (AC1); NO vehicle step (D20); id/selfie present — verified green.
- [x] K1 KYC gateway path reconciled (`/v1/kyc` rewrite); `kyc_submit_cta` → `funding_explainer` — verified green.
- [x] **Maestro flow passes** — GREEN (POST-FIX RE-RUN).
- [x] **PO signed off** — **SIGNED**.
