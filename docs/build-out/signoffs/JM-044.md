# JM-044 — Offering KYC Gate (D38 invariant)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W2 FINAL sign-off — residual verify). **Wave:** W2.
> **Flow:** `.maestro/flows/jm-044-offer-kyc-gate.yaml`.
> **Authoritative run (`66_W2_QA_RESULTS.md` §W2 RESIDUAL VERIFY, RD-1):** **FAIL** at AC3 — `gate_register_link` tap lands on `jeeber_feed_root` instead of `delivery_register_prompt`.
> **Demo-critical:** P0 — the D38 offering gate.

## STATUS: PARTIAL — APP_DEFECT (AC3 only). AC1/AC2/AC5 PASS; the gate is reached and routes correctly except the register-link target is mis-wired.

The clean-install residual verify confirms the gate is now fully REACHABLE (the earlier feed-seed
MOCK_GAP is resolved): the unapproved jeeber routes through `offer_kyc_gate`, `gate_start_kyc_cta`
and `gate_back_cta` behave, and the top-up note shows. The **single remaining defect** is AC3: the
`gate_register_link` route target is mis-wired — tapping it navigates **back to `jeeber_feed_root`**
(screenshot-confirmed: the jeeber feed reappears) instead of pushing `delivery_register_prompt`.

**PO code verification:** the gate screen + register link ARE built
(`offer_kyc_gate_screen.dart`, route `/jeeber/offer-gate` → name `offer-kyc-gate`,
`app_router.dart` L1067-1068). The defect is narrow and precise: the `gate_register_link` handler
emits a feed navigation (likely `context.go('/')` / pop-to-feed) where it must navigate to the
delivery register-prompt (`context.goNamed('delivery-register-prompt')` or the equivalent
`/jeeber/onboarding` register-prompt route). This is a one-line route-target fix, not a structural
gap.

### AC → Evidence

| AC | Requirement | Evidence (Residual Verify) | Result |
|----|-------------|----------------------------|--------|
| AC1 | unapproved `feed_make_offer_cta` → `offer_kyc_gate` (not composer) | gate reached | **PASS** |
| AC2 | `gate_start_kyc_cta` → `kyc_wizard_root` | KYC start routes correctly | **PASS** |
| AC3 | `gate_register_link` → `delivery_register_prompt` | tap lands on `jeeber_feed_root` instead | **FAIL — APP_DEFECT (mis-wired route target)** |
| AC5 | approved → composer directly (gate skipped) + top-up note | gate skip + note correct | **PASS** |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: delivery_register_prompt is visible` (after `gate_register_link` tap; app shows `jeeber_feed_root`).
- **Category:** **APP_DEFECT** — the `gate_register_link` route target is wrong (navigates to feed, not register-prompt). AC1/AC2/AC5 all pass; the gate body is correct.

### Owner action to close

1. **app-eng (JM-044), P0 — the single fix:** in `offer_kyc_gate_screen.dart`, re-wire the
   `gate_register_link` handler to navigate to `delivery_register_prompt` (the delivery
   register-prompt route / `goNamed('delivery-register-prompt')` or `/jeeber/onboarding`) instead of
   the current feed navigation (`go('/')`/pop). Confirm via `maestro hierarchy` that
   `delivery_register_prompt` appears after the tap.
2. **QA (JM-044), P1:** re-run; AC1/AC2/AC5 already green — AC3 lands the register-prompt to SIGN.

### Definition of Done checklist (CTO brief §10)

- [x] `offer_kyc_gate` built + routed; AC1 (gate reached), AC2 (`gate_start_kyc_cta`), AC5 (approved skip + note) — verified green.
- [ ] AC3 `gate_register_link` → `delivery_register_prompt` — **FAILS** (mis-wired to feed; one-line route-target fix).
- [ ] **Maestro flow passes** — FAIL at AC3 (RD-1).
- [ ] **PO signed off** — **PARTIAL** pending the AC3 register-link route-target fix (app-eng, P0).
