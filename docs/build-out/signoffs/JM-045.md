# JM-045 — Structured Offer Composer (economics layer, G3)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W2 FINAL sign-off — residual verify). **Wave:** W2.
> **Flow:** `.maestro/flows/jm-045-offer-composer.yaml`.
> **Authoritative run (`66_W2_QA_RESULTS.md` §W2 RESIDUAL VERIFY, RD-4):** **FAIL** at AC5 only — insufficient-sheet not raised. AC1–AC4 fully PASS.
> **Demo-critical:** P0 — the jeeber offer composer (spine). The happy-path send is GREEN.

## STATUS: PARTIAL — composer + send happy-path GREEN (AC1-4); AC5 insufficient-sheet BLOCKED on backend mock-fix O1 (offer 402) + W1m (jeeber wallet seed honored). No app defect.

The clean-install residual verify confirms the composer happy path is **fully working**: AC1-3
economics (`offer_composer_fee_line` 10% / `offer_composer_net_line` / `offer_composer_reserve_note`),
the SLA-bounded ETA dropdown, and the `offer_composer_order_ref` header all pass; **AC4 is now GREEN**
— a sufficient send returns to `jeeber_feed_root` (the `go('/')` success-nav fires correctly). The
prior "send does not return to feed" framing is fully resolved.

The **only** remaining failure is AC5: with `wallet_state=insufficient`, the mock
`POST /offer-service/v1/offers` returns **200** (not 402), so the app accepts the offer and navigates
to the feed instead of raising `insufficient_balance_sheet`. This is a **backend MOCK_GAP** — O1
(offer-service 402 path) + W1m (jeeber wallet balance honored per the insufficient seed) are not
implemented. There is **NO seam bypass** for this path; the app cannot raise the sheet until the mock
returns 402.

### AC → Evidence

| AC | Requirement | Evidence (Residual Verify) | Result |
|----|-------------|----------------------------|--------|
| AC1 | `offer_composer_fee_line` exact 10% [D37/D44] | reached + passed | **PASS** |
| AC2 | `offer_composer_net_line` "you earn (cash)" + `offer_composer_reserve_note` (reserved/charged/released) [D1/D44] | passed | **PASS** |
| AC3 | `offer_composer_eta_dropdown` bounded by tier SLA [D14] + `offer_composer_order_ref` header | passed | **PASS** |
| AC4 | `offer_composer_send_cta` (sufficient) → `jeeber_feed_root` (10% reserved) | **GREEN** — send returns to feed | **PASS** |
| AC5 | insufficient → `insufficient_balance_sheet` [JM-046] | mock returns 200 not 402; app navigates to feed | **FAIL — MOCK_GAP (O1+W1m)** |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: insufficient_balance_sheet is visible` (after send with `wallet_state=insufficient`; app shows `jeeber_feed_root`).
- **Category:** **MOCK_GAP (O1+W1m)** — `POST /offer-service/v1/offers` does not return 402 for the insufficient wallet seed. NOT an app defect; the composer + send + insufficient-handling are app-side ready, blocked on the mock 402 path.

### Owner action to close

1. **backender (mock), P0 — O1 + W1m (the single blocker):** `POST /offer-service/v1/offers` must
   return **402 {needed, available}** when the seeded jeeber wallet (`POST /__mock/seed/wallet
   {state:"insufficient", userId:"user-jeeber-002"}`) is insufficient — and `GET /wallet-service/v1/
   jeeb/wallet` must resolve to the seeded `user-jeeber-002` balance (W1m), not fall back to
   `user-client-001`. Files: `jeeb-mock-backend` offer-service (402 path) + wallet-service (jeeber
   seed honored). Shared with JM-046.
2. **QA (JM-045), P1:** after O1/W1m, re-run AC5 — the app already routes `offer-insufficient-balance`
   on a 402; verify `insufficient_balance_sheet` raises with needed-vs-available. AC1-4 already green.

### Definition of Done checklist (CTO brief §10)

- [x] composer built; AC1-3 economics + ETA + order-ref — verified green.
- [x] AC4 send → `jeeber_feed_root` (sufficient path) — **GREEN** (resolved).
- [ ] AC5 insufficient → `insufficient_balance_sheet` — **BLOCKED on O1+W1m** (mock 402 path; no seam bypass).
- [ ] **Maestro flow passes** — FAIL at AC5 only (RD-4).
- [ ] **PO signed off** — **PARTIAL**; AC1-4 signed, AC5 parked on backend O1+W1m.
