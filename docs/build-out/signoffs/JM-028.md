# JM-028 — Offer Review (route the orphaned ClientOffersScreen)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W2 FINAL sign-off — W1 residual re-check). **Wave:** W1 (re-verified under W2 build).
> **Flow:** `.maestro/flows/jm-028-offer-review.yaml`.
> **Authoritative run (`66_W2_QA_RESULTS.md` §W2 RESIDUAL VERIFY, RD-3):** **FAIL** at AC4 only — `offer_review_cancel_cta` Semantics id not placed. AC1–AC3 PASS with the route-pin fix.
> **Demo-critical:** **YES** — the OFFER step of the customer demo path.

## STATUS: PARTIAL — NO W1 REGRESSION. AC1-3 (cards + sort, jeeber-profile, accept→sheet) PASS; AC4 fails on a missing `offer_review_cancel_cta` Semantics id (pre-existing APP_DEFECT, not a W2 regression).

The clean-install residual verify with the updated route-pin lands JM-028 **AC1-3 GREEN**: the
offer-review list renders all `offer_card_<id>` cards with price/ETA/rating + sort controls (AC1),
the jeeber-profile push/close works (AC2), and the accept CTA raises `offer_accept_sheet` (AC3). The
route-pin fix fully resolves the prior W1 Replies→offers seed-ordering blocker (the Replies tab now
opens the seeded `req-client-001-offers`, not a stale base-fixture request). The earlier Batch-B
first-step `shell_tab_requests` failure was the systemic boot-hold (seam mock POSTs + Firebase init),
now fixed.

The **single remaining defect** is AC4: no widget on the offer-review list screen carries
`Semantics(identifier: 'offer_review_cancel_cta')`. A cancel/close control may exist but the
identifier is missing — a pre-existing APP_DEFECT (the cancel-request edge `JM-030` target), **not** a
W2 regression.

### AC → Evidence

| AC | Requirement | Evidence (Residual Verify) | Result |
|----|-------------|----------------------------|--------|
| AC1 | per-Jeeber `offer_card_<id>` price/ETA/rating + "Pay $X cash" [D11]; sort controls `offer_review_sort_<key>` | cards + sort render | **PASS** |
| AC2 | `offer_card_<id>_name` → jeeber-profile; `..._close` returns | jeeber-profile push/close | **PASS** |
| AC3 | `offer_card_<id>_accept_cta` → `offer_accept_sheet` [JM-029] | accept → sheet | **PASS** |
| AC4 | `offer_review_cancel_cta` → `cancel-request-confirm` [JM-030] | no widget with `offer_review_cancel_cta` id | **FAIL — APP_DEFECT (id not placed)** |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: offer_review_cancel_cta is visible` (no such Semantics id on the offer-review list screen).
- **Category:** **APP_DEFECT** — missing Semantics identifier on the cancel/close control (pre-existing, not a W2 regression). AC1-3 pass.
- **W1-regression verdict:** **NO REGRESSION.** Customer session seam, route-pin landing, offer-card rendering, and accept sheet are all intact.

### Owner action to close

1. **app-eng (JM-028), P0 — the single fix:** on the offer-review list screen
   (`client_offers_screen.dart`), place `Semantics(identifier: 'offer_review_cancel_cta')` on the
   cancel control and wire it to `cancel-request-confirm` (`cancel_request_sheet`, JM-030). Confirm
   via `maestro hierarchy` that `offer_review_cancel_cta` appears.
2. **QA (JM-028), P1:** re-run; AC1-3 already green — AC4 cancel CTA asserts to SIGN.

### Definition of Done checklist (CTO brief §10)

- [x] route-pin fix lands the offer-review list; AC1 (cards + sort), AC2 (jeeber-profile), AC3 (accept → sheet) — verified green.
- [ ] AC4 `offer_review_cancel_cta` placed + wired → `cancel-request-confirm` — **FAILS** (id missing; one-line + wiring fix).
- [ ] **Maestro flow passes** — FAIL at AC4 only (RD-3).
- [ ] **PO signed off** — **PARTIAL** (no W1 regression) pending the AC4 `offer_review_cancel_cta` id + wiring (app-eng, P0).
