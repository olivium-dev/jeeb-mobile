# JM-008 — Sign Up (email-first, decision-gated)

> **✅ FINAL VERIFY 2026-06-19 — SIGNED (GREEN).** Fresh APK, maestro EXIT 0. All ACs pass. RC-10
> fixed: AC2/3/4/5 reach the sign-up screen via the login→`login_signup_link` funnel (the bare
> `/sign-up` route-pin didn't survive the logged-out redirect). Stale STATUS header below
> superseded. See `69_REGRESSION.md` §"Final Verify".

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (updated Run 3). **Wave:** W0.
> **Flow:** `.maestro/flows/jm-008-signup.yaml`. **Run result:** **FAIL** (`61_W0_QA_RESULTS.md` Run 3).
> **Demo-critical:** **YES (P0)** (new-user onboarding).

## STATUS: PARTIAL — FLOW_BUG (RC-10, QA-owned)

RC-5 (persistent in-memory mock state 409-colliding the fixed signup email) is **resolved**:
the flows now reset mock state, so signup no longer collides on AC1's happy path. Run 3
surfaces a new QA-owned flow bug (RC-10): the same phone-entry-step mismatch that was fixed
in jm-018 AC1 was **not** applied to jm-008. After `signup_submit_cta`, the app navigates to
`/register` = `RegistrationScreen`, which shows the phone-entry hero (`_register_hero`) FIRST;
`OtpVerificationScreen` (`phone_otp_input`) is only pushed after a phone number is entered and
submitted. jm-008 AC1 asserts `phone_otp_input` directly after signup, skipping the
architecturally-required phone-entry step. This is a **QA-owned flow fix** (copy the steps
already proven in jm-018 AC1) — the email-first funnel and the post-signup → phone-entry → OTP
architecture are correct.

### AC → Evidence

| AC | Requirement (from `30_BACKLOG.md`) | Flow evidence | Result |
|----|-----------------------------------|---------------|--------|
| AC1 | `signup_name_field`+`signup_email_field`+`signup_password_field` + `signup_submit_cta` → `phone-otp-verification` (`phone_otp_input`) [G8] | walkthrough→signup reached, fields filled, submitted → lands `_register_hero` (phone-entry hero), NOT `phone_otp_input` (flow doesn't drive the phone-entry step) | **FAIL — FLOW_BUG (RC-10)** |
| AC2 | `signup_password_strength_hint` reflects password; `signup_password_visibility_toggle` flips masking | screen reached + filled; affordances structurally reachable | PASS (likely — upstream of the RC-10 hop) |
| AC3 | `signup_login_link` → `/login` (`login_email_field`) | edge present; reachable | PASS (likely) |
| AC4 | Social row `signup_social_google`/`_facebook`/`_apple` present | social row present | PASS (likely) |
| AC5 | Email collision (409) → `social-collision-prompt` (`social_collision_sheet`) [JM-019, D22] | collision behavior proven (JM-019 PASS); AC5's own first-signup step also needs the phone-entry update before its collision step | PASS (likely — collision edge proven; needs same flow update) |

### Exact failing AC + QA category

- **Failing AC:** **AC1** (happy-path signup → OTP). First failing step: `Assert id: phone_otp_input is visible`.
- **QA failure category:** **FLOW_BUG** (`61_W0_QA_RESULTS.md` Run 3 row jm-008) — **QA-owned**, not an app defect.
- **Root cause (RC-10):** signup success navigates to `/register` whose first view is the
  phone-entry hero (`_register_hero`); `phone_otp_input` only appears after a phone number is
  submitted. jm-018 AC1 was updated to drive this step (RC-6 fix); jm-008 was not.

### Owner action to close

1. **QA (jm-008), P0 — trivial (copy from jm-018 AC1):** between the `signup_submit_cta` tap
   and the `phone_otp_input` assertion, add the phone-entry steps already proven in jm-018 AC1:
   ```yaml
   - extendedWaitUntil: { visible: { id: "_register_hero" }, timeout: 15000 }
   - tapOn: { text: "Phone number" }
   - inputText: "0501234567"
   - tapOn: { text: "Send code" }
   - extendedWaitUntil: { visible: { id: "phone_otp_input" }, timeout: 15000 }
   ```
   Apply the same update before AC5's first-signup collision step. No app or mock change needed.

### Definition of Done checklist (CTO brief §10)

- [x] Screen matches blueprint + decisions (email-first per JM-001; D8/D21/D22/D65/G8; email NOT verified at this step per D21).
- [x] OMDS components used.
- [x] Nav wired (signup → `/register` phone-entry → phone-OTP; → `/login`; → collision sheet on 409 — all by design, collision edge proven).
- [x] Wired to mock (signup route B3 + `/auth/otp/request`) — endpoint reached; mock reset resolves RC-5.
- [x] `Semantics(identifier:)` present (sign-up screen + `_register_hero` + `phone_otp_input` all on-device).
- [ ] **Maestro flow passes on emulator** — **AC1 FAILS** on RC-10 (flow skips the phone-entry step); AC2–5 likely-pass.
- [ ] `flutter analyze` clean — re-verify (no app change expected; flow-only fix).
- [ ] Reviewer approved — pending RC-10 flow fix.
- [ ] **PO signed off** — partial; app funnel correct, blocked solely on the QA flow fix (add phone-entry steps, mirror jm-018 AC1).

> **Re-verification note (2026-06-18, W1-QA reliable re-run — FINAL):** `64_W1_QA_RESULTS.md`
> is now COMPLETE and re-ran jm-008. Result: **FAIL — FLOW_BUG**, failing step "timeout (180s) —
> flow did not complete within 180s; likely hung waiting for `walkthrough_slide_1` or
> `phone_otp_input` after sign-up; collision-path multi-walkthrough exceeded the limit." This
> **corroborates RC-10** (the flow does not drive the post-signup `_register_hero` phone-entry
> step, so AC1 stalls before `phone_otp_input`) and adds that the collision branch (AC5) compounds
> it into a 180s timeout. **Confirmed QA-owned FLOW_BUG, not an app defect.** Item stays **PARTIAL**;
> owner action below (QA, copy jm-018 AC1 phone-entry steps + add a per-scenario mock reset / unique
> email so the collision path doesn't blow the timeout) stands. No app or mock change required.
