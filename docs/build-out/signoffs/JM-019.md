# JM-019 — Social/Email Collision Prompt

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18. **Wave:** W0.
> **Flow:** `.maestro/flows/jm-019-collision-prompt.yaml`. **Run result:** **PASS** (`61_W0_QA_RESULTS.md` Run 2 + Run 3 — held green; Run 3 records all 3 ACs).
> **Demo-critical:** No (breadth — collision-sheet edge case).

## STATUS: SIGNED

All ACs pass on-device (Run 2, held green in Run 3 — Run 3 records all 3 ACs explicitly). The Run 1 PRECONDITION blocker (RC-1 —
`jeeb.seam.social_login=collision_409` dropped) is resolved: the seam is now wired through
`MainActivity.kt seamKeys` + `DevSeamConfig` + the `SocialAuthSeamResolver`, so the social
cubit returns a 409, the `social_collision_sheet` raises, and both exits route correctly.

### AC → Evidence

| AC | Requirement (from `30_BACKLOG.md`) | Flow evidence | Result |
|----|-----------------------------------|---------------|--------|
| AC1 | Collision (email registered via another method) → `social_collision_sheet` visible | `collision_409` seam → social cubit 409 → sheet raises | **PASS** |
| AC2 | `social_collision_continue_cta` → `/login` (`login_email_field`) [D22] | Continue → `login_email_field` | **PASS** |
| AC3 | `social_collision_other_email_cta` → sign-up (`signup_name_field`) [D22] | Other email → `signup_name_field` | **PASS** |

> **Note:** QA's Run 2 summary records "Both ACs pass" for jm-019 — the flow asserts the
> two exit edges (AC2/AC3) gated on the sheet being raised (AC1), so AC1 is implicitly
> proven by the exits landing. All three backlog ACs are satisfied.

### Exact failing AC + QA category

- None. PASS (`61_W0_QA_RESULTS.md` Run 2 row jm-019).

### Definition of Done checklist (CTO brief §10)

- [x] Screen matches blueprint + decisions (`social-collision-prompt`; D22; sheet, not a route).
- [x] OMDS components used.
- [x] Nav wired both directions (Continue → registered `/login`; Other email → registered sign-up) — both proven on-device.
- [x] Wired to mock — triggered by upstream 409 via the social seam resolver.
- [x] `Semantics(identifier:)` present (`social_collision_sheet`/`_continue_cta`/`_other_email_cta`).
- [x] **Maestro flow passes on emulator** — PASS (Run 2, both ACs).
- [x] `flutter analyze` clean (gate held through Run 2 rebuild).
- [x] Reviewer approved (shared social-login seam landed).
- [x] **PO signed off.**
