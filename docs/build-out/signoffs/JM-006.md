# JM-006 — Session-aware Splash routing

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18. **Wave:** W0.
> **Flow:** `.maestro/flows/jm-006-splash-routing.yaml`. **Run result:** **PASS** (`61_W0_QA_RESULTS.md` Run 2 + Run 3 — held green).
> **Demo-critical:** **YES** (cold-start routing determines what every user sees first).

## STATUS: SIGNED

All six session branches pass on-device (Run 2, held green in Run 3). RC-1 (seam not wired) and RC-2 (wrong
AC1 assertion id) — the dual root cause that blocked this item in Run 1 — are both
resolved: `MainActivity.kt seamKeys` now whitelists all `jeeb.seam.*` keys,
`DevSeamConfig.fromMap()` maps them, `SessionSeamBootstrap.seed()` applies them before the
router fires, and the AC1 flow now asserts `walkthrough_slide_1` (the correct first-launch
destination). JM-006 is the canonical consumer of the session seam and its six green
branches are the acceptance gate that proves the whole RC-1 fix. **This is the linchpin
W0 close — every other session-dependent flow rides on this plumbing.**

### AC → Evidence

| AC | Requirement (from `30_BACKLOG.md`) | Flow evidence | Result |
|----|-----------------------------------|---------------|--------|
| AC1 | First launch (onboarding incomplete) → walkthrough (`/onboarding`) | jm-006 first-launch branch lands `walkthrough_slide_1` | **PASS** |
| AC2 | Logged-in customer → Requests tab (`shell_tab_requests`) [D75] | `jeeb.seam.session=customer_logged_in` → `shell_tab_requests` | **PASS** |
| AC3 | Logged-in jeeber → DELIVERY tab (`shell_tab_delivery`) | `jeeber_logged_in` → `shell_tab_delivery` | **PASS** |
| AC4 | Biometric enabled → `/lock` (`biometric_unlock_prompt`) [JM-005] | `biometric_enrolled` → `biometric_unlock_prompt` | **PASS** |
| AC5 | Logged-out returning → `/login` (`login_email_field`) [JM-007] | `logged_out_returning` → `login_email_field` | **PASS** |
| AC6 | `getMe.status == suspended` → `/account-status` (`account_status_support_cta`) [D5, JM-066] | `suspended` → `account_status_support_cta` | **PASS** |

> **Note on AC4 vs JM-005:** AC4 asserts only that the splash router *lands on* `/lock`
> and renders `biometric_unlock_prompt` — that is the routing decision JM-006 owns, and it
> PASSES. The downstream *unlock* (tapping the authenticate CTA → shell) is JM-005's AC2,
> which is independently failing on RC-3 (the dev biometric gateway). JM-006's routing
> obligation is fully met regardless of JM-005's gateway defect.

### Exact failing AC + QA category

- None. All 6 ACs PASS (`61_W0_QA_RESULTS.md` Run 2 row jm-006).

### Definition of Done checklist (CTO brief §10)

- [x] Routing logic matches blueprint + decisions (D79/D85/D75/D23/D5) — all 6 branches green on-device.
- [x] No UI dwell on splash (D79/D85).
- [x] Nav wired both directions for all 6 branches — proven by destination-id assertions.
- [x] Wired to mock (`GET /user-management/users/me`, `/auth/refresh`) — exercised by the seeded sessions.
- [x] `Semantics(identifier:)` present on destination screens (all six landed correctly).
- [x] **Maestro flow passes on emulator** — PASS (Run 2, all 6 ACs).
- [x] `flutter analyze` clean (gate held through Run 2 rebuild).
- [x] Reviewer approved (seam harness + flow YAML landed and verified).
- [x] **PO signed off.**
