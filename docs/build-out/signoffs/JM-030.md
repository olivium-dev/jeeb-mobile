# JM-030 — Cancel Request Confirm sheet (pre-accept, free)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (CORRECTED — COMPLETE W1 artifact). **Wave:** W1.
> **Flow:** `.maestro/flows/jm-030-cancel-request-confirm.yaml`.
> **Run result:** **FAIL — APP_DEFECT** (`64_W1_QA_RESULTS.md` Batch B row 13).
> **Demo-critical:** breadth (cancel is a side-path of the waiting state, not the demo happy path).

## STATUS: BLOCKED — APP_DEFECT (`waiting_cancel_cta` absent on the waiting screen)

> **CORRECTION (PO):** an earlier revision of this file marked jm-030 **SIGNED/PASS**. That was
> grounded in run residue, not the QA artifact. The **authoritative, complete**
> `64_W1_QA_RESULTS.md` (row 13, Batch B) records jm-030 as **FAIL** at
> `Assertion is false: id: waiting_cancel_cta is visible`. The cancel-confirm sheet is **never
> reached** because the entry point — the `waiting_cancel_cta` on the waiting screen — is not
> present. This item is therefore **BLOCKED**, not signed. The sheet's own ACs (free-before-accept
> copy, confirm→home, keep→dismiss) are **unverified** because the flow halts before opening the sheet.

The flow seeds a waiting (pre-accept) request and taps `waiting_cancel_cta` to raise the
cancel-confirm sheet. That CTA is absent, so the sheet (`cancel_request_sheet`) is never shown and
none of JM-030's ACs are exercised. Note this is the **JM-026 waiting-screen** that must host the
cancel CTA (`30_BACKLOG.md` JM-026 AC3 / `63_W1_TEST_PLAN §3` jm-026 `waiting_cancel_cta` →
`cancel_request_sheet`) — so the blocker is the waiting screen not surfacing the cancel entry point,
shared with the JM-026 defect.

### AC → Evidence

| AC | Requirement (`30_BACKLOG.md`) | Evidence (`64_W1_QA_RESULTS.md` row 13) | Result |
|----|-------------------------------|------------------------------------------|--------|
| AC1 | `cancel_request_sheet` shows "free before accept, nothing charged" [D69] | sheet never opened (`waiting_cancel_cta` absent) | NOT-REACHED |
| AC2 | `cancel_request_confirm_cta` → `customer-orders-home` (`orders_home_new_order_fab`) | not reached | NOT-REACHED |
| AC3 | `cancel_request_keep_cta` dismisses the sheet | not reached | NOT-REACHED |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: waiting_cancel_cta is visible`.
- **Category:** **APP_DEFECT** — the waiting screen does not render `waiting_cancel_cta`, so the
  cancel-confirm sheet entry point is missing. (Shared with the JM-026 waiting-screen defect.)

### Owner action to close

1. **app-eng (JM-026/JM-030 entry), P0:** in the waiting screen
   (`no_offer_timeout_screen.dart` → `/requests/:id/waiting`), render `waiting_cancel_cta` with
   `Semantics(identifier: 'waiting_cancel_cta', container: true)`, wired to open the
   `cancel_request_sheet` (JM-030, D69). Confirm the id via `maestro hierarchy` under the
   `pending_request` / waiting seed. This is the same waiting-screen surface that JM-026 reds.
2. **app-eng (JM-030 sheet), P0 — once the CTA opens the sheet:** verify the cancel-confirm sheet
   (NEW OMDS sheet, NOT `cancellation_screen.dart`) renders `cancel_request_sheet` + the free-note
   id, `cancel_request_confirm_cta` → `customer-orders-home`, `cancel_request_keep_cta` dismisses.
   File: the new cancel-confirm sheet under `lib/features/...`.
3. **QA (JM-030), P1:** once `waiting_cancel_cta` lands, re-run the full flow (AC1 copy → AC2 confirm
   → AC3 keep). `.maestro/flows/jm-030-cancel-request-confirm.yaml`.

### Definition of Done checklist (CTO brief §10)

- [ ] `waiting_cancel_cta` present on the waiting screen (sheet entry point) — MISSING.
- [ ] Cancel-confirm sheet (`cancel_request_sheet`) + free note + confirm/keep — unverified.
- [ ] Nav wired (confirm → home; keep → dismiss) — unverified.
- [ ] **Maestro flow passes on emulator** — jm-030 **FAIL** in `64_W1_QA_RESULTS.md` (row 13).
- [ ] Reviewer approved / **PO signed off** — BLOCKED pending the `waiting_cancel_cta` entry + sheet proof.
