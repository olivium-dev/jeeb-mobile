# JM-063 — Support Ticket / Contact Us

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (updated — Punch-list Verify). **Wave:** W4.
> **Flow:** `.maestro/flows/jm-063-support-ticket.yaml`.
> **Authoritative run (`69_REGRESSION.md` Punch-list Verify — 2026-06-19):** **PASS** — all ACs EXIT 0 on clean install, including AC6 (`kyc_rejected_appeal_cta` → `support_root`).
> **Demo-critical:** P1.

## STATUS: SIGNED ✅ — all ACs GREEN on clean install (AC6 CLOSED)

The Punch-list Verify (2026-06-19, clean install) re-ran the full flow and recorded **all ACs
EXIT 0**: AC1 form fields (`support_category`/`support_body`/`support_attach`/`support_order_link`),
AC2 submit → confirmation → `customer-profile`, AC3 `support_dispute_link` → `dispute_root`,
AC4 `account_status` → support, and **AC6 `kyc_rejected_appeal_cta` → `support_root`**. The prior
Batch-A `support_attach` id gap is resolved and the kyc-rejected appeal edge is wired. JM-063 is
fully signed.

### AC → Evidence

| AC | Requirement | Evidence (Punch-list Verify 2026-06-19) | Result |
|----|-------------|------------------------------------------|--------|
| AC1 | `support_category` + `support_body` + `support_attach` + `support_order_link` | all form fields present on `support_root` | **PASS** |
| AC2 | `support_submit_cta` → confirmation → `customer-profile`; `support_dispute_link` → `dispute-open-evidence` | submit → confirmation → profile; dispute link → `dispute_root` | **PASS** |
| AC6 | kyc-rejected `kyc_rejected_appeal_cta` → `support_root` | appeal CTA reaches `support_root` (EXIT 0) | **PASS** |

### Exact result + category

- **Result:** **PASS — all ACs EXIT 0** (`69_REGRESSION.md` Punch-list Verify — 2026-06-19, clean install).
- **Category:** — (no defect; the prior `support_attach` id gap and the AC6 appeal-edge are both closed).

### Owner action to close

None — SIGNED. AC6 (`kyc_rejected_appeal_cta` → `support_root`) confirmed on clean install.

### Definition of Done checklist (CTO brief §10)

- [x] support-ticket route + `support_root` built.
- [x] `support_attach` Semantics id placed — verified present.
- [x] AC2 submit → confirmation → profile — verified green.
- [x] AC6 `kyc_rejected_appeal_cta` → `support_root` — verified green.
- [x] **Maestro flow passes** — all ACs EXIT 0 (Punch-list Verify 2026-06-19).
- [x] **PO signed off** — **SIGNED**.
