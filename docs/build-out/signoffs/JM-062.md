# JM-062 — Logout / Delete Account

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W4 FINAL sign-off). **Wave:** W4.
> **Flow:** `.maestro/flows/jm-062-logout-delete.yaml`.
> **Authoritative run (`68_W34_QA_RESULTS.md` Batch A #9):** **FAIL** — `logout_delete_sheet` not visible after `customer_profile_logout_row` tap.
> **Demo-critical:** P1.

## STATUS: PARTIAL — APP_DEFECT. The logout row tap does not open a sheet/dialog carrying `logout_delete_sheet`; the confirm surface is not wired or the Semantics id is absent.

The customer-profile logout row is present. The failure is the confirm surface: tapping
`customer_profile_logout_row` does not open a sheet/dialog with the `logout_delete_sheet` Semantics
id, so the confirm-and-clear-session AC times out at the first step.

### AC → Evidence

| AC | Requirement | Evidence (Batch A #9) | Result |
|----|-------------|-----------------------|--------|
| AC1 | `customer_profile_logout_row` tap opens confirm sheet `logout_delete_sheet` | sheet not opened / id absent | **FAIL — APP_DEFECT (sheet not wired / id absent)** |
| AC2 | `logout_confirm_cta` / `delete_confirm_cta` → session cleared → `splash` [D5] | not reached | not reached |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: logout_delete_sheet is visible` (after `customer_profile_logout_row` tap).
- **Category:** **APP_DEFECT** — the logout/delete confirm sheet is not wired (or `logout_delete_sheet` id not placed) on the logout row.

### Owner action to close

1. **app-eng (JM-062), P0 — the fix:** wire `customer_profile_logout_row` to open the logout/delete
   confirm sheet (extend `SettingsScreen._AccountSection`) and place
   `Semantics(identifier: 'logout_delete_sheet')` on the sheet root, with `logout_confirm_cta` /
   `delete_confirm_cta` clearing the session and routing to `splash` (`/`) [D5]. Confirm via
   `maestro hierarchy`.
2. **QA (JM-062), P1:** re-run; verify AC2 session-clear → splash.

### Definition of Done checklist (CTO brief §10)

- [ ] `customer_profile_logout_row` → `logout_delete_sheet` confirm surface — **FAILS** (not wired / id absent).
- [ ] AC2 confirm → session cleared → splash — not reached.
- [ ] **Maestro flow passes** — FAIL at AC1.
- [ ] **PO signed off** — **PARTIAL** pending the `logout_delete_sheet` wiring + id (app-eng, P0).
