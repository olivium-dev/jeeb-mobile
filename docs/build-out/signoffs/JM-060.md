# JM-060 — Dispute (open + evidence) [repoint + extend]

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W4 FINAL sign-off). **Wave:** W4.
> **Flow:** `.maestro/flows/jm-060-dispute-open-evidence.yaml`.
> **Authoritative run (`68_W34_QA_RESULTS.md` Batch A #7):** **FAIL** — `dispute_root` not visible (route `/orders/del-client-001-active/escalate` does not land on the dispute screen).
> **Demo-critical:** P0.

## STATUS: PARTIAL — APP_DEFECT. The escalate route `/orders/:id/escalate` does not produce a screen carrying `dispute_root`; either the route is not landing on the dispute-open-evidence screen or the root Semantics id is absent.

The dispute-open-evidence screen extends `EscalateScreen` at `/orders/:id/escalate`. The flow pins
`jeeb.route=/orders/del-client-001-active/escalate` and asserts `dispute_root`, but the screen that
lands does not carry the `dispute_root` Semantics identifier, so the first assertion times out. Either
the route is not registered to the dispute screen in `app_router.dart` or `dispute_root` is not placed
on the screen root.

### AC → Evidence

| AC | Requirement | Evidence (Batch A #7) | Result |
|----|-------------|-----------------------|--------|
| AC1 | `dispute_reason` + `dispute_photos` (≤5) + `dispute_voice` evidence [D53] + auto-attached chat + GPS/status timeline | `dispute_root` not visible — route does not land on dispute screen | **FAIL — APP_DEFECT (root id / route)** |
| AC2 | `dispute_submit_cta` → `POST /compliment-service/v1/disputes` → `dispute-status` [JM-065] | not reached | not reached |
| AC3 | `dispute_support_link` → `support-ticket`; `dispute_back` → `order-chat` | not reached | not reached |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: dispute_root is visible` (after route pin `/orders/del-client-001-active/escalate`).
- **Category:** **APP_DEFECT** — route `/orders/:id/escalate` does not land on a widget with `dispute_root`.

### Owner action to close

1. **app-eng (JM-060), P0 — the fix:** confirm `/orders/:id/escalate` is registered to the
   dispute-open-evidence screen (extended `EscalateScreen`) in `app_router.dart`, and place
   `Semantics(identifier: 'dispute_root')` on that screen's root. Confirm via `maestro hierarchy` that
   `dispute_root` appears under the escalate route.
2. **QA (JM-060), P1:** re-run; verify AC1 evidence widgets, AC2 submit → `dispute-status`, AC3 links.

### Definition of Done checklist (CTO brief §10)

- [ ] `/orders/:id/escalate` lands on dispute-open-evidence with `dispute_root` — **FAILS** (route/id).
- [ ] AC1 evidence + AC2 submit + AC3 links — not reached.
- [ ] **Maestro flow passes** — FAIL at `dispute_root`.
- [ ] **PO signed off** — **PARTIAL** pending the escalate-route landing + `dispute_root` id (app-eng, P0).
