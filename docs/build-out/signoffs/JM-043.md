# JM-043 — KYC Rejected (appeal-only, D52/D87)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-19 (W2 FINAL sign-off). **Wave:** W2.
> **Flow:** `.maestro/flows/jm-043-kyc-rejected.yaml`.
> **Authoritative run (`66_W2_QA_RESULTS.md` §POST-FIX RE-RUN):** **GREEN (AC2 AP-9)** — AC1 (no-resubmit/appeal/back); AC2 appeal→support is an AP-9-honest cross-wave edge (support = W4/JM-063); AC3 back→profile reachable.
> **Demo-critical:** P1.

## STATUS: SIGNED ✅

The POST-FIX RE-RUN lands JM-043 GREEN. The kyc-rejected screen is appeal-only per D52/D87 (NO
`kyc_rejected_resubmit_cta`), the `kyc_rejected_appeal_cta` is present, and `kyc_rejected_back_cta` →
`customer-profile` is reachable. AC2 (`kyc_rejected_appeal_cta` → support-ticket / `support_submit_cta`)
is handled as an **AP-9 guarded coming-soon**: support-ticket belongs to W4/JM-063 (the `/support`
route does not land until W4), so the appeal tap is accepted and the kyc-rejected root survives rather
than navigating to an unregistered route. The flow asserts AP-9-honestly.

### AC → Evidence

| AC | Requirement | Evidence (POST-FIX RE-RUN) | Result |
|----|-------------|----------------------------|--------|
| AC1 | NO `kyc_rejected_resubmit_cta` [D52/D87] | assertNotVisible passed | **PASS** |
| AC2 | `kyc_rejected_appeal_cta` → support-ticket (W4/JM-063) | AP-9 coming-soon: appeal tap accepted + rejected screen survives | **PASS (AP-9)** |
| AC3 | `kyc_rejected_back_cta` → `customer-profile` | back → profile reachable | **PASS** |

### Exact result + category

- **Result:** GREEN — AC1/AC3 verified; AC2 AP-9-honest (W4/JM-063 deferred per `41_GUARDRAILS_TESTING §6/§7`).
- **Category:** — (no defect; `support_submit_cta` correctly absent in W2 — it is a W4/JM-063 id).

### Owner action to close

None — SIGNED. AC2's `support_submit_cta` landing is owned by the JM-063 (W4) flow.

### Definition of Done checklist (CTO brief §10)

- [x] kyc-rejected screen built + routed; appeal + back CTAs; NO resubmit (D52/D87) — verified green.
- [x] AC2 appeal edge asserted AP-9-honestly (W4/JM-063 deferred) — verified green.
- [x] AC3 back → profile reachable — verified green.
- [x] **Maestro flow passes** — GREEN (POST-FIX RE-RUN).
- [x] **PO signed off** — **SIGNED**.
