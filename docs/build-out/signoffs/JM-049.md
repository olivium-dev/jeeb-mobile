# JM-049 — Saved Addresses Manager (default + entry edge)

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (FINAL, W1-QA reliable re-run). **Wave:** W1.
> **Flow:** `.maestro/flows/jm-049-saved-addresses.yaml`.
> **Run result:** **FAIL — APP_DEFECT** at `shell_tab_requests` (`64_W1_QA_RESULTS.md` Batch B row 19).
> **Demo-critical:** No (P2 — address manager; may defer per `30_BACKLOG.md` W1 gate note). It does
> NOT sit on the customer demo path. But it BLOCKS jm-024 AC2 and jm-050 (shared root cause).

## STATUS: BLOCKED — APP_DEFECT (same `shell_tab_requests` shell-landing blocker as jm-035)

`64_W1_QA_RESULTS.md` (authoritative, complete — row 19) records jm-049 **FAIL** at
`Assertion is false: id: shell_tab_requests is visible` — the **same root failure as jm-035**: with
the `customer_logged_in` seed the shell does not render its bottom-nav tab ids, so the flow (which
reaches saved-addresses via the Profile tab → `customer_profile_addresses_row`) never lands the
shell and the saved-addresses screen is **never reached**. So jm-049's own ACs are **unverified** —
the failure is the upstream shell-tab-after-login defect, not (this run) the saved-addresses CTA
render.

> **Note vs prior residue:** an earlier read had jm-049 failing at `saved_address_add_cta`. The
> authoritative artifact fails it earlier, at `shell_tab_requests` — before the saved-addresses
> screen is reached. Both the shell blocker AND the saved-addresses render-state (the jm-024 AC2
> failing id) must be confirmed in order; the shell blocker is first.

### AC → Evidence

| AC | Requirement | Evidence (`64_W1_QA_RESULTS.md` row 19) | Result |
|----|-------------|------------------------------------------|--------|
| AC1 | `saved_address_default_badge` marks the default | shell never landed; screen not reached | NOT-REACHED |
| AC2 | `saved_address_add_cta` / `saved_address_<id>_edit` → `address-detail-form` [JM-050] | not reached (shell + profile chain broken) | NOT-REACHED |
| AC3 | reachable from `customer-profile` (via shell Profile tab) and `location-select` | profile path blocked at shell-tab precondition | NOT-REACHED |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: shell_tab_requests is visible` (precondition, before the
  Profile tab → saved-addresses chain).
- **Category:** **APP_DEFECT** — the shell bottom-nav tab ids are absent after `customer_logged_in`
  (shared root with jm-035 row 18 + jm-050 row 20). Separately, the saved-addresses screen render
  (the `saved_address_add_cta` id, surfaced by jm-024 AC2 row 7) must also be confirmed once the
  shell lands. Note source-checked: the route + screen + id all exist in code (`app_router.dart`
  `settings-addresses` → `SavedLocationsScreen`; `saved_locations_screen.dart` `saved_address_add_cta`),
  so the saved-addresses leg is a render-state/seed issue, not missing code.

### Owner action to close

1. **app-eng (shell tab ids after login), P0 — FIRST; same fix as jm-035 (cluster):** render
   `shell_tab_requests` / `shell_tab_profile` after the `customer_logged_in` seed lands the shell
   (`shell_screen.dart`, tab scaffold first, async content). Confirm via `maestro hierarchy`. This
   single fix unblocks jm-035 + jm-049 + jm-050 reaching their screens.
2. **app-eng (JM-049 render), P0 — then:** in
   `lib/features/location/presentation/saved_locations_screen.dart`, ensure `saved_address_add_cta`
   renders in **all** states (loading/empty/error/loaded), not gated on a non-empty list (this is the
   jm-024 AC2 / jm-050 failing id).
3. **backender (mock), P0:** seed `GET /user-management/users/:userId/saved-locations` for
   `user-client-001` (≥1 row, one default) on the `has_saved_addresses` / profile-reached branch —
   `jeeb-mock-backend/src/fixtures/` + the seed branch (per `63_W1_TEST_PLAN §4.1`).
4. **QA (JM-049), P1:** once shell + saved-addresses render, confirm AC1 default badge + AC2 → form.

### Definition of Done checklist (CTO brief §10)

- [ ] Shell renders `shell_tab_requests` after `customer_logged_in` (precondition) — FAILS (row 19).
- [x] Route + screen + id present in code (`settings-addresses`, `SavedLocationsScreen`, `saved_address_add_cta`).
- [ ] Saved-addresses screen reached + `saved_address_add_cta` rendered in all states — not reached.
- [ ] **Maestro flow passes** — jm-049 **FAIL** at `shell_tab_requests` (row 19).
- [ ] Reviewer approved / **PO signed off** — BLOCKED pending the shell-tab-after-login fix, then the saved-addresses render + seed.
