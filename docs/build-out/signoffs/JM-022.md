# JM-022 — Set Password (dual exit)

> **✅ FINAL VERIFY 2026-06-19 — SIGNED (GREEN).** Fresh APK, maestro EXIT 0. All ACs pass. AC1
> (recovery→login) fixed by the JM-021 OTP re-key. AC2 (in-app-social→customer-profile) fixed by
> a mock bearer-email resolution for the email-less deep-link + a profile-body assertion (the
> `wallet_chip` is a shell overlay absent on the bare route). Stale STATUS header below superseded.

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (updated Run 3). **Wave:** W0.
> **Flow:** `.maestro/flows/jm-022-set-password.yaml`. **Run result:** **FAIL** (`61_W0_QA_RESULTS.md` Run 3).
> **Demo-critical:** No (breadth — recovery sub-step).

## STATUS: PARTIAL — FLOW_BUG (RC-7 PERSISTS, App engineer + QA)

The session, recovery, and `set_password_mode` seams remain wired. AC1 enters via the full
recovery chain and fails at the **same RC-7 as JM-021**, which **persists** in Run 3 despite
the per-cell Semantics IDs: the recovery `verify_code_input` cells are tappable but `inputText`
does not set their value (the addressed Semantics node is a container, not the editable
`TextField` leaf), so the code never submits and the chain stops before `setpw_new_field`.
AC2–AC4 enter directly via `jeeb.seam.set_password_mode=in-app-social`, which bypasses OTP
entirely and are independently reachable; they remain green-once-decoupled from the broken OTP
entry step (AC2's destination id belongs to JM-035, Wave 1).

### AC → Evidence

| AC | Requirement (from `30_BACKLOG.md`) | Flow evidence | Result |
|----|-----------------------------------|---------------|--------|
| AC1 | `setpw_new_field`+`setpw_confirm_field` match/valid + `setpw_submit_cta`, mode=recovery → `/login` (`login_email_field`) [D90] | recovery chain stops at `verify_code_root` (RC-7: cells empty after tap+type); `setpw_new_field` never reached | **FAIL — FLOW_BUG (RC-7 PERSISTS)** |
| AC2 | mode=in-app-social + valid submit → `customer-profile` (`customer_profile_wallet_chip`) [D90, JM-035] | `set_password_mode=in-app-social` seam wired; direct launch bypasses OTP; destination `customer_profile_wallet_chip` belongs to JM-035 (Wave 1) | PASS (likely once flow decouples from the RC-7 OTP step; AC2 destination unassertable until JM-035 lands) |
| AC3 | Mismatch/weak → `setpw_validation_error`; form not submitted (`setpw_submit_cta` stays) | direct-launch via in-app-social seam; does NOT go through OTP | PASS (likely — independent of RC-7) |
| AC4 | Both eye toggles work (`setpw_new_visibility_toggle`, `setpw_confirm_visibility_toggle`); fields stay visible | direct-launch via in-app-social seam; does NOT go through OTP | PASS (likely — independent of RC-7) |

### Exact failing AC + QA category

- **Failing AC:** **AC1** (recovery chain). First failing step: `Assert id: setpw_new_field is visible` — the chain halts at the verify-code OTP step.
- **QA failure category:** **FLOW_BUG** (`61_W0_QA_RESULTS.md` Run 3 row jm-022) — same RC-7 as jm-021.
- **Root cause (RC-7 PERSISTS):** per-cell Semantics IDs are present and tappable but are not
  the editable leaf; `inputText` leaves the recovery `verify_code` cells empty, so the recovery
  chain never reaches set-password. AC2–AC4 (in-app-social) do not traverse OTP and are
  independently reachable.

### Owner action to close

1. **App engineer + QA, P1 — shared with jm-021:** make each `OmdsOtpInput` cell the editable
   Semantics leaf (per-cell `key: Key('${identifier}_$index')` with no wrapping container, or
   `Semantics(identifier, child: TextField)` as the leaf), **OR** add the
   `jeeb.route=/recover/verify?preset_code=654321` dev-seam bypass — then update the
   recovery-chain flow accordingly so AC1 reaches `setpw_new_field`.
2. **QA, P1:** ensure the AC2–AC4 in-app-social scenarios assert from the direct
   `set_password_mode=in-app-social` launch (not through the recovery/OTP chain) so they are
   not falsely coupled to RC-7.
3. **AC2 dependency note:** the AC2 destination `customer_profile_wallet_chip` belongs to
   JM-035 (Wave 1 customer-profile). AC2's *submit + route* is JM-022's obligation; the
   destination id is honest-but-unassertable until JM-035 lands.

### Definition of Done checklist (CTO brief §10)

- [x] Screen matches blueprint + decisions (`auth-set-password`; D65/D90/D23; dual mode; `set_password_mode` seam→route pin wired).
- [x] OMDS components used.
- [x] Nav wired both directions (recovery → `/login`; in-app-social → profile).
- [x] Wired to mock (set-password route B3 + `/auth/refresh`).
- [x] `Semantics(identifier:)` present (`setpw_new_field`/`_confirm_field`/`_submit_cta`/both toggles/`_validation_error`).
- [ ] **Maestro flow passes on emulator** — **AC1 FAILS** on RC-7 (recovery OTP step persists); AC2–4 (in-app-social) likely-pass once decoupled from the OTP chain.
- [ ] `flutter analyze` clean — re-verify after the shared `OmdsOtpInput` leaf-Semantics/seam-bypass change.
- [ ] Reviewer approved — pending the RC-7 widget/seam fix.
- [ ] **PO signed off** — partial; both modes implemented, blocked on the shared OTP-cell editable-leaf fix (or preset-code dev-seam bypass) for the recovery-chain AC1 — RC-7.

> **Re-verification note (2026-06-18, W1-QA reliable re-run — FINAL):** `64_W1_QA_RESULTS.md`
> is now COMPLETE and re-ran jm-022. Result: **FAIL — APP_DEFECT**, failing step
> "`Assertion is false: id: setpw_new_field is visible` — set-password screen not reachable (route
> `/set-password` not rendering semantics id)." This **corroborates RC-7 persisting** on the AC1
> recovery chain (same multi-cell OTP-input bug as jm-021 — the chain halts at verify-code before
> `/set-password`). The set-password screen itself is implemented and reachable directly via the
> in-app-social seam (AC2–AC4); only the recovery-chain AC1 is blocked by the upstream OTP-cell
> input bug. The fix is **shared with jm-021** (app-engineer: editable-leaf OTP cells, or dev-seam
> preset-code bypass). Item stays **PARTIAL**; owner action below stands.
