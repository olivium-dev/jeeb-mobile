# JM-027 — Replies sub-tab CTAs

> **Sign-off owner:** Product Owner (Opus). **Date:** 2026-06-18 (FINAL, W1-QA reliable re-run). **Wave:** W1.
> **Flow:** `.maestro/flows/jm-027-replies-sub-tab.yaml`.
> **Run result:** **FAIL — APP_DEFECT** (`64_W1_QA_RESULTS.md` row 10).
> **Demo-critical:** breadth (Replies sub-tab is an alt entry to the offer list).

## STATUS: BLOCKED — APP_DEFECT (`replies_accept_cta` does not open `offer_accept_sheet`)

`64_W1_QA_RESULTS.md` (authoritative, complete — row 10) records jm-027 **FAIL** at
`Assertion is false: id: offer_accept_sheet is visible` — the Replies sub-tab is reached and the
reply card's `replies_accept_cta` is tapped, but the accept-confirm sheet (JM-029's
`offer_accept_sheet`) does **not** open. AC2 is the failing leg. Per `63_W1_TEST_PLAN §3`,
`replies_accept_cta` is expected to raise `offer_accept_sheet` (JM-029). The sheet not opening means
either the Replies accept CTA is not wired to the JM-029 sheet or the JM-029 accept-confirm sheet is
not built/wired (shared root with jm-029, which also reds at `offer_accept_sheet`).

### AC → Evidence

| AC | Requirement | Evidence (`64_W1_QA_RESULTS.md` row 10) | Result |
|----|-------------|------------------------------------------|--------|
| AC1 | `replies_check_offers_cta` → `offer-review-list` (`offer_review_list_root`, not /chat/:id) | reached (not the failing step) | NOT-CONFIRMED |
| AC2 | `replies_accept_cta` → `offer-accept-confirm` (`offer_accept_sheet`) [JM-029] | tap does not open `offer_accept_sheet` | **FAIL — APP_DEFECT** |

### Exact failing step + category

- **Failing step:** `Assertion is false: id: offer_accept_sheet is visible`.
- **Category:** **APP_DEFECT** — `replies_accept_cta` does not trigger the accept-confirm sheet.
  Shared root with jm-029 (the JM-029 sheet surface). NOT a shell-landing defect — the Replies
  sub-tab was reached this run.

### Owner action to close

1. **app-eng (JM-029 sheet + JM-027 wiring), P0 — unblocks jm-027 + jm-029:** build/wire the
   accept-confirm bottom sheet (JM-029, NEW OMDS confirm sheet) so it renders
   `offer_accept_sheet` (+ `offer_accept_sheet_confirm_cta` / `offer_accept_sheet_cancel_cta`), and
   wire `replies_accept_cta` (`replies_tab.dart`) to raise it. Confirm `offer_accept_sheet` via
   `maestro hierarchy` after tapping `replies_accept_cta` under the `offers_received` seed. Files:
   the JM-029 sheet widget + `lib/features/.../replies_tab.dart`.
2. **backender (mock), P0:** confirm `jeeb.seam.journey=offers_received` seeds the offers-received
   request + ≥2 offers (`req-client-001-offers`, `offer-001`/`offer-002`) so the Replies card +
   accept CTA have data (per `63_W1_TEST_PLAN §4.1/§4.3`).
3. **QA (JM-027), P1:** once the sheet opens, confirm AC1 (`replies_check_offers_cta` →
   `offer_review_list_root`) and AC2 (accept → sheet) green. `.maestro/flows/jm-027-replies-sub-tab.yaml`.

### Definition of Done checklist (CTO brief §10)

- [ ] `replies_accept_cta` opens `offer_accept_sheet` (AC2) — FAILS (sheet not opened).
- [ ] `replies_check_offers_cta` → offer list (AC1) — unconfirmed.
- [ ] **Maestro flow passes** — jm-027 **FAIL** at `offer_accept_sheet` (row 10).
- [ ] Reviewer approved / **PO signed off** — BLOCKED pending the JM-029 sheet + Replies accept wiring.
