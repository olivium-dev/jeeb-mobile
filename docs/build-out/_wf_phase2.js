export const meta = {
  name: 'jeeb-phase2-guardrails-execplan',
  description: 'Phase 2: principal engineers write guardrail files, backender lands W0-critical mock fixes, lead principals produce the execution plan',
  phases: [
    { title: 'Guardrails', detail: '3 principal engineers: architecture, testing/semantics, mock/networking' },
    { title: 'Backend+Plan', detail: 'backender lands W0 mock fixes; 3 lead principals draft exec-plan perspectives' },
    { title: 'Plan Synthesis', detail: 'consolidate into 50_EXECUTION_PLAN.md' },
  ],
}

const ROOT = '/Users/oudaykhaled/Desktop/olivium/jeeb'
const OUT = ROOT + '/jeeb-mobile/docs/build-out'
const COMMON = 'Jeeb mobile build-out engagement. READ FIRST: ' + OUT + '/00_CTO_BRIEF.md, ' + OUT + '/01_CTO_DECISIONS.md, ' + OUT + '/30_BACKLOG.md (58 JM items, 5 waves), ' + OUT + '/21_NAV_PLAN.md, ' + OUT + '/20_GAP_MAP.md. Flutter app at ' + ROOT + '/jeeb-mobile; mock at ' + ROOT + '/jeeb-mock-backend. Be concrete, cite file paths, JM ids, decision ids. Do not invent product decisions (CTO-D rulings already cover the open ones).'

phase('Guardrails')
const guardrails = await parallel([
  () => agent(COMMON + '\n\nROLE: Senior Principal Engineer - ARCHITECTURE GUARDRAIL. Read representative real code first: lib/core/router/app_router.dart, lib/core/di/injection_container.dart, lib/features/shell/shell_screen.dart, and ONE complete feature with data/domain/presentation layers plus its bloc/cubit (e.g. lib/features/offers/ or lib/features/live_tracking/). Then write ' + OUT + '/40_GUARDRAILS_ARCH.md: the canonical way to add a screen and feature here - clean-arch layer rules, flutter_bloc cubit/state conventions, GoRouter route-registration pattern (named routes, extra payloads, redirect gates), GetIt DI registration, Dio data sources, feature-folder layout, naming, error/empty/loading state conventions, and an explicit DO and DO-NOT list. Include the SHARED-FILE protocol: which files (app_router.dart, injection_container.dart, l10n, shell_screen.dart) may only be edited by a single per-wave integrator, and how an engineer requests an edge or route be added. Keep it prescriptive and copy-pasteable (include a tiny end-to-end skeleton of a new feature plus route).', { label: 'guardrail:arch', phase: 'Guardrails' }),
  () => agent(COMMON + '\n\nROLE: Senior Principal Engineer - TESTING and SEMANTICS GUARDRAIL. Read .maestro/smoke.yaml, a couple of .maestro/flows, and the backlog Identifier convention section. Note: the Foundation engineer is concurrently enabling semantics export at boot and adding Semantics identifiers to onboarding as the proof; your guardrail documents the STANDARD for all screens. Write ' + OUT + '/41_GUARDRAILS_TESTING.md covering: (1) the Semantics identifier rule - every interactive or asserted widget gets a stable id of the form screenId_element, tabs use shell_tab_id, sheets use screenId_sheet_element; HOW to add it in Flutter without breaking layout; (2) the test-first pipeline per JM item (QA writes the Maestro flow keyed by id BEFORE the engineer implements; flow lives at .maestro/flows/jm-NNN-slug.yaml); (3) the exact run recipe (set JAVA_HOME via /usr/libexec/java_home since the shell default is broken; boot AVD jeeb_test; build the dev flavor app.jeeb.mobile.dev against the mock on port 4010 using the emulator host mapping documented in the CTO brief; run maestro test with APP_ID=app.jeeb.mobile.dev pointed at the flow); (4) assert on ids never on visible text (i18n); (5) regression: how flows accumulate plus a per-wave suite. Make it copy-pasteable with a template flow.', { label: 'guardrail:testing', phase: 'Guardrails' }),
  () => agent(COMMON + '\n\nROLE: Senior Principal Engineer - MOCK and NETWORKING GUARDRAIL. Read lib/core/network/mock_gateway_client.dart and the Dio client and interceptors in lib/core/network/, plus jeeb-mock-backend/src/server.ts and 2-3 services. Write ' + OUT + '/42_GUARDRAILS_MOCK.md covering: (1) how the app reaches the mock (useMockPrefixes true, service-prefixed routes, the rewrite map, the known v1-auth rewrite gap labelled B1 owned by Foundation); (2) the emulator host mapping to the host mock; (3) how to add a new mock endpoint and keep app DTO and mock response in lockstep; (4) THE CONSOLIDATED MOCK-FIX REGISTER: extract every B, U, T, W-m, O, S, R1m item flagged in 20_GAP_MAP.md and 30_BACKLOG.md into one table (id, what, which service file, which JM items it unblocks, which wave), so backenders have a single worklist; (5) the error/empty/loading contract (D30) and offline guards on money actions (D35).', { label: 'guardrail:mock', phase: 'Guardrails' }),
])

phase('Backend+Plan')
const backendAndPlans = await parallel([
  () => agent(COMMON + '\n\nROLE: Principal Backender - W0-CRITICAL MOCK FIXES. You own ' + ROOT + '/jeeb-mock-backend ONLY (do NOT touch the Flutter app; the app-side gateway fix B1 belongs to the Foundation engineer). Land the fixes the W0 auth wave needs so the app can complete sign-up, login, and recovery against the mock. B3: ensure the auth-service exposes the app-client routes the Flutter auth datasources call - email/password login, email-first signup (find-or-create by email, returns tokens, 409 on collision per CTO-D1 and D22), request-recovery-code, verify-recovery-code, set-password. Inspect what the Flutter auth datasources actually POST (grep lib/features/auth and lib/features/registration for paths) and reconcile the mock to match (path plus request/response shape plus tokens). B4: OTP length - make the OTP request and verify contract self-consistent and matching what the app expects (check the app OTP input length). U1: GET /user-management/users/me must surface the account status (active, suspended, locked per D5) and the jeeber kycStatus (none, pending, approved, rejected per D38 and D52) so splash, account-status, and KYC-gate routing work. Write or extend the service files, keep them consistent with existing patterns and the store. Run npm run build and npm test (vitest) in jeeb-mock-backend and report results. Then append a section titled W0 mock fixes DONE to ' + OUT + '/42_GUARDRAILS_MOCK.md listing exactly which endpoints you added or changed with their final request/response shapes. Do NOT do the wallet, support, or reviews fixes (those are later-wave work); just confirm they remain on the register.', { label: 'backend:w0-fixes', phase: 'Backend+Plan' }),
  () => agent(COMMON + '\n\nROLE: Lead Principal 1 - CRITICAL PATH and PARALLELIZATION. Produce ' + OUT + '/50_PLAN__critical_path.md: take the 58 JM items and 5 waves and define the concrete execution schedule - the critical path (the P0 chain that gates the demo: auth backbone, then shell/profile, then request to offer to track to receipt to rate), what can run fully in parallel within each wave (list the parallel batches by JM id), and the cross-wave unblock points (e.g. wallet JM-053/054 precede JM-045/046; notifications JM-057 needs its deep-link targets). Give a wave-by-wave ordered batch list with max concurrency per batch.', { label: 'plan:critpath', phase: 'Backend+Plan' }),
  () => agent(COMMON + '\n\nROLE: Lead Principal 2 - INTEGRATION and MERGE STRATEGY. Produce ' + OUT + '/50_PLAN__integration.md: how to run many parallel engineers without clobbering shared files. Define the per-wave INTEGRATOR pattern (one agent batches all app_router.dart route additions plus injection_container.dart registrations plus shell tab wiring plus l10n keys for the wave FIRST, from 21_NAV_PLAN.md; then per-screen engineers only touch their own feature files because the central edges already exist). Specify a stable id namespace so engineers never collide, the order (integrator routes, then engineers screens, then reviewer, then QA, then PO signoff), and how a screen that needs a NEW route requests it. Include the exact list of routes the W0 and W1 integrators must add (pull from 21_NAV_PLAN.md).', { label: 'plan:integration', phase: 'Backend+Plan' }),
  () => agent(COMMON + '\n\nROLE: Lead Principal 3 - QA GATING and DEFINITION OF DONE. Produce ' + OUT + '/50_PLAN__qa_gating.md: the per-item pipeline (PO acceptance criteria, then QA authors a Maestro flow keyed by Semantics id RED first, then engineer implements, then reviewer reviews the diff, then QA runs the flow on emulator against mock until GREEN, then PO signs off in signoffs/JM-id.md). Define the signoff artifact format, the per-wave regression suite (accumulating flows), the analyze and test gates, and a wave EXIT checklist. Note model policy: QA authoring and execution use Sonnet, all other roles use Opus. Reference 41_GUARDRAILS_TESTING.md for the run recipe.', { label: 'plan:qa', phase: 'Backend+Plan' }),
])

phase('Plan Synthesis')
const synth = await agent(COMMON + '\n\nROLE: TECH LEAD - EXECUTION PLAN SYNTHESIS. Read the three perspective drafts (' + OUT + '/50_PLAN__critical_path.md, ' + OUT + '/50_PLAN__integration.md, ' + OUT + '/50_PLAN__qa_gating.md) and the W0 mock-fix outcome (below). Consolidate into ONE authoritative ' + OUT + '/50_EXECUTION_PLAN.md: the wave-by-wave plan with, per wave: (1) the integrator central edits (routes, DI, tabs, l10n) done first; (2) the parallel engineer batches (JM ids) with max concurrency; (3) the QA, review, and PO gating; (4) the wave entry gate plus exit checklist; (5) which backend mock-fixes must land before the wave. Make W0 immediately actionable (it is unblocked once Foundation and the W0 mock fixes are green). End with a section titled READY TO EXECUTE W0 listing the exact JM items, their integrator route additions, and the order. Return a concise summary: is W0 ready to execute, what if anything still gates it, and the top risks.\n\n=== W0 MOCK-FIX OUTCOME ===\n' + (backendAndPlans[0] || '(backender result unavailable)'), { label: 'synthesis:exec-plan', phase: 'Plan Synthesis' })

return {
  guardrails_written: guardrails.filter(Boolean).length,
  backend_done: !!backendAndPlans[0],
  plan_perspectives: backendAndPlans.slice(1).filter(Boolean).length,
  exec_plan_summary: synth,
}
