export const meta = {
  name: 'jeeb-w1-code',
  description: 'Wave 1 customer journey code build + W0 punch-list patches: parallel infra (integrator+RC9, journey seam+test-data, RC7 OTP), then ~15 screen engineers, static-green, clustered review, W0 flow patches',
  phases: [
    { title: 'Infra', detail: 'integrator (W1 routes+RC9+profile tab+chips+DI+l10n), journey seam+mock test-data (+T1+D1m), RC7 OTP-leaf fix' },
    { title: 'Engineers', detail: '~15 parallel customer-journey screen engineers' },
    { title: 'StaticGreen', detail: 'flutter analyze + test green (bounded fix loop)' },
    { title: 'Review', detail: '4 cluster reviewers vs ACs + guardrails' },
    { title: 'W0Patch', detail: 'QA: RC-10 + RC-7 W0 flow fixes (for the batched QA pass)' },
  ],
}

const APP = '/Users/oudaykhaled/Desktop/olivium/jeeb/jeeb-mobile'
const MOCK = '/Users/oudaykhaled/Desktop/olivium/jeeb/jeeb-mock-backend'
const OUT = APP + '/docs/build-out'
const COMMON = 'Jeeb mobile build-out, WAVE 1 (customer journey). READ FIRST: ' + OUT + '/00_CTO_BRIEF.md (S6 contracts), ' + OUT + '/01_CTO_DECISIONS.md, ' + OUT + '/40_GUARDRAILS_ARCH.md, ' + OUT + '/41_GUARDRAILS_TESTING.md, ' + OUT + '/42_GUARDRAILS_MOCK.md (incl W-1 FLOOR CLOSED + seed creds), ' + OUT + '/30_BACKLOG.md (Wave 1 items + ACs), ' + OUT + '/50_EXECUTION_PLAN.md (WAVE 1 section + integrator route list), ' + OUT + '/21_NAV_PLAN.md, ' + OUT + '/20_GAP_MAP.md (per-screen flutter target), ' + OUT + '/60_W0_TEST_PLAN.md + ' + OUT + '/63_W1_TEST_PLAN.md (EXACT Semantics identifiers + W1 test-data seeds), ' + OUT + '/62_SEAM_HARNESS.md (session seam). The red W1 Maestro flows already exist at ' + APP + '/.maestro/flows/jm-02*.yaml and jm-03*.yaml. App at ' + APP + ', mock at ' + MOCK + ' (running :4010, emulator host 10.0.2.2:4010). OMDS components only; clean arch; flutter_bloc; GoRouter; GetIt; Dio. Every interactive/asserted widget carries Semantics(identifier:) with the EXACT id from 63_W1_TEST_PLAN.md. Do NOT invent product decisions (CTO-D + R-F cover the gaps). Do NOT run flutter/emulator/maestro (dedicated phases handle that).'

// ---------- Phase 1: Infra (parallel, disjoint files) ----------
phase('Infra')
const [integrator, seam, otpfix] = await parallel([
  () => agent(COMMON + '\n\nROLE: W1 INTEGRATOR (Opus). You alone edit the shared files this wave: lib/core/router/app_router.dart, lib/core/di/injection_container.dart, lib/features/shell/shell_screen.dart (+ tabs), lib/l10n/*.arb. Leave the app COMPILING. Do the W1 integrator edits from 50_EXECUTION_PLAN.md (WAVE 1 section) + 21_NAV_PLAN.md:\n1. ROUTES (S1): ADD /requests/:id/offers (offer-review, wire existing client_offers_screen.dart, JM-028); /requests/:id/waiting (waiting-no-coverage, target no_offer_timeout_screen.dart rewrite, JM-026); /orders/:id/receipt (delivered-receipt, target delivery_receipt_screen.dart rewrite, JM-033); /orders/:id/summary (order-summary deep-link target, CTO-D3, JM-031); /settings/addresses/edit (address-detail, JM-050). Point each route at its existing target widget (create a minimal compiling stub ONLY if the target truly does not exist). Sheets (JM-029 accept-confirm, JM-030 cancel-confirm) and the JM-031 pinned widget are NOT routes.\n2. RC-9 (close W0 jm-007 AC6): in app_router.dart the biometric gate (~line 352) must NOT send a logged-out user to /lock. Guard it with a session check (add && !session.isUnauthenticated) so a biometric-enrolled but logged-out user lands /login.\n3. SHELL (S3): swap the Profile tab body from the dev shell/tabs/profile_tab.dart to the real CustomerProfileScreen (JM-035, shell_tab_profile); add the persistent header WALLET CHIP (orders_home_wallet_chip / customer_profile_wallet_chip -> goNamed wallet) and BELL (orders_home_bell -> goNamed notifications) on Requests + Profile headers. Cross-wave targets (wallet=W2.5/W3, notifications=W4) stay guarded coming-soon until those routes land (AP-9: tap accepted, root survives).\n4. DI (S2): register offer-review/waiting/receipt/order-summary/customer-profile repos; bind real Dio for delivery/offer/chat services (they rewrite to :4010). Tier endpoint bound (T1 data is backender).\n5. l10n (S4): all W1 customer-screen keys into both ARBs.\nGATE: flutter analyze clean on touched files; flutter test test/core/router/ resolves new routes. Return: routes/names added, RC-9 fix, shell changes, DI/l10n, analyze result.', { label: 'w1:integrator', phase: 'Infra' }),
  () => agent(COMMON + '\n\nROLE: W1 TEST-HARNESS + BACKEND (Opus). Own the journey test-data seam END TO END (app seam + mock seeds) so W1 flows can start mid-journey deterministically. Per 63_W1_TEST_PLAN.md the flows need a NEW seam key jeeb.seam.journey with these values: pending_request, offers_received, order_accepted, active_delivery, delivery_marked_done, pending_request_no_coverage, jeeber_rating_pending, has_saved_addresses (see 63 for exactly what each must seed, all for user-client-001 with the stable ids listed there). Implement, extending the 3-layer pattern in 62_SEAM_HARNESS.md:\n1. App: add jeeb.seam.journey to android/app/src/main/kotlin/app/jeeb/mobile/MainActivity.kt seamKeys; add a journey field to lib/core/dev_seam/dev_seam_config.dart; extend SessionSeamBootstrap to, for each journey value, ensure the mock has the seeded data (call a mock seed endpoint) AND set any client-side state, before the router fires.\n2. Mock (' + MOCK + '): add deterministic seed support so each journey value yields the required request/offer/delivery/conversation rows for user-client-001 with the stable ids in 63 (e.g. via the /__mock/reset + a seed param, or dedicated dev seed endpoints). Also land T1 (GET /delivery-service/v1/tiers returns the 5-tier catalog Flash/Express/Standard/On-the-Way/Eco) and D1m (a proof-photo upload sink for JM-033). Keep npm build + npm test green.\n3. Append the journey seam contract + the seed endpoints to 62_SEAM_HARNESS.md (and note T1/D1m done in 42_GUARDRAILS_MOCK.md).\nDo NOT edit app_router.dart/injection_container.dart/shell/l10n (the integrator owns those) or run emulator/maestro. flutter analyze clean on your app files; npm test green on mock. Return: the journey seam contract (value -> seeded state -> stable ids), the mock seed endpoint(s), and T1/D1m status.', { label: 'w1:seam-backend', phase: 'Infra' }),
  () => agent(COMMON + '\n\nROLE: App Engineer (Opus) - W0 RC-7 OTP-LEAF FIX. The W0 jm-021/jm-022 flows fail because Maestro inputText does not reach the editable leaf of the multi-cell OTP widget even with per-cell container ids. Fix OmdsOtpInput (locate it; likely ../omds-flutter/omds_library or lib) so EACH cell is an editable Semantics LEAF that Maestro can type into: make each cell TextField carry the per-index Semantics identifier directly as a leaf (Semantics(identifier: id_index, textField: true, child: TextField(...)) with no wrapping container intercepting), OR expose each cell so tapOn id:<id>_<index> + inputText lands one digit. Keep it backward-compatible (phone OTP + recovery code both use it). flutter analyze clean. Do NOT touch flows (QA does that) or run emulator. Return: the file changed and the exact per-cell id scheme that now accepts inputText.', { label: 'w0:otp-leaf', phase: 'Infra' }),
])

// ---------- Phase 2: Engineers (parallel) ----------
phase('Engineers')
const ITEMS = [
  { jm: 'JM-035', label: 'customer-profile', file: 'lib/features/customer_profile/presentation/customer_profile_screen.dart (real Profile tab body; integrator swapped the tab to it)', note: 'Real profile: avatar/name/per-role rating + wallet chip + bell; all 7 rows navigate (register-delivery, password, notifications, language, contact, rate-app, logout, addresses) per AC. Cross-wave targets guarded.' },
  { jm: 'JM-023', label: 'requests-home', file: 'lib/features/home_client/.../client_home_screen.dart', note: 'Requests tab header (wallet chip + bell), pending row -> waiting, FAB -> request-type-selection.' },
  { jm: 'JM-024', label: 'create-flow', file: 'request_type_screen.dart + location/client_location_screen.dart + location/capture_location_screen.dart (3 coupled files, one owner)', note: '5-tier radios -> location-select -> map-pin -> order-chat; saved-addresses entry. Tier list from GET /tiers (T1).' },
  { jm: 'JM-025', label: 'order-chat', file: 'lib/features/chat/.../chat_screen.dart (via chat_detail_screen.dart)', note: 'First message -> broadcast -> waiting; pinned summary strip + view-summary link; active dispute CTA.' },
  { jm: 'JM-026', label: 'waiting', file: 'rewrite lib/features/no_offer_timeout/.../no_offer_timeout_screen.dart -> /requests/:id/waiting', note: 'notified count + countdown; no-coverage variant; offers-arrived -> offer-review; retarget; cancel -> cancel sheet.' },
  { jm: 'JM-027', label: 'replies-subtab', file: 'replies/replies_tab.dart (or my-orders replies sub-tab)', note: 'check-offers -> offer-review (not chat); accept -> accept-confirm sheet.' },
  { jm: 'JM-028', label: 'offer-review', file: 'lib/features/client_offers/.../client_offers_screen.dart (wired to /requests/:id/offers by integrator)', note: 'Per-jeeber cards (price/eta/rating + cash-on-delivery), sort; tap name -> jeeber profile; accept -> sheet; cancel -> sheet.' },
  { jm: 'JM-029', label: 'accept-sheet', file: 'new OMDS confirm bottom sheet under lib/features/offers/ (or client_offers)', note: 'offer_accept_sheet: jeeber name + price + other-offers-close note; confirm captures fee -> order-chat; cancel -> offer list.' },
  { jm: 'JM-030', label: 'cancel-sheet', file: 'new OMDS confirm sheet (do NOT reuse cancellation_screen.dart)', note: 'cancel_request_sheet: free-before-accept note; confirm -> home; keep dismisses.' },
  { jm: 'JM-031', label: 'order-summary-widget', file: 'pinned-price header widget injected into chat + tracking (+ /orders/:id/summary route exists)', note: 'order_summary_pinned: price/jeeber/eta/tier/item + cash label; open-chat; track.' },
  { jm: 'JM-032', label: 'order-tracking', file: 'lib/features/live_tracking/.../live_tracking_screen.dart', note: '4-step stepper (ordered/picked/in-transit/delivered) + pinned summary header; auto-advance to receipt; dispute CTA; no-show sheet (reassign/rebroadcast).' },
  { jm: 'JM-033', label: 'confirm-receipt', file: 'rewrite lib/features/delivery_receipt/.../delivery_receipt_screen.dart -> /orders/:id/receipt', note: 'receipt prompt + cash-to-jeeber + proof photo; NO commission line; confirm -> rating; not-yet -> dispute.' },
  { jm: 'JM-034', label: 'rating', file: 'lib/features/rating/.../rating_screen.dart + mutual_rating_screen.dart', note: 'No skip on mandatory; customer submit -> home; jeeber submit (mode=jeeber) -> dashboard; reconcile /feedback vs /mutual-rate canonical.' },
  { jm: 'JM-049', label: 'saved-addresses', file: 'lib/features/location/.../saved_locations_screen.dart (/settings/addresses)', note: 'P2: default badge; add/edit -> address-detail; reachable from profile + location-select.' },
  { jm: 'JM-050', label: 'address-form', file: 'promote location/.../add_edit_location_sheet.dart -> /settings/addresses/edit', note: 'P2: map pin + label + building + floor/apt + notes + COD phone; save -> saved-addresses.' },
]
const engResults = await parallel(ITEMS.map((it) => () =>
  agent(COMMON + '\n\nROLE: Senior Principal Flutter Engineer (Opus). Implement ' + it.jm + ' (' + it.label + '). Target: ' + it.file + '. ' + it.note + '\n\nRead the ' + it.jm + ' ACs in 30_BACKLOG.md, the per-screen gap in 20_GAP_MAP.md, and the EXACT identifiers in 63_W1_TEST_PLAN.md - expose EVERY one via Semantics(identifier:). Wire data to the real mock endpoints (delivery/offer/chat/score services per 42 + 12_MOCK_INVENTORY) using existing Dio/datasource patterns (40_GUARDRAILS_ARCH). Touch ONLY your own feature file(s); do NOT edit app_router.dart/injection_container.dart/.arb/shell (integrator did those - reference its routes/keys, see integrator output below). If you need a missing route/key, append to ' + OUT + '/50_ROUTE_REQUESTS.md and code against the intended name. Do NOT run flutter/emulator. Return: files changed, the exact Semantics identifiers exposed (must match 63), endpoints wired, contract gaps.\n\n=== INTEGRATOR OUTPUT ===\n' + integrator + '\n\n=== JOURNEY SEAM/TEST-DATA OUTPUT ===\n' + seam,
    { label: 'eng:' + it.label, phase: 'Engineers' })
)).then(r => r)

// ---------- Phase 3: Static green ----------
phase('StaticGreen')
const staticGreen = await agent(COMMON + '\n\nROLE: Build Integration Engineer (Opus). All W1 engineers implemented their screens. Make the project STATICALLY GREEN: cd ' + APP + ' && /Users/oudaykhaled/flutter/bin/flutter analyze (fix compile errors / new analyze issues from W1: missing l10n keys, import/signature mismatches, unused imports). Then flutter test test/ and report. Bounded effort (no feature rewrites). If something needs a screen rewrite to compile, name the JM + file + exact error. Return: final analyze result (CLEAN of errors), test summary, files you fixed + why.\n\n=== ENGINEER OUTPUTS ===\n' + JSON.stringify(engResults.map((r, i) => ({ jm: ITEMS[i].jm, summary: typeof r === 'string' ? r.slice(0, 1200) : r }))), { label: 'w1:static-green', phase: 'StaticGreen' })

// ---------- Phase 4: Review (4 cluster reviewers) ----------
phase('Review')
const CLUSTERS = [
  { key: 'profile+home', items: 'JM-035, JM-023, JM-049, JM-050' },
  { key: 'create+chat', items: 'JM-024, JM-025, JM-031' },
  { key: 'offer+cancel', items: 'JM-026, JM-027, JM-028, JM-029, JM-030' },
  { key: 'track+receipt+rating', items: 'JM-032, JM-033, JM-034' },
]
const reviews = await parallel(CLUSTERS.map((c) => () =>
  agent(COMMON + '\n\nROLE: Senior Principal Flutter Engineer - CODE REVIEW (Opus) for the ' + c.key + ' cluster: items ' + c.items + '. Read the actual implemented files. Review each item against: its ACs in 30_BACKLOG.md; the identifier contract in 63_W1_TEST_PLAN.md (every required Semantics id present, exact spelling, no leading underscores); 40_GUARDRAILS_ARCH (clean arch, bloc, OMDS, l10n not hardcoded strings, no shared-file edits); navigation honesty (targets are real routes). Return one verdict object per item: {jm, approved, must_fix:[], nits:[], identifiers_ok}. Block only on real AC/guardrail violations.',
    { label: 'review:' + c.key, phase: 'Review' })
)).then(r => r)

// ---------- Phase 5: W0 flow patches (QA Sonnet) ----------
phase('W0Patch')
const w0patch = await agent(COMMON + '\n\nROLE: Senior Principal QA Engineer (Sonnet) - W0 PUNCH-LIST FLOW PATCHES (no emulator; flow YAML only). Close the W0 flow-side reds so the batched QA pass can verify them:\n1. RC-10 (jm-008-signup, jm-009-phone-otp): after signup_submit_cta the app shows the _register_hero phone-entry step before phone_otp_input. Add steps mirroring jm-018 AC1: assert _register_hero, tap the phone-number field, inputText a phone (0501234567), tap Send code, THEN assert phone_otp_input. Apply to jm-008 AC1 (and its AC5 first-signup leg) and jm-009 AC1.\n2. RC-7 (jm-021-verify-code, jm-022-set-password): the OTP-leaf fix landed (see otp-fix output below) - update these flows to enter the recovery code using the per-cell leaf ids the fix exposed (tap+inputText one digit per cell verify_code_input_0..5), so the code reaches the editable leaf and submit fires.\nValidate YAML parses; id-only assertions. Return: per-flow edits.\n\n=== OTP-LEAF FIX OUTPUT ===\n' + otpfix, { label: 'w0:flow-patch', phase: 'W0Patch', model: 'sonnet' })

return {
  integrator: !!integrator, seam_backend: !!seam, otp_fix: !!otpfix,
  engineers: engResults.length, static_green: staticGreen,
  reviews: reviews.length, w0_flow_patch: !!w0patch,
}
