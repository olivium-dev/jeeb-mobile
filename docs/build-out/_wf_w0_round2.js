export const meta = {
  name: 'jeeb-w0-round2',
  description: 'Close W0: fix the 5 precisely-owned root causes (RC-3..RC-7), rebuild + re-run all 11 flows, update sign-offs',
  phases: [
    { title: 'Fixes', detail: 'parallel: app fixes (DevBiometricGateway + OTP per-cell ids) and mock fixes (seed creds + /__mock/reset)' },
    { title: 'FlowFix', detail: 'Sonnet: align flows (reset prologue, phone-entry step, per-cell OTP taps, seeded creds)' },
    { title: 'Rerun', detail: 'Sonnet: rebuild, reinstall, re-run all 11 flows' },
    { title: 'Signoff', detail: 'Opus PO: update sign-offs; report W0 closure' },
  ],
}

const APP = '/Users/oudaykhaled/Desktop/olivium/jeeb/jeeb-mobile'
const MOCK = '/Users/oudaykhaled/Desktop/olivium/jeeb/jeeb-mock-backend'
const OUT = APP + '/docs/build-out'
const BASE = 'Jeeb mobile build-out, WAVE 0 round 2 (close W0). Context: ' + OUT + '/00_CTO_BRIEF.md, ' + OUT + '/61_W0_QA_RESULTS.md (Run 2: 4/11 pass; the 7 fails with root causes RC-3..RC-7 and precise owner actions - READ IT), ' + OUT + '/60_W0_TEST_PLAN.md, ' + OUT + '/62_SEAM_HARNESS.md (session seam contract), ' + OUT + '/41_GUARDRAILS_TESTING.md. App at ' + APP + '. Mock at ' + MOCK + ' (running on :4010; fixed dev codes OTP=123456, RECOVERY=654321). Android emulator jeeb_test, host 10.0.2.2:4010, run with --device emulator-5554, JAVA_HOME via /usr/libexec/java_home.'

// ---------- Phase 1: app + mock fixes (parallel, disjoint repos) ----------
phase('Fixes')
const [appFix, mockFix] = await parallel([
  () => agent(BASE + '\n\nROLE: Senior Principal Flutter Engineer (Opus) - W0 APP FIXES. You own jeeb-mobile app code only (NOT the mock). Fix two precisely-diagnosed defects from 61_W0_QA_RESULTS.md:\n1. RC-3 (jm-005 biometric, demo-critical): the wired UnavailableBiometricGateway.authenticate() always returns false, so biometric unlock can never succeed on the emulator. Add a DevBiometricGateway whose authenticate() returns true under kDebugMode (only in debug), and wire it in lib/core/di/injection_container.dart (around the biometric gateway registration, ~line 104) so debug/dev builds use it. Release behavior unchanged. Confirm the BiometricLockCubit then reaches unlocked and routes to the shell.\n2. RC-7 (jm-021/jm-022 OTP cells): Maestro inputText on the single verify_code_input / phone_otp_input Semantics container does not distribute digits across the multi-cell OTP widget (OmdsOtpInput has N separate TextFields), so the code is never entered and submit() never fires. Locate OmdsOtpInput (likely in ../omds-flutter/omds_library or lib; find it) and add a per-cell Semantics identifier of the form <containerId>_<index> (e.g. verify_code_input_0 .. verify_code_input_5) to each cell, KEEPING the existing container id too (additive, backward-compatible). Document the exact per-cell id scheme so QA can tap each cell.\nRun flutter analyze (clean of new errors). Do NOT run the emulator/maestro. Return: files changed, the DevBiometricGateway wiring, and the exact per-cell OTP id scheme (so QA taps verify_code_input_0..5 and phone_otp_input_0..5).', { label: 'r2:app-fix', phase: 'Fixes' }),
  () => agent(BASE + '\n\nROLE: Principal Backender (Opus) - W0 MOCK FIXES. You own ' + MOCK + ' only. Two fixes:\n1. RC-4 (jm-007 login, demo-critical): the appCredentials map is empty at startup so email/password login has no valid account. At module load in src/services/auth-service.ts, pre-seed a known test credential: appCredentials.set("test@jeeb.app", { password: "Password123!", userId: "user-client-001" }) and the corresponding emailToUserId mapping, consistent with the existing user store (user-client-001 should exist / resolve via getMe). Verify login with those creds returns tokens.\n2. RC-5 support (jm-008/009 signup persistence): add a POST /__mock/reset endpoint (mounted early in server.ts, dev-only) that resets the in-memory store.users (and any per-test mutable state) back to the initial seed, so a Maestro flow can call it before signup scenarios to avoid 409 collisions on fixed emails. Keep the seeded test credential (RC-4) present after reset.\nRun npm run build and npm test in ' + MOCK + '; fix anything you break; report results. Append a short note to ' + OUT + '/42_GUARDRAILS_MOCK.md documenting the seeded test credential and the /__mock/reset endpoint. Return: endpoints/changes + the exact test credential + how to call reset.', { label: 'r2:mock-fix', phase: 'Fixes' }),
])

// ---------- Phase 2: flow fixes (Sonnet) ----------
phase('FlowFix')
const flowFix = await agent(BASE + '\n\nROLE: Senior Principal QA Engineer (Sonnet) - W0 FLOW FIXES (round 2). The app + mock fixes just landed (summaries below). Update the failing flows to match:\n1. RC-4/jm-007: make jm-007-login type the seeded credential email test@jeeb.app and password Password123! into login_email_field/login_password_field.\n2. RC-5/jm-008,jm-009: add a prologue to each signup-dependent flow that resets mock state before signup (call POST http://10.0.2.2:4010/__mock/reset via a Maestro runScript/HTTP step, or use a unique per-run email via evalScript) so the fixed email does not 409-collide. Keep jm-008 AC5 (the intentional collision test) working by signing up twice within that one flow.\n3. RC-6/jm-018: social-login lands on /register phone-entry hero (_register_hero) first; drive that step (input a phone number + submit) BEFORE asserting phone_otp_input. Update jm-018 AC1 accordingly.\n4. RC-7/jm-021,jm-022: tap each OTP cell individually using the per-cell ids the app engineer added (see app-fix summary: verify_code_input_0..5, and phone_otp_input_0..5 for jm-009 if needed) and input one digit per cell, instead of one inputText on the container.\nKeep id-only assertions (no visible text). Validate all edited YAML parses. Return: per-flow edits made.\n\n=== APP-FIX SUMMARY (per-cell OTP ids etc.) ===\n' + appFix + '\n\n=== MOCK-FIX SUMMARY (test creds + reset) ===\n' + mockFix, { label: 'r2:flow-fix', phase: 'FlowFix', model: 'sonnet' })

// ---------- Phase 3: rebuild + rerun all 11 (Sonnet) ----------
phase('Rerun')
const rerun = await agent(BASE + '\n\nROLE: Senior Principal QA Engineer (Sonnet) - W0 RE-RUN (round 2). App (native+dart) changed, so rebuild. Steps: confirm mock up on :4010 (restart from ' + MOCK + ' with npm run dev if down); ensure emulator jeeb_test booted (boot in background, adb wait-for-device, poll sys.boot_completed=1; if a stray flutter run / iOS Runner process contends, ignore it - target --device emulator-5554 explicitly). Rebuild+install once: cd ' + APP + ' ; /Users/oudaykhaled/flutter/bin/flutter build apk --debug --flavor dev --dart-define=JEEB_MOCK_BASE_URL=http://10.0.2.2:4010 ; ~/Library/Android/sdk/platform-tools/adb install -r build/app/outputs/flutter-apk/app-dev-debug.apk . export JAVA_HOME via /usr/libexec/java_home. Re-run ALL 11 W0 flows (jm-005..jm-022) with ~/.maestro/bin/maestro --device emulator-5554 test -e APP_ID=app.jeeb.mobile.dev --format JUNIT <flow>. Overwrite ' + OUT + '/61_W0_QA_RESULTS.md with a Run 3 section (per-flow PASS/FAIL + category + failing step for any remaining fail). Return: PASS/FAIL counts and any remaining failure with category + precise owner action.', { label: 'r2:rerun', phase: 'Rerun', model: 'sonnet' })

// ---------- Phase 4: sign-off (Opus PO) ----------
phase('Signoff')
const signoff = await agent(BASE + '\n\nROLE: Product Owner (Opus) - W0 SIGN-OFF (round 2). Using the Run 3 results below + W0 ACs in 30_BACKLOG.md, update each ' + OUT + '/signoffs/JM-###.md: SIGNED for now-green flows (AC-to-evidence + DoD), PARTIAL/BLOCKED with exact remaining owner action otherwise. State the demo-critical four (JM-007/009/006/005) status explicitly. Return: SIGNED vs PARTIAL/BLOCKED counts, demo-critical status, whether W0 is now CLOSED (all 11 flows green), and any residual fix list.\n\n=== RUN 3 RESULTS ===\n' + rerun, { label: 'r2:signoff', phase: 'Signoff' })

return { app_fix: !!appFix, mock_fix: !!mockFix, flows_fixed: !!flowFix, rerun_summary: rerun, signoff_summary: signoff }
