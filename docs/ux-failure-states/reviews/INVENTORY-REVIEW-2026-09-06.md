# Independent review of the session inventory — 2026-09-06

Method: one independent Fable 5.1 reviewer per inventory point (16), read-only, fresh context each, Workflow run `wf_20157fed-4f3`. Each reviewer re-derived every claim from GitHub, fresh clones, the worktree, or the live host, then reviewed the attached work. Verdict = CONFIRMED only when every claim held. Raw results: session scratchpad `review/RESULTS.json`.

## Scorecard

| Point | Verdict | Claims C / Co / R / U | Findings B / M / m / n |
|---|---|---|---|
| mobile-branch | CORRECTED | 10 / 1 / 1 / 0 | 0 / 2 / 2 / 3 |
| pr-335 | CORRECTED | 7 / 2 / 3 / 0 | 2 / 3 / 2 / 2 |
| fastlane-patch | CORRECTED | 17 / 5 / 1 / 1 | 0 / 2 / 3 / 3 |
| rubyzip-patch | CORRECTED | 15 / 2 / 2 / 0 | 0 / 2 / 4 / 2 |
| rahmah-gateway | CORRECTED | 12 / 6 / 0 / 0 | 0 / 2 / 4 / 2 |
| rahmah-fe | CORRECTED | 10 / 3 / 1 / 0 | 0 / 1 / 3 / 2 |
| ofc-form-builder | CORRECTED | 6 / 3 / 1 / 0 | 0 / 2 / 3 / 1 |
| rahmah-admin-panel | CONFIRMED | 6 / 0 / 0 / 0 | 0 / 1 / 2 / 2 |
| jeeb-gateway | CORRECTED | 10 / 2 / 0 / 1 | 0 / 2 / 3 / 2 |
| form-builder-service | CORRECTED | 10 / 7 / 0 / 0 | 0 / 3 / 3 / 3 |
| mobile-prs-330-274 | CORRECTED | 12 / 1 / 0 / 0 | 0 / 2 / 3 / 3 |
| advisory-db | CORRECTED | 15 / 2 / 2 / 1 | 0 / 3 / 3 / 2 |
| planned-mobile-branches | CORRECTED | 6 / 2 / 1 / 0 | 0 / 3 / 3 / 1 |
| planned-gateway-branches | CORRECTED | 9 / 2 / 1 / 0 | 0 / 2 / 4 / 1 |
| planned-fb-branch | CORRECTED | 15 / 4 / 1 / 0 | 0 / 2 / 2 / 3 |
| links | CORRECTED | 15 / 2 / 0 / 1 | 0 / 2 / 2 / 1 |

## Cross-cutting findings (owner-facing, ranked)

1. **BLOCKER, new: PR #335 has a second merge blocker besides rubyzip.** The required `CI ready` check is red on 12 of 13 commits because `Flutter stage / Test` is cancelled at the 20-minute cap (`.github/workflows/ci-flutter-stage.yml:55`) with 0 failures; the 40-minute coverage workflow passes (27m33s). OD-12 (raise to 35 min) is approved in DECISIONS.md but no commit applies it. Android/iOS stages are skipped as a consequence.
2. **BLOCKER, known: Release security scans** fail on rubyzip 2.4.1 (advisory-db drift; main would fail on re-run too). Only the upstream fixes or an owner-gated gate change clear it.
3. **Inventory wrong-repo trap.** `fix/p03-create-request-validation` and `fix/notifications-inbox-target-ref` are jeeb-GATEWAY branches (P03-G, P02-G, wave 0 worktrees off `origin/main` 6679f6ee); only `fix/dm-onboarding-route` is jeeb-mobile. P02/P03 mobile work has no branch: under OD-0 widen it rides PR #335. Fixed in the corrected inventory.
4. **Plan-set contradictions after the owner decisions.** EXECUTION-PLAN (07:10Z) still parks P01 and says "no P01 v2 draft"; P01 v3 (later) says start now. P02 §5 and P03 §5 still carry the voided C1 scope-freeze instructions (stacked branches off `ecfd3cc1`, "never push onto #335"). PR #335's body says 4 commits and comment 3 still presents the freeze; OD-0 widen is recorded nowhere on the PR.
5. **Upstream CVE kit needs edits before filing.** (a) `fastlane.patch` lines 20-21, the rubyzip patch and both PR texts carry the private `Claude-Session` URL and the Co-Authored-By trailer: strip before pushing. (b) Commit author is `appsaawt@gmail.com`; confirm it is a verified email on the GitHub account or re-author. (c) `ADVISORY-DB-NOTE.md`/README cite a non-existent `spec/gems/rubyzip_spec.rb` and claim the spec checks rubygems.org; the real suite (`spec/advisories_spec.rb`, 47/47 + schema 1393/1393) passes even before 2.4.2 exists. (d) GHSA-47m2-wp7j-p9vc is `unreviewed` with no package or range, so the "add 2.4.2 to the `< 2.4.2` range" step has nothing to attach to and Dependabot ignores it; ruby-advisory-db is the ONLY database our gate reads. (e) README says `evidence/` and the clones are "in this folder": they live only in the session scratchpad. (f) Bundler is 4.0.16 not 2.5.23; the rubyzip cherry-pick reset the author date; PR text should cite `#7525 (issue #7534)` and `#16660` only (#15970 was never merged). Patch content itself: fileable (fastlane: single call site, 8/0 on four rubyzip versions, master unmoved; rubyzip: entry.rb hunk byte-identical to 17edfbf, 326/0, mutation-proven, base `2.4` unmoved).
6. **P01 v3 citation and design corrections (plan still sound, "MIRROR" claims overstated).** Rahma's renderer dispatches on `componentID`, never `componentName` (0 reads), so jeeb's "componentName = UI kind" template is a deviation, not a mirror. `ofc_form_builder` IS configurable (`FormBuilderWrapper(provider:)`, `FormBuilderProvider(baseUrl, getScreensEndpoint, submitScreenEndpoint)`, non-Rahma screen keys fall to the generic submit) so a reuse path exists that the plan did not weigh. `form_builder_template.json` provenance is inverted: the gateway repo is the source the deployed builder clones from. `GET api/User/data` returns `RemoteUserPreferencesWithIds` (IdValue-typed), not the flat DTO. `[Authorize]` is per-action; `liveIn` has a matching side-effect. Stale refs: staging workflow `:197` not `:188`; vocab exclude `:26` not `:12-13`; `screens_helper.dart:158-181,357-386`; provider `:7,27-100`.
7. **P01 v3 deploy steps would mis-deploy on MSI.** The live unit runs from `WorkingDirectory=/home/ec2-user/iter5-services/form-builder-service` (plain copy, not a symlink, not git); D-FB4 targets a new `iter5-native/releases/...` dir, so its restart would keep serving old code. `generated_jeeb_jeeber_v1.json` is in no git repo; the iter5-services copy is the single source of truth. The FB5 vocabulary gate is ALREADY red on pristine `origin/main` 801ef01 (`scripts/audit-fail-closed-deployments.py:9,11,13`) and its workflow is disabled; no CI runs pytest. "Non-breaking for Rahma" is argued against code Rahma does not run (Rahma prod = f1e9d9f / 1b6b4c8).
8. **P15 corrections.** F2 ("epic changes zero wallet-service lines") is a tautology: the gateway repo holds no wallet-service source. The epic's pinned wallet OpenAPI slice is a 465-byte PLACEHOLDER (`paths {}`) while main's is a real 28-path slice: the replay must not carry the placeholder back. The validator throws in every environment that is not Development/Testing (`RequiredInProduction` default true), not only Production. The PR train never marks its stacking (G3 needs G2's enum, G6 needs G1-G5). P01 and P15 both edit `Program.cs`, `appsettings*.json`, `scripts/gwdbx-flag-registry.txt` with no stated merge order. `ADR-005` cited by P01 does not exist on main. F1/F3/F7 confirmed at file:line.
9. **Decision site defects.** The P01 plan card and the OD-1 options/CTO recommendation still describe the v1 design (`/v1/jeebers/me/onboarding`, bbox in appsettings). All 13 plan cards cite `PLAN-Pnn.md` paths that do not exist (real files are `PLAN-Pnn-<slug>.md`, P01 at `.v3.md`). OD-15 text: "one mobile commit" should be "one mobile PR"; "three things would have broken" should be "two" (item 3 is additive). Everything else on the page verified (18 records, db-backed, effort figures match).
10. **Attribution hygiene.** 6 of 13 commits and 3 of 5 PR comments lack the session URL (all have Co-Authored-By). Fixing commits means rewriting pushed history: owner call.
11. **Smaller facts.** `db83ba7a` buries a CI policy change (design-token gate made diff-scoped) inside a feature commit. `ofc-form-builder` main is not the last published state (branch `PROF-592-salehly-widgets`, 1.0.6-beta.1, was the last pub push). PR #274's headline regime was reversed by PR #276 (configs untracked + CI-injected; doctor retargeted by #332); the doctor hook still runs and passes on the branch. Rahma offers NO admin precedent for a preferences view (its gateway has no admin-scoped read). 4 tracked plans embed the session scratchpad path; 22 lines carry the home path (repo precedent: 59 files).

## Verified-true highlights (so they are not re-checked)

- Branch facts exact: merge-base `ab610933`, HEAD `050051d6`, 13/0, worktree clean, gradlew mode untouched, only 4 commits touch `lib/`+`test/`, forward-only checker green.
- PR #330 invariants intact on the branch (single change `db83ba7a` adds two classification flags); #330/#274 merge SHAs correct and ancestors.
- jeeb-gateway `origin/main` 6679f6e; epic `dfa9159d`, 5 ahead / 97 behind, no PR ever opened; zero non-comment Npgsql in `src/` on both trees.
- form-builder-service `origin/main` 801ef01 = the SHA running on MSI :10070 (`jeeb_jeeber_v1`, 11 components); F1 primary-only loader reproduced by simulation; plan template JSON boots as a second file (6 components, `attributes.step` survives).
- ruby-advisory-db entry: `patched_versions: [">= 3.4.0"]`, no open PR; bundler-audit 0.9.3 `--update` flags 2.4.1 and clears 2.4.2 against the patched DB.
- Artifact page: only fonts.googleapis.com external, JSON parses, 3 inline scripts pass `node --check`, 18 decisions in the artifact db match DECISIONS.md.


## Per-point detail

### mobile-branch — CORRECTED

The branch facts are exact: it exists on origin, merge-base is ab610933, HEAD is 050051d6 locally and remotely, 13 ahead / 0 behind, the worktree is clean and gradlew's mode is unchanged. The 13 commits split cleanly — four code commits are the only ones touching lib/ and test/, nine touch only docs/ — and the fail-closed deployment check passes at HEAD. Two claims did not hold: only 7 of 13 commits carry the Claude-Session trailer (six mid-programme docs commits have only Co-Authored-By), and, more importantly for merge time, PR #335 is blocked not just by the known rubyzip security scan but also by the required `CI ready` check — the branch's test suite now exceeds the 20-minute `timeout-minutes` on the `Flutter stage / Test` job and gets cancelled on 11 of 13 commits, while main finishes in about 17 minutes. That timeout needs raising or the suite sharding before the PR can ever go green. The four leftover scratchpad paths in plan docs are harmless (no secrets), just non-reproducible; the CI-gate rewrite hidden in the `feat(core)` commit and the unrelated rubyzip patches riding on the UX PR are worth flagging to the owner.

Claims not confirmed:

- [CORRECTED] Every commit message has the Co-Authored-By and Claude-Session trailers
  - Evidence: `git log -1 --format=%B <sha> | grep -c` per commit: Co-Authored-By present 13/13; Claude-Session present only 7/13 — missing on e1b1c993, e212f8d3, 36ae0f36, 125ef39a, 201659dc, e14d0a18 (their footers end with only `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`). Present on db83ba7a, a48444ba, 7a0c386b, ecfd3cc1, f4a209d4, 111cf177, 050051d6.
- [REFUTED] Branch is mergeable / gate green (implied by 'draft PR #335' context)
  - Evidence: `gh pr view 335` -> state OPEN, isDraft true, mergeable MERGEABLE but mergeStateStatus BLOCKED. Required checks on main: verify, CI ready, Flutter CI + coverage (79%), L10n parity gate, Release security scans. On 050051d6: `CI ready` = failure because `Flutter stage / Test` was cancelled at 08:06:26->08:26:43 (20m17s) by `timeout-minutes: 20` (.github/workflows/ci-flutter-stage.yml:55). `CI` workflow is cancelled on 11/13 branch commits (only 7a0c386b passed, Test 19m29s); main's Test job runs 16.5–17.5 min and passes. `Release security scans` = failure (known rubyzip). `Flutter CI + coverage (79%)` passes on every commit (33–34 min, in_progress on HEAD at 08:30Z).

Findings:

- **major** — Merge is blocked by a SECOND, unreported cause besides rubyzip: the required `CI ready` check fails because `Flutter stage / Test` (ci-flutter-stage.yml:55, `timeout-minutes: 20`) is cancelled at the 20-minute mark on 11 of 13 branch commits. Main's Test job takes 16.5–17.5 min; this branch's suite (362 test files touched) takes ≥20 min (the one pass, 7a0c386b, took 19m29s). Even with rubyzip fixed, PR #335 cannot merge until the timeout is raised (e.g. 30–35 min, matching the 40-min budget of flutter-ci.yml), the suite is sharded, or the suite is made faster. The parallel `Flutter CI + coverage (79%)` workflow does pass, which is why the branch looks 'green' from the coverage angle.
- **major** — Trailer hygiene is inconsistent: 6 of the 13 commits (e1b1c993, e212f8d3, 36ae0f36, 125ef39a, 201659dc, e14d0a18 — all docs commits from 2026-09-05 16:19Z to 2026-09-06 07:45Z) lack the `Claude-Session:` trailer that the four code commits and the three latest docs commits carry. All 13 have Co-Authored-By. Not a functional defect, but the inventory's 'every commit has both trailers' statement is false; fixing it would require rewriting pushed history (owner call — probably not worth it on a draft PR).
- **minor** — db83ba7a is labelled `feat(core)` but also changes CI policy: it adds a new `Design token compliance gate (diff-scoped)` step to both workflows (fetch-depth: 0) and rewrites tool/check_design_tokens.sh from a full-tree scan to a diff-vs-`DESIGN_TOKENS_BASE` scan by default (`--all` = audit only). The script itself documents that on a push to main the scope is empty, so the gate is effectively advisory on main. Reasonable, but the owner should know the CI diff is buried in a feature commit.
- **minor** — 4 tracked plan docs (P04:12, P07:97, P09:8, P11:8) embed the absolute session scratchpad path `/private/tmp/claude-501/…/6a29e634-…/scratchpad` and refer to helper scripts (`dump.sh`, `shot.sh`) that live only there, plus 22 `/Users/oudaykhaled/…` lines. Not harmful (no secrets; the repo already has 59 files with the home path) but the plans are not reproducible by anyone else; a one-line `$SCRATCH` placeholder edit would clean it.
- **note** — 050051d6 puts an unrelated security-topic (rubyzip CVE upstream patches, 6 files under docs/security/) on the UX PR. The patch files are well-formed text, but mixing that with a 915-file / +70,124 / −8,256 UX PR makes the PR harder to review; consider moving it to its own branch.
- **note** — `Flutter CI + coverage (79%)` now takes 33–34 min of its 40-min budget on this branch; it will hit the same timeout wall as `CI` if the suite grows further.
- **note** — Everything else checked out: branch/remote/merge-base/ahead-behind exact, worktree clean, gradlew mode untouched, only the 4 code commits touch lib/+test/, no google-services/local.properties/keystore/.claude files in the diff, fail-closed policy check passes locally and in GitHub Actions, all commits authored by the owner's account.

Corrected wording:

- olivium-dev/jeeb-mobile — branch ux/api-error-handling-empty-states (off origin/main ab610933; HEAD 050051d6; 13 ahead / 0 behind; worktree clean); worktree /Users/oudaykhaled/Desktop/olivium/jeeb/jeeb-mobile-worktrees/ux-api-errors. 4 code commits (db83ba7a, a48444ba, 7a0c386b, ecfd3cc1) are the only ones touching lib/+test/ (db83ba7a also edits both CI workflows + tool/check_design_tokens.sh); 9 commits are docs-only. Trailers: Co-Authored-By on 13/13, Claude-Session on 7/13 (missing on e1b1c993, e212f8d3, 36ae0f36, 125ef39a, 201659dc, e14d0a18). check_fail_closed_deployment.py passes. PR #335 is BLOCKED by two required checks: Release security scans (rubyzip) AND `CI ready` — the Test job exceeds ci-flutter-stage.yml's 20-min timeout on 11/13 commits (main: 16.5–17.5 min). 4 scratchpad paths remain in plan docs (harmless).

### pr-335 — CORRECTED

PR #335 is a real, open draft from ux/api-error-handling-empty-states onto main, 13 commits ahead and 0 behind, with no merge conflicts but a BLOCKED merge state. Right now four checks pass (Analyze, Flutter CI + coverage at 10539/0 tests and 84.64%, verify, L10n parity) and two fail: Release security on the rubyzip CVE exactly as expected, and — not expected — the composite CI workflow, because the 'Flutter stage / Test' job hits its 20-minute cap with zero failures and takes 'CI ready' down with it; that has been true for every commit on the PR except 7a0c386b, so the branch has never been fully green and the approved OD-12 timeout bump has not been committed. The body is stale (says 4 commits, 84.65%), the two device-run comments renumber the defects and disagree on 7/9 vs 8/9, and only two of the five comments carry the Claude Code footer plus session URL. The scope-freeze statement is no longer true: the owner answered OD-0 with 'widen' and the execution plan declares the freeze void, though no code outside docs/ has landed since. Evidence files are under scratchpad/review/pr-335/.

Claims not confirmed:

- [CORRECTED] test is green
  - Evidence: Two test gates exist. (a) 'Flutter CI + coverage (79%)' (flutter-ci.yml, timeout 40) PASSED: 10539 ✅ / 0 ❌ in 27m33s (run 34020816486, flutter-ci-050051d6.log). (b) 'Flutter stage / Test' (ci-flutter-stage.yml:55 timeout-minutes: 20) was CANCELLED at 20m17s: last progress line '18:50 +10280 ~107' with 0 failures, then '##[error]The operation was canceled.' (ci-test-job-api.log:15531). Because of that 'CI ready' FAILS ('FLUTTER_RESULT: cancelled ... exit code 1', job 101455803015) and Android/iOS stage are SKIPPED. run-history.txt: the CI workflow has been cancelled on EVERY commit of this PR (a48444ba, ecfd3cc1, e1b1c993 ... 050051d6) except 7a0c386b, whose Test job took 19m29s (31s under the cap). Fix = OD-12 (raise to 35) is approved in DECISIONS.md but NOT yet committed
- [CORRECTED] The PR body is consistent with the commits and nothing is stale
  - Evidence: Body: 'Landed in 4 commits: db83ba7a, a48444ba, 7a0c386b, ecfd3cc1' but gh api pulls/335/commits lists 13 (9 docs commits since: e1b1c993 e212f8d3 36ae0f36 125ef39a 201659dc e14d0a18 f4a209d4 111cf177 050051d6) -> stale. Body 'Gate ... 10539 pass / 0 fail; coverage 84.65%' -> 10539/0 confirmed, 84.64% actual; body never says the composite CI workflow is red by timeout. Body item 4 says stray request defb1f07 'left pending ... auto-expires 2026-09-06 11:21' -> not re-verified here (memory says cancelled 07:27Z); body item 7 'still a draft' true. Post-freeze diff e1b1c993..050051d6: 13 files, all under docs/ -> code claims (AppFailure mapper, RetryInterceptor, GatewayProblem, request_id_interceptor X-Request-Id, OfflineBannerHost, sendTimeout 30s in lib/core/network/mock_gateway_client.dart:121 which is the REAL client factory despite its name) all present
- [REFUTED] The comment trail is consistent
  - Evidence: comments.json: comment 1 (5550802845) numbers F3=profile blank fields, F4=offline banner semantics, F7=notification dead tap, F8=cancel-request stale list; comment 2 (5551323408) is titled 'Run 2 summary — 7/9 fixed' and says it 'closes the remaining 2 items (F4, F8)' where F4=customer profile placeholders and F8=notification tap snack -> F-numbering contradicts comment 1, and '7/9 fixed' contradicts the PR body's 'Run 2 ... 8/9 confirmed fixed; 1 residual'. Comment 3 (5553131174) still frames OD-0 as 'default yes' freeze; no comment records the owner's actual OD-0 answer
- [REFUTED] Every comment carries the Claude Code footer plus session URL
  - Evidence: 5 issue comments: #1 5550802845 has '🤖 Generated with [Claude Code]' but NO session URL; #2 5551323408 has only 'Co-Authored-By: Claude Fable 5.1' (no footer, no URL); #3 5553131174 has neither; #4 5557883274 and #5 5557912192 have footer + https://claude.ai/code/session_01VydEU5hq3ihQ5j6ooPFaEq. So 2/5 compliant. Commit messages: 6/13 lack the session URL (e1b1c993 e212f8d3 36ae0f36 125ef39a 201659dc e14d0a18), all 13 have Co-Authored-By
- [REFUTED] The scope-freeze statement on #335 is still true
  - Evidence: docs/ux-failure-states/DECISIONS.md:5 '| OD-0 | Let follow-ups ride #335 (`widen`) | No — recommended freeze |' (commit e212f8d3); docs/ux-failure-states/plans/EXECUTION-PLAN-2026-09-06.md:22 'Landing rule under **OD-0 = widen**: the C1 scope freeze is VOID; every mobile follow-up lands as commits on' #335. The owner overruled the freeze; the PR comment trail (comment 3) still reads as freeze-pending. Code-wise nothing outside docs/ has landed since the freeze comment (git diff --stat e1b1c993..050051d6 = 13 docs files), but the rule itself is void

Findings:

- **blocker** — 'CI ready' is RED on the current head and on every commit of this PR except 7a0c386b: the 'Flutter stage / Test' job dies at the ci-flutter-stage.yml:55 timeout-minutes: 20 cap (+10280 of 10539 tests, 0 failures) and CI ready hard-requires FLUTTER_RESULT==success, which also skips the Android/iOS stages entirely. 7a0c386b only passed with 31s headroom. The 'Flutter CI + coverage' job on the 40-minute workflow is the only green test signal (27m33s now; plan L5 measured 34m13s at ecfd3cc1, so ~6 min headroom there too). OD-12 (raise to 35) is approved in DECISIONS.md but no commit applies it; nobody can mark the PR ready until it lands, and any further test growth will also break the 40-minute job.
- **blocker** — 'Release security scans' fails on rubyzip 2.4.1 / CVE-2026-85396 (bundler-audit exit 1). Gemfile.lock is unchanged on the branch and main's last run (2026-09-04) was green, so this is advisory-db drift that will hit main and every other PR on re-run; the branch's only contribution is docs/security/upstream-rubyzip-cve-2026-85396/ (patches for the owner to file upstream). With --ignore forbidden, #335 cannot merge until fastlane/rubyzip ship or the owner relaxes the gate.
- **major** — The comment trail is internally inconsistent: comment 2 ('Run 2 summary — 7/9 fixed', closes 'F4, F8' = profile placeholders + notification snack) renumbers the defects relative to comment 1 (F3=profile, F4=offline banner, F7=notification tap, F8=cancel list) and contradicts the body's 'Run 2: 8/9 fixed, 1 residual'. A reader cannot map defect ids across the two comments and the body without re-reading the fix commits.
- **major** — OD-0 was decided as WIDEN (DECISIONS.md:5; EXECUTION-PLAN:22 'the C1 scope freeze is VOID'), but neither the PR body nor any comment records it — comment 3 still presents the freeze as the default. The memory line 'scope freeze on #335' is stale. The PR will now absorb P02/P03/P05/P06/P13... commits on top of 915 files / +70,124 -8,256, so a squash merge (OD-13) and a body rewrite are needed before it is marked ready.
- **major** — Attribution rule not met: only comments 4 and 5 carry the Claude Code footer plus session URL; comment 1 has the footer without a URL, comment 2 only a Co-Authored-By line, comment 3 nothing. Six of 13 commit messages (e1b1c993, e212f8d3, 36ae0f36, 125ef39a, 201659dc, e14d0a18) lack the Claude-Session URL.
- **minor** — PR body is stale: 'Landed in 4 commits' (now 13), coverage '84.65%' (log says 84.642%), no mention of the 9 docs commits (plans, 18 owner decisions, P15 wallet rule, rubyzip upstream patches), and owner-decision item 4 (defb1f07 'left pending, auto-expires 11:21 UTC') was not re-verified here and is reported elsewhere as cancelled at 07:27Z.
- **minor** — The 'verify' (fail-closed deploy policy) gate was red for five consecutive docs commits (e1b1c993..201659dc) purely because PLAN-P10 used the word 'rollback'; e14d0a18 reworded it. The check scans all 3041 tracked text files including plans, so every future plan doc on this branch must avoid the reversion vocabulary.
- **note** — The real gateway Dio client lives in lib/core/network/mock_gateway_client.dart (it wires AuthInterceptor, RequestIdInterceptor, AppFailureMapper and sets sendTimeout 30s at :121). The filename is a trap for reviewers verifying the body's sendTimeout/X-Request-Id claims; consider a rename in a follow-up.
- **note** — Flutter CI logs 'Skip dev FCM APK for fork PR' on this same-repo PR (secret-gated step skipped), so the secret-dependent dev APK build path is not exercised by PR CI; it will only be proven on the RC/distribute dispatch (OD-14).

Corrected wording:

- olivium-dev/jeeb-mobile — draft PR #335 https://github.com/olivium-dev/jeeb-mobile/pull/335 (head ux/api-error-handling-empty-states @ 050051d6, 13 commits, base main @ ab610933, 0 behind; MERGEABLE but mergeStateStatus BLOCKED). Checks @ 2026-09-06 08:32Z: PASS — Flutter stage/Analyze, Flutter CI + coverage (79%) [10539/0, 84.64%], verify (fail-closed policy), L10n parity gate; FAIL — Release security scans (rubyzip 2.4.1 CVE-2026-85396, advisory-db drift, Gemfile.lock unchanged, main would fail too), Flutter stage/Test (CANCELLED at the 20-min cap, +10280 tests 0 failures — red on every commit except 7a0c386b) and therefore CI ready; SKIPPED — Android stage, iOS stage. OD-12 (timeout 20→35) approved but not committed. OD-0 = WIDEN: the scope freeze is VOID (DECISIONS.md:5, EXECUTION-PLAN:22) and the PR body/comments do not yet say so. Body stale ('4 commits', 84.65%); comment trail renumbers the run-1 defects and says '7/9' vs body '8/9'; only 2 of 5 comments (and 7 of 13 commits) carry the Claude Code footer + session URL.

### fastlane-patch — CORRECTED

The patch itself checks out: its parent is fastlane master 280c8e887, which has not moved (GitHub compare shows 0 commits since); it applies cleanly with `git am -k` to a fresh shallow clone; the diff is exactly the gemspec range line, the one-line `Zip::File.open(path)` fix and two Gemfile.lock lines; a repo-wide grep of master confirms ipa_file_analyser.rb is the only rubyzip call site (zip/dsym_zip actions shell out); and I reproduced the 8/0 spec result on rubyzip 3.6.0 and 2.3.2, the 4/8 ArgumentError on unpatched code, 739/0 for fastlane_core/spec, and rubocop-clean files. No open fastlane PR or issue covers rubyzip 3 or CVE-2026-85396, so there is nothing to link. It is not fileable verbatim, though: the commit message and PR body carry a private Claude session URL and an AI co-author trailer that must be stripped, the author email (appsaawt@gmail.com) may not be verified on the owner's GitHub account, and the PR narrative has four small provenance errors (Dependabot claim, #7534 is an issue, #15970 was never merged, the gemspec comment predates #12615) plus one testing-table row (rubyzip 2.3.2) with no evidence log. Fix those wording/trailer items and the patch is ready to file.

Claims not confirmed:

- [CORRECTED] Patched code passes on rubyzip 2.3.2 / 2.4.1 / 3.4.0 / 3.6.0 (PR text 'Testing Steps' table)
  - Evidence: 2.4.1 and 3.4.0: evidence/fastlane-03-ipa-spec-range-edges-2.4.1-and-3.4.0.log (8/0 each); 3.6.0: log 02 + my re-run. The '2.3.2 (previously locked)' row has NO log in evidence/. I ran it myself in the fresh clone (lock edited to 2.3.2, `loaded rubyzip 2.3.2`, 8 examples 0 failures) — the row is true but undocumented; add a log or drop the row.
- [CORRECTED] Downstream Gemfiles 'now fail bundler-audit / Dependabot' (PR text, Motivation)
  - Evidence: `gh api /advisories/GHSA-47m2-wp7j-p9vc --jq '{type,vulnerabilities}'` -> type 'unreviewed', vulnerabilities [] (no package/range data). Dependabot alerts fire only on reviewed advisories, so the Dependabot half is unbacked; bundler-audit (ruby-advisory-db) is the thing that fails. Replace with 'fails bundler-audit'.
- [CORRECTED] fastlane previously widened this range for downstream resolution (#7525, #7534)
  - Evidence: `gh api repos/fastlane/fastlane/pulls/7525` -> merged 2016-12-16 'Losen up rubyzip dependency'. `pulls/7534` -> 404; `issues/7534` -> closed ISSUE 'rubyzip-1.1.7 conflicts with rubyzip (< 2.0.0, >= 1.2.0)'. Cite as 'PR #7525 (issue #7534)'.
- [CORRECTED] '#15420 pinned >= 1.3.0, #15970/#16660 moved to 2.x' (PR text)
  - Evidence: #15420 merged 2019-10-01 'Pin rubyzip to >=1.3.0'; #16660 merged 2020-06-28 'Require rubyzip >= 2.0.0'; but #15970 'Security: Updating rubyzip version to 2.2.0' is closed UNMERGED (merged_at null). Cite #16660 only.
- [REFUTED] The '# fix swift/ipa in gym' gemspec comment dates from #12615 (2018)
  - Evidence: `gh api repos/fastlane/fastlane/pulls/12615/files` shows the comment already on the removed line ('-  spec.add_dependency('rubyzip', '>= 1.1.0', '< 2.0.0') # fix swift/ipa in gym'); `git log -S"fix swift/ipa in gym" origin/master -- fastlane.gemspec` -> introduced by 6649d0daa 2016-12-15 'Migrate fastlane to be a mono gem (#7436)'. The conclusion (comment documents the floor, not the cap) still holds.
- [UNVERIFIABLE] Commit authorship is attributable to the owner's GitHub account
  - Evidence: Patch/commit author is 'Ouday Khaled <appsaawt@gmail.com>' (clone `git config user.email`), while `gh api user` primary email is ouday.khaled@gmail.com. `gh api user/emails` needs the `user` OAuth scope (not granted; I did not refresh auth), so whether appsaawt@gmail.com is a verified address on the account cannot be checked read-only. If it is not, the upstream commit will not link to the owner's profile.
- [CORRECTED] The patch/PR text are ready to file verbatim
  - Evidence: Commit message in fastlane.patch ends with 'Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>' and 'Claude-Session: https://claude.ai/code/session_01VydEU5hq3ihQ5j6ooPFaEq'; FASTLANE-PR.md body ends with the same private session URL. Those trailers must be stripped before pushing to a public fork/PR (`git commit --amend` or edit the patch header).

Findings:

- **major** — fastlane.patch commit message carries a private 'Claude-Session: https://claude.ai/code/session_…' trailer and a 'Co-Authored-By: Claude Fable 5.1' trailer, and FASTLANE-PR.md's body ends with the same session URL. Filing as-is leaks an internal session link into a public upstream repo. Strip both before `git push`/PR creation (patch lines 20-21 and the last two lines of FASTLANE-PR.md).
- **major** — Author identity: the commit is authored as <appsaawt@gmail.com> while the owner's GitHub primary email is ouday.khaled@gmail.com. If appsaawt@gmail.com is not a verified email on the account, the upstream commit will show as unlinked. Owner should confirm or re-author with `git commit --amend --reset-author` after setting user.email.
- **minor** — FASTLANE-PR.md has four provenance inaccuracies a fastlane maintainer can check in seconds: (1) 'bundler-audit / Dependabot' — the GHSA is type=unreviewed with no vulnerable ranges, so Dependabot does not alert today; (2) '#7525, #7534' — #7534 is an issue, not a PR; (3) '#15970/#16660 moved to 2.x' — #15970 was closed unmerged; (4) the '# fix swift/ipa in gym' comment predates #12615 (already present on the removed line there; introduced to fastlane.gemspec by #7436 in Dec 2016).
- **minor** — The PR 'Testing Steps' table claims a run on rubyzip 2.3.2 (previously locked) but evidence/ has no log for it (log 03 covers only 2.4.1 and 3.4.0). I reproduced it (8/0) so the claim is true, but either add the log or drop the row so every table row is backed.
- **minor** — PR text lists pilot/deliver as 'the IpaFileAnalyser callers' but omits fastlane/lib/fastlane/actions/get_ipa_info_plist_value.rb (calls fetch_info_plist_file). Covered by the full-suite run, but the audit sentence is incomplete.
- **note** — No local run on the other CI legs (Ruby 3.1/3.2/3.4/4.0, Windows). ruby@3.4 is installed on this Mac and could have been used. Risk is low (rubyzip's own CI covers 3.0–4.0 and the change is one dropped positional arg), but the two unticked checklist boxes correctly leave this to upstream CI.
- **note** — Housekeeping in the clone: the local `master` branch in scratchpad/upstream/fastlane was fast-forwarded onto the patch commit (ahead 1 of origin/master). Harmless for filing, but README's 'cd upstream/fastlane; git push -u fork fastlane-core/allow-rubyzip-3' still pushes the right branch; do not push `master`.
- **note** — Substance of the patch is sound and minimal: one true call site, dual-compatible one-liner, range widened not narrowed, lock moved to 3.6.0 so CI exercises 3.x, rubocop clean, spec 8/0 on 2.3.2/2.4.1/3.4.0/3.6.0, repro 4/8 ArgumentError on master. Patch is fileable once the trailers/author are fixed and the PR-text wording corrected.

Corrected wording:

- fastlane/fastlane — local branch fastlane-core/allow-rubyzip-3 (base master 280c8e887, master unchanged as of 2026-09-06) → fastlane.patch (applies cleanly with `git am -k`; 3 files, +4/−4; single rubyzip call site; spec 8/0 on rubyzip 2.3.2/2.4.1/3.4.0/3.6.0). Before the owner files it: strip the `Claude-Session:` and `Co-Authored-By:` trailers from the commit/patch and the session-URL footer from FASTLANE-PR.md; confirm author email appsaawt@gmail.com is verified on the GitHub account; fix PR-text wording — 'bundler-audit' (not 'bundler-audit / Dependabot': GHSA is unreviewed), 'PR #7525 (issue #7534)', '#16660' (not '#15970/#16660'), and the gemspec comment predates #12615 (introduced by #7436, 2016); add the missing rubyzip 2.3.2 run log to evidence/ or drop that table row.

### rubyzip-patch — CORRECTED

The patch itself is sound and fileable: I re-verified from a fresh shallow clone that branch 2.4 is still at 6c4b7a9 (which is exactly the v2.4.1 annotated tag target), that `git am --3way` applies both commits cleanly, that the lib/zip/entry.rb hunk is byte-identical to upstream 17edfbf and the out_evil.zip fixture is the same blob as main, that the patched suite runs 326/3017 with 0 failures (pristine 325/3010/0), that reverting the one-line guard makes the new test fail, and via my own PoC that Entry#extract_v3 on pristine 2.4 really writes ../out_evil/breakout.txt into the sibling directory while the legacy extract() is blocked by name_safe?. version.rb is 2.4.2, the Changelog entry is present, rubocop 0.79 is clean and the gem builds. What is not fileable as-is is the surrounding paperwork: ADVISORY-DB-NOTE.md points at a spec file that does not exist in ruby-advisory-db and misdescribes what the specs check; the committed docs folder omits the evidence/ directory and uses scratchpad-relative paths its README and PR body cite; the README's Bundler version is wrong; and the cherry-picked commit lost its upstream author date and carries Claude co-author/session trailers that should be stripped before pushing to a maintainer's repo. Also expect maintainer pushback since the 2.4 README declares 2.4 the last 2.x release. Verdict: CORRECTED — file the rubyzip PR after amending trailers/date and fixing the advisory-note command and docs paths.

Claims not confirmed:

- [CORRECTED] Cherry-pick preserves upstream authorship metadata
  - Evidence: Author name/email preserved, but the author DATE was reset: upstream 17edfbf author date = 2026-06-12T17:47:54Z (gh api), patch commit 024f00a author date = 2026-09-06T10:54:09+03:00 (git log --format=%aI). A plain `git cherry-pick -x` keeps the author date; the commit was evidently re-created/amended with --reset-author or --date. Cosmetic, but a maintainer will notice.
- [REFUTED] ADVISORY-DB-NOTE.md: 'run bundle exec rspec spec/gems/rubyzip_spec.rb (the schema spec validates the YAML and that each patched version exists on rubygems.org)'
  - Evidence: gh api repos/rubysec/ruby-advisory-db/contents/spec/gems -> HTTP 404; spec/ contains advisories_spec.rb, advisory_example.rb, gem_advisory_example.rb, versions_example.rb, schema_validation_spec.rb, schemas/. versions_example.rb only checks operator syntax and Gem::Requirement parse; gem_advisory_example.rb only checks the highest patched range is open-ended ('>='/'>') plus Kwalify schema. Nothing queries rubygems.org. The repo README says 'bundle exec rspec' (line 235) / 'rake lint'. The proposed YAML itself (~> 2.4.2 before >= 3.4.0) does pass those checks.
- [REFUTED] README.md in the docs folder: 'evidence/ — every test/lint log quoted in the PR bodies ... in this folder' and RUBYZIP-PR.md paths 'upstream/rubyzip-2.4.2.patch', 'upstream/rubyzip/'
  - Evidence: git ls-files docs/security/upstream-rubyzip-cve-2026-85396 (commit 050051d6 on ux/api-error-handling-empty-states) lists only ADVISORY-DB-NOTE.md, FASTLANE-PR.md, README.md, RUBYZIP-PR.md, fastlane.patch, rubyzip-2.4.2.patch — no evidence/, no rubyzip/ clone. Those exist only in the ephemeral scratchpad upstream/ dir (evidence/ has 16 logs there).
- [CORRECTED] README.md: environment 'Ruby 3.3.12, Bundler 2.5.23'
  - Evidence: `bundle -v` under /opt/homebrew/opt/ruby@3.3 = 'Bundler version 4.0.16'; evidence/00-environment.txt line 3 also records 4.0.16. Ruby 3.3.12 is correct.

Findings:

- **major** — ADVISORY-DB-NOTE.md tells the owner to run `bundle exec rspec spec/gems/rubyzip_spec.rb` and says that spec checks the version exists on rubygems.org. Neither is true: spec/gems/ does not exist in rubysec/ruby-advisory-db (404) and no spec queries rubygems.org (versions_example.rb = operator/Gem::Requirement checks; gem_advisory_example.rb = 'last patched range must be open-ended' + Kwalify schema). The owner would hit 'No examples found'/file-not-found. Replace with `bundle exec rspec` (or `bundle exec rake lint`, which also runs yamllint). The 'send only after 2.4.2 is on rubygems' advice is still correct for policy reasons, just not because of the spec.
- **major** — The committed docs folder (050051d6) is self-inconsistent: README.md says `evidence/` (16 logs + 00-environment.txt + the advisory YAML) is 'in this folder' and the row for `rubyzip/`/`fastlane/` clones and worktrees points at siblings; RUBYZIP-PR.md's header table cites `upstream/rubyzip-2.4.2.patch`, `upstream/rubyzip/`. All of that lives only in the session scratchpad (/private/tmp/claude-501/.../scratchpad/upstream/), which is not durable. Either commit evidence/ (it is small text) or fix the README/PR paths; otherwise the owner filing from the branch has no logs to back the PR body's test table.
- **minor** — Cherry-pick faithfulness: author name/email preserved, but the author date of 024f00a is 2026-09-06 instead of upstream's 2026-06-12T17:47:54Z. `git cherry-pick -x` would have kept it; the commit was re-created. Cosmetic; `git am` on the maintainer side keeps whatever the patch says.
- **minor** — The cherry-picked commit (authored as Robert Haines) carries 'Co-Authored-By: Claude Fable 5.1' and 'Claude-Session:' trailers, and the PR body ends with the Claude Code footer + session link. README acknowledges this and says to strip if desired; recommend stripping before filing (an upstream maintainer's own commit with an AI co-author trailer and a private session URL is odd), e.g. `git commit --amend` on both commits and regenerate the patch with `git format-patch --binary -2`.
- **minor** — Acceptance risk, not a defect: the 2.4 branch README line 12 says 'Rubyzip 2.4 is intended to be the last release in the 2.x series. Please get ready for version 3.0.' The maintainer may decline a 2.4.2; the PR body's 'Why a 2.x release is needed' section (fastlane cap) is the right argument but the owner should expect pushback and treat the fastlane PR as primary (README already says so).
- **minor** — README.md environment line says Bundler 2.5.23; actual Bundler under ruby@3.3 is 4.0.16 (also what evidence/00-environment.txt records). Fix the README.
- **note** — 2.4 CI matrix runs Ruby 2.4–2.7 as well as 3.x (tests.yml); the fix uses only ::File::SEPARATOR and the test uses minitest assert_output/keyword args, all valid on Ruby 2.4, and rubocop 0.79 passes, so the older-Ruby legs should stay green. Not run here (only Ruby 3.3.12 available).
- **note** — Everything technical about the patch itself checks out: applies cleanly with git am --3way on a fresh shallow clone of 2.4 (head unchanged at 6c4b7a9 = v2.4.1), library hunk byte-identical to 17edfbf, fixture blob identical to main, 326/3017/0 on patched vs 325/3010/0 pristine, mutation check fails without the fix, PoC proves extract_v3 breakout on pristine 2.4 and the legacy extract() is blocked by name_safe?, version.rb 2.4.2 + Changelog entry, gem builds. No upstream 2.4 backport PR/issue exists yet and no 2.4.2 on rubygems, so the PR is not a duplicate.

Corrected wording:

- rubyzip/rubyzip — local branch backport/2.4.2-cve-2026-85396 (base branch 2.4 @ 6c4b7a9 = v2.4.1, NOT main; head unchanged as of 2026-09-06) → rubyzip-2.4.2.patch (applies with git am --3way; entry.rb hunk byte-identical to 17edfbf; 326/3017/0 tests, mutation check proven), for the owner to file. Fix before filing: ADVISORY-DB-NOTE.md's `bundle exec rspec spec/gems/rubyzip_spec.rb` is wrong (no such file; use `bundle exec rspec` / `rake lint`, and the specs do not check rubygems.org); the committed docs folder lacks the `evidence/` dir and `upstream/…` paths its README/PR body reference (they exist only in the session scratchpad); README's 'Bundler 2.5.23' is actually 4.0.16; cherry-pick author date was reset to 2026-09-06 (upstream 2026-06-12) and both commits carry Claude co-author/session trailers — strip/amend before pushing upstream.

### rahmah-gateway — CORRECTED

The repo pin is right: the clone at scratchpad/rahma/rahmah-gateway is master 1cc9596e…, identical to what GitHub reports, clean and untouched. The controller-level claims in row R4 hold up almost entirely — nickname/basics/liveIn/isOnboarded routes really are thin me-scoped writes into Data_SetSinglePreferenceAsync / Data_SetNestedPreferenceAsync with string values and Dictionary<string,string> groups, and register does seed isOnboarded="false" at the cited lines — but several citations are off ([Authorize] is per-action, NestedPreferenceInput lives at :1675 not in the cited DTO range, liveIn has an unmentioned matching side-effect that can 500 after persisting). Two mischaracterisations matter for the jeeb design: (1) R5's read-back is not the flat string RemoteUserPreferences DTO the plan cites — GET api/User/data returns RemoteUserPreferencesWithIds with id+value pairs resolved through extra nested-preference reads, so "typed read-back like Rahma" is a different contract than the plan implies; (2) R1's form_builder_template.json is not "a copy of the deployed file" — the form-builder-service FDS1 deploy workflow clones templates FROM the rahmah-gateway repo, making the gateway repo the template's source of record, which is a repo-of-record decision jeeb's plan should make explicitly rather than assume. The env value TEMPLATE_JSON_FILES=form_builder_template.json,partner_form_builder.json and the zero-admin-panel-usage claim are confirmed from form-builder-service@801ef01 and rahmah-admin-panel@873f4bb respectively. Notes and evidence: /private/tmp/claude-501/-Users-oudaykhaled-Desktop-olivium-jeeb/6a29e634-9ff5-4e5b-b358-a1a84368ab4f/scratchpad/review/rahmah-gateway/NOTES.md.

Claims not confirmed:

- [CORRECTED] R4: RemoteUserPreferencesController has [Route("api/[controller]")] and [Authorize], userId from token ClaimTypes.Sid -> sid -> sub (cited :17-46)
  - Evidence: Controllers/RemoteUserPreferencesController.cs:17-20 = [ApiController][Route("api/[controller]")][Produces("application/json")] — there is NO class-level [Authorize]; [Authorize] is applied per action (grep: 39 [Http*] routes, 39 [Authorize]). GetUserIdFromToken at :31-46 does ClaimTypes.Sid -> "sid" -> "sub" exactly as stated. Net effect (every route authorized, me-scoped) is right; the attribute placement is wrong.
- [CORRECTED] R4: POST preferences/liveIn is a thin single-preference write (cited :650-677)
  - Evidence: RemoteUserPreferencesController.cs:650-677: `Data_SetSinglePreferenceAsync(userId, "liveIn", value)` is followed by `_matchingClient.Upsert_location_api_v1_user_info__user_id__location_patchAsync(userId, int.Parse(value.Value))` — a second matching side-effect the plan attributes only to basics, and int.Parse on a non-numeric value throws -> 500 after the preference was already written.
- [CORRECTED] R4: values are STRINGS (PreferenceValue.Value), groups are Dictionary<string,string> (NestedPreferenceInput.Value); DTOs at Services/ServiceRemoteUserPreferences.cs:1727-1770
  - Evidence: ServiceRemoteUserPreferences.cs:1727 PreferenceInput{Key,Value:string}, :1749 PreferenceValue{Value:string} — inside the cited range. NestedPreferenceInput is at :1675-1687 with `IDictionary<string,string> Value` — OUTSIDE the cited 1727-1770 range. Types as described are right; citation should read :1675-1687,1727-1770.
- [CORRECTED] R5: read-back = Data_GetPreferencesAsync(userId) (all keys) + Data_GetAllItemsAsync(userId,"profile_photos") (cited Services/UserDataService.cs:940-1000)
  - Evidence: UserDataService.cs:940-1000 GetRemoteUserPreferencesWithIdsAsync does call Data_GetPreferencesAsync(userId) (:948) and Data_GetAllItemsAsync(userId, "profile_photos") (:949) — but basics, lifeStyle and socialMedia are NOT taken from the flat map: they are separate nested reads via Data_GetNestedPreferenceAsync at :1080 ("basics"), :1138 ("lifeStyle"), :1253 ("socialMedia"). Cite :940-1000,1080,1138,1253.
- [CORRECTED] R5: result is typed RemoteUserPreferences DTO (nickname, liveIn, basics{}, lifestyle{}) inside UserCompleteData.RemoteUserPrefs (cited DTOs/RemoteUserPreferences.cs:6-77, DTOs/UserCompleteData.cs:7-22)
  - Evidence: Both files exist as cited (RemoteUserPreferences.cs:6-77 has nickname/liveIn/basics/lifestyle; UserCompleteData.cs:13 `RemoteUserPreferences RemoteUserPrefs`). But GET api/User/data does NOT return them: UserDataService.cs:48-63 returns `UserCompleteDataResponse { RemoteUserPrefs = aggregate.RemoteUserPrefsWithIds }` whose type is `RemoteUserPreferencesWithIds` (DTOs/Response/UserCompleteDataResponse.cs:116+, basics/lifeStyle are `Dictionary<string, IdValue<string>>`, liveIn is `IdValue<string>`, JSON name `lifeStyle`). UserCompleteData/RemoteUserPreferences are the INTERNAL base shape used only for ProfileCompletionPercentage (UserDataService.cs:72-80,251-266).
- [CORRECTED] R1: rahmah-gateway/form_builder_template.json is 'a copy of the deployed file'
  - Evidence: No code in rahmah-gateway references the file (grep over *.cs/*.csproj/Dockerfile = 0 hits). form-builder-service@801ef01 contains NO form_builder_template.json (find = empty) — it was MOVED to the gateway (PR #175 'feat/move-json-files-from-formbuilder'). form-builder-service/.github/workflows/deploy-to-fds1.yml:37 default GATEWAY_REPO_URL=https://github.com/olivium-dev/rahmah-gateway, :203/:211 pass it as env — the deployed builder pulls templates FROM the gateway repo at deploy time. So the gateway file is the SOURCE OF RECORD for the deployed template, not a copy of it.

Findings:

- **major** — R5 mischaracterises the read-back wire shape. GET api/User/data returns UserCompleteDataResponse.RemoteUserPrefs of type RemoteUserPreferencesWithIds (DTOs/Response/UserCompleteDataResponse.cs:116+), where basics/lifeStyle are Dictionary<string, IdValue<string>> and liveIn is IdValue<string> — NOT the flat string-valued RemoteUserPreferences DTO the plan cites. If jeeb's typed read-back (GET /form-builder/templates/{t}/submission) is modelled on 'Rahma returns {componentID: string}', that premise is wrong: Rahma's read-back resolves ids to localized id+value pairs, which is a materially different contract.
- **major** — R1 inverts the provenance of form_builder_template.json: the gateway repo is where the deployed builder CLONES its templates from (form-builder-service deploy-to-fds1.yml GATEWAY_REPO_URL default = rahmah-gateway; the builder repo no longer holds the file). For jeeb this matters: the 'MIRROR Rahma' template registration in §1 R1 assumes the template lives in form-builder-service/pipeline env, whereas the Rahma pattern actually keeps the template DATA in the gateway repo and lets the builder pull it — a different repo-of-record decision the plan should make explicitly.
- **minor** — R5 omits that basics/lifeStyle/socialMedia are read via three extra Data_GetNestedPreferenceAsync round-trips (UserDataService.cs:1080,1138,1253), not from Data_GetPreferencesAsync — relevant to the 'read budget' G3 sizes in the plan if jeeb copies the store idiom.
- **minor** — R4 DTO citation (ServiceRemoteUserPreferences.cs:1727-1770) misses NestedPreferenceInput (:1675-1687), which is the one DTO the plan's whole 'basics idiom' depends on.
- **minor** — R4 says [Authorize] as if class-level; it is per-action on all 39 routes (RemoteUserPreferencesController.cs:17-20 has only ApiController/Route/Produces). Harmless today, but a future action added without the attribute would be anonymous — worth noting if jeeb copies the controller shape.
- **minor** — R4 under-reports side-effects: liveIn (:668) also upserts matching with int.Parse(value.Value) and will 500 on a non-numeric value AFTER the preference is persisted (non-atomic). Social login (:398) also seeds isOnboarded=false. POST preferences/isOnboarded takes no body (server hard-codes "true"), so 'the app POSTs … -> "true"' is server-decided, not client-sent.
- **note** — GET api/User/data (UserController.cs:857) reads only ClaimTypes.Sid with no sid/sub fallback, unlike RemoteUserPreferencesController.GetUserIdFromToken — the two 'me' resolutions in Rahma are inconsistent; jeeb should not copy either blindly.
- **note** — Line-range hygiene: :1385-1432 should be :1385-1443 (PostIsOnboarded ends at 1443); generic nested route claimed 'also open' lives at :421-511, outside every cited range.

Corrected wording:

- olivium-dev/rahmah-gateway — master @ 1cc9596 (matches origin; shallow clone, working tree clean), Rahma pattern reference (read-only). Plan P01 v3 §0 fixes: R4 — `[Authorize]` is per-action on all 39 routes, not class-level (`RemoteUserPreferencesController.cs:17-20` carries only ApiController/Route/Produces); DTO cite must be `Services/ServiceRemoteUserPreferences.cs:1675-1687 (NestedPreferenceInput, IDictionary<string,string>),1727-1770 (PreferenceInput, PreferenceValue: string)`; isOnboarded range `:1385-1443`; `preferences/liveIn` (`:650-677`) ALSO upserts matching via `int.Parse(value.Value)`; social login (`UserController.cs:398`) also seeds `isOnboarded="false"`; `POST preferences/isOnboarded` takes no body (server writes "true"). R5 — `GET api/User/data` (`UserController.cs:848-882`) returns `UserCompleteDataResponse.RemoteUserPrefs` of type `RemoteUserPreferencesWithIds` (`DTOs/Response/UserCompleteDataResponse.cs:116+`: basics/lifeStyle = `Dictionary<string, IdValue<string>>`, liveIn = `IdValue<string>`), built by `UserDataService.cs:48-63` → `:940-1000` (`Data_GetPreferencesAsync` + `Data_GetAllItemsAsync("profile_photos")`) PLUS `Data_GetNestedPreferenceAsync` for basics/lifeStyle/socialMedia at `:1080,1138,1253`; `DTOs/RemoteUserPreferences.cs` / `DTOs/UserCompleteData.cs` are the internal completion-percentage shape, not the wire DTO. R1 — `rahmah-gateway/form_builder_template.json` is the SOURCE the deployed builder clones at deploy time (form-builder-service `.github/workflows/deploy-to-fds1.yml:37,45,203` — `GATEWAY_REPO_URL=https://github.com/olivium-dev/rahmah-gateway`, `TEMPLATE_JSON_FILES=form_builder_template.json,partner_form_builder.json`), not 'a copy of the deployed file'; the builder repo no longer holds the file (moved by gateway PR #175). Shape claims (flat screen-N keys, componentID/componentName/attributes/%label::/output.type/validations) confirmed.

### rahmah-fe — CORRECTED

Fresh clone of olivium-dev/rahmah-fe confirms master is at 03890f1 (matches origin). The three things I was asked to confirm all hold in the code: the template is fetched with GET /formbuilder/templates/{male|female} which nginx routes straight to form-builder-service :10032 (not the C# gateway); per-screen submit is a switch on Rahma screen keys calling ~44 typed provider methods that hit the gateway's RemoteUserPreferences/Matching routes, with the builder's /forms/{screen} route only as the default fallback (in the deployed template exactly one screen, screen-8, falls through); and the renderer is unreusable because it bakes 49 Rahma endpoints, Rahma screen numbers and Rahma component ids into the code. One real mischaracterisation: R2 says the renderer dispatches on componentName, but componentName is never read anywhere — rendering, validation and storage all key on componentID ('text-input-1', 'group-1' ...), which undercuts the plan's claim that jeeb's 'componentName = UI kind' template shape mirrors Rahma's mobile. Smaller fixes: the screens_helper cite is ambiguous (two files) and points at callback wiring rather than validation parsing; the isOnboarded POST is an empty-body Skip-button call with the gateway writing "true", not the app; and F2's ofc line range covers half the endpoint table. Verdict CORRECTED, with replacement wording supplied.

Claims not confirmed:

- [REFUTED] R2: FormBuilderScreen walks the map and renders each component by `componentName` (form_builder_screen.dart:78-119)
  - Evidence: `grep -rn componentName lib libraries --include='*.dart'` (non-generated) → only the model field `screen_model.dart:21 @Default("") String componentName` in both copies; ZERO reads. Rendering dispatch is `_buildComponent` (form_builder_screen.dart:2068-2074 `final componentId = component.componentID;`) → `switch (componentId)` at :2099 with cases 'header-1','header-2','text-input-1','text-input-2','date-input-1','button-selection-1','image-picker-1','image-picker-2','selection-1','country-picker-1','switch-1','label-1','slider-1','review-page-1','group-1'. :78-120 is `_loadScreens` (calls getScreens and injects a synthetic 'review-screen'), it neither walks nor renders.
- [CORRECTED] R2: validates client-side from `validations` (screens_helper.dart:226-245)
  - Evidence: Client-side validation is real: form_builder_screen.dart:603-643 `_isCurrentScreenValid` iterates `component.validations` and `_validationState`; required checks at :478-481, :545-548, :563-566. But the cite is off: there are TWO screens_helper.dart files (libraries/fanus_form_builder/lib/presentation/screens/ and lib/features/user_setup/presentation/screens/); in the fanus one :226-245 is the `onSelectionChanged`/`onValidationChanged` callbacks of `buildItemSelectionComponent` — `validations` are actually read at :158-181 (min/max) and :357-386, :898-918. Validation also hardcodes Rahma ids ('group-1', 'image-picker-2') at :621-623.
- [CORRECTED] R4 cites: app GETs preferences/isOnboarded at login to skip the wizard and POSTs it when the wizard ends (api_constant.dart:153-154, login_service.dart:29, verification_service.dart:30)
  - Evidence: api_constant.dart:153-154 `checkIfUserOnboarded = '$gateway${normalPath}RemoteUserPreferences/preferences/isOnboarded'`; login_service.dart:29-30 `@GET(...) Future<UserOnboardedModel> checkIfUserOnboarded()`; login_bloc.dart:267-285 `userOnboarded: r.value.toLowerCase()=='true'`; login_screen.dart:126-139 → HomeLayout else `_navigateToFormBuilder`. verification_service.dart:30-31 `@POST(APIConstant.checkIfUserOnboarded) Future<void> skipVerify()` — NO @Body: the app sends an empty POST and the GATEWAY writes the literal "true" (rahmah-gateway RemoteUserPreferencesController.cs:1432). It fires only from VerificationScreen `_handleSkip` (:243-246), i.e. the user's Skip on the verification screen after the wizard, not automatically 'when the wizard ends'.
- [CORRECTED] F2: ~50 Rahma endpoints baked into FormBuilderProvider (ofc-form-builder/.../form_builder_provider.dart:7-48) and Rahma screen keys into the submit switch
  - Evidence: Count is right (49 `_gatewayApiPath/` endpoints, plus 2 builder defaults) but the line range is wrong: the endpoint table spans the constructor initializer :27-100 in BOTH twins (`_gatewayApiPath` const is :7; :7-48 covers only ~half). Submit switch keys 'screen-1'…'screen-20', 'review-screen' (form_builder_screen.dart:655-758) and render/validation keys 'text-input-1', 'group-1', 'image-picker-2' (:2099, :621-623) are all Rahma-specific.

Findings:

- **major** — R2 mischaracterises the Rahma renderer: it dispatches on `componentID` ('text-input-1', 'date-input-1', 'image-picker-2', ...), never on `componentName` (0 reads anywhere in lib/ or libraries/). This matters because §1 row 'R1 template SHAPE' and §2.2 build jeeb's template on 'componentName = UI kind, componentID = field id' and call it MIRROR Rahma — it is actually a deviation from what Rahma's mobile does; Rahma's componentID encodes kind+instance. The jeeb design may still be the right one, but the plan should stop citing Rahma's renderer as precedent for it.
- **minor** — screens_helper.dart is cited with no path while two files exist (fanus lib and lib/features/user_setup/, the latter only imported by stepper_screen.dart); the cited :226-245 in the fanus copy is selection-callback wiring, not the lines that read `validations` (:158-181, :357-386, :898-918). Client-side validation lives mainly in form_builder_screen.dart:603-643.
- **minor** — R4's 'app POSTs preferences/isOnboarded → "true" when the wizard ends' overstates the client: verification_service.dart:30 `skipVerify()` has no body, the gateway hardcodes "true" (RemoteUserPreferencesController.cs:1432), and it only fires from the verification screen's Skip button. If jeeb mirrors this as 'marker written by the client at wizard end' it should be the gateway's submit handler that sets the marker, which is what §1 R4 row proposes anyway — make the text match.
- **minor** — F2's ofc citation `:7-48` covers about half of the endpoint table; the table is :27-100 (same in fanus). Count '~50' is accurate (49 + 2 defaults).
- **note** — Useful concrete fact the plan omits: of the 10 screens in the deployed template only screen-8 (phone HorizontalGroup) falls through to the builder's `/forms/{screen}` route — so 'exception, not the rule' is 1/10, and that one exception stores a phone number in the builder's PG, an accidental second store. jeeb's single-route design avoids this.
- **note** — Quality of the pattern being copied: the fanus provider POSTs `'additionalProperties': {'aliqua207': {}}` on nickname (provider :396, a swagger-example leftover), and the screen is full of `print()` debug spam and hard-coded Rahma keys. Reinforces F2 — copy the ARCHITECTURE (template fetch → gateway writes to preferences), not the code.

Corrected wording:

- R2 (replace 'FormBuilderScreen walks the map, renders each component by `componentName`, validates client-side from `validations`'): `FormBuilderScreen` iterates the map (`_buildDynamicScreen` :1998-2015) and renders each component by its `componentID` — `_buildComponent` :2068-2074 switches on `component.componentID` (:2099: 'header-1', 'text-input-1', 'date-input-1', 'image-picker-2', 'group-1' …); `componentName` is parsed into the model (`screen_model.dart:21`) but never read. Per-screen gating is client-side from `validations` (`form_builder_screen.dart:603-643` `_isCurrentScreenValid`, required checks :478-481/:545-548/:563-566; min/max read in `libraries/fanus_form_builder/lib/presentation/screens/screens_helper.dart:158-181`).
- R2 cite list: replace `screens_helper.dart:226-245` with `libraries/fanus_form_builder/lib/presentation/screens/screens_helper.dart:158-181,357-386` and add `form_builder_screen.dart:603-643,2068-2099`.
- R3 (append): Of the 10 screens in the deployed template (screen-2,3,4,20,7,8,9,10,13,19) only `screen-8` (phone HorizontalGroup) has no typed handler and hits `formbuilder/forms/screen-8`. Typed routes are `<baseUrl>gateway/api/…` (`_gatewayApiPath = 'gateway/api'`, provider :7) → nginx `/gateway/` → :10000.
- R4 cites (replace 'the app POSTs `preferences/isOnboarded` → `"true"` when the wizard ends'): the app POSTs `preferences/isOnboarded` with an EMPTY body from the verification screen's Skip (`verification_service.dart:30-31`, `verification_screen.dart:243-246`); the gateway itself writes the literal `"true"` (`RemoteUserPreferencesController.cs:1432`). Login GETs it and routes Home vs form builder (`login_service.dart:29`, `login_bloc.dart:267-285`, `login_screen.dart:126-139`).
- F2 cite: replace `ofc-form-builder/lib/data/provider/form_builder_provider.dart:7-48` with `:7,27-100` (49 gateway endpoints; identical table in `rahmah-fe/libraries/fanus_form_builder/lib/data/provider/form_builder_provider.dart:27-100`).

### ofc-form-builder — CORRECTED

The repo, head SHA, version, private pub host and twin relationship all check out: main is 7c2bc3b on GitHub and locally, pub.fds-4.site serves 1.0.5 as latest, and after the package rename the provider and the 3,341-line screen are byte-identical to rahmah-fe's fanus_form_builder (39 of 57 files identical). Two things need correcting. First, the endpoint cite is wrong: there are 49 Rahma endpoints and they live at lines 27-100 of the provider, not 7-48. Second, and more important, the plan's reason for calling the renderer non-reusable does not hold — the wrapper accepts any provider, the concrete provider takes baseUrl/getScreensEndpoint/submitScreenEndpoint, and non-Rahma screen keys bypass all 49 endpoints via the generic submit path. The real blocker the plan missed is that the renderer picks widgets by componentID from a closed set of 15 Rahma ids and renders nothing otherwise, which directly conflicts with the plan's own decision that componentID is the field id; it also force-injects a Rahma-branded review screen. So the conclusion (write a small jeeb renderer) stands, but F2 should be reworded with the correct reason. Also note that an unmerged branch (1.0.6-beta.1) was the last thing actually published to the host.

Claims not confirmed:

- [CORRECTED] main @ 7c2bc3b is the latest work in the repo
  - Evidence: `gh api repos/olivium-dev/ofc-form-builder/branches` -> `PROF-592-salehly-widgets 216f36f` besides main; `gh api .../compare/main...PROF-592-salehly-widgets` -> ahead=5 behind=0, files: deploy.yml, lib/fanus_form_builder.dart, app_localizations.dart, +bordered_label_textfield.dart, +primary_location_widget.dart, +searchable_selector.dart, pubspec.yaml; branch pubspec `version: 1.0.6-beta.1`; `gh run list` -> last successful 'Deploy to Private Pub Server' is run 22036632487 on 216f36f (2026-02-15T13:38Z), after main's run on 7c2bc3b (11:04Z).
- [CORRECTED] lib/data/provider/form_builder_provider.dart:7-48 bakes ~50 Rahma endpoints in
  - Evidence: `grep -c '_gatewayApiPath/'` -> 49; `grep -o "_gatewayApiPath/[A-Za-z_/.-]*" | sort -u | wc -l` -> 49 distinct literals (identical 49 in fanus). They span lines 27-100 (first at :27 `_fetchImagesEndpoint = '$_gatewayApiPath/RemoteUserPreferences/data/profile_photos/all'`, last at :100 `_partnerSectsEndpoint`), inside the constructor initializer :9-100; line 7 is only `static const String _gatewayApiPath = 'gateway/api';`. The cited range :7-48 covers just 17 of the 49. Correct cite: `form_builder_provider.dart:7,27-100` (49 endpoints under RemoteUserPreferences/*, Matching/*/upsert, User/profile/*, OTP/*). 52 `_…Endpoint` fields declared.
- [CORRECTED] Rahma's renderer is not reusable by jeeb; jeeb must write its own renderer (F2 conclusion)
  - Evidence: A configuration path EXISTS and the plan does not mention it: `FormBuilderWrapper(provider: FormBuilderProviderAbstract, …)` (form_builder_wrapper.dart:11-20; abstract has 54 methods) and the concrete `FormBuilderProvider(dio, baseUrl, getScreensEndpoint, submitScreenEndpoint, imageUploadEndpoint, imageDeleteEndpoint, userToken, languageCode)` (:9-18) — exactly how Rahma wires it (rahmah-fe gender_selection_screen.dart:270-282). With screen keys outside 'screen-1..20'/'review-screen', every submit falls to the generic `submitScreen` and the 49 Rahma endpoints are dead constants — so the endpoint table is NOT what blocks reuse. What DOES block it: (1) the render switch (:2099-2422) keys on `component.componentID` (:130,:159,:188) against a closed vocabulary {header-1, header-2, text-input-1, text-input-2, date-input-1, button-selection-1, image-picker-1, image-picker-2, selection-1, country-picker-1, switch-1, label-1, slider-1, review-page-1, group-1}, `default: return Container()` (:2421-2422); Rahma's real template (`rahmah-gateway/form_builder_template.json`, jq) uses exactly those as componentID, while plan §2.2 (lines 74-84) makes componentID a field id ('country','street') -> this renderer would render nothing; text-input values are stored under '$componentId-$index' (:2107-2109), so field identity is lost in the generic submit body. (2) `_loadScreens` :78-106 unconditionally appends a Rahma-branded 'review-screen' ('Jazakallah. We're reviewing your profile.' + review_form.png) whenever `!isPartnerPreferencesForm`; flipping the flag suppresses it but routes to the partner switch which never submits unknown keys (:683-685). (3) screen-key-specific storage rules for screen-5/6/8/11/12 (:204-222), Urbanist font + Rahma l10n bundled. So the F2 conclusion (write own renderer) stands, but for a different reason than the plan states.
- [REFUTED] jeeb currently depends on / references ofc_form_builder
  - Evidence: grep over the worktree pubspec.yaml + lib for ofc_form_builder|fanus_form_builder -> no hits; pub.fds-4.site appears only in pubspec.yaml:16,23 comments about omds and ofl_geo_capture.

Findings:

- **major** — F2's stated reason is wrong even though its conclusion survives. The 49 baked endpoints (form_builder_provider.dart:27-100) are only reached via the screen-key switch for Rahma keys 'screen-1..20'; any other key falls to the generic `submitScreen` on the configurable `submitScreenEndpoint`, so a jeeb `FormBuilderProvider(baseUrl:…, getScreensEndpoint:'/form-builder/templates/jeeb_jeeber_onboarding_v1', submitScreenEndpoint:'/form-builder/templates/{screen}/submit', userToken:…)` would run without touching a Rahma route. The actual non-reusability is the render vocabulary: `componentID` is the widget-type selector (closed set of 15 ids, `default: return Container()` at :2421), which directly contradicts plan §2.2 where `componentID` is the field id the gateway validates against. The plan should say this, otherwise a reviewer who reads the constructor signature will (correctly) object that the endpoint argument is hollow.
- **major** — The bullet's '@ 7c2bc3b' is main, but it is NOT the latest published state: branch PROF-592-salehly-widgets @ 216f36f (5 ahead, version 1.0.6-beta.1, +3 widgets, edited barrel/deploy.yml/pubspec) was the last thing pushed to pub.fds-4.site (run 22036632487, 2026-02-15 13:38Z). Anyone resolving `ofc_form_builder: ^1.0.5` gets 1.0.5, but `any`/pre-release constraints or the CHANGELOG would mislead. Cite the branch if the plan claims to describe the published package.
- **minor** — Cite fix: `form_builder_provider.dart:7-48` -> `:7,27-100` (49 endpoints, not ~50); the current range stops at `_liveInEndpoint` and misses 32 of them including all `/OTP/*`, `/User/profile/*` and every `partner-*` route.
- **minor** — Twin claim is solid but should be quantified: after the package rename 39/57 files are byte-identical (provider and the 3,341-line screen included); 17 text files diverge, only phone_number_component.dart materially (400 diff lines). fanus is at 0.0.2 with method-channel/platform-interface plugin scaffolding that ofc dropped.
- **minor** — Package hygiene that matters if anyone ever proposes depending on it: main is a single squashed commit; barrel is still `lib/fanus_form_builder.dart`; pubspec has no `publish_to:` (only `--server` keeps it off pub.dev); publish.yml hardcodes a LAN unpub `http://192.168.2.94:4000`; test.yml swallows failures (`flutter analyze || echo …`, `flutter test --coverage || echo …`); the only test is a 15-line export smoke test; CHANGELOG 1.0.1 claims `flutter_form_builder ^10.2.0` while pubspec pins `^9.7.0`; CI Flutter 3.27.1 vs jeeb's pinned 3.44.2.
- **note** — Even ignoring rendering, adopting the package would pull Rahma UI baggage into jeeb: an unconditionally injected 'Jazakallah…' review screen (form_builder_screen.dart:78-106), Urbanist font asset, Rahma ar/en l10n, `image_cropper`/`intl_phone_field`/`country_flags`/`retrofit`/`injectable` transitive deps, and a lowercase `bearer` Authorization header (:102). None of that is configurable.

Corrected wording:

- olivium-dev/ofc-form-builder — main @ 7c2bc3b (single squashed commit; published twin of rahmah-fe libraries/fanus_form_builder: provider and the 3,341-line screen byte-identical after the package rename, 39/57 files identical, 17 diverge). 1.0.5 is the latest stable on private pub.fds-4.site; an unmerged branch PROF-592-salehly-widgets @ 216f36f (1.0.6-beta.1, +3 widgets) was published after it.
- F2 replacement: `fanus_form_builder` (3,341-line `form_builder_screen.dart`) and its published twin `ofc_form_builder` 1.0.5 (private pub `pub.fds-4.site`) hard-code 49 Rahma gateway endpoints in `FormBuilderProvider` (`ofc-form-builder/lib/data/provider/form_builder_provider.dart:7,27-100`) behind a submit switch keyed on Rahma screen keys `screen-1..20` (`form_builder_screen.dart:689-758`); unknown keys DO fall to the configurable generic `submitScreen`, so endpoints alone would not block reuse. What blocks it is the renderer: it selects widgets by `componentID` from a closed set of 15 Rahma ids (`header-1`, `text-input-1`, `date-input-1`, …; `default: return Container()` at `:2421`) and stores text values as `'$componentId-$index'`, which is incompatible with §2.2 where `componentID` is the field id the gateway validates; it also injects a Rahma-branded review screen (`:78-106`). jeeb must write its own (small) renderer (§1 row M-R2).

### rahmah-admin-panel — CONFIRMED

The bullet is accurate: olivium-dev/rahmah-admin-panel's main head is 873f4bb on GitHub and in the scratchpad clone, and it is the only commit lineage (one merged PR, May 2025). The v3 plan's R5 claim holds — a whole-repo grep for RemoteUserPreferences, nested-preferences, User/data, preference(s) and form-builder returns zero hits; every 'FormBuilder' hit is Angular's reactive-forms class. The panel's complete backend surface is chat packages, catalog items, pricing SKUs, CDN uploads and admin login; the only user read is the signed-in admin's own profile via GET /api/User/profile/{tokenUserId}, which the gateway serves from the user-management client (username/email/profilePic/DOB), not from preferences. The important consequence for the jeeb CMS follow-up: Rahma has no admin view of onboarding answers AND no admin-scoped preference endpoint on its gateway (GET api/User/data is me-scoped), so a jeeb CMS view is new scope requiring a new admin-authorised gateway read, not a mirror. Two small plan-text nits: the rahmah-gateway default branch is `master` (SHA 1cc9596 is correct), and the admin panel's channel dialogs / menu.json are dead starter code that must not be mistaken for a users/admins feature.


Findings:

- **major** — Implication for the jeeb CMS follow-up: Rahma provides NO precedent to copy for an admin view of onboarding answers. The admin panel is a chat-packages/pricing/CDN portal only (routes: packages, package/create, package/:id/edit) and never touches end users. Rahma's only preference read-back, `GET api/User/data` (rahmah-gateway UserController.cs:848), is me-scoped (userId from the token) — there is no admin-scoped read of another user's preferences anywhere in the Rahma stack. So a jeeb CMS 'view onboarding answers' feature is net-new work needing a NEW admin-authorised gateway endpoint (e.g. admin/users/{id}/preferences → Data_GetPreferencesAsync(targetUserId)), not a mirror; the plan should call it out as new scope rather than 'copy Rahma'.
- **minor** — Plan header (PLAN-P01 v3 line 15) says rahmah-gateway clone is 'HEAD of origin/main (1cc9596)'. The rahmah-gateway GitHub default branch is `master`, not `main` (gh api repos/olivium-dev/rahmah-gateway --jq .default_branch -> master; branches/main -> 404). The SHA 1cc9596 IS the master head, so the evidence is right; only the branch name is wrong. Fix wording to 'origin/master (1cc9596)'.
- **minor** — Dead/uncompilable code in the admin panel: src/app/dialogs/channel/{create,edit,delete} import `@services/channel.service` and `app/models/channel.model`, neither of which exists in the repo (grep 'class ChannelService' -> none; src/app/services = general, package, pricing only). They only escape the build because tsconfig.app.json uses `files: [src/main.ts]` and nothing references them. public/data/menu.json (admins/channels/audio) is likewise stale — the live menu is hardcoded in login.service.ts menu() = dashboard + packages. Do not read menu.json or the channel dialogs as evidence of an 'admins' or 'users' feature.
- **note** — Repo is dormant: single PR (#1, merged 2025-05-27), no commits since, internal visibility, README is the stock ng-matero template. Prod baseUrl = https://al-rahmah.com/gateway (environment.prod.ts:3); deploy is a manual workflow_dispatch to an EC2 host on port 10034. Treat it as a thin packages portal, not a CMS.
- **note** — Minor hygiene in the starter leftover: src/app/core/authentication/user.ts hardcodes an `admin` User const with the owner's name/email (typo 'ouday.kahled@gmail.com'); harmless (fallback constant) but shows the auth layer is barely customised from ng-matero.

### jeeb-gateway — CORRECTED

The inventory bullet itself is accurate: origin/main is 6679f6e, epic/wallet-guard-fix is at dfa9159d, 5 ahead / 97 behind, with no PR ever opened, and PLAN-P15 is about landing it. Of the four PLAN-P15 findings I was asked to check, F1 and F3 are confirmed at the exact files and lines (zero non-comment Npgsql in src/ on both trees; CurrencyId 1→2 in PartnerWalletOptions.cs:41 and CommissionCollector.cs:31, plus a validator guard at BffStartupValidator.cs:132-156 that throws whenever RequiredInProduction is true and the environment is not Development/Testing). F2 is a tautology as written, since the gateway repo contains no wallet-service source; the meaningful version (the epic needs no wallet-service change because abort/execute/validate/by-external-reference already exist on wallet-service master and the MSI build) is confirmed. F7 is only half right: the script exists on main with no callers, but it is absent on the epic and main already has an enforced route-retention test, so it matters only for the wallet slice — whose pinned copy on the epic is an empty placeholder, which is the most important replay risk I found. MSI env-level facts could not be verified from this session because SSH requires a password.

Claims not confirmed:

- [UNVERIFIABLE] F1 sub-claim: MSI gateway.env has the WalletPostgres DSN commented out and WalletLedgerMigration__Authority='wallet-api'
  - Evidence: ssh msi-ec2-cloudflare: 'sudo: a password is required' (ec2-user has no NOPASSWD sudo); ssh msi-cloudflare: 'Permission denied (password)' in BatchMode. Only indirect evidence: GET https://msi.olivium.space/gateway/health/ready -> status Degraded, entry wallet-service=Healthy (HTTP wallet check present).
- [CORRECTED] F2: the epic changes zero lines of wallet-service
  - Evidence: Literally true but tautological: the jeeb-gateway repo contains no wallet-service source at all (find main epic -iname '*wallet-service*' -> only the pinned openapi slice), so a jeeb-gateway branch cannot change wallet-service; 'git diff --stat ... (no wallet-service path)' proves nothing. The 90 changed files = docs 2, src 36, tests 42, tools 10 (HoldsAbort 3, WalletCurrencyMigration 4, WalletHolderBackfill 3) — matches 'three tools/* programs'. The MEANINGFUL claim — the epic needs NO wallet-service change — is CONFIRMED in substance: epic src/JeebGateway/Financials/WalletCommissionDebitClient.cs:197 POST Transaction/initiate, :220/:229/:258 GET Transaction/by-external-reference/{ref}, :295 POST Transaction/{id}/execute, :302 POST Transaction/{id}/abort; wallet-service master 1fa46a83 (2026-08-30) and MSI build 73f2cfe4 (2026-08-19) Controllers/TransactionController.cs both expose [HttpPost("validate")]:35, initiate:52, by-external-reference:118, {id}/abort:182, {id}/execute:197 (fetched via gh api contents).
- [CORRECTED] F7: scripts/check-openapi-path-method-compatibility.sh exists and nothing calls it
  - Evidence: main/scripts/check-openapi-path-method-compatibility.sh exists (786 B, jq path/method diff, exit 1 on removed) and grep -rn 'check-openapi-path-method-compatibility|path-method' across main incl .github -> zero callers: CONFIRMED for main. BUT (a) the script is ABSENT on the epic (ls: No such file) — it was added in the 97 commits the epic lacks; (b) 'nothing gates removed routes' is false for the gateway artifact: main tests/JeebGateway.IntegrationTests/OpenApiCompatibilityContractTests.cs GatewayArtifactRetainsEveryReviewedBasePathAndMethod already asserts baseline.Except(candidate) is empty against tests/JeebGateway.IntegrationTests/Contracts/jeeb-gateway.path-methods.baseline.txt (400 entries, 17 wallet routes) and artifacts/openapi/jeeb-gateway.v1.json, and ci.yml is green on 6679f6ee. The unguarded gap is only the wallet-service slice.

Findings:

- **major** — Epic's pinned wallet slice is a 465-byte PLACEHOLDER (epic src/JeebGateway/contracts/wallet-service.openapi.json: title 'wallet-service (placeholder)', paths {}), while main's is a real 28-path slice (x-source-commit 5fa9f7b, incl. /Transaction/{transactionHeaderId}/abort and /execute). Replaying the epic onto main must not carry the placeholder back, and the epic's hand-written HTTP in Financials/WalletCommissionDebitClient.cs:197-304 should be routed through the generated ServiceWalletClient (PLAN-P15 F6 already says this; the placeholder fact strengthens it).
- **major** — F3's 'throws in Production' is really 'throws in every environment that is not Development/Testing while DownstreamServicesOptions.RequiredInProduction stays at its default true' (epic BffStartupValidator.cs:60-66, DownstreamServicesOptions.cs:29). Whether staging boots after a partial pin depends on its ASPNETCORE_ENVIRONMENT, which I could not read. PLAN §4's 'degrade /health/ready first, throw later' remains the right fix, but the gate condition should be stated precisely.
- **minor** — F2 as worded ('epic changes zero lines of wallet-service') is a tautology for a jeeb-gateway branch and should be rephrased as 'the epic requires no wallet-service change: every endpoint it calls (validate/initiate/by-external-reference/abort/execute) already exists on wallet-service master 1fa46a8 and the MSI build 73f2cfe'. That is the claim that actually supports the sibling-products argument.
- **minor** — F7 undersells the current state: main already has an enforced route-retention gate (OpenApiCompatibilityContractTests.GatewayArtifactRetainsEveryReviewedBasePathAndMethod, 400-entry baseline, green in ci.yml at 6679f6e). Wiring the shell script (G3) only adds value for the wallet-service slice, and the script does not exist on the epic — the replay must take it from main.
- **minor** — F1's 'zero Npgsql' is true for src/ only. tools/CmsBundlerSnapshotExporter (both trees) and the epic's tools/WalletHolderBackfill (Npgsql 8.0.5, raw read of user-management per its csproj comment, --um-connection) still carry raw DSNs; the ADR text 'no other service, tool or job opens a wallet database connection' is satisfied today but the tools line needs the F6 --um-connection removal to be fully consistent.
- **note** — MSI env facts in F1/F3/F5 (DSN commented out, PartnerWallet__CurrencyId=2, Authority=wallet-api) are UNVERIFIABLE from this reviewer's session (both SSH aliases need a password in batch mode); only indirect corroboration: public /health/ready = Degraded overall, wallet-service=Healthy.
- **note** — Plan's appsettings hunk omits that appsettings.Production.json is also flipped on the epic (:191 CurrencyId 2, :192 CurrencyCode USD, :210 PartnerWallet CurrencyId 2) — relevant because Production json overrides appsettings.json, so the 'repo default' the plan wants to keep at 1 must be held in BOTH files during the replay.

Corrected wording:

- olivium-dev/jeeb-gateway — origin/main 6679f6e (ci.yml green 2026-09-04); branch epic/wallet-guard-fix (head dfa9159d, 5 ahead / 97 behind, no PR ever opened, never merged; subject of PLAN-P15). PLAN-P15 §0 as re-reviewed: F1 holds for src/ only (zero non-comment Npgsql on main and epic; tools/ still carry Npgsql; MSI env unverified); F2 should read 'the epic requires no wallet-service change — every endpoint it calls exists on wallet-service master 1fa46a8 and MSI build 73f2cfe' (the gateway repo has no wallet-service source, so 'changes zero lines' is a tautology); F3 confirmed at PartnerWalletOptions.cs:41, CommissionCollector.cs:31/35, appsettings.json + appsettings.Production.json, BffStartupValidator.cs:132-156 (throws in any non-Dev/Testing env while RequiredInProduction=true, default true); F7: script exists on main only (absent on the epic), has no callers, but main's OpenApiCompatibilityContractTests already gates removed gateway routes — the script's value is limited to the wallet slice, whose pinned copy on the epic is an empty placeholder.

### form-builder-service — CORRECTED

The bullet's core facts hold: origin/main is 801ef01, and MSI :10070 runs exactly that SHA as a native systemd service serving a single flat template jeeb_jeeber_v1 (11 components, 11 required). The plan's F1 analysis is correct and I reproduced it with a two-file simulation on 801ef01: the second file gets POST /forms/* routes and tables but GET /templates/{name} and /schema 404, and the partner route is the only way to read it. Three things need correcting. First, the live MSI template file generated_jeeb_jeeber_v1.json is not in git anywhere, and in the release dir it is a root-owned symlink into /home/ec2-user/iter5-services/form-builder-service/ — D-FB4 must copy from or re-link to that source, not the release dir. Second, the plan's gate step is already broken: scripts/check-generic-vocabulary.sh fails on clean 801ef01 (3 hits inside the new audit script) and the gate workflow is disabled, and no workflow runs pytest, so the promised unit test is not CI-enforced. Third, 'non-breaking for Rahma / byte-identical' is overstated: the lookup chain is the right design (primary wins on colliding screen keys), but partner-only names flip 404→200, the fallback must read the generated file and localize per file, and Rahma production actually runs Feb-2026/Sep-2025 SHAs with its deploy workflows disabled, so D-FB2 only reaches Rahma through an owner-gated redeploy that ships months of loader drift. Several line references in the plan are stale (staging add_env is :197, vocab exclude is :26).

Claims not confirmed:

- [CORRECTED] GET /templates/{name} serves ONLY the primary (first) file (app/template_endpoints.py:20-21,36-42)
  - Evidence: True in substance; line ref slightly off. template_endpoints.py:20-21 `_PRIMARY_TEMPLATE = get_primary_template_file(); TEMPLATES_FILE = generated_<primary>`; the load is at :44 (`templates_data = normalize_template_document(load_config(TEMPLATES_FILE))`, block :36-44), and `get_template_by_name` :173-184 / `get_template_schema` :202-210 only look in templates_data / get_all_templates (:58-105, localizes the primary only). Proven by simulation (sim/sim.py, TEMPLATE_JSON_FILES=a.json,b.json): GET /templates keys=['screen-2','only_a']; GET /templates/only_b -> 404; GET /templates/only_b/schema -> 404.
- [CORRECTED] POST /forms/* and the PG tables are built from ALL files (app/endpoints.py:19-27, app/main.py:96-112)
  - Evidence: True; line refs partly off. endpoints.py:19-30 loops `get_template_files()` and merges (raise if empty). main.py:32-40 builds TEMPLATES_FILES from ALL base files (+ optional flavors) and the all-files load loop is :106-123 (not :96-112; :96 is `ensure_templates_processed()`); endpoints registered :154-172. database.py:110-135 `initialize_db` also loops all files. Simulation: app.routes contains /forms/only_a, /forms/only_b, /forms/screen-2 (+ /{form_id}); POST /forms/only_b -> 422 (route live), while GET /templates/only_b -> 404.
- [CORRECTED] flavors/form_builder_template/{male,female}.json are Rahma's flavors (app/main.py:35-39)
  - Evidence: Rahma lineage confirmed: added by Ahmad Younes 2025-10-20 (52b9859 'add flavor feature, and remove gender and zodiac', fdba95d), directory named after Rahma's primary form_builder_template.json, and main.py:35-39 appends flavors/<primary_stem>/{male,female}.json. BUT both files on main are literally `{}` (cat = '{}', 1 line each) and Rahma's form_builder_template.json/partner_form_builder.json were DELETED from this repo on 2026-02-13 (628003e 'delete formbuilder json files'; now fetched at deploy time from GATEWAY_REPO_URL=rahmah-gateway by deploy-to-fds1.yml). On jeeb deployments the flavor path is inert (primary stem is generated_jeeb_jeeber_v1 / jeeb_kyc_form_builder).
- [CORRECTED] generated_jeeb_jeeber_v1.json is NOT in git — copy it forward from …-801ef01-20260828/
  - Evidence: Not tracked: `git ls-files | grep generated` empty in the clone and in the MSI release dir; `.gitignore:57 generated_*.json` (added by #29 b34bf09); `git log --all -- generated_jeeb_jeeber_v1.json` = 0 commits; `gh api search/code q='generated_jeeb_jeeber_v1 org:olivium-dev'` = 0 hits. CORRECTION: in the release dir the file is a ROOT-OWNED SYMLINK (`lrwxrwxrwx root root 2026-08-28 19:35 generated_jeeb_jeeber_v1.json -> /home/ec2-user/iter5-services/form-builder-service/generated_jeeb_jeeber_v1.json`); the real file is that iter5-services copy (4734 B, mtime 2026-06-02 14:18, keys ['jeeb_jeeber_v1'] x11). D-FB4 must copy from / re-symlink to the iter5-services source, not 'copy the release dir file' (a `cp -P`/rsync -a of the symlink is fine only while iter5-services stays).
- [CORRECTED] staging swarm registers add_env TEMPLATE_JSON_FILES jeeb_kyc_form_builder.json at jeeb-staging-deploy.yml:185-192 / D-FB3 edits :188
  - Evidence: The form-builder-service case block is at .github/workflows/jeeb-staging-deploy.yml:193-200 and the `add_env TEMPLATE_JSON_FILES jeeb_kyc_form_builder.json` line is :197 (:185-192 is the contract-signing-service block). Last staging runs: 801ef01 2026-08-19 success x2. Also `deploy-to-jeeb.yml:42` default template_json_files=jeeb_kyc_form_builder.json (workflow disabled_manually; last run 2026-06-14 61ada49).
- [CORRECTED] scripts/check-generic-vocabulary.sh:12-13 excludes ONLY jeeb_kyc_form_builder.json (D-FB1)
  - Evidence: The exclude is at scripts/check-generic-vocabulary.sh:24-26 (`':(exclude)jeeb_kyc_form_builder.json') # vocab-allow: …`), not :12-13 (:12-13 are ALLOW_STRIP). ESCAPE_BUDGET 'scripts/check-generic-vocabulary.sh=2' at :17-21 confirmed. ADDITIONALLY the gate is already RED on 801ef01: `bash scripts/check-generic-vocabulary.sh` → 3 findings in scripts/audit-fail-closed-deployments.py:9,11,13 (workflow filenames containing 'jeeb'/'rahmah', introduced by #39 40d5fe4), rc=1; the Vocabulary Gate workflow is `disabled_manually` (gh api actions/workflows) and last ran 2026-08-12 (33cedcd), so nothing caught it.
- [CORRECTED] D-FB2 fallback-chain (primary first, then other files; get_all_templates and flavors untouched) is non-breaking for Rahma / output byte-identical
  - Evidence: Mechanically sound: names present in the primary keep resolving from the primary (sim: 'screen-2' → a-name), so colliding Rahma screen keys are not clobbered, and a lookup chain (not a merge) is the right shape; the only observable change for Rahma is that names existing ONLY in partner_form_builder.json flip from 404 to 200 on GET /templates/{name} and /schema — 'byte-identical' holds for every request that succeeds today, not for those 404s. Caveats the plan omits: (1) the fallback must read generated_<file> (endpoint-resolved), not the raw file, and its non-en branch must localize per file (existing partner path uses a fixed temp_partner_form_builder.json name — pre-existing race); (2) Rahma production is NOT on 801ef01 at all — deploy-to-fds1.yml last ran f1e9d9f (2026-02-14) and deploy-to-ec2-rahmah.yml 1b6b4c8 (2025-09-10), both workflows now disabled_manually, and app/ has drifted +209/-12 lines (template_normalizer, loader) since; D-FB2 cannot reach Rahma without an owner-gated redeploy that would ship far more than D-FB2; (3) no CI runs pytest (only fail-closed-audit + the disabled vocab gate), so the promised unit test is not gated.

Findings:

- **major** — Vocabulary gate (scripts/check-generic-vocabulary.sh, the plan's FB5 gate) is already RED on origin/main 801ef01: 3 findings in scripts/audit-fail-closed-deployments.py:9,11,13 (workflow filenames 'deploy-to-ec2-rahmah', 'deploy-to-jeeb', 'jeeb-staging-deploy' introduced by #39). It went unnoticed because the Vocabulary Gate workflow is disabled_manually (last run 2026-08-12, before #39-#42). Any branch off 801ef01 fails FB5 before the plan's own changes; the plan must budget a fix (escape-budget entry for the audit script or renaming) or the gate step is dead.
- **major** — D-FB4 provenance is wrong in a way that can bite a deploy: in the release dir generated_jeeb_jeeber_v1.json is a root-owned symlink to /home/ec2-user/iter5-services/form-builder-service/generated_jeeb_jeeber_v1.json (real file, 2026-06-02). The plan says 'copy it forward from …-801ef01-20260828/'. The file exists nowhere in git or GitHub code search; the iter5-services copy is the single source of truth for the live jeeb_jeeber_v1 template. It should be committed somewhere owned (jeeb-gateway product/form-builder already carries the source flavors jeeb_jeeber_v1/{national_id,passport,residency}.json but no generator output) before more env changes pile on it.
- **major** — 'Non-breaking for Rahma' is argued against a codebase Rahma does not run. Rahma prod deploys are f1e9d9f (FDS1, 2026-02-14) and 1b6b4c8 (ec2-rahmah, 2025-09-10); all Rahma/FDS1/Salhley deploy workflows are disabled_manually. app/ on main has +209/-12 lines of loader changes (template_normalizer.py, database/endpoints/main/template_endpoints) since f1e9d9f. D-FB2 itself is small and lookup-only, but the claim 'Rahma output stays byte-identical' should be restated as: no change to any request that succeeds today; partner-only names flip 404→200; and Rahma is only affected if/when the owner redeploys from main, which ships the whole Feb→Aug drift.
- **minor** — No CI test runner exists in the repo (workflows: fail-closed-deployment-audit active, jeeb-staging-deploy active, everything else disabled; none runs pytest). The plan's 'unit test in tests/ with two temp files' will run only on a developer's machine. Existing tests/test_step_based_loader.py (9 tests) do pass on 801ef01 with a local venv (Python 3.11; the pinned requirements do not build on Python 3.14).
- **minor** — D-FB2 implementation detail the plan does not state: the fallback must load generated_<file> (endpoint-resolved by ensure_templates_processed, which already writes one per file) rather than the raw file, and its non-en branch needs a per-file temp name — the existing partner path hardcodes temp_partner_form_builder.json, a latent concurrent-request race that a generic fallback should not copy.
- **minor** — Line references in the plan are stale: jeeb-staging-deploy.yml add_env is :197 (plan :188 / :185-192); vocab exclude is scripts/check-generic-vocabulary.sh:26 (plan :12-13); the all-files template load in app/main.py is :106-123 (plan :96-112); the primary load in template_endpoints.py is :44 (plan :36-42). iter5_native_run.sh:103 and config.py:14-23 and main.py:212-216 / :35-39 are correct.
- **note** — flavors/form_builder_template/{male,female}.json are `{}` placeholders (Rahma lineage, Ahmad Younes 2025-10-20); Rahma's real template files were removed from this repo 2026-02-13 (628003e) and are fetched from rahmah-gateway at deploy time. Calling them 'Rahma's flavors' is fine as lineage but they carry no content on main.
- **note** — GET /templates/partner-form-builder/generated returns 200 on MSI today because with a single file it serves the primary (templates[0]); after D-FB3/D-FB4 add a second file it will silently switch to serving jeeb_onboarding_form_builder.json. Nothing in jeeb calls it, but note it when validating.
- **note** — MSI naming trap: TEMPLATE_JSON_FILES='generated_jeeb_jeeber_v1.json' makes the service write and delete generated_generated_jeeb_jeeber_v1.json (main.py:90-93 deletes only generated_<f>, so the source is safe). Do not 'clean up' the double-prefixed file by hand — it is the live artifact (mtime 2026-09-05 09:15 = last restart).

Corrected wording:

- olivium-dev/form-builder-service — origin/main 801ef01 (confirmed; tracked template = jeeb_kyc_form_builder.json, step-based, canonicalTemplateId jeeb_jeeber_v1). MSI :10070 = native systemd `jeeb-form-builder.service` on release dir form-builder-service-801ef01-20260828 with TEMPLATE_JSON_FILES='generated_jeeb_jeeber_v1.json' — an UNTRACKED, gitignored flat file that in the release dir is a root-owned symlink to /home/ec2-user/iter5-services/form-builder-service/generated_jeeb_jeeber_v1.json (the only source); live GET /templates = ['jeeb_jeeber_v1'] (11 comps / 11 required).
- F1 stands (verified by simulation): GET /templates/{name} and /schema resolve from generated_<first file> only (template_endpoints.py:20-21,44; :58-105), POST /forms/* + PG tables from all files (endpoints.py:19-30, main.py:32-40,106-123, database.py:110-135), partner served by the bespoke route main.py:212-216 (which serves the PRIMARY when only one file is configured).
- D-FB2: lookup-chain fallback is the right shape (primary wins on colliding keys), but say 'no change to any request that succeeds today; partner-only names flip 404→200', not 'byte-identical'; fallback must read generated_<file> and use per-file temp names for non-en. Rahma runs f1e9d9f/1b6b4c8, not main, and its deploy workflows are disabled — D-FB2 reaches Rahma only via an owner-gated redeploy carrying the whole Feb→Aug loader drift.
- Fix stale refs: jeeb-staging-deploy.yml add_env = :197; scripts/check-generic-vocabulary.sh exclude = :26 (budget line :20). Add to FB5: the vocab gate is ALREADY red on 801ef01 (scripts/audit-fail-closed-deployments.py:9,11,13) and its workflow is disabled_manually; no CI runs pytest.

### mobile-prs-330-274 — CORRECTED

Both PRs check out exactly as stated: #330 and #274 are merged with merge commits 0be517e1 and 357cd655, and both are ancestors of the UX branch at 050051d6. The branch's only change to the token-refresh interceptor (commit db83ba7a) adds two classification flags and leaves every #330 invariant in place — session-bearer gating, terminal-only-on-refresh-401/403 in the reactive lane, the dedicated retry/refresh Dios, the 20 s cooldown and the FormData no-replay rule — and the new tests plus the green 79%-coverage CI job on the same SHA back that up. The Firebase doctor is still executed on every flutter test and passes (CI, a local scratchpad clone, and the repo's 14-mutation prover). The one correction: the guards are not 'untouched' relative to 357cd655 — main reversed #274's committed-config regime two days after it merged (PR #276) and rewrote the doctor and integrity test (#332); the UX branch merely inherits that, having changed none of those files. Separately, PR #335's 'Flutter stage / Test' check is being killed by its own 20-minute workflow timeout, so 'CI ready' fails on every push regardless of content.

Claims not confirmed:

- [CORRECTED] The 5 red guards from PR #274 are untouched (diff vs 357cd655)
  - Evidence: Untouched BY THE BRANCH: git diff origin/main HEAD --stat over PR #274's file set shows only lib/app/app.dart (+28/-2, offline banner/cubit wiring; the JEEB-PUSH-DEGRADED tripwire _logPushDegraded at app.dart L573/582/656/660 is intact). NOT identical to 357cd655: git diff 357cd655 HEAD --stat -> tool/firebase_doctor.sh (283 lines changed), test/firebase_config_integrity_test.dart (314; 11 tests -> 6), test/dev_firebase_workflow_contract_test.dart (44), docs/firebase-invariants.md (79). All from main: c771bf51 2026-08-25 (doctor), b645b8a7 2026-09-02 (workflow-contract test), 271ec07a = PR #332 2026-09-03 (doctor + integrity test). Root cause: PR #276 e208a4c8 (merged 2026-08-25, two days after #274) deleted the tracked android/app/google-services.json + android/app/src/dev/google-services.json (present in git ls-tree 357cd655, absent in git ls-files HEAD; both now .gitignore:98-99). The doctor's check_protected_config now FAILs when the file IS tracked; the integrity test changed from '$path is tracked in git and not gitignored' to '$path remains an absent, untracked, ignored build input'. The other guard files (push_degrade_tripwire_test, push_drop_tripwire_test, firebase_doctor_test, .firebaserc) are byte-identical to 357cd655; test/dev_firebase_config_repository_test.dart was deleted by #274 itself.

Findings:

- **major** — PR #335's 'Flutter stage / Test' check cannot pass on any push: its 'Run tests' step is CANCELLED at ~20 min (job 101453259806, steps show every step success except 'Run tests => cancelled'; .github/workflows/ci-flutter-stage.yml:55 timeout-minutes: 20) while the same suite takes 27m33s in the 'Flutter CI + coverage (79%)' job, which passes. 'CI ready' fails as a consequence. This is main's CI split (b645b8a7), not the branch, but it hides real signal on the branch and needs the timeout raised on main.
- **major** — The inventory's mental model of PR #274 is stale: #274's headline fix (M1 — commit google-services.json as the one canonical regime) was reversed on main by PR #276 (e208a4c8, merged 2026-08-25): real Firebase configs are untracked+gitignored+CI-injected, and the doctor/integrity test were retargeted (c771bf51, #332) to assert that new regime. What still 'runs' from #274 on the branch is the doctor test hook, the tripwires, the .firebaserc pin and the committed pubspec.lock; the guards themselves are main's rewritten guards. Any memory line saying 'tracked-AND-gitignored dev google-services.json fixed by committing it' should be updated.
- **minor** — lib/core/network/auth_interceptor.dart L281-283: _logout gained an optional {AuthLossReason reason} but every call site uses the default; AuthLossReason.storeUnavailable is never emitted by production code (only test/features/registration/session_expired_message_test.dart:88 signals it). Either wire it (the NET-02 path deliberately does not log out, so there may be no caller) or drop the parameter.
- **minor** — auth_interceptor.dart L131-135: the NET-02 early return is behaviourally redundant — when the store read throws, sessionBearerFlag is never set, so the next guard (L138) would already pass the 401 through untouched. The flag's real value is only classification downstream; the extra branch is harmless but the comment implies it is what prevents logout.
- **minor** — auth_interceptor.dart L196-199 _inCooldown duplicates the inline cooldown test at L204-207 inside _refreshSession; the latter should call the getter so the two cannot drift.
- **note** — Branch appends RetryInterceptor (NET-06) after TokenRefreshInterceptor and UnversionedPathFallbackInterceptor, sharing the same retry Dio. Ordering is sound (auth refresh runs first; replays carry the already-attached bearer; a 401 on a replay is surfaced because _retriedFlag/attempts are on the same RequestOptions). A 401 that occurs on a RetryInterceptor replay is not refreshed — documented in the code comment, acceptable edge case.
- **note** — resolveGatewayDio() fallback (_fallbackDio) builds a second TokenRefreshInterceptor with its own single-flight/cooldown state over the same AuthTokenStore when no Dio is registered. Pre-existing at 0be517e1 (worse then: fresh AuthTokenStore()); the branch improved it by sharing the store and caching the client. Two interceptors could still race a refresh in un-DI'd contexts.
- **note** — Local targeted run in the scratchpad clone: auth_interceptor_test + app_failure_mapper_test + all Firebase guard tests = 65 passed, 1 skipped (auth_interceptor_test.dart:407 skip: kObsCompiledIn, pre-existing, not a branch change). The 5 new NET-02/NET-17 tests are well-targeted: locked keystore never calls refresh/clear/logout; cooldown window reopens at +21 s; gateway-rejected refresh is terminal and not 'recovering'.

Corrected wording:

- olivium-dev/jeeb-mobile — PR #330 (token refresh, merged 0be517e1 on 2026-09-03) and PR #274 (Firebase forever-fix, merged 357cd655 on 2026-08-23); both are ancestors of ux/api-error-handling-empty-states @050051d6. #330's interceptor invariants are intact on the branch (single commit db83ba7a adds only storeUnavailableFlag/recoveringFlag + tests). #274's doctor hook (test/firebase_doctor_test.dart) still runs on every flutter test and passes in CI and locally, but the guards on the branch are MAIN's rewritten guards, not #274's: PR #276 (e208a4c8, 2026-08-25) reversed #274's committed google-services.json regime to untracked+CI-injected and #332/c771bf51 retargeted tool/firebase_doctor.sh and test/firebase_config_integrity_test.dart accordingly; the UX branch itself changed none of these files.

### advisory-db — CORRECTED

The bullet's own facts hold: the ruby-advisory-db file exists with patched_versions '>= 3.4.0' only and no unaffected_versions, GHSA-47m2-wp7j-p9vc is the matching advisory, the CVE is the Entry#extract prefix-traversal, bundler-audit 0.9.3 --update flags rubyzip 2.4.1 today, 2.4.2 is unreleased, and no open PR touches the file. The note's YAML diff is correct and idiomatic — I applied it to a fresh clone and the repo's real spec passed 47/47 plus schema 1393/1393, and bundle-audit against that patched DB clears 2.4.2 while still flagging 2.4.1. Three things in the note/README are wrong and should be fixed before the owner files anything: it cites a nonexistent spec file (spec/gems/rubyzip_spec.rb) and a nonexistent rubygems-existence check; it calls the GHSA 'what Dependabot reads' although the GHSA is unreviewed with zero package ranges and GitHub documents that Dependabot ignores unreviewed advisories; and README line 55 says bundle audit reads GHSA not ruby-advisory-db, which my repro disproves. Minor: '#664' is an issue not a PR, and ruby-advisory-db now has an explicit no-unreviewed-AI-output policy the owner must respect. Working notes and evidence are in scratchpad/review/advisory-db/ (advisory.yml, ghsa.json, ba/audit.out, radb/ patched clone, NOTES.md).

Claims not confirmed:

- [CORRECTED] spec/gems/rubyzip_spec.rb would accept '~> 2.4.2' (note line 39, README line 54)
  - Evidence: ls radb/spec -> advisories_spec.rb, advisory_example.rb, gem_advisory_example.rb, schema_validation_spec.rb, versions_example.rb ...; NO spec/gems/ directory and NO rubyzip_spec.rb. The per-file checks come from spec/advisories_spec.rb globbing gems/*/* into the 'Gem Advisory' shared example. Correct command: `bundle install && bundle exec rspec` (or `rake lint`, which also needs yamllint).
- [REFUTED] The schema spec 'validates that each patched version exists on rubygems.org — hence after release' (note line 39; README line 54 'it checks the version exists on rubygems')
  - Evidence: spec/versions_example.rb only checks: 1-2 ranges per string, operator regex /^(?:<=|<|>=|>|~>|=) /, and Gem::Requirement.new does not raise. No network/rubygems lookup anywhere in spec/ (grep rubygems spec/ -> none). Proof: the full spec passed 47/47 on the patched file on 2026-09-06 while rubyzip 2.4.2 does not exist on rubygems.org. Waiting for the release is still correct etiquette (maintainers verify by hand), but the stated reason is false.
- [REFUTED] GHSA-47m2-wp7j-p9vc is 'what Dependabot reads' (note line 8) and 'Dependabot/bundle audit via GHSA read this, not ruby-advisory-db' with a '< 2.4.2 range' to edit (README line 55)
  - Evidence: ghsa.json: type 'unreviewed', vulnerabilities [] (no package, no affected ranges — there is no '< 2.4.2' range to add 2.4.2 to). docs.github.com about-the-github-advisory-database: 'Dependabot doesn't create Dependabot alerts for unreviewed advisories as this type of advisory isn't checked for validity or completion.' bundle audit provably reads ruby-advisory-db, not GHSA (repro printed 'Updating ruby-advisory-db ... commit e7179ad'); README line 59 of the same package says so correctly, so line 55 contradicts it.
- [CORRECTED] Fix commit rubyzip/rubyzip@17edfbf 'Prevent entries from being extracted outside specified directory', '#664'
  - Evidence: gh api commits/17edfbf: message 'Prevent entries from being extracted outside specified directory. ... Fixes #664.' dated 2026-06-12; files lib/zip/entry.rb, test/data/path_traversal/out_evil.zip, test/path_traversal_test.rb. #664 is an ISSUE ('rubyzip allows escaping of output directory with crafted zip file', closed) — gh api pulls/664 -> 404. The note's '(#664)' should read 'fixes issue #664'.
- [UNVERIFIABLE] Related URLs rubygems.org/gems/rubyzip/versions/2.4.2 and releases/tag/v2.4.2 are valid
  - Evidence: Both 404 today because 2.4.2 is unreleased; by design they only resolve after the maintainer tags/pushes. Cannot be checked before then.

Findings:

- **major** — ADVISORY-DB-NOTE.md line 39 and README.md line 54 tell the owner to run `bundle exec rspec spec/gems/rubyzip_spec.rb` and say it 'checks the version exists on rubygems'. That file does not exist in rubysec/ruby-advisory-db and no spec performs a rubygems.org lookup (I applied the diff to a fresh clone and the full suite passed 47/47 + schema 1393/1393 while 2.4.2 is unreleased). Replace with: `bundle install && bundle exec rspec` (plus `pip install yamllint && rake yamllint` for the lint the maintainers' CI runs). The 'send only after release' rule is still right, but for human-verification reasons, not because CI would fail.
- **major** — The GHSA half of the plan is mis-scoped. GHSA-47m2-wp7j-p9vc is `type: unreviewed` with `vulnerabilities: []` — it names no package and no version range, so README.md line 55's instruction to 'add 2.4.2 as a patched version for the `< 2.4.2` range' has nothing to attach to, and GitHub's docs state Dependabot does not alert on unreviewed advisories at all. A useful 'Suggest improvements' submission must add package rubyzip (RubyGems) with two affected ranges — `< 2.4.2` (first patched 2.4.2) and `>= 3.0.0, < 3.4.0` (first patched 3.4.0) — which is also what would make it eligible for GitHub review. The 'what Dependabot reads' framing in the note (line 8) should be dropped or reworded to 'what Dependabot would read once GitHub reviews it'.
- **major** — README.md line 55 claims 'Dependabot/`bundle audit` via GHSA read this, not ruby-advisory-db'. That is backwards for bundle audit: my repro shows bundler-audit 0.9.3 `--update` cloning rubysec/ruby-advisory-db (commit e7179ad) and matching from it; README.md line 59 in the same package says exactly that. For the jeeb-mobile release-security gate, the ruby-advisory-db PR is the ONLY lever; the GHSA edit is cosmetic for our purposes. Fix the sentence so the owner does not prioritise the wrong submission.
- **minor** — The PR-body paragraph cites 'commit rubyzip/rubyzip@17edfbf ("Prevent entries...", #664)'. #664 is an issue ('rubyzip allows escaping of output directory with crafted zip file'), not a PR (pulls/664 -> 404). Say 'fixes issue #664' to avoid the maintainer flagging it.
- **minor** — rubysec/ruby-advisory-db README (line ~290) now has an explicit AI policy: a human must personally review and take responsibility; 'contributions that appear to be unreviewed machine output will be closed immediately, and repeat offenders will be banned'. The note's ready-made paragraph is AI-drafted; the owner should trim it to first-person facts they have verified (the 2.4.2 release date, that the backport merged) before filing. Worth an explicit warning line in the note.
- **minor** — The shorthand 'the lib/zip/entry.rb change is identical to the 3.4.0 one (`start_with?(dest_dir + ::File::SEPARATOR)`)' omits the second half of the fixed condition (`|| dest_dir == ::File::SEPARATOR`). The worktree patch does carry it, so the code is right; only the prose is incomplete. Quote the full line in the PR body.
- **note** — Positives verified: the YAML diff is correct and idiomatic (`~>` for the non-latest branch matches 411 existing entries and the sync bot's own output format), the ordering passes the 'last range must be >=' test, the related URLs use the schema's `url` key correctly, no competing open PR exists, and the sync bot would not overwrite a manually added patched_versions entry (github_advisory_sync.rb L422). bundle-audit against the patched DB clears rubyzip 2.4.2 and still flags 2.4.1, so the change achieves the intended effect on our gate as soon as `--update` fetches it.
- **note** — The advisory has no `unaffected_versions`; the note correctly does not add one (3.0.0-3.3.1 and all 2.x are genuinely affected). If the maintainer asks, the honest answer is that only the legacy positional Entry#extract(dest_path) path on 2.x escapes via name_safe?, which is not expressible as a version range.

Corrected wording:

- rubysec/ruby-advisory-db — gems/rubyzip/CVE-2026-85396.yml (live c4520ba 2026-09-04: `patched_versions: [">= 3.4.0"]`, no `unaffected_versions`; no open PR touches it; this is the ONLY database our `bundle-audit check --update` gate reads); GHSA-47m2-wp7j-p9vc is `unreviewed` with no affected-package ranges, so Dependabot ignores it today — both to update after rubyzip 2.4.2 ships. Pre-PR check is `bundle install && bundle exec rspec` in the advisory-db clone (there is no spec/gems/rubyzip_spec.rb and the spec does not verify versions on rubygems.org); the GHSA suggestion must add package rubyzip with ranges `< 2.4.2` and `>= 3.0.0, < 3.4.0`, not edit a range that does not exist.

### planned-mobile-branches — CORRECTED

The "not created yet" half of the bullet is true: I checked all 368 jeeb-mobile remote heads, local branches, all 335 jeeb-mobile PRs, all 487 jeeb-gateway remote branches, gateway PRs, the stale local gateway clones and the jeeb-gateway-worktrees directory — none of the three names exist anywhere. The "jeeb-mobile branches" half is wrong: the plans and the execution plan define `fix/p03-create-request-validation` and `fix/notifications-inbox-target-ref` as jeeb-GATEWAY branches (worktrees off fresh origin/main 6679f6ee, created in wave 0), and only `fix/dm-onboarding-route` is a jeeb-mobile branch (P01, off post-merge main, after the gateway route is deployed). Each name maps cleanly to exactly one plan (P01 / P03 / P02 — not P05/P06), bases are stated, and the three gateway file fences are disjoint; the only mobile overlap (P02-M vs P03-M on the l10n triple) is deliberately serialized. The real hazards for a developer are documentary: P02 §5 and P03 §5 still print the voided stacked-branch instructions from the C1 freeze, the execution plan (07:10 UTC) predates P01 v2/v3 and still says P01 is parked and "no v2 draft exists" while v3 says "nothing to decide" and names three new branches in three repos, and the conflict-review ownership map still lists v1's P01 file/worktree names. Also note nobody has yet executed wave 0's gateway-worktree step.

Claims not confirmed:

- [REFUTED] The three names are planned jeeb-mobile branches
  - Evidence: Only `fix/dm-onboarding-route` is a jeeb-mobile branch (PLAN-P01 v1:193, v2:236, v3:252 '### jeeb-mobile — branch `fix/dm-onboarding-route` off post-merge `main`'). `fix/p03-create-request-validation` is a jeeb-GATEWAY branch: PLAN-P03:70 '## 4. Fix steps — GATEWAY (`olivium-dev/jeeb-gateway`, new branch `fix/p03-create-request-validation` off `origin/main`)', EXECUTION-PLAN:113 'git -C jeeb-gateway worktree add ../jeeb-gateway-worktrees/p03-validation -b fix/p03-create-request-validation origin/main'. `fix/notifications-inbox-target-ref` is a jeeb-GATEWAY branch: PLAN-P02:150 '### Gateway — repo `olivium-dev/jeeb-gateway`, new branch `fix/notifications-inbox-target-ref` off `origin/main`', EXECUTION-PLAN:108. The corresponding MOBILE work for P02/P03 has no branch at all under OD-0 widen: EXECUTION-PLAN:30-31 make P02-M c1 and P03-M commits on #335 batch 1.
- [CORRECTED] The plans do not conflict over the same files (mobile)
  - Evidence: P01-M (v3 M1–M7): lib/features/jeeber_onboarding/**, lib/devtool/catalog/fixtures/dm_onboarding_screen_fixtures.dart, 4 test files, docs/build-out/20_GAP__jeeber-onboarding.md, optional lib/core/network/mock_gateway_client.dart; 'No ARB change' (v3:272), no registry edit. P02-M (EXEC:172-173): lib/features/notifications/**, l10n triple, notifications_list_screen_fixtures.dart. P03-M (EXEC:174-176): lib/features/location/**, prohibited_acknowledgment_repository_impl.dart, registry, l10n triple, client_location_screen_fixtures.dart, batch_06_entries.dart. One overlap exists — the l10n triple (app_en.arb, app_ar.arb, app_localizations.dart) between P02-M and P03-M — and it is deliberately serialized (EXEC:79-80 'P13 → P05 → P06(none) → P07 → P02 → P03 → P12-B'; CONFLICT-REVIEW.md:45). P01-M overlaps neither and lands on post-merge main.
- [CORRECTED] The execution plan's wave assignment matches the plans
  - Evidence: Internally consistent: EXECUTION-PLAN §2.2 item 3 (lines 107-118) creates G2 (P02-G) + G3 (P03-G) worktrees in wave 0, 'No P01 worktree (parked)'; §1 rows 30-31 say 'create in wave 0', row 29 '— (none created)'; wave 4 (line 165) = owner merges P02-G + P03-G → combined deploy → P02 V1–V12 + P03 Phase B; the P01 mobile branch appears in no wave. BUT the execution plan (committed 201659dc, dated 07:10 UTC) predates P01 v2 (125ef39a) and v3 (111cf177, file mtime 10:56): its L6 says 'no P01 v2 draft anywhere', §4 item 1 is a v2 'placeholder', and P01 is 'PARKED until v2 is approved' — while P01 v3 §11 (line 362) says 'Owner: nothing to decide' and defines three branches across three repos. Nothing reconciles them. Also, ~4h after wave 0 was to start, neither gateway worktree/branch exists (see existence claims).

Findings:

- **major** — Wrong-repo trap in the inventory bullet itself: `fix/p03-create-request-validation` and `fix/notifications-inbox-target-ref` are jeeb-GATEWAY branches (PLAN-P03:70, PLAN-P02:150, EXECUTION-PLAN:108,113). Only `fix/dm-onboarding-route` is a jeeb-mobile branch. A developer following the bullet would create two branches in the wrong repo, and might then put the P02/P03 MOBILE work on them — which under OD-0 widen must ride PR #335 as batch-1 commits (EXECUTION-PLAN:30-31), not a branch.
- **major** — P01 status is contradictory across documents. EXECUTION-PLAN (07:10 UTC) parks P01 'until v2 is approved', states 'no P01 v2 draft anywhere' (L6) and leaves §4 item 1 as a placeholder; P01 v3 (committed later, 111cf177) says '## 11. Owner: nothing to decide' and specifies three new branches: form-builder-service `feat/jeeb-onboarding-template` off 801ef01, gateway `feat/form-submissions-preferences` off 6679f6ee, mobile `fix/dm-onboarding-route` off post-merge main. A developer reading v3 starts now; one reading the execution plan creates nothing. Neither file references the other, and the execution plan's wave 4 still says 'P01 §7A/§7B do NOT run'.
- **major** — P02 §5 (line 222-226, 376) and P03 §5 (lines 131-133) still carry the C1 scope-freeze instructions — stacked mobile branches `fix/notifications-inbox-kinds` and `ux/p03-create-request-validation` off `ux/api-error-handling-empty-states@ecfd3cc1` with 'Never push this work onto #335 itself' — which OD-0 widen voided (EXECUTION-PLAN:47-48 'the C1 scope freeze is VOID'). The plan files were not edited after the decisions (mtime Sep 5 19:18), so a developer reading P02/P03 alone creates two stale-named stacked branches that the execution plan says must not exist.
- **minor** — P01 gateway branch/worktree naming drifted across versions without the ownership map following: v1 = `feat/jeeber-onboarding-route` in worktree `p01-onboarding` (P01:123-125), v2/v3 = `feat/form-submissions-preferences` (v2:154-157, v3:211); CONFLICT-REVIEW §2 (line 68) still lists v1 files `Onboarding/**`, `JeeberOnboardingBffController.cs` and worktree `p01-onboarding`, while v3 uses `FormSubmissions/**`, `FormSubmissionsBffController.cs` and gives no worktree path at all.
- **minor** — P02 mobile 'commit 2' has no home when the combined deploy misses batch 2: EXECUTION-PLAN:30,94 say it 'is the first post-merge follow-up PR' but name no branch (the old name `fix/notifications-inbox-kinds` was voided). Give it a name now so the post-merge PR is not improvised.
- **minor** — Cross-plan gateway fences are disjoint on paper, but P01 v3 G13 commits `artifacts/openapi/jeeb-gateway.v1.json` after an export + compat check; P03 adds a new 400 response on POST /v1/requests and POST /requests and never mentions the OpenAPI artifact/compat check. If that check runs in gateway CI, P03-G's PR may go red or later conflict with P01-G on the artifact. Unverifiable from plan text (would need the gateway CI workflow); flagging as a gap in P03, not an overlap.
- **note** — Schedule slip, not a defect: wave 0 item 3 ('create in wave 0', 07:10 UTC) has not been executed — at review time no `jeeb-gateway-worktrees/{notif-target-ref,p03-validation}` and no such remote branches exist. The mobile plans branch is `docs`-only so far (last 7 commits on the branch are all docs).

Corrected wording:

- Planned branches (none created yet, verified 2026-09-06 on both remotes): jeeb-mobile `fix/dm-onboarding-route` (P01 v3, off post-merge `main`, only after the P01 gateway route is live on MSI+staging — P01 is still marked parked in EXECUTION-PLAN-2026-09-06); jeeb-gateway `fix/notifications-inbox-target-ref` (P02-G, worktree `jeeb-gateway-worktrees/notif-target-ref` off fresh `origin/main` 6679f6ee, wave 0); jeeb-gateway `fix/p03-create-request-validation` (P03-G, worktree `jeeb-gateway-worktrees/p03-validation` off fresh `origin/main` 6679f6ee, wave 0). P02/P03 MOBILE work has no branch — it rides PR #335 as batch-1 commits under OD-0 widen (the stacked names `fix/notifications-inbox-kinds` / `ux/p03-create-request-validation` still printed in PLAN-P02 §5 and PLAN-P03 §5 are void).

### planned-gateway-branches — CORRECTED

The bullet is essentially right: no feat/form-submissions-preferences branch, no wgf2/* branch, and no PR for either exists on olivium-dev/jeeb-gateway (487 branches paginated, PR search across all states), and main is 6679f6ee as both plans state. Two things need tightening. First, P15's train is not named "wallet-independence" anywhere — its branches are wgf2/g0-gate … wgf2/g6-defaults plus mobile wgf2/m1-guard-ui, so the bullet should carry that prefix. Second, "local clone is 515 commits stale" is true of the checked-out branch but the clone's origin/main ref is already current, and a stale `main` is pinned by another worktree, so the plan's worktree recipe is correct but the reason given is misleading. ADR numbering is clean: P01 v3 proposes no ADR (it does cite a non-existent ADR-005), P15's 0014/0015 are free on every ref, and the epic's 0012 clash with main's 0012 is real. The biggest gap a developer would hit is that both plans edit Program.cs, appsettings.json/Production.json, the gwdbx flag registry and the exported OpenAPI artifact, yet neither plan (nor the execution plan, which parks P01) says which lands first, and P15's train says "cut from origin/main, stacked only where noted" without noting any stacks even though G2→G3→G6 clearly depend on each other.

Claims not confirmed:

- [CORRECTED] The P15 'wallet-independence PR train' branches are not created yet
  - Evidence: P15 §6 L120 names the branches `wgf2/g0-gate` … `wgf2/g6-defaults` (gateway) and `wgf2/m1-guard-ui` (mobile) — there is no branch literally called 'wallet-independence'. grep -E '^wgf2/' branches.txt = none; gh pr list --search wgf2 = []; gh api jeeb-mobile branches grep wgf2 = none. The bullet should carry the real prefix so a developer can grep for it.
- [CORRECTED] The local jeeb-gateway clone is 515 commits stale, so a worktree must be used
  - Evidence: git -C /Users/oudaykhaled/Desktop/olivium/jeeb/jeeb-gateway rev-list --count HEAD..origin/main = 515 (HEAD = gwdbx/w2-r11-settlement-cutover 37e783c). BUT the clone's origin/main ref is ALREADY 6679f6ee (fetched 2026-09-04), so 'stale' means the checked-out branch, not the remote-tracking ref; `git worktree add -b feat/form-submissions-preferences ../… origin/main` works without a fetch. Also .claude/worktrees/wf_fe2b403c-988-9 already holds branch `main` at 5edb8b6 (stale), so `git checkout main` in the primary clone will be refused.
- [REFUTED] P01 v3 proposes an ADR too (asked to check for numbering collision)
  - Evidence: grep -n -i adr PLAN-P01-dm-onboarding-route.v3.md -> only L148 'lat/lng range + coverage in gateway code (STATE rules, as ADR-005 puts them)'. P01 adds no docs/adr file (G1–G15 list none), so no 0014/0015 collision. However ADR-005 does not exist on main: docs/adr/0006-…md:10 'It also cites ADR-0001 and ADR-0005, neither of which exists in this repository'.

Findings:

- **major** — Merge order between the two plans is undefined. P01 G7/G8/G9 and P15 PR-G0/G1/G3/G6 all edit Program.cs, appsettings.json, appsettings.Production.json and scripts/gwdbx-flag-registry.txt, and P15 §6 says 'Steps 0–2 can ride the P10 C14 combined MSI deploy (with P01/P02/P03)' while EXECUTION-PLAN-2026-09-06.md:29 says P01 is PARKED and the combined deploy is P02-G + P03-G only. Neither plan states which lands first or who rebases; whichever is second will re-run export-openapi (P01 G13 commits artifacts/openapi/jeeb-gateway.v1.json; P15 G0 gates on that same file).
- **major** — P15 §6 writes 'Each PR below is cut from origin/main, stacked only where noted' but the table never marks a stack, while PR-G3 (holds) depends on PR-G2's WalletGuard:Mode enum and PR-G6 depends on all of G1–G5. A developer cutting every wgf2/* branch from origin/main in parallel will produce non-compiling PRs; the plan should say explicitly 'g1 off main, g2 off g1, …' or 'sequential, each merged before the next is cut'.
- **minor** — The inventory bullet says 'wallet-independence PR train' but P15 names the branches wgf2/g0-gate … wgf2/g6-defaults and wgf2/m1-guard-ui (mobile); grepping GitHub for 'wallet-independence' finds nothing by design. Record the wgf2/ prefix.
- **minor** — P01 v3 L148 justifies gateway-side coverage/lat-lng validation with 'STATE rules, as ADR-005 puts them' — ADR-0005 does not exist in jeeb-gateway (docs/adr/0006 L10 says so; main has 0002–0004, 0006–0013). Cite ADR-0006 or drop the reference.
- **minor** — 'local clone is 515 commits stale → worktree' is true of the checked-out branch (gwdbx/w2-r11-settlement-cutover, 515 behind) but the clone's origin/main is already 6679f6ee; a dev who reads 'stale' and runs `git fetch && git checkout main` will hit 'main is already checked out at .claude/worktrees/wf_fe2b403c-988-9' (that worktree's main is 5edb8b6, itself stale). The plan's `git worktree add -b … origin/main` recipe is the right one; say why.
- **minor** — Flag-registry conflict risk is real but small: scripts/gwdbx-flag-registry.txt is grouped by section not sorted, P01's JeeberOnboarding:* rows and P15's WalletGuard:Mode / Holds:Enabled / Offers:MaxLiveOffersPerJeeber / CommissionCollection:CurrencyCode rows will land near different existing rows (L76/L87 vicinity for P15). The epic itself never registered its new keys, so PR-G0 must add them or check-gwdbx-flag-registry.sh will red the train.
- **note** — Verified positives: main 6679f6ee, epic dfa9159 ahead 5/behind 97/90 files, exactly the 4 conflicts P15 lists, ADR 0012 clash real, 0014/0015 free on all 488 refs, no Npgsql in src, compat checker unwired, P01's Program.cs:1734 / UpstreamFeatureFlags.cs:182 / FormBuilderController.cs:72 anchors all correct. A jeeb-gateway-worktrees/wgf-epic worktree already sits at dfa9159, so cherry-pick sources are local.

Corrected wording:

- Planned jeeb-gateway branches (verified absent on GitHub 2026-09-06, 487 branches / no matching PR): `feat/form-submissions-preferences` off `origin/main` 6679f6ee (P01 v3, no ADR) and the P15 wallet-independence PR train `wgf2/g0-gate` … `wgf2/g6-defaults` (+ mobile `wgf2/m1-guard-ui`; ADR-0014 new, epic refund ADR renumbered 0012→0015). Both edit Program.cs, appsettings*.json and scripts/gwdbx-flag-registry.txt — landing order is not yet decided.

### planned-fb-branch — CORRECTED

The bullet itself is accurate: the branch feat/jeeb-onboarding-template does not exist on olivium-dev/form-builder-service (404 on GitHub, absent from the 33-branch list) and none of the 42 PRs concerns a jeeb onboarding template. I booted origin/main 801ef01 in a Python 3.11 venv with the plan's exact JSON as a second TEMPLATE_JSON_FILES entry: the flat document survives normalisation untouched with attributes.step/stepOrder intact, the bare label keys pass through verbatim in en and ar (there are no assets/lang files anywhere in the repo or image, so the service never translates them — an unknown %label:: key would just return the bare key), the schema comes out as 6 components / 4 required exactly as the plan says, POST /forms/jeeb_jeeber_onboarding_v1 registers, and GET /templates/jeeb_jeeber_onboarding_v1 404s — confirming the F1 gap that D-FB2 is meant to close. The staging workflow's comma-list env is compatible and the fail-closed deployment audit still passes with the edit. Two things would bite on first execution: D-FB4 tells the owner to deploy into a releases/ directory, but the live systemd unit runs from /home/ec2-user/iter5-services/form-builder-service (a separate plain copy), so the restart would change nothing; and the FB5 vocabulary gate is already red on pristine main and disabled on GitHub, so it cannot pass as a gate regardless of the branch. Several line references in the plan are also stale (workflow :197 not :188; vocabulary script :26 not :12-13). Verdict CORRECTED: the bullet stands, the plan needs those fixes before the branch is cut.

Claims not confirmed:

- [CORRECTED] i18n label keys resolve on the service / assets/lang/{en,ar}.json; unknown key behaviour
  - Evidence: The plan's labels are bare keys (no '%label::' prefix) so the service never tries to translate them — they pass through verbatim in en and ar (boot: bare key ar -> 'onboarding.jeeb.v1.state.label'). assets/lang does NOT exist in the repo or the Docker image (find: no assets dir; Dockerfile COPY . .), jeeb-staging-deploy.yml sets no LOCALIZATION_ASSETS_PATH, so translations={} ('Translation file not found: assets/lang/en.json' at import, harmless). An unknown '%label::x' returns the bare key 'x' (localization.py:66). The plan states this correctly in §4.4 ('ours are keys and pass through'); the fix is only to the review question's premise: no lang files, no resolution, mobile ARBs do it.
- [CORRECTED] Plan line refs: jeeb-staging-deploy.yml:188 (D-FB3) and :185-192 (§1 R1)
  - Evidence: grep -n TEMPLATE_JSON_FILES .github/workflows/jeeb-staging-deploy.yml -> 197; form-builder-service) case spans 193-201. Replace ':188' with ':197' and ':185-192' with ':193-201'.
- [CORRECTED] Plan line refs: scripts/check-generic-vocabulary.sh:12-13 (exclude) and FB3 ':13'
  - Evidence: Lines 12-13 are ALLOW_STRIP; the SCOPE exclude ':(exclude)jeeb_kyc_form_builder.json' is line 26 (SCOPE block 24-26); the budget entry 'scripts/check-generic-vocabulary.sh=2' is line 19. Replace ':12-13'/':13' with ':26'.
- [REFUTED] FB5 gate 'bash scripts/check-generic-vocabulary.sh' will pass on the branch
  - Evidence: On PRISTINE origin/main (git stash) the gate already FAILS: scripts/audit-fail-closed-deployments.py:9,11,13 ('.github/workflows/deploy-to-ec2-rahmah.yml', 'deploy-to-jeeb.yml', 'jeeb-staging-deploy.yml' literals added by PRs #38/#39 on 2026-08-19, after the gate's last green run 2026-08-12 33cedcd). The workflow itself is 'disabled_manually' on GitHub (gh api actions/workflows), so it neither blocks the PR nor can be reported green; PR #42 shows only the 'audit' check.
- [CORRECTED] D-FB4: deploy the new SHA into a new /home/ec2-user/iter5-native/releases/form-builder-service-<sha>-<date>/ and restart
  - Evidence: systemd jeeb-form-builder.service: WorkingDirectory=/home/ec2-user/iter5-services/form-builder-service, ExecStart sources the env file then 'python -m app.main' from that dir; that path is a plain directory (not a symlink, not a git checkout) and is DIFFERENT from releases/form-builder-service-801ef01-20260828. A release dir + restart changes nothing unless iter5-services/form-builder-service (or the unit's WorkingDirectory) is updated; both dirs must carry generated_jeeb_jeeber_v1.json and the new jeeb_onboarding_form_builder.json because database.py:123 and endpoints.py:22 open template files relative to the CWD while process_templates.py uses BASE_DIR.

Findings:

- **major** — D-FB4 (MSI ops steps) targets the wrong directory. The live unit runs from WorkingDirectory=/home/ec2-user/iter5-services/form-builder-service (a plain copy, not a symlink, not git), while the plan tells the owner to populate a new /home/ec2-user/iter5-native/releases/form-builder-service-<sha>-<date>/ and restart. That restart would keep serving the old code and would 500/fail on the second file (database.py:123 / endpoints.py:22 open template files relative to CWD; process_templates uses BASE_DIR). Fix: copy the merged SHA + generated_jeeb_jeeber_v1.json + jeeb_onboarding_form_builder.json into iter5-services/form-builder-service (or repoint WorkingDirectory), then edit env + run.sh:103 and restart; verify with the plan's two curls.
- **major** — FB5 'Gate: bash scripts/check-generic-vocabulary.sh' cannot go green as written: the G-28 gate is ALREADY red on pristine origin/main (scripts/audit-fail-closed-deployments.py:9,11,13 carry jeeb-named workflow literals since PRs #38/#39, 2026-08-19) and the vocabulary-gate workflow is disabled_manually on GitHub. Either budget/exclude those three lines in the same PR (owner decision — the script says .github naming is an open owner decision) or state explicitly that the gate is disabled and red-on-main, so the branch is not blamed for it. The plan's own glob exclude is otherwise correct: the new file adds zero findings.
- **minor** — Line references in §1/§2.3/§5 are stale against origin/main 801ef01: jeeb-staging-deploy.yml TEMPLATE_JSON_FILES is line 197 (case block 193-201), not 188 / 185-192; the vocabulary exclude is scripts/check-generic-vocabulary.sh:26 (SCOPE 24-26), not :12-13 / :13. An implementer following the numbers edits the wrong lines.
- **minor** — D-FB2 code and its docstrings/comments in app/template_endpoints.py must contain no 'jeeb|jeeber|rahma|saawt' token (gate scans app/** content, app/main.py budget=1 is already used); tests/ is excluded (icase) so the fallback test may name jeeb freely. Worth stating in the plan since the gate gives no other escape.
- **note** — Staging first dispatch with the comma list will create table jeeb_jeeber_onboarding_v1_submissions in the shared JEEB_DATABASE_URL Postgres (main.py:100 initialize_db; observed in the SQLite boot) — plan acknowledges 'unused, harmless'; the workflow's post-deploy curl /templates only proves the PRIMARY, so nothing in the pipeline verifies the onboarding template is served; add curl /templates/jeeb_jeeber_onboarding_v1/schema to the form-builder-service branch of the workflow after D-FB2 (audit script tolerates it: .github is out of the vocab gate's scope and the audit only matches reversion/mutable-image patterns).
- **note** — Localization is a no-op for this template on every jeeb environment: assets/lang does not exist in the repo or image and LOCALIZATION_ASSETS_PATH is unset on staging/MSI, so Accept-Language ar returns identical bare keys — consistent with the plan (mobile ARBs resolve them) but the D-FB2 localized path for the second file will spend a process_template_with_language pass per language for nothing; caching per (file, lang) as get_all_templates does for the primary is advisable.
- **note** — The plan correctly predicts the gateway list route defect (G10): FormBuilderServiceClient.cs:47 binds List<FormTemplateSummary> while the service emits {name:[...]}; mobile does not use it, so it is not a first-deploy blocker.

Corrected wording:

- Planned form-builder-service branch (not created yet — gh 404, no PR): feat/jeeb-onboarding-template (P01 v3). Template JSON + comma-list TEMPLATE_JSON_FILES verified against a booted origin/main 801ef01; before cutting the branch fix the plan's stale refs (jeeb-staging-deploy.yml:197 not :188; check-generic-vocabulary.sh:26 not :12-13), note the G-28 vocab gate is disabled on GitHub and already red on main (audit-fail-closed-deployments.py:9,11,13), and correct D-FB4 to the live MSI working dir /home/ec2-user/iter5-services/form-builder-service (not releases/…).

### links — CORRECTED

The artifact is real, private, owned by you, and its published body is byte-identical to the scratchpad source file. The page is technically clean: only Google Fonts is loaded externally, the single inline JSON block parses to exactly 18 explainers OD-0..OD-17, the three inline scripts pass node --check, storage and db access are guarded, the stamp reads v4, and the artifact db holds the 18 owner decisions that DECISIONS.md records. The OD-1 and OD-15 owner_answer paragraphs match the committed v3 and P15 plans on every effort figure (half day / 1–1.5 d / 2–3 d; 8–10 dev-days; about 2 h owner time), the seven gateway PRs, the eight gate points, and the six open confirmations. Two wording drifts need fixing in the OD-15 answer ('one mobile commit' should be 'one mobile PR'; the plan counts two truly breaking items, not three). The serious problem is that the rest of the page was not re-cut for v3: the P01 plan card and the OD-1 option list still present the old kyc.submit.self / /v1/jeebers/me/onboarding / mobile-S design, and all 13 plan cards link to PLAN-Pnn.md files that do not exist. The owner will see two different P01 plans on the same page.

Claims not confirmed:

- [UNVERIFIABLE] The 'session link' referenced in the bullet
  - Evidence: The bullet gives no URL text to check. The only session URL available to me is the one in my own system context (https://claude.ai/code/session_01VydEU5hq3ihQ5j6ooPFaEq), which I cannot open from the CLI.
- [CORRECTED] OD-15 owner_answer: 'a train of seven gateway PRs plus one mobile commit'
  - Evidence: PLAN-P15 §6 rows 0-6 = PR-G0..PR-G6 (seven gateway PRs) — matches. The mobile leg is 'PR-M1 mobile ... 1.0 d' (§6 row M1, §11 table), i.e. a separate PR on branch wgf2/m1-guard-ui, not a commit. Fix: 'plus one mobile PR'.
- [CORRECTED] OD-15 owner_answer: 'Three things would have broken and each becomes safe' (currency flip, startup check, holds/cap/error types)
  - Evidence: PLAN-P15 §3 'Net: two items are truly breaking as written (C1 data/currency, C2 boot)'; C3 new error types = additive, C5 holds = 'Behaviour change, not a wire break', C6 cap = 'No wire break'. The page's own sentence concedes item 3 is 'all additive'. Fix: 'Two things would truly have broken (currency default flip, startup validator) and one behaviour change (holds, cap, new error types) is flag-gated'.

Findings:

- **major** — The page contradicts itself on P01. The P01 'Plan detail' card (line ~1166) and the OD-1 decision options/CTO recommendation (lines ~383-400, explainer 'options'/'recommended_reason') still describe the superseded v1 design: 'Add POST and GET /v1/jeebers/me/onboarding ... capability kyc.submit.self', 'ship the Lebanon bbox in appsettings.Production.json', 'Eleven endpoint cases ... plus five resolver cases', 'Effort M — gateway M, mobile S', 'Owner decision OD-1 route and capability'. Those numbers come from PLAN-P01-dm-onboarding-route.md (v1) lines 222 and 284; the committed v3 has zero occurrences of 'kyc.submit.self' or '/v1/jeebers/me/onboarding', uses profile.write.self and /form-builder/templates/jeeb_jeeber_onboarding_v1/submit, rates mobile M (~2–3 days) not S (~2 h), ships the bbox dormant per OD-2 nobbox, and says 'nothing to decide'. Only the OD-1 owner_answer paragraph reflects v3. The owner reading the card gets the wrong plan.
- **major** — All 13 plan cards end with a path label 'plan docs/ux-failure-states/plans/PLAN-Pnn.md' (P01..P13). None of those files exist; the real files are PLAN-Pnn-<slug>.md (and P01 is at .v3.md). Only the two owner_answer paragraphs cite correct paths.
- **minor** — OD-15 owner_answer wording drifts from PLAN-P15 in two places: 'one mobile commit' (plan: PR-M1, a separate mobile PR, 1.0 d) and 'Three things would have broken' (plan §3 Net: two truly breaking, C1 currency default and C2 boot validator; holds/cap/types are additive/behaviour-change).
- **minor** — P15 has no plan-detail card; it is reachable only inside the OD-15 explainer text. The 'Pending point plans' section still says 'thirteen plans' and the 'Twenty cross-plan conflicts' figure (CONFLICT-REVIEW.md C1..C20, confirmed) predates P15, so P15 is outside the reconciled set the page describes.
- **note** — Verified sound: no CDN-allowlist violations, single JSON block parses (18 explainers), 3 inline scripts pass node --check, localStorage/db access guarded, published body byte-identical to the scratchpad source, artifact db 'decisions' holds 18 documents matching DECISIONS.md (OD-1 and OD-15 stored as 'other' free-text).

Corrected wording:

- Decision site artifact https://claude.ai/code/artifact/f06f55d4-e977-4455-9b0a-4061ea2089fd (v4 stamp; 18 OD records OD-0..OD-17 backed by the artifact db; 13 plan cards P01–P13; published body identical to scratchpad/site/jeeb-ux-decision-record.html; fonts.googleapis.com is the only external resource) and session link https://claude.ai/code/session_01VydEU5hq3ihQ5j6ooPFaEq. KNOWN DEFECTS: the P01 plan card and OD-1 options/CTO recommendation still show the superseded v1 design (kyc.submit.self, POST/GET /v1/jeebers/me/onboarding, Lebanon bbox, 'eleven endpoint cases', mobile S) while the OD-1 owner_answer is v3; all 13 'plans/PLAN-Pnn.md' path labels point to non-existent files; OD-15 owner_answer should read 'seven gateway PRs plus one mobile PR' and 'two truly breaking items (currency default flip, startup validator)'; P15 has no plan card.

