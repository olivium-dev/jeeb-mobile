<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application>
        <!-- getLaunchIntentForPackage (FCM taps, flutter_local_notifications) picked
             the Dev Tool; a CATEGORY_INFO front door wins. Both home icons remain. -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.INFO"/>
            </intent-filter>
        </activity>
        <!-- The legacy full developer tool remains a debug-source-set only
             launcher. Production and internal release graphs cannot reference it. -->
        <activity
            android:name=".LegacyDevToolLauncher"
            android:exported="true"
            android:enabled="@bool/jeeb_devtool_enabled"
            android:launchMode="singleTask"
            android:label="@string/devtool_name"
            android:icon="@mipmap/ic_launcher"
            android:theme="@style/LaunchTheme"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
