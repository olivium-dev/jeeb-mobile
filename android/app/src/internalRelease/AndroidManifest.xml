<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- DevToolLauncher below is a second LAUNCHER entry, so notification taps
             need this CATEGORY_INFO front door too. It adds no home icon. -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.INFO"/>
            </intent-filter>
        </activity>
        <!-- This dedicated Activity and engine exist only in the internalRelease
             source graph. Native policy must pass before it emits /devtool. -->
        <activity
            android:name=".DevToolLauncher"
            android:exported="true"
            android:enabled="@bool/jeeb_devtool_enabled"
            android:label="@string/devtool_name"
            android:icon="@mipmap/ic_launcher"
            android:launchMode="singleTask"
            android:taskAffinity="com.olivium.jeeb.internalqa"
            android:theme="@style/LaunchTheme"
            android:screenOrientation="portrait"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
