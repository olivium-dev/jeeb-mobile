import 'request_draft.dart';

/// Stub created by sanity-build pass (2026-05-17). Real impl is the POST
/// /api/requests RPC against jeeb-gateway.
class RequestSubmissionService {
  const RequestSubmissionService();

  Future<String> submit(RequestDraft draft) async => 'stub-request-id';
}
